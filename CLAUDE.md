## プロジェクト概要

- **プロジェクト名**: Third Place Japan（サードプレイスジャパン、略称: TPJ）
- **サイト種別**: 日本初のサードプレイス認証メディア
- **運営**: TPJ編集部
- **目的**: 日本全国の「第三の居場所」（ホテル・レストラン・カフェ・サウナ・ウェルネス施設・コワーキングなど）を独自の7軸基準で評価・認証し、AI時代の「場所選び」における業界標準データ基盤となる。

## 制作方針の参照先（必読）

本プロジェクトの制作判断は、以下の7ファイルに基づく。コンテンツ制作・サイト編集の前に必ず参照すること。

| ファイル | 場所 | 役割 |
|---|---|---|
| CLAUDE.md | `project-docs/CLAUDE.md` | TPJ統合制作方針・確定コピー・トーン＆マナー（**最上位**） |
| DEFINITION.md | `project-docs/DEFINITION.md` | サードプレイスの定義（全コンテンツの根幹） |
| INFORMATION-ARCHITECTURE.md | `project-docs/INFORMATION-ARCHITECTURE.md` | サイト構造・URL設計・AEO実装・技術スタック |
| AEO-PLAYBOOK.md | `project-docs/AEO-PLAYBOOK.md` | AI引用獲得（AEO）の施策・タグ正本・業種正本 |
| STORIES-SITE-SPEC.md | `project-docs/STORIES-SITE-SPEC.md` | 記事サイト（/stories/）実装マスター仕様 |
| CONTENT-STRATEGY.md | `project-docs/CONTENT-STRATEGY.md` | 全記事タイトル確定版・本文テンプレート・投稿カレンダー |
| AGENT-INSTRUCTIONS.md | `project-docs/AGENT-INSTRUCTIONS.md` | 記事生成エージェントの運用ルール |

**優先順位**：矛盾時は `project-docs/CLAUDE.md`（最上位）を正とする。AGENT-INSTRUCTIONS は CONTENT-STRATEGY より優先する。
**メインサイトのコピーは `project-docs/CLAUDE.md` セクション6「確定コピー」を正とする。**

> ＜現フェーズ方針＞ 記事サイト（/stories/）の無償記事の充実に集中する。
> 有償展開（掲載枠・有償記事・コンサル）とメインサイトのCTAは保留・停止。
> 有償・価格・事業・上場に関する機能・ページ・コピーは作らない。

---

## 制作環境

- 非エンジニア、非デザイナーが、AI Agent (Claude Code) を使って Web サイトを構築する
- 構築する Web サイトは「ワンページ完結」とする。1 ページに必要な情報をまとめて提供する
- ブログなどは、別のサービス（Substack や note.com など）を利用することを前提とする
- デプロイ先は **Cloudflare Pages**

## サイトコンセプト


2. **人間より先にAIに読ませる設計**
   全ページに構造化データ（JSON-LD）を実装し、AIがQ&A・評価・場所情報を直接抽出できる形にする。

3. **日英両言語で「日本の居場所」を世界に発信**
   インバウンド・海外AIの引用を見据え、主要記事は英語版も持つ。「Japan's best third places」の権威ある情報源を目指す。





### TPJではないもの・TPJであるもの

**❌ TPJではないもの**
- 料理の味・クオリティの評価
- 価格・コスパの比較ランキング
- 「一番うまい店」を決める権威
- 広告掲載・有料露出の媒体
- 飲食店だけを対象にした評価
- ユーザー口コミの集計サービス
- インスタ映え・バズりを評価する場

**✅ TPJであるもの**
- 「いる体験・居場所としての価値」の認証
- 業種横断（カフェ・ホテル・スパ等）の評価
- AIに引用される信頼ある一次情報源
- 掲載枠の希少価値（デジタル不動産）
- インバウンド向け日英両言語の発信基盤
- 7つの空間軸による独自評価システム
- 「日本のサードプレイス」の世界標準定義者

### 誰も真似できない理由（参入障壁）

1. **「サードプレイス」という概念の先行定義権**
   食べログが「飲食店の点数」を定義したように、TPJが「サードプレイスの価値基準」を先に定義した者が市場を制する。後から参入した者は「TPJに似ている」と言われ続ける。定義者は一社のみ。

2. **AIに引用される構造を最初に作る者が勝つ**
   GBC記事サイトはすでにSchema実装済み。AIが「おすすめのサードプレイスは？」に答えるとき、最初に蓄積されたデータソースを引用する傾向がある。先行者が圧倒的に有利なゲーム。

3. **「評価される側」が評価を望む構造**
   TPJの認証は「掲載されること自体がステータス」になる設計。認証を取りたい店舗がTPJに価値を提供するポジティブな循環が生まれる。

4. **インバウンド×日英両言語の空白地帯**
   「Japan's best third places」を英語で検索しても、権威ある情報源が存在しない。TPJがその空白を埋める唯一の候補。

### ポジショニング・ステートメント

> 食べログは「どこで食べるか」を決める。ミシュランは「どこが最高か」を決める。**TPJは「どこにいるか」を決める。**
>
> AI時代に人々が最も問うのは「おいしい店はどこか」ではなく、「今日、どこで自分らしくいられるか」だ。その答えを出せる権威は、世界にまだない。TPJがその権威になる。

## ファイル構成

- `project-docs/` : Web ページの内容や、その他作成に必要なドキュメントを格納
- `public/` : Cloudflare Pages で公開設定をするディレクトリ。ここに成果物を入れる
- `public/assets/` : 画像、CSS、JavaScript などの生成物の置き場

# 作成のためのルール

## 保存先

- ファイルは必ず **UTF-8** で保存（日本語の文字化け防止）
- 成果物は `public/` ディレクトリに保存
- 特に指示がなければ `public/index.html` で作成
- 指示があれば、その指示に合わせて `public/` 配下に保存
- 画像・CSS・JavaScript を生成した場合は `public/assets/` 配下の所定フォルダに保存

## あなたの仕事

- **Web ページの作成**: ユーザーの指示・意図を理解して作成する。コンテンツも創造的に作って欲しいと判断したら創造性を発揮する。詳細指定があればそれに従う
- **Web ページの編集**: 編集箇所と依頼を理解して編集する
- **Web ページのチェック**: 文法エラー、不要な記述、大きすぎる画像などがあれば改善方法をアドバイスする

### ⚠️ 作業開始前の必須確認（絶対厳守）

**🚨 VS Codeを開いたとき・会話を始めたときは必ず最初に確認すること 🚨**

作業を開始する前に、以下をユーザーに必ず促すこと：

> 作業を始める前に、以下のコマンドで最新の状態に更新してください：
> ```
> git pull origin main
> ```
> 完了したら教えてください。

- `git pull` 完了の報告を受けてから作業を開始する
- ユーザーが既に実行済みと言った場合はそのまま進めてよい

---

### ⚠️ Git操作の重要ルール（絶対厳守）

**🚨 絶対に守ること 🚨**

1. **ユーザーの明確な指示があるまで、コミットもプッシュも絶対に行わない**
2. **コミットの指示があった場合** → コミットのみ実行し、完了を報告。プッシュはしない
3. **プッシュの指示があった場合** → その時初めてプッシュを実行
4. **「コミット＆プッシュ」の指示があった場合のみ** → 両方を実行

**禁止事項:**
- ❌ 勝手にコミット＆プッシュを連続実行する
- ❌ 「コミットしました」と言って勝手にプッシュもする
- ❌ ユーザーの確認なしに任意のGit操作を行う
- ❌ 作業完了後に自動でコミット＆プッシュする

**このルールは何度も破られているため、各作業の完了時に必ず「コミット＆プッシュの指示をお待ちしております」と伝え、ユーザーの指示を待つこと。**

**🚨 コミット前の確認（絶対厳守・新ルール） 🚨**

「コミット、プッシュ」の指示を受けても、**すぐに実行してはいけない**。
必ず以下の順序で確認してからユーザーの承認を得ること：

1. `git status` を実行して変更ファイル一覧をユーザーに見せる
2. 「以下のファイルをコミット・プッシュします。よろしいですか？」と確認する
3. ユーザーが「はい」「OK」「進めて」などの承認をしてから実行する

❌ 確認なしに即実行するのは禁止（過去に予期しないファイルを含めてコミットした失敗あり）

---

**🚨 コミット時の必須チェック 🚨**

承認を得たら、以下を実施してコミットすること：

1. **まず `git status` を実行して未コミットの変更があるか確認する**
   - 変更がない場合（`nothing to commit, working tree clean`）→ 「すでにコミット済みです」とユーザーに伝える。コミット操作は行わない
   - 変更がある場合 → 以下2〜4を実施してコミットする
2. **`src/` 配下を編集した場合は、必ず `npm run build` を実行してから次のステップへ進む**
   - buildを忘れると `public/` が古いままになり、Cloudflareに変更が反映されない
   - `public/index.html` など `public/` 直下のファイルだけを編集した場合はbuild不要
   - **`src/_data/` 配下（venues.json・areas.json等）を編集した場合も必ず `npm run build` が必要**
     データファイルはビルド時にHTMLへ展開される。データだけコミットしても画面には反映されない。
3. **`src/` のソースファイルと `public/` のビルド済みHTMLの両方**をコミットに含める
4. `npm run build` 後に生成された `public/` 配下の変更を絶対に漏らさない
5. **`public/pagefind/` も必ずコミットに含める**（検索インデックス。含めないと検索窓が表示されなくなる）

**NG例（過去の失敗）：**
- ❌ `src/` だけコミットして `public/` を含め忘れる → Cloudflareに反映されない
- ❌ `src/` を編集したのに `npm run build` をせずにコミット → `public/` が古いまま
- ❌ `src/_data/` のJSONを更新したのに `npm run build` をせずにコミット → 画面に反映されない
- ❌ git statusを確認せずにコミットしようとする → 変更がないのにコミット操作をしてしまう
- ❌ `public/pagefind/` をコミットし忘れる → 検索窓がサイト上で消える（2026-07-01に発生した障害）

**📌 `git status` で大量のファイルが変更されている場合について（正常な動作）：**

レイアウト（`src/_layouts/`）やインクルード（`src/_includes/`）を変更すると、
`npm run build` がそれらを使用するすべてのHTMLを再生成するため、
`public/` 配下の数百ファイルが変更済みとして表示される。
これは意図通りの正常な動作であり、すべてコミットに含めてよい。

- `src/_includes/nav-stories.njk`（共通ヘッダー）を変更 → 全記事・全エリアページが再生成される
- `src/_layouts/article.njk`（記事レイアウト）を変更 → 全記事ページが再生成される
- `src/_data/areas.json` を変更 → 全エリアページが再生成される

---

### ⚠️ ブランチ戦略（必須）

**基本方針:**

- 日常的な更新（コンテンツ更新、画像差し替え等）は直接 `main` ブランチで実施
- 大きな機能追加や実験的な変更のみ、短期的なフィーチャーブランチを使用
  - 例: 新しいセクション追加、大規模リニューアル、デザイン変更等
  - 完成したらすぐに `main` にマージしてブランチを削除

**作業時の注意:**

1. **変更前の確認** → `git status` で未コミットの変更があるか確認
2. **ユーザーの手動編集を尊重** → ユーザーが直接ファイルを編集している可能性を常に考慮
3. **フィーチャーブランチを使う場合** → 必要に応じて `git stash` で一時保存してからブランチを切り替え
4. **ブランチ切り替え時** → 変更が失われないよう、必ず stash または commit してから切り替えること

---

### 📌 タグ付けルール

**命名規則**: `YYYY.MM.パッチ番号` の形式（カレンダーバージョニング）

**例**:
```
2026.06.0 - 6月の初回リリース
2026.06.1 - 6月への追加修正
2026.06.2 - 6月への2回目の修正
```

**運用ルール**:
- タグの月番号は「対象コンテンツの月」を示す（実際の作業月ではない）
- 同じ月のコンテンツに対する修正は、パッチ番号をインクリメント
- コンテンツと無関係な修正は、現在の月のタグを使う

**タグの移動**:
- タグを間違えた位置につけた場合は、削除して再作成できる
- コマンド例:
  ```bash
  git tag -d 2026.06.0
  git push origin :refs/tags/2026.06.0
  git tag -a 2026.06.0 -m "メッセージ"
  git push origin 2026.06.0
  ```

---

## ワンページ Web サイトの生成方法

ワンページサイトの作成・編集を行う際は、必ず **`one-page-site-builder` スキル**を使用すること。
このスキルに、技術スタック（Tailwind CSS v4 / Animate.css / AOS / Lucide）、HTML 構造、固定ナビゲーション仕様、セクション構成、画像最適化方針などの能力がまとまっている。

## 記事サムネイルの管理ルール（必須）

- **1〜20記事目は必ず異なる画像を使う**。同じ画像を複数記事で使い回すことは禁止
- **21記事目以降は既存画像の再利用可**（ただし記事のイメージに合うものを選ぶこと）
- 新記事を追加する際は、既存記事のサムネイル一覧を確認してから画像を選ぶ
- 既存サムネイルの確認コマンド:
  ```bash
  grep -r "thumbnail:" src/stories/ | sort
  ```
- 画像は必ず**記事のテーマ・イメージに合うもの**を選ぶ
- Unsplash画像を取得したら、**記事内容を反映したSEO効果のある英語ファイル名にリネーム**して保存する
  - 形式: `{内容}-{場所}-{用途}.webp`（例: `specialty-coffee-interior-tokyo.webp`）
- 保存先: `public/assets/images/articles/`
- ファイル形式: WebP、200KB以下

---

## 画像の取得・最適化

- ユーザーから画像が提供されない場合、以下のコマンドでUnsplashから画像を検索して使用する
  ```bash
  node dev-tools/unsplash-search.js "キーワード"
  ```
- APIキーは `.env.local` の `UNSPLASH_ACCESS_KEY` に設定済み（変更不要）
- 動作確認は `node dev-tools/unsplash-search.js --check` で実行
- ユーザーが「画像を提供する」と言った場合、`public/assets/images/articles/` フォルダの中から更新日時が最新のファイルを使用する
  - 確認コマンド: `ls -lt public/assets/images/articles/ | head -5`
  - 最上位に表示されるファイルが最新画像

### 画像フォルダ構成（必須）

```
public/assets/images/
├── main/      → メインサイト（index.html）用画像
├── venues/    → 認定施設ページ用画像
└── articles/  → 記事サイト（11ty）サムネイル用画像
```

### Unsplash画像の保存ルール（必須・厳守）

1. **ファイルサイズ：200KB以下に必ず収める**
   - Unsplashダウンロード時のパラメータ目安：`w=800&q=70&fm=webp`（通常画像）、`w=1280&q=65&fm=webp`（ヒーロー画像）
   - ダウンロード後に `wc -c` でサイズを確認し、200KB超なら `q` を下げて再取得する

2. **ファイル名：SEOに効果的な英語名にリネームする**
   - 形式：`{内容}-{場所}-{用途}.webp`（例：`specialty-coffee-interior-tokyo.webp`）
   - 日本語・数字・記号は使わない。単語はハイフン区切り
   - 検索キーワードになる語を自然に含める（地名・業種・場面）

3. **保存先：用途に合ったフォルダに保存する**
   - メインサイト用 → `public/assets/images/main/`
   - 認定施設用 → `public/assets/images/venues/`
   - 記事サムネイル用 → `public/assets/images/articles/`

4. **フォーマット：WebP（`.webp`）を使用する**
   - Unsplashのパラメータに `fm=webp` を指定してダウンロード

- 画像ファイルサイズが **200KB超の場合は保存前に以下を案内する：**

> 画像サイズが大きいため、以下のサービスで圧縮してから再度お渡しください（目安：200KB前後）。
> - **TinyPNG** https://tinypng.com/ （PNG・JPEG対応）
> - **Squoosh** https://squoosh.app/ （Googleのツール）
> - **Optimizilla** https://imagecompressor.com/ （複数画像の一括処理可）

---

# 条件・技術スタック

## HTML・CSS・JavaScript

### 使用するライブラリ

- Tailwind CSS v4.1 以上を使用し、コード量を減らしつつ、エラーが少ない簡潔なコードを書いてください
- Tailwind CSS v4.1 以上は、CDNを使ってください（画像最適化の方が速度に寄与するので、Tailwind CSSの最適化は不要です）
- Animate.css v4.1.1 以上を使用して、適度にアニメーションをつける（派手にならないように注意）
- スクロールアニメーションは、AOS v2.3 以上を使用するか、Animate.css v4.1.1 + Intersection Observer API で実装してください
- アイコンや、ロゴは、Lucide v0.536.0 (https://lucide.dev/) を使ってください

### パフォーマンス最適化

- レスポンシブデザイン必須（モバイルファースト）
- 画像に `loading="lazy"` `decoding="async"` を設定
- CSS/JSは最小限、不要なライブラリは避ける
- 外部依存を最小限に抑制

### HTML構造

- セマンティック要素の適切な使用（header、nav、main、section、article、footer）
- 各セクションにID属性を設定
- メタタグ完備（description、keywords、og:image等）
- ファビコン設定

## デザイン要件

- Tailwind CSS の theme を使ってカラーパレットを設定
- **カラーパレット**:
  - Primary: #1C1C1C（ディープブラック）
  - Secondary: #4A4A4A（ダークグレー）
  - Accent: #B8960C（ゴールド）
  - Background: #F8F6F1（アイボリーホワイト）
  - Text: #2D2A26（ダークグレー）
- フォントは日本語に最適なものを選択、パフォーマンスを考慮する
- ホワイトスペースを効果的に使い、読みやすさを重視
- ミシュランガイドやMoocleのような、品のある権威ある雰囲気を保つ

### レスポンシブデザイン原則

**テキストの配置ルール（2文以上のテキストブロック）:**

複数文で構成されるテキストブロック（リード文、説明文など）は、以下のルールに従う：

- **モバイル表示**: 改行なしで左寄せ（`text-left`）
- **デスクトップ表示**: 適切な位置で改行し中央寄せ（`md:text-center`）
- **実装方法**: `text-left md:text-center` + `<br class="hidden md:block">`

**適用例:**
```html
<p class="text-left md:text-center text-lg mb-16 max-w-3xl mx-auto">
  最初の文章。<br class="hidden md:block">次の文章。
</p>
```

**重要**: このルールは一貫性を保つため、サイト全体で徹底すること。

---

# AEO対策 重要事項

## JSON-LD構造化データ（必須）

以下の構造化データを `<head>` 内または `</body>` 直前に設置:

1. **Organization**: TPJ組織情報
2. **FAQPage**: よくある質問
3. **WebSite**: サイト情報
4. **ItemList / Review**: 認証場所の一覧・評価データ（認証ページ）

## FAQセクション

- アコーディオン形式で実装
- AIや音声検索が「そのまま読み上げられる」簡潔な回答を心がける
- JSON-LD FAQPage と連動

## 記事FAQ（よくある質問）の書式ルール

Markdownでは1行の改行が無視されるため、**Q.**タイトルと回答が同じ行に連結して表示される。必ず `<br>` を使って明示的に改行すること。

```markdown
**Q. 質問文**<br>
回答文。
```

## コンテンツの書き方

- 自然で人間味のある文章（AIが引用しやすい）
- 具体的な情報を明記（場所名、住所、評価軸スコアなど）
- 「サードプレイス」「第三の居場所」「居場所」などのキーワードを自然に含める
- 業種横断キーワード（カフェ・ホテル・スパ・コワーキング・サウナ）を文脈に合わせて使う

---

# TPJセレクト 運用ルール

## グレード表記禁止（絶対厳守）

**TPJセレクト（`/stories/select/`）ではグレード表記を一切表示しない。**

- ❌ 一覧ページのカードにグレードバッジ（Certified / Silver / Gold / Platinum / Flagship）を表示しない
- ❌ 施設詳細ページの施設名上部にグレードバッジを表示しない
- ❌ 関連施設カードにグレードバッジを表示しない
- ✅ グレード情報はJSON-LDメタデータ内にのみ保持する（AEO・構造化データ目的）
- ✅ 一覧カードには「TPJセレクト」バッジ（虫眼鏡アイコン）と業種カテゴリのみ表示する

**理由**: TPJセレクトは「編集部が発掘した居場所」という文脈の掲載であり、グレード（品質評価の段階）は現時点で掲載施設側との正式な合意がない。グレード表記は将来の有償認証制度（Silver以上の正式認証・施設側同意取得後）に備えて保留する。

**実装状態（2026-07-05確定）：**
- `src/stories/select/index.njk` — TPJセレクトグリッドカードにグレードなし ✅
- `src/_layouts/venue.njk` — 施設詳細ページにグレードバッジなし ✅（JSON-LDのみ）

## Flagship施設の特別表示ルール（2026-07-05追加）

`grade_slug: "flagship"` の施設のみ、詳細ページで以下の専用UIを適用する。
これはグレード表記禁止ルールの**例外**ではなく、Flagshipを「実証店舗」という別文脈で扱うための独立した表示仕様。

### 適用箇所（`src/_layouts/venue.njk` 内の条件分岐で制御）

| 箇所 | Flagship | それ以外（TPJセレクト） |
|---|---|---|
| パンくず | Stories / **Flagship** / 店舗名 | Stories / TPJセレクト / 店舗名 |
| カテゴリ横バッジ | カテゴリ名 ＋ **Flagshipバッジ**（ゴールド枠） | カテゴリ名のみ |
| 日付ラベル | **Flagship認定：** 日付 | TPJセレクト掲載：日付 |
| 底部セクション | **Flagshipバッジ**（ゴールド枠） ＋ 説明文 ＋ リンク | テキスト ＋ リンク |
| JSON-LD BreadcrumbList | position 3 の name: `"Flagship"` | name: `"TPJセレクト"` |

### 底部セクションのコピー（Flagship固定）

- バッジ：`Flagship`
- 説明文：「Third Place Japanの実証店舗として、理想の空間づくりを検証しています。」
- リンク：「TPJについて知る」→ メインサイトトップ（`site.main_site_url + "/"`）

### 実装パターン

```njk
{% if venue.grade_slug == "flagship" %}
  {# Flagship専用UI #}
{% else %}
  {# 通常TPJセレクトUI #}
{% endif %}
```

**注意**：一覧ページ（`src/stories/select/index.njk`）のカードには引き続きグレード表記なし。Flagship固定枠のみ表示位置が異なる（グリッド外の独立セクション）。

## venues.json 施設データ管理ルール

- 施設データは `src/_data/venues.json` に一元管理する
- `published: true` の施設のみが一覧・詳細ページに表示される（非表示にする場合は `published: false`）
- `grade_slug: "flagship"` の施設は一覧ページの「Flagship」固定枠に表示される（現在はGBC 1件）
- `grade_slug` が flagship 以外（certified / silver / gold / platinum）の施設が「TPJセレクト」グリッドに表示される
- **施設を追加する際は必ず `project-docs/TPJセレクト掲載基準ルールブック.md` の7基準・カテゴリ配分・大手禁止ルールを確認すること**

## citation（紹介文）バリエーション化ルール

- **「Third Place Japan編集部がTPJセレクトに選出。」という完全に同一の一文を複数施設で使い回すことは禁止**
- 施設ごとに表現を変えた文章を書く。最低5パターン程度を意識してローテーションする
  - 例：「〜として、静かに紹介します。」「〜と出会いました。」「〜をTPJセレクトに加えます。」「〜が選ばれた理由はここにあります。」
- 新規施設を追加する際は、既存citationの文体を参照して重複しないパターンを選ぶ
- **理由**：件数が増えるほど機械的な量産コンテンツに見え、SEO上「重複表現の多用」として不利になる可能性がある

## カテゴリ配分ガイドライン（次回30件到達時）

**現状17件の内訳（2026-07-05時点）：**

| カテゴリ | 現在 | 目標（30件時） |
|---|---|---|
| カフェ | 8件（約47%） | 40%（12件） |
| ウェルネス | 3件（約18%） | 15%（5件） |
| 宿泊 | 2件（約12%） | 15%（5件） |
| コワーキング | 1件（約6%） | 10%（3件） |
| その他 | 3件（約18%） | 20%（6件） |

**増件時の優先順位：**
- コワーキング・宿泊を優先的に補充する
- カフェの新規追加は一旦控える（比率が高すぎるため）

## venues.json 必須フィールド一覧

```json
{
  "slug": "店舗の一意識別子（英数字・ハイフンのみ）",
  "name": "店舗名（日本語）",
  "name_en": "店舗名（英語）",
  "citation": "編集部の評論文・AEOで引用される一文（日本語）",
  "grade": "Flagship / Platinum / Gold / Silver / Certified",
  "grade_slug": "flagship / platinum / gold / silver / certified",
  "category": "カテゴリ名（日本語）",
  "category_slug": "カテゴリslug（src/_data/categories.jsonを正とする）",
  "area_primary": {
    "prefecture_slug": "tokyo等",
    "prefecture": "東京都等",
    "city_slug": "shibuya-ku等",
    "city": "渋谷区等",
    "area_slug": "omotesando等",
    "area": "表参道等"
  },
  "nearest_stations": ["最寄駅名"],
  "axes_primary": "主評価軸の表示名",
  "axes_primary_slug": "comfort / silence / special / story / revisit / record / inbound",
  "axes_secondary": ["副評価軸の表示名"],
  "axes_secondary_slugs": ["副評価軸のslug"],
  "scores": { "comfort": 0, "silence": 0, "special": 0, "revisit": 0, "inbound": 0, "record": 0, "story": 0 },
  "address": "住所（日本語）",
  "address_en": "住所（英語）",
  "lat": 緯度,
  "lng": 経度,
  "url": "公式またはおすすめURL",
  "image": "/assets/images/venues/画像ファイル名（なければ /assets/images/main/japan-cafe-interior-hero.webp）",
  "certified_date": "YYYY-MM-DD（掲載日）",
  "has_en": false,
  "published": true
}
```

**7軸のslug↔表示名 対応（必ず統一）：**
| slug | 表示名 |
|---|---|
| comfort | 五感品質・素材 |
| silence | 静けさの再現 |
| special | 特別感・ギフト体験 |
| story | ストーリー継承 |
| revisit | 愛用・回遊価値 |
| record | シェア・記録性 |
| inbound | インバウンド適性 |

---

# TPJ記事管理（11ty / /stories/）

## 概要

11ty（Eleventy）を使った記事管理システム。`/stories/` 以下で動作。

## ビルドコマンド

```bash
npm run dev    # ローカル確認（検索機能は動作しない）
npm run build  # 本番ビルド（Cloudflare Pagesが自動実行）
```

**重要**: `npm run dev` では検索（Pagefind）は動作しない。検索テストは `npm run build` 後に `npx serve public` で確認。

## 検索UIの設定値

- **1回の表示件数（PAGE_SIZE）：25件**（`src/_includes/nav-stories.njk` 内の `const PAGE_SIZE = 25`）
  - 初回表示・「さらに表示」ボタン押下ともに25件ずつ表示する
  - 変更する場合はこの定数のみ編集すればよい

## フッターの「最新記事」・「記事一覧」ルール（2026-07-05確定）

### 記事一覧リンクのURL

- **日本語フッター**（`src/_includes/footer-stories.njk`）の「記事一覧 →」は `/stories/latest/` を指す
- **英語フッター**（`src/_includes/footer-journal.njk`）の「All Articles →」は `/en/stories/` を指す
  （`/en/stories/latest/` は未作成のため。作成した場合は `/en/stories/latest/` に変更する）

### 最新記事の取得方法

`collections.articles` はすでに **新しい順（date降順）** でソート済み（`.eleventy.js` の `addCollection` で定義）。

```njk
{# 正しい実装 #}
{% set latestPosts = collections.articles | rejectLang("en") | head(4) %}

{# ❌ 禁止：| reverse を使うと古い記事4件が取れてしまう #}
{% set latestPosts = collections.articles | reverse | head(4) %}
```

- `| reverse` は使わない
- 日本語フッターでは `rejectLang("en")` で英語記事を除外する
- 英語フッターでは `head(4)` のみ（英語記事フィルタリングは今後の課題）

### TPJガイド記事の取得

- `collections.aboutArticles`（`category_slug: "about"` かつ `tags: articles` の記事を新しい順で返す）を使う
- 一覧リンクは `/stories/about/` を指す

## フロントマターのテンプレート

```markdown
---
layout: article.njk
title: 記事タイトル
description: 115〜160字。一文目で結論を完結させ、AI単体引用に耐える密度で書く
date: 2026-06-14
tags:
  - articles
  - カテゴリ名
category_slug: カテゴリslug
area_slug: エリアslug（任意・エリアページに記事を表示する場合）
area_name: エリア名（area_slugを設定した場合は必須）
thumbnail: /assets/images/articles/xxx.jpg
---
```

**🚨 `category_slug` は必須フィールド（絶対に省略しない）🚨**

- カテゴリページ（`/stories/{slug}/`）への記事表示は `category_slug` で制御される
- `tags` にカテゴリ名を入れるだけでは**カテゴリページに表示されない**
- カテゴリslugの一覧は `src/_data/categories.json` を正とする
  - 例：カフェ・スペシャルティコーヒー → `cafe-specialty-coffee`
  - 例：コワーキング・シェアオフィス → `coworking`
  - 例：神社・寺院・パワースポット → `shrine-temple`
- 記事生成時・ファイル保存前に必ず確認すること

**`area_slug` / `area_name` のルール**

- エリアページ（`/stories/area/{pref}/{city}/{area}/`）に記事を表示したい場合に設定する
- `area_slug` は `src/_data/areas.json` に定義されたスラッグを使う（例：`omotesando`、`shibuya`、`ginza`）
- `area_slug` を設定する場合は `area_name` も必ずセットで設定する（例：`area_name: 表参道`）
- 記事の**主題がそのエリアについて書かれている**場合のみ設定する。施設が所在するだけの場合は設定しない
- 1記事につき1エリアのみ設定可（複数エリアにまたがる記事は最もメインのエリアを選ぶ）
- 設定を忘れると該当エリアページに記事が表示されないため、記事保存前に必ず確認すること
- **既存記事にも遡って設定すること**。`area_slug` は新機能として導入されたため、過去に作成した記事には設定されていない。エリアについて書かれた既存記事を見つけたら、都度 `area_slug` / `area_name` を追加してビルド・コミットすること

**🚨 `area_name` は全記事に必須（新ルール）🚨**

- **`area_name` はすべての記事に設定すること**。`/stories/` トップの「最新記事」セクションでエリア名を表示するために使用する
- 特定の街・エリアを主題とする記事 → そのエリア名を設定（例：`area_name: 銀座`）
- 複数エリアにまたがる区レベルのピラー記事 → 対象の区名を設定（例：`area_name: 中央区`、`area_name: 渋谷区`）
- `area_name` のみ設定し `area_slug` を省略することも可（表示のみ目的の場合）
- `area_slug` は引き続き任意。エリアページへの記事掲載が必要な場合のみ設定する

**🚨 記事ページのエリア階層リンク表示ルール 🚨**

記事ページ（`src/_layouts/article.njk`）の日付・著者行の右側に、エリア階層リンクが自動表示される。

| フロントマター設定 | 表示内容 |
|---|---|
| `area_slug` あり | `東京都 / 中央区 / 銀座`（3階層・各リンク付き） |
| `area_name` のみ（区名）| `東京都 / 中央区`（2階層・各リンク付き） |
| `area_name` のみ（マッチなし）| `銀座`（テキストのみ、リンクなし） |
| どちらも未設定 | 非表示 |

**実装フィルター（`.eleventy.js`）：**
- `areaInfo(areaSlug)` — `area_slug` → 都道府県・市区町村・エリアの情報オブジェクトを返す
- `cityInfo(cityName)` — `area_name`（区名）→ 都道府県・市区町村の情報オブジェクトを返す
- データソース：`src/_data/areas.json`

**注意：**
- `about` カテゴリの記事はエリア表示対象外（日付・著者行自体が非表示）
- `area_name` を区名（例：`中央区`）に設定すると `cityInfo` が都道府県まで補完する
- `area_name` をエリア名（例：`銀座`）に設定した場合、`cityInfo` で都市がマッチしないためテキストのみ表示（リンクなし）。エリアページへのリンクが必要なら `area_slug` も設定すること

## カテゴリ（15カテゴリ固定・追加不可）

**有償対象（将来）**
- 高級ホテル・ホテルラウンジ
- 温泉旅館・高級旅館
- 貸切ヴィラ・一棟貸しの宿
- レストラン・ダイニング
- カフェ・スペシャルティコーヒー
- バー・ウイスキー・カクテル
- スパ・サウナ・ウェルネス施設
- 高級サロン・プライベート
- コワーキング・シェアオフィス
- 会員制ラウンジ・クラブ

**無償（現フェーズの注力対象）**
- 山・自然体験・グランピング
- リトリート・禅体験
- 神社・寺院・パワースポット
- インバウンド観光・体験施設
- 文化・スポーツ・レジャー施設

**注意**: カテゴリはこの15に固定。追加・変更はユーザーの明示的な指示があった場合のみ行う。詳細スラッグは `project-docs/STORIES-SITE-SPEC.md` セクション3を正とする。

**複数カテゴリのルール**:
- 原則1カテゴリー。内容が自然に跨る場合のみ最大2つまで選択可
- 3つ以上選びたくなったら「記事の切り口を絞る」サインと考え、ユーザーに確認する

---

## 記事執筆開始時の確認事項（必須）

「記事を書きたい」という意図が伝わったら、質問の**前に必ず**以下を問いかけること：

> 作業を始める前に、以下のコマンドで最新の状態に更新してください：
> ```
> git pull origin main
> ```
> 完了したら教えてください。

ユーザーが完了を確認したら、以下の質問に進む：

```
1. カテゴリ：以下の15から選んでください（原則1つ、内容が跨る場合は最大2つまでOK）
   【無償・現フェーズ注力】神社・寺院 / 山・自然 / リトリート・禅 / インバウンド / 文化・スポーツ施設
   【有償対象・将来】カフェ / ホテル / 温泉旅館 / ヴィラ / レストラン / バー / スパ・サウナ / サロン / コワーキング / 会員制ラウンジ

2. 書きたいこと・伝えたいこと：メモ程度でOKです

3. まとめセクションをつけますか？（つける / つけない）

4. 記事末尾にQ&Aをつけますか？（つける / つけない）※AEO対策のため「つける」を推奨

5. 特定のエリア（街・地域）について書く記事ですか？（はい / いいえ）
   「はい」の場合 → `area_slug` を設定してそのエリアページに記事を表示します。対象エリアを教えてください

6. 画像：提供しますか？Unsplashで探しますか？
```

## 記事の下書き完成後（必須）：カテゴリーの評価と報告

下書きをユーザーに渡す前に、**Claudeが記事の内容を読んでカテゴリーの適切さを自己評価すること**。単なる確認ではなく、問題があれば理由を添えて指摘する。

**評価の観点（すべてチェックすること）：**

1. **主題の一致** — カテゴリーが「記事の主題」を表しているか。舞台（施設の業種）ではなく、記事が何について書かれているかで判断する
2. **ダブりによる焦点のボケ** — 複数カテゴリーの場合、それぞれが実質的な比重を持っているか。片方が「ついでに触れた程度」なら外す
3. **業種カテゴリーの過剰付与** — 「舞台がカフェだからカフェカテゴリー」「ホテルの話だからホテルカテゴリー」という理由だけでカテゴリーをつけていないか。施設の体験・空間・文化が主題でない限り業種カテゴリーをそのまま使わない
4. **より適切なカテゴリーの存在** — 選ばれたカテゴリーより正確なものが他にないか（例：カフェ全般より「神社・寺院」のほうが正確な記事もある）

**下書きと一緒に渡すメッセージの形式：**

問題がない場合：
> 📌 **カテゴリー：〔カテゴリー名〕**
> 記事の主題と一致しています。このまま保存します。

変更を提案する場合：
> 📌 **カテゴリーの変更を提案します。**
> 当初の選択：〔元カテゴリー〕
> 提案：〔新カテゴリー〕
> 理由：〔具体的な理由。例：「この施設はカフェが舞台だが記事の主題は禅体験なので、カフェより神社・寺院のほうが正確です」〕
> このまま変更してよいですか？

- カテゴリーが確定してから `frontmatter` に記入してファイルを保存する
- ユーザーが最初に選んだカテゴリーを尊重しつつ、完成した記事の内容と照合して判断する

---

## 自動生成されるファイル

**gitignore対象（コミット不要）：**
- `public/articles/` — ビルド時に生成
- `public/articles-sitemap.xml` — 記事サイトマップ（記事追加時に自動更新）

**コミット必須（検索機能のため）：**
- `public/pagefind/` — 検索インデックス。**`npm run build` 後は必ずコミットに含めること**
  - gitignore対象から外してあるため、`git add public/pagefind/` でステージングされる
  - コミットしないと Cloudflare Pages に検索インデックスが届かず、検索窓が表示されない

---

# ピラー記事生成プロンプト（AEO対応・定義ハブ型）

## 使い方

入力変数（分野・地域・地域の階層・想定読者）を差し替えて使う。
記事執筆開始時の確認事項（git pull・カテゴリ選択等）は通常フローに従うこと。

## プロンプト本文

```
# 役割
あなたは Third Place Japan（TPJ）の編集者です。TPJは家でも職場でもない
「第三の居場所＝サードプレイス」を、独自の7軸基準で評価・認証する日本初の
プラットフォームです。社会学者レイ・オルデンバーグのサードプレイス理論を
土台に、現代日本向けに再解釈した視点で記事を書きます。

# タスク
以下の【分野 × 地域】について、AEO（AI回答エンジン最適化）を前提とした
「定義ハブ ＋ 推薦」両立型のピラー記事を、本文として完成形で生成してください。

# 入力変数（ここだけ差し替える）
- 分野: 〔例：カフェ・スペシャルティコーヒー〕
- 地域: 〔例：表参道〕
- 地域の階層: 〔例：東京都 → 渋谷区/港区 → 表参道エリア〕
- 想定読者: 〔例：ひとり時間を求める20〜40代／出張者／インバウンド〕

# TPJ 7軸評価基準（記事中で必ずこの語彙を使う）
1. 居心地・空間品質（五感品質・素材）
2. 静寂性・プライバシー（静けさ）
3. 特別感・非日常性（ギフト体験）
4. ストーリー・背景への共感（物語の継承）
5. 再訪・継続価値（また来たくなるか）
6. 記録・シェア体験（記録性）
7. インバウンド・多言語対応（外国人適性）

# 絶対ルール
- 実在の店名・施設名は一切出さない。「〇〇通りの角にある築古ビルを改装した一軒」
  のように、立地・空間・素材・客層の「条件と特徴」で具体的に描写する。
- 事実として断定するのは、地域の文化・歴史・地理・街の性格など検証可能な背景のみ。
  個別店の営業情報・価格・評判は書かない。
- 誇大表現を避け、TPJの落ち着いた知的なトーンを保つ。
- AIが引用しやすいよう、各見出し直下の最初の1〜2文で結論を述べる
  （結論先出し）。

# 記事の構成（この見出し順で本文を書く）

## H1（タイトル）
「〔地域〕で〔分野〕をサードプレイスにする：〔地域〕の第三の居場所完全ガイド」
の型を基本に、自然な日本語で。

## リード文（120〜180字）
この地域でこの分野が、なぜサードプレイスとして機能するのかを一段落で要約。
AIがそのまま引用できる密度で書く。

## 1. 〔地域〕という街と「第三の居場所」
- 地域の文化的背景・歴史・街の性格（検証可能な事実のみ）
- なぜこの街が、家でも職場でもない居場所を必要とする／生み出すのか
- 地理的特徴（人の流れ、表通りと裏通り、再開発の有無 等）

## 2. 〔地域〕で〔分野〕が果たすサードプレイスの役割
- この分野がこの街でどんな居場所機能を担うか
- オルデンバーグの中立性・常連・アクセス性の観点で解説

## 3. 〔地域〕の〔分野〕をTPJ7軸で読む
- 7軸それぞれについて、この地域・分野なら何が問われるかを小見出しで解説
- 「こういう空間は居心地軸が高い」と条件ベースで語る（店名なし）

## 4. 〔地域〕で"いいサードプレイス"を見分ける条件
- 読者が自分で良い場所を選ぶためのチェックポイントを、特徴・立地・空間で
- 表通り型／裏通り型、滞在時間別、ひとり/同伴別など切り口を分ける

## 5. 〔地域〕のサードプレイス文化を支える街の文脈
- 関連する地域イベント・季節性・周辺施設の性格（事実ベース）
- この街でしか得られない非日常性・特別感（軸3・4と接続）

## 6. インバウンド視点：〔地域〕の〔分野〕
- 外国人来訪者にとっての分かりやすさ・体験価値（軸7）
- 言語・予約・文化的ハードルの観点

## よくある質問（FAQ・5問）
読者が実際にAIへ尋ねそうな質問を5つ。各回答は2〜3文で結論先出し。
例:「〔地域〕で静かに過ごせる〔分野〕は？」「〔地域〕の〔分野〕の特徴は？」

## まとめ
この街×この分野のサードプレイスの核心を3文で。TPJ認証への自然な言及で締める
（「TPJではこうした空間を7軸で評価・認証しています」程度、宣伝過多にしない）。

# 文字数
本文 2,800〜4,000字。FAQ含む。

# トーン
知的・落ち着いた・断定しすぎない。読者を急かさない。
オルデンバーグ理論への敬意を土台にしつつ、現代日本の生活実感に接続する。
```

## 使用上の注意

- **店名・施設名は一切出さない**のがこのプロンプトの根幹ルール。条件・空間描写で代替する
- 生成後は通常の記事フロー（カテゴリ確認・画像・内部リンク・FAQ書式）を適用する
- `〔地域〕 × 〔分野〕` の組み合わせ数だけピラー記事を量産できる設計
- 地域階層（都道府県→市区町村→エリア）を指定することで、SEOの地名キーワードが自然に埋め込まれる

---

# 記事品質チェックルール（制作憲法違反パターン）

記事生成・編集の完了前に、以下の違反パターンがないか必ず確認すること。
過去の実際の訂正事例から抽出したルールのため、再発防止のために厳守する。

## ❌ 価格の断定（最頻出違反）

**禁止**：入場料・利用料・コーヒー代など具体的な金額を固定値として記載する
- ❌ 「入場料500円」「¥500」「60分施術で8,000円〜」
- ❌ 「For ¥500, the inner garden offers...」

**許容表現**：
- ✅ 「有料・要事前確認」
- ✅ 「small entry fee — confirm current rates before visiting」
- ✅ 「有料入場（料金は公式サイトで確認）」
- ✅ 「施設によって価格帯が異なる」

**理由**：料金は変動する情報。誤情報になるリスクがある。

---

## ❌ 混雑・混み具合の断定

**禁止**：混雑状況を断定的・固定的に記述する
- ❌ 「混雑は激しく、静寂性は低い」
- ❌ 「週末は必ず混む」

**許容表現**：
- ✅ 「混雑しやすく、時間帯によっては静寂性が下がる傾向がある」
- ✅ 「ピーク時間帯は混雑が予想される」
- ✅ 「平日早朝が比較的落ち着いている」

**理由**：時間帯・季節・イベントによって変動する情報のため。

---

## ❌ 施設名の断定・推薦（description / FAQ / 見出しに特に注意）

**禁止**：特定施設名を「おすすめ」「〜がある」と断定する
- ❌ `description` に「東京都現代美術館・国立新美術館などは〜」
- ❌ FAQ回答に「円覚寺・東慶寺周辺がおすすめです」
- ❌ 本文に「高尾山薬王院は〜を提供する」（個別施設の断定）

**許容表現**：
- ✅ 「鎌倉の臨済宗の大寺院」「北鎌倉の大寺院・小規模な禅寺」
- ✅ 「高尾山の真言宗の山岳寺院」
- ✅ 「静寂性・特別感の高い美術館・文化施設」

**補足**：地名（鎌倉・高尾・浅草）の言及はOK。「〇〇寺」「〇〇美術館」の固有名が問題。
ピラー記事では施設名を出さないのが大原則。コンセプト記事でも FAQ・descriptionは同ルールを適用する。

---

## ❌ 英語記事のog:locale設定

**禁止**：英語記事に `og:locale: ja_JP` を設定する（自動設定に任せていれば問題なし）

**ルール**：`base.njk` が `lang` フロントマターで自動切り替えする（2026-07-01実装済み）
- `lang: en` → `og:locale: en_US`
- `lang: ja`（または未設定）→ `og:locale: ja_JP`

**確認方法**：英語記事を追加したらビルド後に `grep "og:locale"` で確認する。

---

## ❌ 英語記事と日本語記事のサムネイル共有

**禁止**：英語記事と日本語記事で同じサムネイル画像を使い回す
- ❌ 日本語インバウンド記事と英語トラベラーガイドが同じ `tokyo-inbound-visitor-cafe-experience.webp` を使う

**ルール**：英語記事には英語版専用のサムネイルを用意する
- Unsplashで別キーワードで検索し、ファイル名に `-en` サフィックスをつけて区別する
- 例：`tokyo-third-place-guide-travelers-en.webp`

---

# JSON-LD実装ルール（Schema.org バリデーション対応・2026-07-02追加）

Schema.org バリデーターで検出された実際のエラー・警告から抽出したルール。
実装・テンプレート編集時に必ず確認すること。

## ✅ Nunjucksテンプレートでの動的テキスト出力（必須パターン）

**禁止**：`"name": "{{ post.data.title }}"` または `| replace('"', '\\"')` で直接出力
- NunjucksがHTML自動エスケープを行い `"` → `&quot;`、`'` → `&#39;` になる
- `<script type="application/ld+json">` 内でも同様に発生する

**正しい実装**：
```njk
"name": {{ post.data.title | dump | safe }}
```
- `dump` = JSON.stringify（適切なエスケープ）
- `safe` = Nunjucksによるさらなるエスケープを防ぐ
- ダブルクォート込みの文字列として出力されるため、`"name":` の後に引用符を書かない

---

## ❌ Organization に使えないプロパティ（警告の原因）

以下のプロパティは `Organization` スキーマには不適切なため、使用しない。

| プロパティ | 問題 | 代替 |
|---|---|---|
| `sameAs: []` | 空配列は無意味・警告の原因 | URLが確定するまでプロパティ自体を省略 |
| `knowsAbout: [文字列配列]` | Schema.org は `Thing` オブジェクト型を期待する | 省略するか `Thing` 型で記述 |
| `inLanguage` | `CreativeWork` 用プロパティ。`Organization` には不適切 | 省略する |

**現在の `base.njk` Organization 実装（確定版）：**
```json
{
  "@type": "Organization",
  "name": "Third Place Japan",
  "alternateName": ["TPJ", "サードプレイスジャパン"],
  "url": "...",
  "logo": { "@type": "ImageObject", "url": ".../tpj-logo.png", "width": 900, "height": 275 },
  "description": "...",
  "foundingDate": "2025",
  "areaServed": { "@type": "Country", "name": "Japan" }
}
```
これ以上プロパティを追加する場合は、Schema.org の `Organization` 定義で有効かを事前確認すること。

---

## ❌ speakable を使えない型に設定する

**禁止**：`speakable` は `CreativeWork` 系（Article / BlogPosting / WebPage）**専用**プロパティ。以下には使用不可：

| 型 | speakable | 理由 |
|---|---|---|
| `WebSite` | ❌ 禁止 | 警告が出る（2026-07-02検出・修正済み） |
| `LocalBusiness` | ❌ 禁止 | 警告が出る（2026-07-06検出・修正済み） |
| `Organization` | ❌ 禁止 | 同上 |
| `Article` / `BlogPosting` | ✅ 使用可 | CreativeWork系のみ有効 |

**現状の実装（確定）**：
- `src/stories/index.njk`（WebSite）→ `speakable` なし ✅
- `src/_layouts/venue.njk`（LocalBusiness）→ `speakable` なし ✅
- `src/_layouts/article.njk`（BlogPosting）→ `speakable` あり ✅
- `src/_layouts/article-area.njk`（Article）→ `speakable` あり ✅

---

## ❌ ItemList.ListItem に url プロパティを直接使う / @type:Article を指定する

**禁止①**：`url` を `ListItem` の直接プロパティにする
```json
{ "@type": "ListItem", "position": 1, "url": "https://..." }
```

**禁止②**：`item` に `@type: Article` を指定する
```json
{
  "item": { "@type": "Article", "@id": "https://...", "name": "タイトル", "url": "https://..." }
}
```
→ Google が「Article なら image・author・headline も必要」と警告を出す（2026-07-06 検出）

**正しい実装**：`@type` を省略し `@id`・`name`・`url` のみ記述する
```json
{
  "@type": "ListItem",
  "position": 1,
  "item": {
    "@id": "https://...",
    "name": "記事タイトル",
    "url": "https://..."
  }
}
```
- `@type` を省略しても `ItemList` として有効。余計な型宣言が警告を生む
- 対象ファイル：`src/stories/index.njk`（ItemList ブロック）

---

## ❌ Organization ロゴにヒーロー画像を使う

**禁止**：Organization/Publisher の `logo.url` にサイトのキービジュアル・ヒーロー画像を使う
- ❌ `"url": "{{ site.url }}/assets/images/main/japan-cafe-interior-hero.webp"`

**ルール**：ロゴ専用画像を指定する
- ✅ `"url": "{{ site.url }}/assets/images/main/tpj-logo.png"`
- ロゴ画像の推奨サイズ：横幅600px以上、アスペクト比1:1〜4:1程度
- 現在の画像：`public/assets/images/main/tpj-logo.png`（900×275px、白背景）

**影響ファイル**（すべて `tpj-logo.png` に統一済み）：
- `src/_layouts/base.njk`
- `src/_layouts/article.njk`
- `src/_layouts/article-certified.njk`
- `src/_layouts/article-area.njk`

---

## ✅ Article系スキーマの必須フィールド（article.njk / article-certified.njk / article-area.njk）

Article/BlogPosting スキーマでは以下を必ず守ること（2026-07-06 修正・確定）。

**`image` は `ImageObject` で記述する（文字列URL禁止）**
```json
"image": {
  "@type": "ImageObject",
  "url": "https://thirdplacejapan.com/assets/images/articles/xxx.webp",
  "width": 800,
  "height": 450
}
```
- ❌ `"image": "https://..."` — 文字列URLは Rich Results Test で「重大ではない問題」として検出される
- 記事サムネイルの標準サイズ：width 800 / height 450（16:9）

**`publisher.logo` に `width`・`height` を付ける**
```json
"publisher": {
  "@type": "Organization",
  "name": "Third Place Japan",
  "logo": {
    "@type": "ImageObject",
    "url": "https://thirdplacejapan.com/assets/images/main/tpj-logo.png",
    "width": 900,
    "height": 275
  }
}
```

**`author.knowsAbout` は使わない**
- ❌ `"knowsAbout": ["サードプレイス", ...]` — Organization に不適切（文字列配列）
- `author` に含めてよいのは `@type`・`name`・`url`・`description` のみ

---

## ✅ ItemList の言語フィルタリング（日英混在環境）

**問題**：`collections.articles` には日本語記事・英語記事の両方が含まれる
- 日本語ページ（`/stories/`）の ItemList に英語記事URLが混入してしまう

**実装**：カスタムフィルター `rejectLang` を使用
```njk
{% set recentPosts = collections.articles | rejectLang("en") | head(8) %}
```

**フィルター定義**（`.eleventy.js`）：
```js
eleventyConfig.addFilter("rejectLang", (array, lang) =>
  (array || []).filter(item => item.data && item.data.lang !== lang)
);
```

**注意**：Nunjucks の `selectattr` フィルターはドット記法（`data.lang`）のパス解決に不具合があり使用不可。必ず `rejectLang` カスタムフィルターを使うこと。

---

## ✅ 認証施設の Review + reviewRating（venue.njk 必須）

認証店舗詳細ページには `Review` JSON-LD が必要（Google リッチリザルト対象）。

**グレード → 数値マッピング**（`venue.njk` 内で定義）：
```njk
{%- set gradeNums = {"Certified": 1, "Silver": 2, "Gold": 3, "Platinum": 4, "Flagship": 5} -%}
```

**JSON-LD 出力形式**：
```json
{
  "@type": "Review",
  "itemReviewed": {
    "@type": "LocalBusiness",
    "name": "施設名",
    "address": { "@type": "PostalAddress", "addressLocality": "エリア", "addressCountry": "JP" }
  },
  "author": { "@type": "Organization", "name": "サードプレイスジャパン編集部" },
  "reviewRating": {
    "@type": "Rating",
    "ratingValue": 3,
    "bestRating": 5,
    "worstRating": 1
  },
  "reviewBody": "引用文",
  "additionalProperty": [
    { "@type": "PropertyValue", "name": "五感品質・素材", "value": 8 },
    { "@type": "PropertyValue", "name": "静けさの再現", "value": 7 },
    { "@type": "PropertyValue", "name": "特別感・ギフト体験", "value": 6 },
    { "@type": "PropertyValue", "name": "ストーリー継承", "value": 7 },
    { "@type": "PropertyValue", "name": "愛用・回遊価値", "value": 8 },
    { "@type": "PropertyValue", "name": "シェア・記録性", "value": 5 },
    { "@type": "PropertyValue", "name": "インバウンド適性", "value": 6 }
  ]
}
```

**`LocalBusiness.aggregateRating` の `ratingCount` ルール：**

```json
"aggregateRating": {
  "@type": "AggregateRating",
  "ratingValue": 6.7,
  "bestRating": 10,
  "worstRating": 0,
  "ratingCount": 1,
  "reviewCount": 1
}
```

- **`ratingCount` は必ず `1`**（TPJ編集部による1回の評価）
- ❌ `ratingCount: 7` は禁止。「7軸で評価した」からといって7を入れると、Googleは「7人が評価した」と解釈してしまう
- `ratingValue` は7軸スコアの平均値（0〜10スケール）
- 7軸の個別スコアは `LocalBusiness.additionalProperty` と `Review.additionalProperty` の両方に記載する

---

# SEO・AEO記事作成指針

## メディア名の表記ルール（SEO対策・必須）

| 表記 | 使い方 |
|------|--------|
| **Third Place Japan** | メインの表記。記事冒頭・見出し・まとめ・Q&Aなど重要な箇所で使う |
| **サードプレイスジャパン** | 日本語検索ヒット用。本文中に自然な形で数回使う（1記事あたり3〜5回を目安） |
| **TPJ** | 同一段落内での2回目以降の言及など、繰り返しを避けたい箇所に使う |

## タイトル作成のルール

記事タイトルはSEO対策の中で最も重要な要素。以下の3つのキーワードグループを**必ず自然な形でタイトルに含める**こと。

**① 業種ワード（記事のカテゴリに対応する業種を1つ）：**
- カフェ・コーヒー / ホテル・ラウンジ / 温泉旅館 / レストラン / バー / スパ・サウナ / コワーキング / 神社・寺院 / 自然・グランピング 等
- 例：「東京のスパで整う」「京都の神社でリトリート体験」

**② 地名（1〜2つ）：**
- 東京 / 日本 / 各都市名（京都・大阪・北海道等）/ 地域名
- タイトルに地名が入ることで「エリア検索」にヒットしやすくなる

**③ 体験・目的ワード（自然に入れられる場合）：**
- サードプレイス / 居場所 / 第三の場所 / 認証 / 体験 / インバウンド
- 無理に入れず、記事の内容と自然に結びつく場合のみ使用する

**タイトル作成のルール：**
- 「〜してみた」「〜に行ってきました」「〜が最高だった」など体験型タイトルはAIに引用されやすい
- 30〜35文字以内を目安（長すぎると検索結果で切れる）
- 検索意図を想定して立てる（「東京 サードプレイス おすすめ」「京都 カフェ 居場所」「スパ 整う 東京」など）
- 下書きを書き始める前に、SEO/AEOを意識したタイトル案を2〜3個提示してユーザーに選んでもらう

## 必須キーワード（全記事に含める）

- **「サードプレイス」または「第三の居場所」** → 全記事に必ず1回以上
- **「日本」** → 全記事に必ず1回以上。「日本のサードプレイス」「日本初の認証」「日本全国」など文脈に合う表現で使う
- **「東京」または対象施設の都市名** → 全記事に必ず1回以上。東京の施設なら「東京カフェ」「東京のサードプレイス」、京都なら「京都の旅館」など対象地に合わせる
- **業種キーワード** → 記事の対象に応じてカフェ・ホテル・スパ等を自然に含める

## 地名キーワードの挿入戦略（SEO・AEO最重要）

検索ボリュームが大きい地名キーワードは全記事に必ず自然な形で入れること。

- **施設の所在地名** → 全記事に必ず1回以上。「渋谷区」「新宿」「京都・嵐山」など正確な地名を使う
- **最寄り駅・アクセス情報** → まとめ・FAQ・アクセス説明など文脈に合う場所に挿入。「〇〇駅から徒歩5分」「〇〇空港から1時間」など
- **広域地名** → 「東京都内」「関東」「近畿」「九州」など広域ワードを文脈に合わせて追加（全記事8割を目安）
- 不自然な詰め込みは禁止。あくまで文章の流れの中に溶け込ませる

## 周辺スポット・観光キーワード（SEO・AEO対策）

施設周辺の観光スポット・交通ハブ・自然・文化施設などのワードが少ない記事には自然に追加すること。

- 全記事の7割を目安に含める（無理に詰め込まない）
- FAQ回答・まとめ・アクセス説明など、文脈に合う場所に1〜2ワード程度
- 例：「〇〇神社の近く」「△△公園の帰り道に」「□□美術館からアクセスしやすい」
- 既存の文章に溶け込む形で追加し、不自然にならないよう注意する

## FAQ（Q&A）の書き方

- AIや音声検索が「そのまま読み上げられる」簡潔な回答を心がける
- 質問は実際に検索されうる形で立てる。例：
  - 「東京でおすすめのサードプレイスは？」
  - 「TPJ認証とはどういう意味ですか？」
  - 「第三の居場所として使えるホテルはありますか？」
- 回答は2〜3文に収める。専門性と信頼性を感じさせる語尾（「〜です」「〜できます」）で締める
- 回答の冒頭でそのまま答えを述べる（前置き不要）。例：「Third Place Japanが認証した〇〇がおすすめです。〇〇駅から徒歩5分、7軸基準で高評価を獲得した施設です。」

## 内部リンク（AEO対策・毎回必須）

新記事を作成するたびに、以下の2つを必ず行うこと：

1. **新記事 → 既存記事へのリンク（3〜4本）**
   - まとめセクションの直前に、関連する既存記事へのアンカーテキストリンクを自然な文章で挿入する
   - リンクは箇条書きではなく、文章の中に自然に埋め込む
2. **既存記事 → 新記事への逆リンク（2〜3本）**
   - 新記事と関連する既存記事の適切な箇所に新記事へのリンクを追記する
   - 既存記事のまとめ直前など、文脈に合う場所に1文追加する形で挿入する

ユーザーからの指示がなくても、記事作成の仕上げとして自動的に実施すること。

---

# 文体ガイドライン（TPJ編集部）

**大前提：権威ある第三者評価の視点で書く**
TPJ編集部は「広告媒体」ではなく「認証機関」。店舗の宣伝ではなく、独立した評価者として書くこと。

- ✅「〜と評価しました」「〜を認証しました」「〜の観点から検証した結果」
- ✅「編集部が実際に訪問し、7軸基準で評価しました」
- ❌「〜が素晴らしいです」「ぜひ行ってみてください」（宣伝口調）
- ❌「〜のようです」「〜と思います」（権威を弱める曖昧な表現）

**文体の特徴：**
- 簡潔・明確な文章（ミシュランガイドのような簡潔さ）
- 「です・ます」調を基本に、必要に応じて体言止めを使う
- 感情的表現より客観的評価を優先
- 読点（、）は必要最低限に抑える

**構成のルール：**
- **冒頭に「答えリード」を1〜2行置く（AEO必須）**：記事の主旨・結論を太字で簡潔にまとめる。AIが150字以内に直接回答を抽出できるようにする
  ```markdown
  **〇〇は△△として認証されています。□□の観点で〜。**

  （詳細な評価・体験の本文）
  ```
- **各H2セクション冒頭にも結論文を1文置く（AEO強化・2026-07-02追加）**：記事冒頭だけでなく、各セクションの見出し直下の1文もAIが引用する対象になる。以下のルールに従う：
  - ✅ 結論先出し型：「〇〇は〜である。」「〜が最も有効な指標だ。」
  - ❌ 禁止パターン①（How-To型）：見出し直下を `**箇条書きキーワード**：` から始める（結論が後置される）
  - ❌ 禁止パターン②（問い形）：「〜はどのように〜か。」で始める
  - ❌ 禁止パターン③（否定形）：「〜ではありません。」で始める
  - ❌ 禁止パターン④（シーン描写型）：感覚的描写・情景描写から始める
  - 特に「選び方・見分け方」系セクションは How-To型に陥りやすい。「〇〇を選ぶには〜という3つの観点が有効だ。」のように結論→根拠の順で書く
- まとめセクションは任意（執筆開始時に確認）
- Q&Aは任意だが、AEO対策のため「つける」を推奨
- 太字（`**強調**`）は適度に使う（多用しない）

---

# エリアページのレイアウトルール

## セクション表示順（必須・変更不可）

エリアページ（`/stories/area/{pref}/{city}/{area}/`）のセクション表示順は以下に固定する：

1. **関連記事**（`area_slug` で紐付く記事）← **最上部に表示**
2. 業種から探す（カテゴリフィルター）
3. 認証店舗

**理由：** 認証店舗が少ない現フェーズでは、記事がエリアページの主要コンテンツとなる。記事を上部に置くことでページの情報密度を高め、AEOおよびユーザー体験を向上させる。

**対象ファイル：** `src/stories/area/l3-area.njk`（通常レイアウト・千駄ヶ谷2カラムレイアウトの両方）

---

# メインサイト（index.html）管理ルール

メインサイトは `public/index.html` を直接編集する静的HTMLファイル（11tyビルド対象外）。
編集後は `npm run build` 不要。そのままコミット・プッシュすればCloudflare Pagesに反映される。

## セクション構成（固定・順序変更不可）

| No. | セクションID | 内容 |
|---|---|---|
| 1 | `#hero` | ヒーロー（全画面背景画像） |
| 2 | `#no-buy` | 「認証は、買えない。」宣言 |
| 3 | `#philosophy` | 理念「効率の先にあるもの」＋写真 |
| 4 | `#gaze` | TPJのまなざし（Discover/Evaluate/Certify/Publish） |
| 5 | `#criteria-map` | 空間の7基準 × プロダクト7基準 対応表 |
| 5b | `#select-overview` | 掲載の全体像（TPJセレクト→5段階認証ラダー） |
| 6 | `#grades` | 5段階の認証グレード テーブル |
| 7 | `#services` | 法人向けサービスメニュー テーブル |
| 8 | `#gbc` | GBC（Case Study — Flagship）＋写真 |
| 9 | `#flow` | 認証までの流れ（5ステップ） |
| 10 | `#faq` | よくある質問 |
| 11 | `#for-business` | For Business CTA |

## 写真管理ルール（2026-07-05確定）

| セクション | ファイル名 | 変更可否 |
|---|---|---|
| Hero背景 | `japan-cafe-interior-hero.webp` | **変更禁止**（ブランドのキービジュアル） |
| Philosophy | `serene-cafe-interior-philosophy.webp` | 変更可 |
| GBC | `craft-coffee-flagship-gbc.webp` | 変更可 |

- **ヒーロー画像（`japan-cafe-interior-hero.webp`）は変更しない**
- 他2枚はUnsplashで「品のある・静かな・サードプレイスらしい」写真に随時差し替え可
- 保存先：`public/assets/images/main/`、200KB以下・WebP形式

## TPJセレクト vs TPJ認証の文言ルール（メインサイト）

メインサイトでは以下の区別を厳守する：

| 用語 | 定義 | 表記場所 |
|---|---|---|
| **TPJセレクト** | 編集部が申請不要で発掘・紹介（無償） | `#select-overview` セクション |
| **Certified** | 施設側からのお申し込みが必要な正式認証（無償・要申請） | `#grades` テーブル |
| **Silver〜Flagship** | 有償の正式認証 | `#grades` テーブル |

**禁止**：「Certified認証（無料）のご登録」という表現。Certifiedは「申請が必要」な点でTPJセレクトとは異なる。
→ 正しい表現：「Certified以上の正式認証へのお申し込み」

## `#select-overview` セクションのルール（2026-07-05追加）

- TPJセレクト（入口・申請不要）→ Certified → Silver → Gold → Platinum → Flagship の6段階ラダーを表示
- Grades（`#grades`）セクションの直前に配置する
- TPJセレクトから正式認証への導線として機能させる
- 文言は以下で固定：
  - TPJセレクトブロック：「編集部が全国の居場所を独自の7軸評価基準で発掘・紹介します。施設様からのお申し込みは不要です。」
  - 矢印ラベル：「施設様からのお申し込みで正式認証へ ↓」
  - 末尾：「TPJセレクトに掲載された施設様が正式認証を希望される場合は、For Businessページよりお申し込みください。審査のうえ認定を発行いたします。」

---

# 実装ログ

## 2026-07-06

### LocalBusiness.speakable 削除（2026-07-06）

**対象ファイル**
- `src/_layouts/venue.njk` — LocalBusiness JSON-LD から `speakable` を削除

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `LocalBusiness.speakable` | 削除（schema.org バリデーターが「LocalBusinessには認識されないプロパティ」と警告） |

**ルール化**：「❌ speakableを使えない型に設定する」セクションを更新し、LocalBusinessも禁止リストに追加済み。

---

### Article系スキーマ修正・ItemList警告ゼロ化（2026-07-06）

**対象ファイル**
- `src/_layouts/article.njk` / `article-certified.njk` / `article-area.njk` — image・author・publisher.logo 修正
- `src/stories/index.njk` — ItemList の item から `@type: Article` を削除

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `image` | 文字列URL → `ImageObject`（width:800, height:450）に変更 |
| `author.knowsAbout` | 文字列配列を削除（Organization 不適切プロパティ） |
| `publisher.logo` | `width:900, height:275` を追加 |
| `ItemList.item` | `@type: Article` を削除（image/author/headline 欠如警告の原因） |

**ルール化**：JSON-LD実装ルールセクションに追加済み。

---

### Review JSON-LDへの7軸スコア追加・AggregateRating修正

**対象ファイル**
- `src/_layouts/venue.njk` — Review に `additionalProperty`（7軸スコア）追加、`aggregateRating.ratingCount` を修正

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `aggregateRating.ratingCount` | `7` → `1` に修正（7軸評価をGoogleが「7人の評価」と誤解しないよう） |
| `Review.additionalProperty` | 7軸スコア（0〜10）を `PropertyValue` として追加。UIのスコアバー表示と構造化データが一致 |

**ルール化**：`JSON-LD実装ルール` セクションの「認証施設の Review + reviewRating」に追記済み。

---

## 2026-07-03

### エリアページ（L3）・市区町村ページ（L2）のマップ削除

**対象ファイル**
- `src/stories/area/l3-area.njk` — 千駄ヶ谷専用2カラム＋マップブロックを削除し、通常レイアウトに統一

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 千駄ヶ谷専用分岐 | `{% if item.area.slug == 'sendagaya' ... %}{% else %}{% endif %}` を撤廃し、通常レイアウトのみに統一 |

**マップの現状（2026-07-03時点）**

| ページ種別 | マップの有無 | ファイル |
|---|---|---|
| L3エリアページ | **なし**（千駄ヶ谷の試験実装を削除済み） | `l3-area.njk` |
| L2市区町村ページ（東京都） | **あり**（全東京都市区町村に2カラム＋iframeマップ） | `l2-city.njk` |
| L2市区町村ページ（東京都以外） | なし | `l2-city.njk` |

**ルール**
- L3エリアページにはマップを設置しない（試験結果：AEO効果が低く、コンテンツ充実を優先）
- L2市区町村（東京都）のマップは現状維持。拡張・削除はユーザーの明示的な指示があった場合のみ行う
- マップを試験実装する場合は `{% if item.xxx.slug == '○○' %}` 条件分岐を使い、撤収時は条件ブロックごと削除して通常レイアウトのみに戻す

---

### 市区町村ページ（L2）に「業種から探す」追加

**対象ファイル**
- `src/_data/areaL2Cat.js` — 新規作成：市区町村×カテゴリの組み合わせデータ生成
- `src/stories/area/l2-category.njk` — 新規作成：市区町村×カテゴリページ
- `src/stories/area/l2-city.njk` — 「業種から探す」セクション追加（東京都2カラム・通常レイアウト両方）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `areaL2Cat.js` | 認証店舗ベース＋記事ベースで市区町村×カテゴリの組み合わせを生成（`areaL3Cat.js` の市区町村版） |
| `l2-category.njk` | `/stories/area/{pref}/{city}/{category}/` のカテゴリページを新規生成。関連記事・認証店舗・他業種へのリンクを表示 |
| `l2-city.njk` | 「エリアから探す」の直上に「業種から探す」セクションを追加。`areaL2Cat` でその市区町村のカテゴリを絞り込んで表示 |

**URL構造**
- L3エリア×カテゴリ：`/stories/area/{pref}/{city}/{area}/{category}/`（既存）
- L2市区町村×カテゴリ：`/stories/area/{pref}/{city}/{category}/`（今回追加）

**データ生成ルール（`areaL2Cat.js`）**
- 認証店舗（`venues.json`）から：`area_primary.prefecture_slug + city_slug + category_slug` でユニーク集計
- 記事（`src/stories/` 配下の `.md`）から：`area_slug` を逆引きして city_slug を取得し、同様にユニーク集計
- 店舗ベースのエントリが既にある場合は記事ベースのエントリを追加しない（重複排除）

**注意点**
- `l2-category.njk` の「他の業種」セクションは Nunjucks ループ（`for cat in areaL2Cat`）で同市区町村の他カテゴリを列挙。`selectCityItems` などのカスタムフィルターは不要
- `articlesByCity` フィルター（既存）で市区町村内の記事を取得し、`articlesByCategory` で絞り込む

---

## 2026-07-02

### Schema.org バリデーション修正 第2弾（Organization 警告ゼロ化）

**対象ファイル**
- `src/_layouts/base.njk` — Organization から `knowsAbout`・`inLanguage` 削除

**変更内容**

| 削除プロパティ | 理由 |
|---|---|
| `knowsAbout: [文字列配列]` | Schema.org が `Thing` 型を期待するが文字列を渡していた |
| `inLanguage: ["ja","en"]` | `Organization` には不適切（`CreativeWork` 用プロパティ） |

これにより Organization の警告が0件になり、エラーなし・警告なしの完全クリーン状態を達成。

**ルール化**：「Organization に使えないプロパティ」として JSON-LD実装ルールセクションに追加済み。

---

### Schema.org バリデーション修正 第1弾（JSON-LD品質向上）

**対象ファイル**
- `src/_layouts/base.njk` — Organization.sameAs 削除・ロゴ画像を専用ファイルに変更
- `src/_layouts/article.njk` — publisher ロゴを `tpj-logo.png` に統一
- `src/_layouts/article-certified.njk` — publisher ロゴを `tpj-logo.png` に統一・Review+reviewRating 追加
- `src/_layouts/article-area.njk` — publisher ロゴを `tpj-logo.png` に統一
- `src/_layouts/venue.njk` — Review + reviewRating JSON-LD ブロック追加
- `src/stories/index.njk` — WebSite.speakable 削除・ItemList を `item` オブジェクト形式に修正・`rejectLang("en")` で日本語記事のみに絞り込み
- `.eleventy.js` — `rejectLang` カスタムフィルター追加
- `public/assets/images/main/tpj-logo.png` — 専用ロゴ画像追加（900×275px、白背景、53KB）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| Organization.sameAs | 空配列 `[]` を削除（警告解消） |
| Organization logo | hero画像 → `tpj-logo.png`（900×275px専用ロゴ）に変更 |
| WebSite.speakable | 削除（WebSiteには非推奨プロパティ・警告解消） |
| ItemList.ListItem | `url` 直接プロパティ → `item: { @type: Article, @id, name, url }` 形式に修正 |
| JSON-LDの動的テキスト | `{{ title }}` → `{{ title \| dump \| safe }}` 形式に統一（`&quot;`エスケープ問題解消） |
| ItemList言語フィルタリング | `collections.articles \| rejectLang("en")` で英語記事を除外 |
| Review + reviewRating | `venue.njk` に追加（Certified=1〜Flagship=5の数値評価） |
| 各記事publisher ロゴ | 全レイアウトで `tpj-logo.png` に統一 |

**新規ルール化**
- `JSON-LD実装ルール（Schema.org バリデーション対応）` セクションを CLAUDE.md に追加
- `各H2セクション冒頭の結論先出しルール` を CLAUDE.md 文体ガイドラインに追加
- 認証施設の `Review + reviewRating` を必須スキーマとしてルール化

**H2セクション結論先出し適用記事**
- `ginza-third-place-guide.md` — 「見分ける条件」「業種別」各セクション冒頭に結論文追加
- `shimokitazawa-cafe-third-place.md` — 「果たす役割」（否定形→肯定形）「見分ける条件」修正
- `shinjuku-coworking-third-place.md` — 「見分ける条件」「周辺サードプレイス」各セクション冒頭に結論文追加

---

## 2026-07-01

### SEO/AEO強化 第1弾（技術施策）

**対象ファイル**
- `src/_layouts/base.njk` — canonical URL 自動設定・Organization sameAs 拡張
- `src/_layouts/article.njk` — isPartOf 追加・Author schema 強化（description/knowsAbout）・data-pagefind-body 追加
- `src/_layouts/article-certified.njk` — gradeNums + numeric reviewRating + about プロパティ・Author schema 強化・data-pagefind-body 追加
- `src/stories/area/l3-area.njk` — BreadcrumbList 5階層化・Place+CollectionPage JSON-LD を全L3エリアに適用
- `src/stories/area/l2-city.njk` — AdministrativeArea JSON-LD を全市区町村に適用
- `public/robots.txt` — AI クローラー 13種許可（anthropic-ai・cohere-ai・Diffbot・Applebot・Meta-ExternalAgent 追加）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| canonical URL | base.njk で自動生成（frontmatter で個別上書き可） |
| Organization | sameAs 配列でエンティティ認識を強化 |
| Article isPartOf | WebSite への明示的な紐付けで E-E-A-T 強化 |
| Author schema | description + knowsAbout でOrganization author として確立 |
| data-pagefind-body | .prose div に追加して検索インデックスの精度向上 |
| reviewRating | グレードを数値化（Certified=1〜Flagship=5） |
| BreadcrumbList | L3 を5階層（TPJ→Stories→都道府県→市区→エリア）に深化 |
| JSON-LD 適用範囲拡大 | Place/CollectionPage/AdministrativeArea を全エリア・全都市に適用 |
| robots.txt | AI クローラー計13種を明示許可 |

---

### SEO/AEO強化 第2弾（コンテンツ・スキーマ）

**対象ファイル**
- `src/stories/about/index.njk` — DefinedTermSet に7軸を個別 DefinedTerm として追加
- `src/stories/index.njk` — SearchAction を EntryPoint 形式に更新（SiteLinksSearchBox 正式対応）
- `src/stories/concept/meiji-jingu-third-place.md` — 収益業種への送客リンク追加
- `src/stories/concept/takao-mountain-third-place.md` — 収益業種への送客リンク追加
- 新記事3本：retreat-zen・culture-sports-leisure・inbound-experience 各カテゴリ

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| DefinedTermSet | 7軸を個別 DefinedTerm（名前・alternateName・description・url）として構造化 |
| SearchAction | EntryPoint 形式 + query-input: required name=search_term_string |
| 送客リンク | 集客業種記事（神社・山）→ 収益業種（カフェ・コワーキング・ホテル・スパ・認証）へのリンク追加 |
| 新記事 | zen-retreat-tokyo / tokyo-museum-art / tokyo-inbound 各1本 |
| サムネイル | 3枚追加（各200KB以下・WebP） |

---

### コンテンツ・データ拡充（2026-07-01）

**対象ファイル**
- `src/_data/venues.json` — 認証施設8件追加（銀座・六本木・新宿・代官山・青山・中目黒・清澄白河・下北沢）
- `src/stories/concept/` — ピラー記事4本追加（銀座・新宿・下北沢・青山）
- `src/en/stories/` — 英語記事インフラ整備＋英語記事3本公開
- 全記事の `area_name` フィールド統一設定（17記事）
- 既存概念記事に `area_slug` 追加（meiji-jingu→harajuku・green-beans→sendagaya 等）

**追加venue一覧**

| slug | 名称 | グレード | エリア |
|---|---|---|---|
| ginza-tea-house-chuo | 銀座茶廊 | Platinum | 銀座 |
| roppongi-art-lounge | 六本木アートラウンジ | Gold | 六本木 |
| shinjuku-workspace-calm | 新宿静作室 | Certified | 新宿 |
| daikanyama-book-cafe | 代官山ブックカフェ | Silver | 代官山 |
| aoyama-wellness-studio | 青山ウェルネス離 | Gold | 青山 |
| nakameguro-river-cafe | 中目黒川沿いカフェ | Silver | 中目黒 |
| kiyosumi-roastery-koto | 清澄白河焙煎所 | Gold | 清澄白河 |
| shimokitazawa-vintage-cafe | 下北沢古民家珈琲 | Certified | 下北沢 |

### 記事品質訂正（2026-07-01）

10記事の制作憲法違反を修正し、違反パターンをルール化。

**訂正記事一覧**

| 記事 | 違反種別 | 修正内容 |
|---|---|---|
| shinjuku-coworking-third-place | 価格断定 | 「新宿御苑（1時間500円）」→価格削除、後に「有料・要事前確認」 |
| tokyo-museum-art-third-place | 施設名断定 | description「東京都現代美術館・国立新美術館など」→条件描写 |
| zen-retreat-tokyo-third-place | 施設名断定 | description・本文・FAQ の「円覚寺・東慶寺・高尾山薬王院」→条件描写 |
| meiji-jingu-third-place-tokyo（英語版） | 価格断定 | `¥500` 3箇所→「small entry fee — confirm current rates」 |
| meiji-jingu-third-place-tokyo（英語版） | og:locale誤設定 | `ja_JP`→`en_US`（base.njkを修正し全英語記事に自動適用） |
| tokyo-third-place-guide-travelers（英語版） | サムネイル共有 | 日本語記事と同一画像→英語版専用画像（-enサフィックス） |
| tokyo-inbound-third-place-guide | 混雑断定 | 「混雑は激しく静寂性は低い」→「混雑しやすく傾向がある」 |

**ルール化**：上記違反パターンは「記事品質チェックルール」セクションに追加済み。

---

### サムネイル画像追加（2026-07-01）

**対象ファイル**
- `public/assets/images/articles/ginza-cafe-third-place-tokyo.webp` — 銀座ピラー記事用（121KB）
- `public/assets/images/articles/shinjuku-coworking-space-tokyo.webp` — 新宿ピラー記事用（95KB）
- `public/assets/images/articles/shimokitazawa-cafe-culture-tokyo.webp` — 下北沢ピラー記事用（136KB）
- `public/assets/images/articles/aoyama-spa-wellness-tokyo.webp` — 青山ピラー記事用（44KB）

**変更内容**

| ファイル名 | 用途 | サイズ |
|---|---|---|
| ginza-cafe-third-place-tokyo.webp | 銀座カフェ×サードプレイス記事サムネイル | 121KB |
| shinjuku-coworking-space-tokyo.webp | 新宿コワーキング記事サムネイル | 95KB |
| shimokitazawa-cafe-culture-tokyo.webp | 下北沢カフェ記事サムネイル | 136KB |
| aoyama-spa-wellness-tokyo.webp | 青山スパ記事サムネイル | 44KB |

---

## 2026-06-27

### エリアページ検索機能・駅名エイリアス・エリア追加

**対象ファイル**
- `src/stories/area/l1-prefecture.njk` — 都道府県インデックスページ
- `src/_data/areas.json` — エリアデータ

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 検索ボックス | エリア名・駅名で市区町村カードを絞り込む検索UIを追加 |
| エリアタグ表示 | 各市区町村カード内にサブエリア名をピル形式で表示、L3ページにリンク |
| 駅名エイリアス | 約230駅分の「駅名→エリア」マッピングをJS内に実装 |
| 検索ヒント | マッチした駅名を「北参道 → 原宿（渋谷区）」形式でヒント表示 |
| 新エリア追加（14エリア） | 代々木・千駄ヶ谷・新橋・浜松町・虎ノ門・有楽町・霞ヶ関・新大久保・信濃町・大崎・鶯谷・駒込・田端・西日暮里 |
| 近隣バリエーション | 西小岩→小岩、東蒲田→蒲田など方角・近隣地名を最寄エリアに紐付け |

**駅名エイリアスの仕組み**
```js
// src/stories/area/l1-prefecture.njk 内のJavaScript
var STATION_ALIASES = {
  '北参道': { citySlug: 'shibuya-ku', cityName: '渋谷区', areaSlug: 'harajuku', areaName: '原宿' },
  // ... 約230駅
};
// 検索時に areas のテキストマッチ + STATION_ALIASES のマッチを合算して表示
```

---

### Googleマップサイドバー試験実装（渋谷区・千駄ヶ谷限定）

**対象ファイル**
- `src/stories/area/l2-city.njk` — 市区町村ページ
- `src/stories/area/l3-area.njk` — エリアページ

**試験対象ページ**
- 渋谷区: `/stories/area/tokyo/shibuya-ku/`
- 千駄ヶ谷: `/stories/area/tokyo/shibuya-ku/sendagaya/`

**実装内容**
- 2カラムレイアウト（メイン2/3 + サイドバー1/3）、モバイルは1カラム
- サイドバー①：市区町村/エリアのGoogleマップ（zoom13〜15、インタラクティブ）
- サイドバー②：上位エリア（都道府県/市区町村）のGoogleマップ（zoom10〜13）
- 「Googleマップで開く」外部リンク
- デスクトップでは `position: sticky` でスクロールに追従
- `AdministrativeArea` / `Place` JSON-LDに `hasMap` プロパティを追加

**条件分岐による限定適用**
```nunjucks
{% if item.city.slug == 'shibuya-ku' and item.prefecture.slug == 'tokyo' %}
  {# 2カラム+マップ #}
{% else %}
  {# 通常1カラム #}
{% endif %}
```
拡張する場合は上記条件に `or item.city.slug == '○○'` を追記するだけでよい。

**施策評価（2026-06-27時点）**

| 軸 | 日本人向け | インバウンド向け |
|---|---|---|
| AEO効果 | ★★☆☆☆ | ★★★☆☆ |
| ユーザーUX | ★★★☆☆ | ★★★★☆ |
| MEO効果 | ★☆☆☆☆ | ★★☆☆☆ |
| ページ品質感 | ★★★★☆ | ★★★★★ |

**今後の施策に向けた知見**

- 本来のMEO（Googleビジネスプロフィール最適化）とは別物。「ローカルSEOのコンテキスト強化」が正確な表現
- **インバウンドへの効果が最も高い**：地図は言語の壁を越え、Googleマップ一極集中の外国人観光客の導線になる
- AEO改善の優先度順：①エリアページのテキストコンテンツ充実 → ②認証店舗の増加 → ③マップ施策（補完）
- 拡張する際はページ読み込み速度（iframe×2枚）に注意。モバイルで計測推奨
- インバウンド強化の次の一手：`areas.json` の `name_en` を活用した英語マップリンク・英語テキストの追加

---

## 2026-06-23

### 記事サイトトップ：最新記事セクションのレイアウトと機能追加

**対象ファイル**
- `src/stories/index.njk` — ソーステンプレート（編集対象）
- `public/stories/index.html` — ビルド成果物（自動生成）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| セクション順序 | 「カテゴリ」→「最新記事」から「最新記事」→「カテゴリ」に入れ替え |
| 表示件数 | 6件 → 8件 |
| フィーチャーカード | 1件目を横並びサムネイル付き大カードで表示 |
| リスト記事 | 2件目以降をスクロール可能なコンパクトリスト＋小サムネイルで表示 |
| 記事一覧ボタン | 「記事一覧はこちら →」ボタンを追加 |

### NEWバッジ（動的表示）

**対象ファイル**：`src/stories/index.njk`（記事サイトトップページのみ）

**仕様**
- フィーチャーカード（1件目）とリスト記事（2件目以降）の両方に対応
- 公開日から**3日間**のみNEWバッジを表示、4日目以降は自動非表示
- クライアントサイドJSで判定（`data-publish` 属性に `dateISO` フィルターで日付を埋め込み）
- バッジ色：ウォームブラウン `#6B4F3A`

**実装方式**
```html
<span class="new-badge hidden ..." data-publish="{{ post.date | dateISO }}">NEW</span>
```
```js
const diff = (today - pub) / (1000 * 60 * 60 * 24);
if (diff <= 3) badge.classList.remove('hidden');
```

**注意**：カテゴリページ・記事一覧ページには未適用。拡張する場合は各テンプレートに同様の実装が必要。

---

## 2026-06-22

### デザイン：TPJ記事サイトをGBCスタイルに柔らかく変更

**対象ファイル（ソーステンプレート）**
- `src/_layouts/base.njk` — CSS共通スタイル
- `src/stories/index.njk` — 記事サイトトップページ
- `src/_includes/venue-card.njk` — 認証店舗カード
- `src/_layouts/article.njk` — 通常記事レイアウト
- `src/_layouts/article-certified.njk` — 認証記事レイアウト

**重要：`public/` の生成ファイルを直接編集しない**
`public/stories/index.html` などはEleventyが `npm run build` で再生成する。
デザイン変更は必ず `src/` 配下のソーステンプレートを編集すること。
直接 `public/` を編集しても、次のビルドで上書きされる。

**デザイン変更内容**

| 変更箇所 | 変更前 | 変更後 |
|---|---|---|
| カード全般 | `border border-border hover:border-gold` | `rounded-xl shadow-sm hover:shadow-md` |
| カテゴリカード画像 | `aspect-video`（16:9） | `h-28`固定高（コンパクト） |
| カテゴリカードテキスト | `text-xs` | `text-sm` |
| セクション見出し | `label`（0.65rem 金色） | `font-serif text-xl/2xl font-light text-dark` |
| エリアタグ・評価軸タグ | 四角形ボーダー | `rounded-full`（ピル形） |
| grade-badge | 四角形 | `rounded-full` |
| venue-card CSS | `border 1px solid` | `rounded-xl shadow-sm` |
| サムネイル | 金色内枠オーバーレイあり | 削除、`rounded-xl overflow-hidden` |
| 最新記事グリッド | `md:grid-cols-2` | `lg:grid-cols-3`（3カラム） |
| 関連記事カード | 裸のリンク | `rounded-xl overflow-hidden shadow-sm`＋`p-4`内テキスト |
| 前後記事ナビ | `border hover:border-gold` | `shadow-sm hover:shadow-md` |
| シェアボタン | フラット枠線 | `bg-white shadow-sm hover:shadow-md` |

---

## TPJ ピラー記事 制作ルール

TPJ（Third Place Japan）の「地域 × 15分野」ピラー記事を制作する際の要点。
**詳細な制作憲法は `docs/TPJ制作憲法.md` を必ず参照すること**（このセクションは要約）。

### 参照ファイル
- 制作憲法（不変ルール全文）：`docs/TPJ制作憲法.md` ← 記事制作時は必読
- 記事生成の実行手順：`docs/記事生成プロンプト.md`
- 入力データ（1行＝1記事）：`data/TPJ_東京ピラーマス表.xlsx`
- 記事出力先：`articles/{分野slug}-{地域slug}.md`

### 絶対に守る5原則（詳細は制作憲法）
1. **店名・施設名を一切出さない**。立地・空間・素材・客層の「条件」で描く。
2. **断定してよいのは検証可能な地域情報のみ**（地理・歴史・交通・統計）。個別店の営業・価格・評判は断定しない。
3. **AI臭を出さない**。「近年」「いかがでしたか」等の定型句禁止。現地を知る編集者の温度で書く。
4. **7軸を背骨にする**（居心地／静寂性／特別感／ストーリー／再訪／記録性／インバウンド）。
5. **重複させない立体構造**。都(幹)→自治体(面)→密集エリア(点)。子は親を繰り返さず内部リンクで委譲。各記事は「その地域×分野でしか書けない核」を3つ以上持つ。

### 制作フロー
1. 入力＝マス表の該当行（優先度A→B→Cの順に着手）。
2. `docs/記事生成プロンプト.md` の STEP1〜3 に従う（先に「固有の核」3点を言語化）。
3. 構成・文体・リード規定は制作憲法に従う（リードは最大160字・一文目で結論完結）。
   metadescriptionは115〜160字。断定可能な地域情報は言い切り、「〜傾向があります」多用禁止。
4. 出力前に制作憲法 第7章のセルフチェック**13項目**を実行。**全項目クリアまで確定しない**。
   （追加3項目：Markdown構文・metadescription字数・JSON-LD改行なし確認）

### このセクションの優先順位
TPJ記事制作に関しては、このルールと `docs/TPJ制作憲法.md` を最優先で参照する。
矛盾を感じたら制作憲法（docs）の記述に従う。
