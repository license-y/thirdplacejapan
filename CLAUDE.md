## プロジェクト概要

- **プロジェクト名**: Third Place Japan（サードプレイスジャパン、略称: TPJ）
- **サイト種別**: 日本初のサードプレイス認証メディア
- **運営**: TPJ編集部
- **目的**: 日本全国の「第三の居場所」（ホテル・レストラン・カフェ・サウナ・ウェルネス施設・コワーキングなど）を独自の7軸基準で評価・認証し、AI時代の「場所選び」における業界標準データ基盤となる。

## 制作方針の参照先（必読）

本プロジェクトの制作判断は、以下の7ファイルに基づく。コンテンツ制作・サイト編集の前に必ず参照すること。

| ファイル | 場所 | 役割 |
|---|---|---|
| CLAUDE.md | `project-docs/CLAUDE.md` | TPJ統合制作方針・確定コピー・トーン＆マナー（**最上位**） |
| DEFINITION.md | `project-docs/DEFINITION.md` | サードプレイスの定義（全コンテンツの根幹） |
| INFORMATION-ARCHITECTURE.md | `project-docs/INFORMATION-ARCHITECTURE.md` | サイト構造・URL設計・AEO実装・技術スタック |
| AEO-PLAYBOOK.md | `project-docs/AEO-PLAYBOOK.md` | AI引用獲得（AEO）の施策・タグ正本・業種正本 |
| STORIES-SITE-SPEC.md | `project-docs/STORIES-SITE-SPEC.md` | 記事サイト（/stories/）実装マスター仕様 |
| CONTENT-STRATEGY.md | `project-docs/CONTENT-STRATEGY.md` | 全記事タイトル確定版・本文テンプレート・投稿カレンダー |
| AGENT-INSTRUCTIONS.md | `project-docs/AGENT-INSTRUCTIONS.md` | 記事生成エージェントの運用ルール |

**優先順位**：矛盾時は `project-docs/CLAUDE.md`（最上位）を正とする。AGENT-INSTRUCTIONS は CONTENT-STRATEGY より優先する。
**メインサイトのコピーは `project-docs/CLAUDE.md` セクション6「確定コピー」を正とする。**

> ＜現フェーズ方針＞ 記事サイト（/stories/）は引き続き無償記事の充実に集中する。
> 有償展開：法人向けサービス（掲載枠・認証グレードSilver以上・コンサル等）の
> 問い合わせ窓口はメインサイト（`#services`・`#for-business`）に設置済み。
> ただし積極的な営業・勧誘・価格の前面訴求は行わず、控えめなトーンを維持する。
> 記事サイト（/stories/）から有償サービスへ直接誘導する導線は原則設けない
> （既存の慣例に従い、`about/certification-grades`等、認証制度の"仕組みの説明"
> として自然に必要な箇所のみ、メインサイトのFor Businessページへのリンクを許容する）。
> 価格・市場規模・上場など事業性の数字はサイト表面に出さない。

---

## 制作環境

- 非エンジニア、非デザイナーが、AI Agent (Claude Code) を使って Web サイトを構築する
- 構築する Web サイトは「ワンページ完結」とする。1 ページに必要な情報をまとめて提供する
- ブログなどは、別のサービス（Substack や note.com など）を利用することを前提とする
- デプロイ先は **Cloudflare Pages**

## サイトコンセプト


2. **人間より先にAIに読ませる設計**
   全ページに構造化データ（JSON-LD）を実装し、AIがQ&A・評価・場所情報を直接抽出できる形にする。

3. **日英両言語で「日本の居場所」を世界に発信**
   インバウンド・海外AIの引用を見据え、主要記事は英語版も持つ。「Japan's best third places」の権威ある情報源を目指す。





### TPJではないもの・TPJであるもの

**❌ TPJではないもの**
- 料理の味・クオリティの評価
- 価格・コスパの比較ランキング
- 「一番うまい店」を決める権威
- 広告掲載・有料露出の媒体
- 飲食店だけを対象にした評価
- ユーザー口コミの集計サービス
- インスタ映え・バズりを評価する場

**✅ TPJであるもの**
- 「いる体験・居場所としての価値」の認証
- 業種横断（カフェ・ホテル・スパ等）の評価
- AIに引用される信頼ある一次情報源
- 掲載枠の希少価値（デジタル不動産）
- インバウンド向け日英両言語の発信基盤
- 7つの空間軸による独自評価システム
- 「日本のサードプレイス」の世界標準定義者

### 誰も真似できない理由（参入障壁）

1. **「サードプレイス」という概念の先行定義権**
   食べログが「飲食店の点数」を定義したように、TPJが「サードプレイスの価値基準」を先に定義した者が市場を制する。後から参入した者は「TPJに似ている」と言われ続ける。定義者は一社のみ。

2. **AIに引用される構造を最初に作る者が勝つ**
   GBC記事サイトはすでにSchema実装済み。AIが「おすすめのサードプレイスは？」に答えるとき、最初に蓄積されたデータソースを引用する傾向がある。先行者が圧倒的に有利なゲーム。

3. **「評価される側」が評価を望む構造**
   TPJの認証は「掲載されること自体がステータス」になる設計。認証を取りたい店舗がTPJに価値を提供するポジティブな循環が生まれる。

4. **インバウンド×日英両言語の空白地帯**
   「Japan's best third places」を英語で検索しても、権威ある情報源が存在しない。TPJがその空白を埋める唯一の候補。

### ポジショニング・ステートメント

> 食べログは「どこで食べるか」を決める。ミシュランは「どこが最高か」を決める。**TPJは「どこにいるか」を決める。**
>
> AI時代に人々が最も問うのは「おいしい店はどこか」ではなく、「今日、どこで自分らしくいられるか」だ。その答えを出せる権威は、世界にまだない。TPJがその権威になる。

## ファイル構成

- `project-docs/` : Web ページの内容や、その他作成に必要なドキュメントを格納
- `public/` : Cloudflare Pages で公開設定をするディレクトリ。ここに成果物を入れる
- `public/assets/` : 画像、CSS、JavaScript などの生成物の置き場

# 作成のためのルール

## 保存先

- ファイルは必ず **UTF-8** で保存（日本語の文字化け防止）
- 成果物は `public/` ディレクトリに保存
- 特に指示がなければ `public/index.html` で作成
- 指示があれば、その指示に合わせて `public/` 配下に保存
- 画像・CSS・JavaScript を生成した場合は `public/assets/` 配下の所定フォルダに保存

## あなたの仕事

- **Web ページの作成**: ユーザーの指示・意図を理解して作成する。コンテンツも創造的に作って欲しいと判断したら創造性を発揮する。詳細指定があればそれに従う
- **Web ページの編集**: 編集箇所と依頼を理解して編集する
- **Web ページのチェック**: 文法エラー、不要な記述、大きすぎる画像などがあれば改善方法をアドバイスする

### ⚠️ 作業開始前の必須確認（絶対厳守）

**🚨 VS Codeを開いたとき・会話を始めたときは必ず最初に確認すること 🚨**

作業を開始する前に、以下をユーザーに必ず促すこと：

> 作業を始める前に、以下のコマンドで最新の状態に更新してください：
> ```
> git pull origin main
> ```
> 完了したら教えてください。

- `git pull` 完了の報告を受けてから作業を開始する
- ユーザーが既に実行済みと言った場合はそのまま進めてよい

---

### ⚠️ Git操作の重要ルール（絶対厳守）

**🚨 絶対に守ること 🚨**

1. **ユーザーの明確な指示があるまで、コミットもプッシュも絶対に行わない**
2. **コミットの指示があった場合** → コミットのみ実行し、完了を報告。プッシュはしない
3. **プッシュの指示があった場合** → その時初めてプッシュを実行
4. **「コミット＆プッシュ」の指示があった場合のみ** → 両方を実行

**禁止事項:**
- ❌ 勝手にコミット＆プッシュを連続実行する
- ❌ 「コミットしました」と言って勝手にプッシュもする
- ❌ ユーザーの確認なしに任意のGit操作を行う
- ❌ 作業完了後に自動でコミット＆プッシュする

**このルールは何度も破られているため、各作業の完了時に必ず「コミット＆プッシュの指示をお待ちしております」と伝え、ユーザーの指示を待つこと。**

**🚨 コミット前の確認（絶対厳守・新ルール） 🚨**

「コミット、プッシュ」の指示を受けても、**すぐに実行してはいけない**。
必ず以下の順序で確認してからユーザーの承認を得ること：

1. `git status` を実行して変更ファイル一覧をユーザーに見せる
2. 「以下のファイルをコミット・プッシュします。よろしいですか？」と確認する
3. ユーザーが「はい」「OK」「進めて」などの承認をしてから実行する

❌ 確認なしに即実行するのは禁止（過去に予期しないファイルを含めてコミットした失敗あり）

---

**🚨 コミット時の必須チェック 🚨**

承認を得たら、以下を実施してコミットすること：

1. **まず `git status` を実行して未コミットの変更があるか確認する**
   - 変更がない場合（`nothing to commit, working tree clean`）→ 「すでにコミット済みです」とユーザーに伝える。コミット操作は行わない
   - 変更がある場合 → 以下2〜4を実施してコミットする
2. **`src/` 配下を編集した場合は、必ず `npm run build` を実行してから次のステップへ進む**
   - buildを忘れると `public/` が古いままになり、Cloudflareに変更が反映されない
   - `public/index.html` など `public/` 直下のファイルだけを編集した場合はbuild不要
   - **`src/_data/` 配下（venues.json・areas.json等）を編集した場合も必ず `npm run build` が必要**
     データファイルはビルド時にHTMLへ展開される。データだけコミットしても画面には反映されない。
3. **`src/` のソースファイルと `public/` のビルド済みHTMLの両方**をコミットに含める
4. `npm run build` 後に生成された `public/` 配下の変更を絶対に漏らさない
5. **`public/pagefind/` も必ずコミットに含める**（検索インデックス。含めないと検索窓が表示されなくなる）

**NG例（過去の失敗）：**
- ❌ `src/` だけコミットして `public/` を含め忘れる → Cloudflareに反映されない
- ❌ `src/` を編集したのに `npm run build` をせずにコミット → `public/` が古いまま
- ❌ `src/_data/` のJSONを更新したのに `npm run build` をせずにコミット → 画面に反映されない
- ❌ git statusを確認せずにコミットしようとする → 変更がないのにコミット操作をしてしまう
- ❌ `public/pagefind/` をコミットし忘れる → 検索窓がサイト上で消える（2026-07-01に発生した障害）

**📌 `git status` で大量のファイルが変更されている場合について（正常な動作）：**

レイアウト（`src/_layouts/`）やインクルード（`src/_includes/`）を変更すると、
`npm run build` がそれらを使用するすべてのHTMLを再生成するため、
`public/` 配下の数百ファイルが変更済みとして表示される。
これは意図通りの正常な動作であり、すべてコミットに含めてよい。

- `src/_includes/nav-stories.njk`（共通ヘッダー）を変更 → 全記事・全エリアページが再生成される
- `src/_layouts/article.njk`（記事レイアウト）を変更 → 全記事ページが再生成される
- `src/_data/areas.json` を変更 → 全エリアページが再生成される

---

**🚨 新規ページを公開したら、プッシュ後に必ず本番URLで実際に表示されるか確認する（2026-08-10追加）🚨**

`git push` が成功し、Cloudflare側のデプロイ履歴（Workers & Pages の「Deployments」タブ）で該当コミットが「アクティブな展開・トラフィック100%」と表示されていても、**それだけでは新規ページが実際に配信されていることの証明にはならない**。

**実際に発生した障害（2026-08-10）**：三鷹市の新規記事3本（`shrine-temple/mitaka`・`inbound-experience/mitaka`・`culture-sports-leisure/mitaka`）を含む716ファイルの大規模デプロイ（GBC施設ページの一連の更新も同時に含まれていた）で、Cloudflareのデプロイ自体は成功と表示されていたにもかかわらず、**新規作成した3ファイルだけ**が本番で配信されず、該当URLにアクセスするとメインサイト（トップページ）にフォールバックする状態になっていた。同じデプロイに含まれていた「今日編集した既存ファイル」（`culture-sports-leisure/fuchu`・`koganei`等）や、過去に公開済みの他のページは正常に配信されていた。

**発見の経緯**：ユーザーがブラウザのシークレットウィンドウで該当URLを直接開いたところ、URLバーは正しいパスを示しているのにメインサイトのトップが表示されるスクリーンショットが決め手になった。原因切り分けとして、`curl`で複数URLの`<title>`タグを比較し、「今日新規作成したファイルのみ失敗し、今日編集した既存ファイル・過去のファイルは正常」というパターンを特定した。

**原因の推定**：大規模デプロイ（数百ファイル規模の変更）において、Cloudflare Workers Static Assetsのアセット登録処理が、**新規パスの登録だけ**部分的に漏れる一時的な不具合が起きたと推定される（既存パスの内容更新は正常に反映されるため、パス自体は既に登録済みのものの更新は問題なく、真に新しいパスの初回登録でのみ問題が起きたとみられる）。

**対処法**：内容を変更しない空コミット（`git commit --allow-empty`）を作成してプッシュし、デプロイをやり直すことで解消した。

**再発防止ルール**：
- 新規ページ（新しいURL・新しいパスを持つページ）を公開した際は、`git push`成功の報告だけで完了とせず、**可能であれば実際の本番URLでの表示確認を依頼する**、または`curl`等で内容を確認する
- 大規模デプロイ（数百ファイル規模の変更を伴うコミット）で新規ページを含む場合は特に注意する。件数が少ない変更では今回のような問題は起きていない
- 「デプロイは成功と表示されているのに特定のページだけ表示されない」という報告を受けた場合、まず「今日新規作成したファイルか、既存ファイルの編集か」を切り分けると原因特定が早い
- 解決しない場合は、空コミットでの再プッシュを試す（内容変更なしで再デプロイをトリガーする、リスクの低い対処法）

---

### ⚠️ ブランチ戦略（必須）

**基本方針:**

- 日常的な更新（コンテンツ更新、画像差し替え等）は直接 `main` ブランチで実施
- 大きな機能追加や実験的な変更のみ、短期的なフィーチャーブランチを使用
  - 例: 新しいセクション追加、大規模リニューアル、デザイン変更等
  - 完成したらすぐに `main` にマージしてブランチを削除

**作業時の注意:**

1. **変更前の確認** → `git status` で未コミットの変更があるか確認
2. **ユーザーの手動編集を尊重** → ユーザーが直接ファイルを編集している可能性を常に考慮
3. **フィーチャーブランチを使う場合** → 必要に応じて `git stash` で一時保存してからブランチを切り替え
4. **ブランチ切り替え時** → 変更が失われないよう、必ず stash または commit してから切り替えること

---

### 📌 タグ付けルール

**命名規則**: `YYYY.MM.パッチ番号` の形式（カレンダーバージョニング）

**例**:
```
2026.06.0 - 6月の初回リリース
2026.06.1 - 6月への追加修正
2026.06.2 - 6月への2回目の修正
```

**運用ルール**:
- タグの月番号は「対象コンテンツの月」を示す（実際の作業月ではない）
- 同じ月のコンテンツに対する修正は、パッチ番号をインクリメント
- コンテンツと無関係な修正は、現在の月のタグを使う

**タグの移動**:
- タグを間違えた位置につけた場合は、削除して再作成できる
- コマンド例:
  ```bash
  git tag -d 2026.06.0
  git push origin :refs/tags/2026.06.0
  git tag -a 2026.06.0 -m "メッセージ"
  git push origin 2026.06.0
  ```

---

## ワンページ Web サイトの生成方法

ワンページサイトの作成・編集を行う際は、必ず **`one-page-site-builder` スキル**を使用すること。
このスキルに、技術スタック（Tailwind CSS v4 / Animate.css / AOS / Lucide）、HTML 構造、固定ナビゲーション仕様、セクション構成、画像最適化方針などの能力がまとまっている。

## 記事サムネイルの管理ルール（必須）

- **1〜20記事目は必ず異なる画像を使う**。同じ画像を複数記事で使い回すことは禁止
- **21記事目以降は既存画像の再利用可**（ただし記事のイメージに合うものを選ぶこと）
- 新記事を追加する際は、既存記事のサムネイル一覧を確認してから画像を選ぶ
- 既存サムネイルの確認コマンド:
  ```bash
  grep -r "thumbnail:" src/stories/ | sort
  ```
- 画像は必ず**記事のテーマ・イメージに合うもの**を選ぶ
- Unsplash画像を取得したら、**記事内容を反映したSEO効果のある英語ファイル名にリネーム**して保存する
  - 形式: `{内容}-{場所}-{用途}.webp`（例: `specialty-coffee-interior-tokyo.webp`）
- 保存先: `public/assets/images/articles/`
- ファイル形式: WebP、200KB以下
- **同一施設・エリアを扱う複数記事（クラスター）を一度に作成する場合は、21記事目以降の再利用ルールより優先して、クラスター内では重複しない画像を選ぶこと（2026-08-02追加）**：21記事目以降は既存画像の再利用が許容されるが、それはクラスター外の遠い記事同士の話であり、同じ施設ページの「エリアの関連記事」セクション等、同一UI上に並んで表示されうる記事同士で画像が重複すると見た目上の手抜き感が生じる。GBCクラスター記事4本のうち2組（記事1×記事2、記事4×既存レビュー記事）が画像重複していたことを公開前レビューで発見し、3枚を新規取得して解消した実例がある

---

## 画像の取得・最適化

- ユーザーから画像が提供されない場合、以下のコマンドでUnsplashから画像を検索して使用する
  ```bash
  node dev-tools/unsplash-search.js "キーワード"
  ```
- APIキーは `.env.local` の `UNSPLASH_ACCESS_KEY` に設定済み（変更不要）
- 動作確認は `node dev-tools/unsplash-search.js --check` で実行
- ユーザーが「画像を提供する」と言った場合、`public/assets/images/articles/` フォルダの中から更新日時が最新のファイルを使用する
  - 確認コマンド: `ls -lt public/assets/images/articles/ | head -5`
  - 最上位に表示されるファイルが最新画像

### 画像フォルダ構成（必須）

```
public/assets/images/
├── main/      → メインサイト（index.html）用画像
├── venues/    → 認定施設ページ用画像
└── articles/  → 記事サイト（11ty）サムネイル用画像
```

### Unsplash画像の保存ルール（必須・厳守）

1. **ファイルサイズ：200KB以下に必ず収める**
   - Unsplashダウンロード時のパラメータ目安：`w=800&q=70&fm=webp`（通常画像）、`w=1280&q=65&fm=webp`（ヒーロー画像）
   - ダウンロード後に `wc -c` でサイズを確認し、200KB超なら `q` を下げて再取得する

2. **ファイル名：SEOに効果的な英語名にリネームする**
   - 形式：`{内容}-{場所}-{用途}.webp`（例：`specialty-coffee-interior-tokyo.webp`）
   - 日本語・数字・記号は使わない。単語はハイフン区切り
   - 検索キーワードになる語を自然に含める（地名・業種・場面）

3. **保存先：用途に合ったフォルダに保存する**
   - メインサイト用 → `public/assets/images/main/`
   - 認定施設用 → `public/assets/images/venues/`
   - 記事サムネイル用 → `public/assets/images/articles/`

4. **フォーマット：WebP（`.webp`）を使用する**
   - Unsplashのパラメータに `fm=webp` を指定してダウンロード

5. **候補画像に他社ブランド名・競合店の看板が写り込んでいないか目視確認する（2026-08-02追加）**
   - Unsplashのキーワード検索は「カフェ」「コーヒー」等の一般語で検索すると、実在するカフェチェーンの店名・ロゴが写った写真がヒットすることがある（例：検索キーワードに一致しても、店内に競合ブランドの看板やロゴが写っている写真）
   - TPJの記事・施設ページで使うと、無関係な第三者ブランドを暗黙に取り上げてしまうことになるため、ダウンロード後は必ずRead toolやプレビューで目視確認し、判読可能な他社ブランド名・ロゴが写っている画像は候補から除外して別の画像を探す
   - 地名を含めて検索しても、検索結果に別の場所の写真が紛れることがある（浅草寺の写真が「torii gate tokyo」で上位に出る等）。地域が特定される記事では、地理的な整合性も含めて目視確認すること

- 画像ファイルサイズが **200KB超の場合は保存前に以下を案内する：**

> 画像サイズが大きいため、以下のサービスで圧縮してから再度お渡しください（目安：200KB前後）。
> - **TinyPNG** https://tinypng.com/ （PNG・JPEG対応）
> - **Squoosh** https://squoosh.app/ （Googleのツール）
> - **Optimizilla** https://imagecompressor.com/ （複数画像の一括処理可）

---

# 条件・技術スタック

## HTML・CSS・JavaScript

### 使用するライブラリ

- Tailwind CSS v4.1 以上を使用し、コード量を減らしつつ、エラーが少ない簡潔なコードを書いてください
- Tailwind CSS v4.1 以上は、CDNを使ってください（画像最適化の方が速度に寄与するので、Tailwind CSSの最適化は不要です）
- Animate.css v4.1.1 以上を使用して、適度にアニメーションをつける（派手にならないように注意）
- スクロールアニメーションは、AOS v2.3 以上を使用するか、Animate.css v4.1.1 + Intersection Observer API で実装してください
- アイコンや、ロゴは、Lucide v0.536.0 (https://lucide.dev/) を使ってください

### パフォーマンス最適化

- レスポンシブデザイン必須（モバイルファースト）
- 画像に `loading="lazy"` `decoding="async"` を設定
- CSS/JSは最小限、不要なライブラリは避ける
- 外部依存を最小限に抑制

### HTML構造

- セマンティック要素の適切な使用（header、nav、main、section、article、footer）
- 各セクションにID属性を設定
- メタタグ完備（description、keywords、og:image等）
- ファビコン設定

## デザイン要件

- Tailwind CSS の theme を使ってカラーパレットを設定
- **カラーパレット**:
  - Primary: #1C1C1C（ディープブラック）
  - Secondary: #4A4A4A（ダークグレー）
  - Accent: #B8960C（ゴールド）
  - Background: #F8F6F1（アイボリーホワイト）
  - Text: #2D2A26（ダークグレー）
- フォントは日本語に最適なものを選択、パフォーマンスを考慮する
- ホワイトスペースを効果的に使い、読みやすさを重視
- ミシュランガイドやMoocleのような、品のある権威ある雰囲気を保つ

### レスポンシブデザイン原則

**テキストの配置ルール（2文以上のテキストブロック）:**

複数文で構成されるテキストブロック（リード文、説明文など）は、以下のルールに従う：

- **モバイル表示**: 改行なしで左寄せ（`text-left`）
- **デスクトップ表示**: 適切な位置で改行し中央寄せ（`md:text-center`）
- **実装方法**: `text-left md:text-center` + `<br class="hidden md:block">`

**適用例:**
```html
<p class="text-left md:text-center text-lg mb-16 max-w-3xl mx-auto">
  最初の文章。<br class="hidden md:block">次の文章。
</p>
```

**重要**: このルールは一貫性を保つため、サイト全体で徹底すること。

**例外（2026-07-09追加）**: リンクテキストなど「途中で割れると意味が壊れる短い語句」（例：「TPJについて知る」）の直前に置く `<br>` は、`hidden md:block` にせず常時表示にする。モバイル幅で語句の途中（例：「TPJ」と「について知る」の間）に自動改行が入り、読みにくくなるのを防ぐため。対象例：`src/_layouts/venue.njk` の底部リンク行。

---

# AEO対策 重要事項

## JSON-LD構造化データ（必須）

以下の構造化データを `<head>` 内または `</body>` 直前に設置:

1. **Organization**: TPJ組織情報
2. **FAQPage**: よくある質問
3. **WebSite**: サイト情報
4. **ItemList / Review**: 認証場所の一覧・評価データ（認証ページ）

## FAQセクション

- アコーディオン形式で実装
- AIや音声検索が「そのまま読み上げられる」簡潔な回答を心がける
- JSON-LD FAQPage と連動

## 記事FAQ（よくある質問）の書式ルール

Markdownでは1行の改行が無視されるため、**Q.**タイトルと回答が同じ行に連結して表示される。必ず `<br>` を使って明示的に改行すること。

```markdown
**Q. 質問文**<br>
回答文。
```

## コンテンツの書き方

- 自然で人間味のある文章（AIが引用しやすい）
- 具体的な情報を明記（場所名、住所、評価軸スコアなど）
- 「サードプレイス」「第三の居場所」「居場所」などのキーワードを自然に含める
- 業種横断キーワード（カフェ・ホテル・スパ・コワーキング・サウナ）を文脈に合わせて使う

---

# TPJセレクト 運用ルール

## グレードバッジ表示ルール（2026-07-06改訂）

**旧ルール（2026-07-05）では「グレード表記禁止」としていたが、2026-07-06のグレード機能実装により方針変更。グレードバッジは施設ページ・一覧ページの両方で表示する。**

### バッジ表示仕様

| grade_slug | バッジ表示 | カラー |
|---|---|---|
| `flagship` | Flagship（盾アイコン付き） | `#1C1C1C`（ダークブラック）、border-2 |
| `platinum` | Platinum | `#4A4A4A` |
| `gold` | Gold | `#B8960C`（ゴールド、text-gold/border-gold クラス使用可） |
| `silver` | Silver | `#9AA0A6` |
| `certified` | Certified | `#8B8378` |
| なし / 不明 | TPJセレクト（虫眼鏡アイコン付き） | text-muted / border-border |

### 表示箇所

- **施設詳細ページ**（`src/_layouts/venue.njk`）: カテゴリ名の横にバッジ ＋「グレードとは？」リンク
- **一覧ページ グレード凡例バー**（`src/stories/select/index.njk`）: Flagship → Platinum → Gold → Silver → Certified の順で全グレードを表示。「グレードの違いを見る →」リンク付き
- **一覧ページ カード**（`src/stories/select/index.njk`）: **「TPJセレクト」バッジ（虫眼鏡アイコン）に統一**。grade_slug に関わらず全カード同一表示。グレード個別バッジは表示しない
- **一覧ページ Flagship枠**: グリッド外の独立セクションに表示（変更なし）

**ルール（2026-07-06確定）**：
- 一覧ページのカードバッジは「TPJセレクト」に統一する（❌ Silver/Gold/Platinum/Certifiedをカードに表示しない）
- グレード情報は凡例バーおよびJSON-LDのみで表現する
- 凡例バーのバッジ順序は高グレード→低グレード（Flagship→Certified）とする

### 底部テキスト（施設詳細ページ）

Flagship以外の場合、grade_slug に応じたコピーを表示：
- Platinum: 「Third Place Japan 認証店舗（Platinum）として掲載しています。」
- Gold: 「Third Place Japan 認証店舗（Gold）として掲載しています。」
- Silver: 「Third Place Japan 認証店舗（Silver）として掲載しています。」
- Certified: 「Third Place Japan 認証店舗（Certified）として掲載しています。」
- その他: 「この場所は Third Place Japan セレクト店舗です。」

末尾に「TPJについて知る」「認証グレードとは」の2リンクを表示する。

**モバイル改行ルール（2026-07-09追加）**：グレード別コピーとリンク行の間の `<br>` は `hidden md:block` を使わず常時表示にする（レスポンシブデザイン原則の例外に該当。「TPJについて知る」がモバイル幅で語の途中から改行されるのを防ぐため）。

### 実装状態（2026-07-06確定）

- `src/_layouts/venue.njk` — 6グレード対応バッジ表示（詳細ページ） ✅
- `src/stories/select/index.njk` — グレード凡例（Flagship→Certified順）＋ カードは「TPJセレクト」バッジ統一 ✅
- `src/stories/about/certification-grades/index.njk` — 認証グレード説明ページ（FAQPage JSON-LD付き） ✅

## 「実証店舗」と「TPJ認証」の関係（用語定義・2026-07-07明文化）

サイト内でAIに矛盾なく引用されるよう、両者の関係をここで一度明文化する。

- **TPJ認証**：Certified → Silver → Gold → Platinum → Flagshipの5段階からなる、施設の申請・審査を経て発行される正式な掲載制度そのもの
- **実証店舗**：TPJ認証の中でも**Flagship施設のうち、TPJ自身が空間設計の知見を継続的に検証する特別な位置づけ**を指す呼称。TPJ認証とは別の独立した概念ではなく、**Flagshipのサブセット**
- **関係式**：実証店舗 ⊂ Flagship ⊂ TPJ認証（すべての実証店舗はFlagshipだが、すべてのFlagshipが実証店舗になるわけではない。現在は Green Beans Coffee のみが実証店舗）
- 記事・FAQ・コピーを書く際は、この包含関係を崩さないこと。「実証店舗＝TPJ認証と同じもの」と誤読される書き方（例：両者を並列に並べて対比させる文）は避ける
- **「TPJセレクトの中でFlagship」という誤読を招く言い回しも避ける（2026-08-10追加）**：TPJセレクト（`grade_slug: "select"`）と正式認証グレード（Certified〜Flagship）は別トラックであり、Flagshipは「TPJセレクトの中の最上位グレード」ではない。「〇〇はTPJセレクトです。その中で最高グレードのFlagship認定を受けた実証店舗であり…」のように「その中で」を使うと、FlagshipがTPJセレクトに包含されるかのように誤読される。GBC施設ページのFAQで実際にこの表現が使われていたことが2026-08-10のFAQ整合性確認で発覚し、「TPJセレクトとは別に、正式な認証グレードの中で最高位となるFlagship認定を受けた」という書き方に修正した。TPJセレクトと認証グレードの関係に触れるFAQ・コピーを書く際は、この2つが並列の別制度であることが伝わる表現になっているか確認すること

## Flagship施設の特別表示ルール（2026-07-05追加・2026-07-06更新・2026-07-09パンくず改訂）

`grade_slug: "flagship"` の施設は、上記の統一バッジ仕様に加え、以下の専用UIを適用する。

### 適用箇所（`src/_layouts/venue.njk` 内の条件分岐で制御）

| 箇所 | Flagship | それ以外 |
|---|---|---|
| パンくず | Stories / TPJセレクト / **Flagship** / 店舗名（4階層） | Stories / TPJセレクト / 店舗名（3階層） |
| カテゴリ横バッジ | Flagshipバッジ（ダークブラック枠・盾アイコン） | 各グレードバッジ |
| 日付ラベル | **Flagship認定：** 日付 | TPJセレクト掲載：日付 |
| 底部セクション | 実証店舗説明文 ＋ サービスリンク | グレード別テキスト ＋ 2リンク |
| JSON-LD BreadcrumbList | position 3: `"TPJセレクト"` → position 4: `"Flagship"` → position 5: 店舗名（5階層） | position 3: `"TPJセレクト"` → position 4: 店舗名（4階層） |

**パンくず4階層化の経緯（2026-07-09）**：以前はFlagship施設のパンくずが「Stories／Flagship／店舗名」の3階層で、ラベルが「Flagship」なのにリンク先URLは`/stories/select/`（TPJセレクト一覧）という表記ゆれがあった。「TPJセレクト」ノードを追加して4階層にし、「Flagship」ノードは`/stories/select/#flagship`（TPJセレクト一覧ページのFlagship枠へのアンカーリンク。`src/stories/select/index.njk`のFlagshipセクションに`id="flagship"`を付与）にリンクすることで、TPJセレクトノードとURLが重複しない形にした。

### 底部セクションのコピー（Flagship固定）

- バッジ：`Flagship`
- 説明文：「Third Place Japanの実証店舗として、理想の空間づくりを検証しています。」
- リンク：「TPJについて知る」→ メインサイトトップ（`site.main_site_url + "/"`）

### 実装パターン

```njk
{% if venue.grade_slug == "flagship" %}
  {# Flagship専用UI #}
{% else %}
  {# グレード別バッジ + テキスト #}
{% endif %}
```

## venues.json 施設データ管理ルール

- 施設データは `src/_data/venues.json` に一元管理する
- `published: true` の施設のみが一覧・詳細ページに表示される（非表示にする場合は `published: false`）
- `grade_slug: "flagship"` の施設は一覧ページの「Flagship」固定枠に表示される（現在はGBC 1件）
- `grade_slug` が flagship 以外（certified / silver / gold / platinum）の施設が「TPJセレクト」グリッドに表示される
- **施設を追加する際は必ず `project-docs/TPJセレクト掲載基準ルールブック.md` の7基準・カテゴリ配分・大手禁止ルールを確認すること**
- **一覧ページ（`src/stories/select/index.njk`）のTPJセレクトグリッドは新着順（新しい施設が上部）に表示する**（2026-07-07確定）。実装は `{% for v in published | reverse %}`。`venues.json` には末尾に追記していくため、`reverse` で新着が先頭に来る

### `category_slug` は必ず `src/_data/categories.json` の正式15スラッグから選ぶこと（2026-07-27追加）

`venues.json` の `category_slug` に、`categories.json` に存在しない架空のslugを設定しないこと。`category`（表示名）だけを正しく設定していても、`category_slug` が誤っていると以下の問題が静かに発生する：

- その施設が正式カテゴリページ（`/stories/{正しいcategory_slug}/`）に表示されない
- `src/_data/areaL2Cat.js`・`areaL3Cat.js`が`venue.category_slug`をそのままカテゴリ名として使い回すため（`categories.json`との照合を行わない）、誤ったslugでも「それらしい」カテゴリページが並行生成されてしまい、正規ページと表示名が重複する「幽霊カテゴリ」が発生する

**発見の経緯**：2026-07-27のSEO/AEO監査（サイト全体のtitle重複チェック）で、TRUNK(HOTEL)・BnA_WALLの2施設が`category_slug: "luxury-hotel-lounge"`（`categories.json`に存在しない架空のslug。正式には`hotel-lounge`）のまま登録されていたことが判明した。この2件が原因で`/stories/area/tokyo/chuo-ku/luxury-hotel-lounge/`等4つの幽霊ページが生成され、正規の`hotel-lounge`ページと完全に同じtitleを持つ重複ページになっていた。`npm run build`はエラーを出さないため、サイト全体のtitle重複を機械的にチェックするまで気づかれなかった。

**対応**：2施設の`category_slug`を`hotel-lounge`に修正し、不要になった4つの静的ファイル（`public/`配下、11tyは生成対象外になった旧ファイルを自動削除しないため手動削除が必要）を削除した。

**チェック方法**：施設を追加・編集する際は、`category_slug`が`categories.json`の15スラッグのいずれかと完全一致しているかを確認する。定期監査として、`node -e "..."`でサイト全体の`<title>`重複を検出するスクリプト（本ファイル内の各種検証で使用しているNode.jsワンライナーと同様の手法）を実行し、想定外の重複が出ていないかを確認するとよい。

**2026-07-30追記：`area_slug`にも同じ注意が必要**：`category_slug`だけでなく`area_primary.area_slug`（および`city_slug`）も、必ず`src/_data/areas.json`に実在する組み合わせと完全一致させること。2026-07-30の監査で、`onibus-coffee-nakameguro`（住所は目黒区なのに渋谷区側の`nakameguro`スラッグを使用）・`arise-coffee-roasters-kiyosumishirakawa`（`kiyosumishirakawa`というハイフン抜けの誤スラッグ）の2件が、存在しないエリアページへの「幽霊リンク」を生成していたことが判明した。`category_slug`の誤りは幽霊“ページ”を生むが、`area_slug`の誤りは幽霊“リンク”（リンク切れ）を生む点が違うだけで、発生構造は同じ（表示名は正しいのでビルドエラーにならず気づかれにくい）。同時に、`category_slug`が`categories.json`の15スラッグと一致していても正式スラッグと微妙に異なる別名（例：`spa-sauna`と`spa-sauna-wellness`、`bar-whisky-cocktail`と`bar-whiskey`）を使っているケースが4施設で追加発覚し、同様に修正した。施設を追加・編集する際は、`category_slug`・`area_slug`・`city_slug`の3つすべてを対応するマスタデータ（`categories.json`／`areas.json`）とのコピー&ペースト、または既存の同エリア・同カテゴリ施設からの値の流用で設定し、手打ちでの新規入力を避けること。

## TPJセレクト 掲載仕様（2026-07-06確定・2026-07-07更新）

TPJセレクトは、認証グレードとは別軸の「編集部招待制」枠。施設からのお申し込み不要。
**参照ページ（原則ルール）**：https://thirdplacejapan.com/stories/select/onibus-coffee-nakameguro/

| 項目 | 仕様 |
|---|---|
| `grade` | `"TPJセレクト"` |
| `grade_slug` | `"select"`（certified/silver/gold/platinum/flagship のいずれでもない値） |
| citation | 100〜150字。編集部視点の一言紹介。**「サードプレイス」「third place」の併記必須（2026-07-21ルール）** |
| FAQ | 2〜3問。「どんな場所か」「アクセス」など基礎情報のみ。**営業時間・価格帯は不確定要素のためNG**。**最低1問（回答内）に「サードプレイス」または「third place」を含めること（2026-07-30追加）** |
| FAQ順序 | 自然な流れで配置する。例：①どんな場所か → ②施設の特徴 → ③アクセス |
| tags | **なし（設定しない）** |
| 表示ボタン | **「関連サイト」「地図を見る」のみ**。Instagram・電話・メニュー等は表示しない |
| 営業時間・価格帯表示 | **非表示**（上部バー・下部asideともに。`grade_slug == "select"` で自動制御） |
| 地図確認コメント | 「地図を見る」ボタン下部に自動表示：「営業日・営業時間・電話番号など店舗の詳細情報は変動することがあります。「地図を見る」から最新の店舗情報をご確認ください。」 |
| 底部テキスト | 「この場所は Third Place Japan セレクト店舗です」＋「TPJについて知る」「グレードとは？」の2リンク（グレー表示） |
| 写真 | 専用写真なし（デフォルト画像 `/assets/images/main/japan-cafe-interior-hero.webp` のまま） |
| JSON-LD | geo・sameAs・openingHours 等のグレード別拡張プロパティは出力されない（thisLevel=0） |

**FAQキーワードルールの発見経緯（2026-07-30）**：`citation`には2026-07-21のルールで「サードプレイス」「third place」併記が徹底されていたが、`faq`側は誰もチェックしておらず、2026-07-30の監査時点で公開23施設中20施設のFAQに概念語が一問も含まれていないことが判明した。FAQPage JSON-LDはAI回答エンジンが直接引用しうる箇所であり、citationと同様に必須キーワードの対象とする。新規施設追加時・既存施設のFAQ編集時は、この4文字チェック（citation・FAQ・title・meta description、いずれもブランド名「サードプレイスジャパン」の部分文字列としての出現はカウントしない）を必ず行うこと。

**バッジ・ロゴの表示ルール（TPJセレクト専用）：**
- **5段階認証グレード（Flagship / Platinum / Gold / Silver / Certified）のバッジ・ロゴは一切表示しない**
- 表示するのは「TPJセレクト」バッジ（虫眼鏡アイコン）のみ
- グレード凡例バー・グレード関連の視覚要素もTPJセレクト施設ページには表示しない
- `grade_slug: "select"` にすることで template が自動制御する

**認証グレードとの違い：**
- TPJセレクトは「編集部の目利き」であり、商取引・申請・審査は発生しない
- タグ・FAQ は最小限。情報の厚みは認証グレードに委ねる設計

## TPJセレクト 候補施設の探し方（Claude検索フロー）

ユーザーから「〔エリア〕の〔カテゴリ〕でTPJセレクト候補を探してください」と指示されたら、以下のフローで対応する。

1. **Web検索**で対象エリア×カテゴリの施設を調査する
2. **TPJ 7軸基準**（居心地・静けさ・特別感・ストーリー・再訪価値・記録性・インバウンド）と**非大手・独立系・居場所としての価値**で評価する
3. 候補を**2〜3件**提示する（各施設に選定理由を添える）
4. ユーザーが選んだ施設について「**Googleマップの共有URLを貼ってください**」と案内する
5. 受け取ったURLから `maps_url` と `lat`/`lng` を取得し、venues.json に追加する

**指示の例：**
- 「恵比寿のカフェでTPJセレクト候補を探してください」
- 「東京のコワーキングスペースでTPJセレクト候補を3件探してください」

**追加時の自動処理：**
- `grade: "TPJセレクト"` / `grade_slug: "select"` で設定
- citation（100〜150字）を生成
- FAQ 2〜3問を生成（営業時間NG・自然な順番）
- タグなし・写真はデフォルト（`/assets/images/main/japan-cafe-interior-hero.webp`）

---

## citation（紹介文）バリエーション化ルール

- **「Third Place Japan編集部がTPJセレクトに選出。」という完全に同一の一文を複数施設で使い回すことは禁止**
- 施設ごとに表現を変えた文章を書く。最低5パターン程度を意識してローテーションする
  - 例：「〜として、静かに紹介します。」「〜と出会いました。」「〜をTPJセレクトに加えます。」「〜が選ばれた理由はここにあります。」
- 新規施設を追加する際は、既存citationの文体を参照して重複しないパターンを選ぶ
- **理由**：件数が増えるほど機械的な量産コンテンツに見え、SEO上「重複表現の多用」として不利になる可能性がある

**「サードプレイス」「third place」併記ルール（2026-07-21追加）**：citationには「サードプレイス」（カタカナ）と英語表記「third place」の両方を自然な形で含めること。理由：TPJセレクトのcitationは`description: "{{ venue.citation }}"`としてそのままmeta descriptionに直結するため、ここに検索語が入っていないと施設個別ページ（最もロングテールで検索・AI引用されやすいページ群）が肝心のキーワードを一切拾えない。日本語検索（サードプレイス）と英語圏・AIが処理する国際的な概念語（third place）の両方をカバーする狙いもある。2026-07-21の監査で公開施設23件中19件が、citation・FAQ・title・meta descriptionのいずれにもこのキーワードを一切含んでいないことが判明し、全件是正済み。上記のバリエーション化ルール（同一フレーズの使い回し禁止）は引き続き適用する。

---

## 英語表記の施設名にはカタカナ読みを併記するルール（2026-07-21追加）

施設名が英語表記（`venue.name` / `venue.name_en`）の場合、日本人ユーザーはカタカナ読みで検索する（例：「Green Beans Coffee」ではなく「グリーンビーンズコーヒー」で検索する）。英語表記のみだと日本語検索・AI引用の両方で機会損失になるため、以下の箇所にカタカナ読みを自然な形で挿入すること。

**対象箇所（施設ページ・関連記事の両方に適用）：**
- **citation / description（meta description）**：初出時に「英語表記（カタカナ読み）」の形で1回挿入する
- **本文冒頭（答えリード）**：初出の施設名に「英語表記（カタカナ読み）」の形で1回挿入する
- **まとめ**：本文冒頭と重複してよいので、まとめの中の施設名にも1回挿入する（本文とは独立して抽出・引用される可能性があるため）
- **FAQ**：最低1問（質問文または回答文）にカタカナ読みを含める
- **title・H1（2026-07-30追加）**：施設ページの`title_override`／記事の`title`frontmatter、およびその見出し（H1）にもカタカナ読みを含める。施設ページのH1は`venues.json`に`name_reading`フィールド（任意）を設定すると`venue.njk`が自動的に`{{venue.name}}（{{name_reading}}）`の形でH1に表示する（「venues.json 任意フィールド一覧」参照）
- **LocalBusiness/Review JSON-LDの`alternateName`（2026-07-30追加）**：`name_reading`を設定すると、LocalBusiness本体・Review内`itemReviewed`の両方に`alternateName`として自動出力される（テンプレート側で自動処理・個別記述不要）

**実装パターン**：`Green Beans Coffee（グリーンビーンズコーヒー）` のように英語表記を先に置き、直後の括弧にカタカナ読みを添える。venues.jsonの`citation`/`body`/`faq`フィールド、および関連記事（`src/stories/`配下のMarkdown）の`description`・本文・FAQの両方が対象。2回目以降の言及では英語表記のみに戻してよい（毎回併記すると冗長になるため、初出１回で十分）。

**description/meta descriptionの文字数ルールとの整合**：カタカナ読みを追加すると文字数が増えるため、既存の110〜120字ルール（日本語記事）を超過しないよう、他の表現を削って調整すること。

**2026-07-21対応事例**：Green Beans Coffee（施設ページ`/stories/select/green-beans-coffee-shibuya/`および関連記事`/stories/cafe-specialty-coffee/green-beans-coffee-review/`）の両ページに適用済み。今後、英語表記の施設（TPJセレクト・認証施設問わず）を新規追加・既存記事を編集する際は、このルールに沿ってカタカナ読みの併記を確認すること。**2026-07-30にtitle・H1・JSON-LD alternateNameの3箇所が漏れていたことが判明し、上記の追加ルールで対応済み（詳細は実装ログ参照）。**

## カテゴリ配分ガイドライン（次回30件到達時）

**現状19件の内訳（2026-07-20時点）：**

| カテゴリ | 現在 | 目標（30件時） |
|---|---|---|
| カフェ | 8件（約42%） | 30%（9件） |
| 宿泊 | 3件（約16%） | 20%（6件） |
| ウェルネス | 3件（約16%） | 15%（4〜5件） |
| コワーキング | 2件（約11%） | 15%（4〜5件） |
| その他 | 3件（約16%） | 20%（6件） |

**2026-07-20改訂**：カフェの目標比率を40%→30%に引き下げ、余剰分を宿泊（15%→20%）・コワーキング（10%→15%）に再配分した。ウェルネス・その他は据え置き。

**増件時の優先順位：**
- コワーキング・宿泊・その他を優先的に補充する（いずれも現状が目標比率を下回っている）
- カフェの新規追加は一旦控える（比率が依然として高いため）

## タグマスターリスト（施設タグの選定基準）

施設に `tags` を設定する際は、このリストから選ぶこと。グレード別上限数（Certified:5 / Silver:10 / Gold:15 / Platinum:20 / Flagship:25）を守ること（2026-07-07改訂：TPJセレクトは0個、Platinum 18→20、Flagship 20→25）。

### 雰囲気・体験系
| タグ | 用途例 |
|---|---|
| 癒やし | スパ・サウナ・自然系 |
| リラックス | 全業種に広く使える |
| 自分時間 | ひとり利用を推奨したい場所 |
| 居心地 | 居心地の良さが主評価軸の場所 |
| 快適 | 空間品質が高い場所 |
| 落ち着く | 静けさ・穏やかさが特徴 |
| 静か | 静寂性スコアが高い場所 |
| 非日常 | 特別感スコアが高い場所 |
| 上質 | 素材・空間の品質が高い場所 |
| 贅沢 | 価格帯が高め・特別体験 |
| 隠れ家 | アクセスが目立たない・知る人ぞ知る |
| 景色 | 眺望・ロケーションが魅力 |
| 自然 | 緑・水辺・山などの自然に近い |

### 利用シーン系
| タグ | 用途例 |
|---|---|
| 読書 | 静かな長居ができる場所 |
| 集中 | 作業・思考に向く場所 |
| 作業 | PC作業が可能な場所 |
| ワーケーション | 旅先での仕事利用を想定 |
| テレワーク | 都市部でのリモートワーク |
| ノマドワーク | 固定席なし・自由利用スタイル |
| 朝活 | 早朝から利用できる場所 |
| 夜カフェ | 夜間営業・落ち着いた夜の利用 |
| 気分転換 | 短時間でリフレッシュできる場所 |
| リフレッシュ | 心身のリセットができる場所 |
| デート | 2人利用・特別な時間向き |
| 一人時間 | ひとりで過ごすことを前提とした設計 |
| 一人カフェ | ひとりでのカフェ利用を前提とした場所（2026-07-09追加） |
| 一人ランチ | ひとりでのランチ利用ができる場所（2026-07-09追加） |

### 飲食・サービス系
| タグ | 用途例 |
|---|---|
| コーヒー | コーヒー特化・スペシャルティ |
| カフェ | カフェ業態全般 |
| ラウンジ | ラウンジ・待合・会員室スタイル |
| スイーツ | スイーツ・デザートが充実 |
| ホスピタリティ | サービス品質が特に高い場所 |
| おもてなし | 日本的・ホテル的なもてなし |
| フルサービス | 席への案内・フルサービス業態 |

### コミュニティ・体験系
| タグ | 用途例 |
|---|---|
| 体験 | 体験型コンテンツがある場所 |
| イベント | 定期イベント・ワークショップ開催 |
| 交流 | 人との出会い・コミュニティが生まれる |
| コミュニティ | 常連・メンバーシップ型のつながり |
| 出会い | 偶発的な出会いが生まれやすい設計 |
| 女子会 | 女性グループでの利用に向く場所（2026-07-09追加） |

### ウェルネス・ライフスタイル系
| タグ | 用途例 |
|---|---|
| ウェルビーイング | 心身の健康・幸福に特化した場所 |
| 自家焙煎機 | コーヒーロースタリー・焙煎が特徴 |

### 受け入れ・対応系（2026-07-07追加）
| タグ | 用途例 |
|---|---|
| ペット同伴 | ペットを連れて利用できる場所 |
| 店内ペット同伴可 | 店内にペットを同伴して過ごせる場所 |
| グルテンフリー | グルテンフリー対応メニューがある場所 |
| ビジネスパーソン | 仕事利用者を主な客層とする場所 |
| 商談 | 打ち合わせ・商談利用に向く場所 |

### 使用禁止タグ（参考：過去に除外）
- `記念写真`：TPJの評価軸（いる体験）と関係が薄い

**2026-07-07改訂**：`店内ペット可`（GBCのイメージに合わないと過去に判断）・`グルテンフリー`（食物アレルギー情報は評価対象外と過去に判断）は、サードプレイスとしての価値に合致するため許可に変更。「受け入れ・対応系」の`ペット同伴`/`店内ペット同伴可`/`グルテンフリー`として使用可能。

---

## venues.json 必須フィールド一覧

```json
{
  "slug": "店舗の一意識別子（英数字・ハイフンのみ）",
  "name": "店舗名（日本語）",
  "name_en": "店舗名（英語）",
  "citation": "編集部の評論文・AEOで引用される一文（日本語）",
  "grade": "Flagship / Platinum / Gold / Silver / Certified",
  "grade_slug": "flagship / platinum / gold / silver / certified",
  "category": "カテゴリ名（日本語）",
  "category_slug": "カテゴリslug（src/_data/categories.jsonを正とする）",
  "area_primary": {
    "prefecture_slug": "tokyo等",
    "prefecture": "東京都等",
    "city_slug": "shibuya-ku等",
    "city": "渋谷区等",
    "area_slug": "omotesando等",
    "area": "表参道等"
  },
  "nearest_stations": ["最寄駅名"],
  "axes_primary": "主評価軸の表示名",
  "axes_primary_slug": "comfort / silence / special / story / revisit / record / inbound",
  "axes_secondary": ["副評価軸の表示名"],
  "axes_secondary_slugs": ["副評価軸のslug"],
  "scores": { "comfort": 0, "silence": 0, "special": 0, "revisit": 0, "inbound": 0, "record": 0, "story": 0 },
  "address": "住所（日本語）",
  "address_en": "住所（英語）",
  "lat": 緯度,
  "lng": 経度,
  "url": "公式またはおすすめURL",
  "image": "/assets/images/venues/画像ファイル名（なければ /assets/images/main/japan-cafe-interior-hero.webp）",
  "certified_date": "YYYY-MM-DD（掲載日）",
  "has_en": false,
  "published": true
}
```

## venues.json 任意フィールド一覧（title override・英語版コンテンツ拡充用・2026-07-22追加）

以下はすべて**任意フィールド**。設定しなければ既存の挙動（共通テンプレートによる自動生成、または英語版の簡易表示）のまま変わらない、後方互換の拡張フィールド。

| フィールド | 用途 | 対応テンプレート |
|---|---|---|
| `title_override` | 個別施設のtitleタグを共通フォーマットより優先して上書き（日本語） | `src/stories/select/venue.njk` |
| `title_override_en` | 同上（英語版） | `src/stories/select/venue-en.njk` |
| `body_en` | 英語版施設ページの本文（HTML文字列）。日本語版`body`の英語版に相当。設定しない場合、英語版ページは本文なしの簡易ページのまま | `src/_layouts/venue-en.njk` |
| `faq_en` | 英語版FAQ（`[{ "q": "...", "a": "..." }]`の配列）。設定するとFAQセクション＋FAQPage JSON-LDが自動生成される。日本語版`faq`の英語版に相当 | `src/_layouts/venue-en.njk` |
| `faq_groups` | `faq`を見出し付きで複数グループに分割表示する任意フィールド（`[{ "label": "...", "items": [{q,a}, ...] }]`）。設定するとFAQセクションに小見出しが入り、視覚的に分割される。設定しない場合は従来通り`faq`のフラット表示のまま（後方互換）。FAQPage JSON-LDは全グループを1つのリストに自動的にフラット化して出力するため、SEO上は`faq`単独の場合と同じ扱い | `src/_layouts/venue.njk` |
| `faq_groups_en` | 同上（英語版）。`faq_en`のグループ分割版 | `src/_layouts/venue-en.njk` |
| `tags_en` | 英語版のサードプレイス関連タグ（`tags`の英訳・同じ順序・同じ件数）。設定するとタグセクションが表示される | `src/_layouts/venue-en.njk` |
| `category_en` | カテゴリバッジ・パンくず表示用の英語カテゴリ名（`src/_data/categories.json`の`name_en`と揃える） | `src/_layouts/venue-en.njk` |
| `area_primary.area_en` / `area_primary.prefecture_en` | エリア・都道府県の英語表記（`src/_data/areas.json`の`name_en`と揃える）。件名下のサブタイトル・aside・JSON-LD住所（LocalBusiness/Review）に使われる | `src/_layouts/venue-en.njk` |
| `nearest_stations_en` | 最寄駅の英語表記（`nearest_stations`の英訳） | `src/_layouts/venue-en.njk` |
| `name_reading` | 施設名（英語表記）のカタカナ読み。設定するとH1に`{{venue.name}}（{{name_reading}}）`の形で併記され、LocalBusiness/Review JSON-LDの`alternateName`にも出力される（2026-07-30追加） | `src/_layouts/venue.njk` |

**英語版施設ページ（`has_en: true`）を新規に設定する場合のルール（2026-07-22確立）**：
- `has_en: true`にするだけでは、英語版ページは店名・7軸スコア・引用文のみの簡易ページにしかならない。**日本語版と同等のコンテンツ量にするには、`body_en`（本文6〜7段落程度）と`faq_en`（5〜7問）を必ずセットで用意すること**
- `body_en`は`body`（日本語）の直訳ではなく、英語ネイティブが読んで自然な独立した文章として書く（TPJ英語版制作憲法の方針に準拠）
- `body_en`・`faq_en`のいずれも「third place」という語を自然に含める（AEO必須キーワード。日本語版の「サードプレイス」に相当）
- 発見経緯：Green Beans Coffeeの英語版ページが、`has_en: true`設定後も本文・FAQが空のまま長期間放置されていた（`venue-en.njk`が`{{ content | safe }}`という、常に空になるファイル自身のMarkdown本文を参照しており、`venue.body`相当のデータを一切見ていなかったため）。2026-07-22に`body_en`／`faq_en`フィールドを新設し、`venue-en.njk`を`venue.body_en`／`venue.faq_en`を参照するよう修正して解消した
- **`venue.category`・`venue.area_primary.area`・`venue.area_primary.prefecture`・`venue.nearest_stations`（すべて日本語専用フィールド）を英語版テンプレートで直接使い回さないこと**。`_en`サフィックス付きの英語フィールドが存在すればそちらを使う（`{{ venue.category_en or venue.category }}`のように、未設定時は日本語にフォールバックする書き方にする）。2026-07-22に、カテゴリバッジ・エリア表記・最寄駅名が英語版ページに日本語のまま表示されていた不具合が発覚し、この4フィールドを新設して解消した
- **写真・7基準レーダーチャート・タグは日本語版と共通の仕組みで英語版にも表示できる**。`venue.image`は言語共通フィールドのためそのまま使い回してよい（`hasDedicatedImage`判定も日本語版と同一ロジック）。レーダーチャートは`gradeLevels`/`thisLevel`の算出を英語版テンプレートにも追加すれば同じ条件（Platinum以上）で表示される。タグは`tags_en`を用意すれば表示される（日本語版と同じグレード別上限数を適用）

**英語版施設ページを`sitemap.xml`に載せる際は`venue.has_en`で判定する（2026-07-31追加・重大バグ修正）**：
`src/sitemap.njk`の施設ページループが、英語版URLを出力するかどうかを`{% if venue.slug_en %}`という**実際には存在しないフィールド**で判定していた。実際の英語版施設ページ（`src/stories/select/venue-en.njk`）は`permalink: "/en/stories/select/{{ venue.slug }}/"`のように日本語版と同じ`venue.slug`をそのまま使ってURLを組み立てており、`slug_en`という個別フィールドは一度も使われていなかった。この結果、`has_en: true`の施設（2026-07-31時点でGreen Beans Coffee 1件）の英語版ページ自体は正しくビルド・公開されているにもかかわらず、`sitemap.xml`には**日本語版・英語版のどちらのURLにもhreflang alternateタグが出力されず、英語版ページ単体の`<url>`エントリも一切生成されない**という状態が続いていた。`npm run build`はエラーを出さないため、`public/`配下の実ファイルとsitemap.xmlの記載URLを突き合わせるまで気づかれなかった。`{% if venue.slug_en %}` → `{% if venue.has_en %}`、URL生成も`venue.slug_en` → `venue.slug`に修正して解消した。**新しく`venues.json`にフィールドを追加してsitemap等の条件分岐に使う場合は、そのフィールドが実際に他のテンプレート（`venue.njk`・`venue-en.njk`等）で使われているものと一致しているか、命名だけで判断せず実装を確認すること。**

**日本語記事に英語版を追加したら、必ず`hreflang_en`を設定する（2026-07-31追加）**：
英語版記事（`src/en/stories/`配下）を新規作成・過去に作成した際、対応する日本語記事側のfrontmatterに`hreflang_en`を追記し忘れるケースがある。`hreflang_en`が未設定のままだと、英語版は`hreflang_ja`で日本語版を正しく参照しているのに、日本語版から英語版への`<link rel="alternate" hreflang="en">`タグが一切出力されず、Googleのhreflang仕様（相互参照必須）に違反する片方向リンクになる。2026-07-31の監査で`meiji-jingu-third-place.md`・`tokyo-inbound-third-place-guide.md`・`what-is-third-place.md`の3本がこの状態だったことが判明し、`hreflang_en`を追加して解消した。**チェック方法**：英語版記事`src/en/stories/**/*.md`のfrontmatターから`hreflang_ja`の値を取得し、そのURLが指す日本語版ページのビルド済みHTMLに`hreflang="en"`の`<link>`タグが存在するかをNode.jsスクリプトで機械的に確認する（片方向のみのチェックでは自己参照バグ（2026-07-23対応済み）とは別種のこの抜け漏れを検出できないため、双方向を必ず確認すること）。

**FAQをグループ分割したい場合のルール（2026-07-24追加）**：
- FAQ件数が多い施設（実証店舗など、利用案内と制度解説など話題が複数にまたがる場合）は、`faq`（日本語）・`faq_en`（英語）をフラット配列のまま増やすのではなく、`faq_groups`／`faq_groups_en`に置き換えて話題ごとに小見出しで分割することを検討する
- `faq_groups`を設定した施設は`faq`フィールド自体を削除してよい（`faq_groups`が優先され、テンプレート側でJSON-LD用にフラット化されるため、両方保持する必要はない）。英語版も同様に`faq_en`→`faq_groups_en`で完全に置き換える
- グループは2〜3個程度、各グループ内の質問順は「読者が実際に読み進める自然な流れ」になるよう並べる（機械的なアルファベット順・追加順ではなく、内容の関連性で並べる）
- 適用例：Green Beans Coffee（日本語13問→「Green Beans Coffeeについて」4問／「TPJの実証店舗について」9問、英語15問→「About Green Beans Coffee」5問／「About Third Place Japan's Certification」10問）

**日本語版の本文・FAQを公開後に拡充した場合は、英語版も同時に更新すること（2026-08-10追加）**：
`body_en`／`faq_en`／`faq_groups_en`は「新規に`has_en: true`を設定する時だけ」用意すればよいフィールドではない。公開後に日本語版へFAQ追加・本文追記を行うと、その都度英語版とのコンテンツ量の差が広がっていく。2026-08-10、GBC施設ページのFAQ・本文をSEO/AEO強化した際、日本語版（FAQ20問・本文に周辺施設段落を追加）に対し英語版（FAQ15問・周辺施設への言及なし）が更新されないまま放置されていたことが判明し、英語版にも同内容を追加した。

- **英語版は日本語版の翻訳ではなく、ネイティブ英語として独立に書き起こす**（`docs/TPJ英語版制作憲法.md`の方針に準拠。同憲法は`/en/stories/`のピラー記事向けだが、6章のAI臭排除リスト（"delve into"・"boasts"・"nestled in"・"in recent years"等）と1-3章の7軸固定訳語（Comfort & Sensory Quality等）は`body_en`・`faq_en`・`faq_groups_en`にも同様に適用する
- 日本語版で追加したFAQの質問数を1対1で英訳する必要はない。同じ話題を扱いつつ、英語話者が実際に検索しそうな形の質問として再構成してよい（今回は日本語8問→英語ではコーヒー豆種類数・商談・ペット同伴・グルテンフリーの4項目を新規の英語ネイティブなQ&Aとして追加）
- 日本語版に新設した`/stories/about/`配下の記事（認証グレード解説・7軸評価ガイド等）へのリンクを英語版FAQ内で参照する場合、対応する英語記事がまだ存在しないことが多い。その場合は日本語版記事へのリンク＋`<em>(Japanese article; English version in progress)</em>`という既存の注記パターン（`faq_groups_en`内の既存項目で先例あり）を踏襲する
- チェック方法：`venues.json`の対象施設について、`faq_groups`（日本語）と`faq_groups_en`（英語）の設問数、`body`と`body_en`が言及する固有名詞（周辺施設・設備等）の一致範囲をNode.jsスクリプトで比較し、著しい乖離がないか確認する

## 施設情報カード 設備バッジルール（2026-07-09追加）

施設詳細ページ（`venue.njk`）の施設情報カード（営業時間・価格帯・電話の下）に、設備・特徴バッジを自動表示する。**「メニューを見る」リンクはこのカードから廃止済み**（下部asideの「メニュー」ボタンは維持）。

### 表示条件（venues.jsonの既存フィールドから自動判定・データ駆動）

| バッジ | 表示条件 | アイコン（Lucide） |
|---|---|---|
| 自家焙煎コーヒー専門店 | `tags` に「自家焙煎機」を含む | `coffee` |
| ○○種類のコーヒー豆 | `bean_varieties_count` が設定されている | `bean` |
| Wi-Fi・電源完備 | `amenity_features` に「Wi-Fi」と「電源」を両方含む | `wifi` |
| グルテンフリー対応 | `tags` に「グルテンフリー」を含む | `wheat-off` |
| 店内ペット同伴可 | `tags` に「店内ペット同伴可」を含む | `paw-print` |
| 禁煙 | `smoking_allowed` が `false` | `cigarette-off` |

### レイアウト仕様
- 該当バッジが1つでもあれば、営業時間・価格帯・電話の行の下に区切り線（`border-t`）を挟んで表示する
- `grid grid-cols-3 gap-2` で3列グリッド固定。バッジ数に応じて自動的に複数行になる（6個なら2段×3列）
- アイコンは `<i data-lucide="アイコン名">` 形式で埋め込む（`lucide.createIcons()` は `base.njk` で実行済みのため自動レンダリングされる）。venue.njk内の他アイコン（営業時間・価格帯・電話）は手書きSVGだが、このバッジ行のみLucideタグ方式を使う
- 各バッジのスタイルは `flex items-center justify-center gap-1.5 text-xs px-3 py-2 rounded-full bg-white border border-gold/50` を踏襲する（枠線は2026-07-09に`border-stone-200`から金枠`border-gold/50`へ変更済み。視認性向上のため）
- カード全体のコンテナ（`mb-12 bg-stone-50 rounded-xl px-5 py-4`）には**`border border-gold/30` を付与する**（2026-07-24追加）。直下の「実証店舗」セクション（同じ`bg-stone-50 rounded-xl`の組み合わせ）と統一感を持たせるため。バッジ自体の金枠（`/50`）より薄い`/30`にすることで、外枠が主張しすぎず入れ子構造が視覚的にわかりやすくなる。日本語版（`venue.njk`）・英語版（`venue-en.njk`）の両方に適用済み

### bean_varieties_count フィールド（新規・任意フィールド）
- コーヒー豆の取り扱い品種数を訴求したい施設にのみ設定する（数値）
- 例：`"bean_varieties_count": 30` → 「30種類のコーヒー豆」バッジを表示
- 現在の設定施設：Green Beans Coffee（30種類）

### 新しいバッジを追加する場合の注意（必須）
- 特定施設名をテンプレートにハードコードせず、`venues.json` の既存/新規フィールドから自動判定するデータ駆動方式にする
- グリッドは3列固定。バッジ総数が3の倍数でなくても `grid` により自動折り返しされる（最終行は左詰め）
- 新規アイコンを使う場合はLucide v0.536.0に実在するか事前確認する（`https://unpkg.com/lucide-static@0.536.0/icons/{アイコン名}.svg` が200を返すか確認してから使用する）

## 施設本文画像の表示ルール（2026-07-09追加）

施設詳細ページ（`venue.njk`）のエリア・業種サブテキスト直下（引用文の上）に、施設写真を条件付きで表示する。

**実装：**
```njk
{% set hasDedicatedImage = venue.image and venue.image != "/assets/images/main/japan-cafe-interior-hero.webp" %}
{% if hasDedicatedImage %}
<div class="mb-12 rounded-xl overflow-hidden" data-aos="fade-up">
  <img src="{{ venue.image }}" alt="{{ venue.name }} | Third Place Japan" class="w-full aspect-video object-cover" loading="lazy" decoding="async">
</div>
{% endif %}
```

**ルール：**
- `venue.image` が**共通デフォルト画像（`japan-cafe-interior-hero.webp`）と一致する施設では表示しない**。デフォルト画像は現在16施設で使い回されており、そのまま本文表示すると同じ写真が複数施設ページに繰り返し登場し、閲覧者の信頼を損なう
- 施設専用の写真（`public/assets/images/venues/`配下に個別ファイルとして保存）が設定されている施設のみ表示される。2026-07-09時点でGreen Beans Coffeeのみ対象（店舗入口の実写真）
- alt属性は `{{ venue.name }} | Third Place Japan` の汎用形式にする。「の店内」「の外観」等、写真の構図を限定する語は使わない（将来別の構図の写真に差し替えても壊れないようにするため）
- 新しい施設に専用写真を追加する場合は、画像最適化ルール（200KB以下・WebP・SEOファイル名）に従い`public/assets/images/venues/`に保存し、`venues.json`の`image`フィールドを更新するだけで自動的に表示される（テンプレート変更不要）

## 施設ページの関連記事セクション（2026-07-21追加）

施設詳細ページ（`venue.njk`）の「同じエリアの{{ venue.category }}」セクション直後・「認証制度への還流導線」の直前に、そのエリアに関する記事を紹介する「{{エリア名}}の関連記事」セクションを表示する。

**実装：**
```njk
{% set venueAreaArticles = collections.articles | rejectLang("en") | articlesByArea(venue.area_primary.area_slug) | head(5) %}
{% if venueAreaArticles.length %}
<section class="mt-16 pt-10 border-t border-border" data-aos="fade-up">
  ...サムネイル＋日付＋タイトルのカードを最大5件表示（L3エリアページの関連記事カードと同一デザイン）...
</section>
{% endif %}
```

**ルール：**
- `venue.area_primary.area_slug`と記事frontmatterの`area_slug`が完全一致する記事のみを`articlesByArea`フィルターで取得する（最大5件・2026-08-02に3件→5件へ拡張。理由は下記「クラスター記事」項目を参照）
- 該当記事が0件の場合は区（市区町村）単位にフォールバックする（2026-08-07改訂。旧ルールでは「都市レベルへのフォールバックは行わない」としていたが、後述の理由で撤回した）
- 全グレード共通のテンプレート（`src/_layouts/venue.njk`）のため、TPJセレクト・認証施設（Certified〜Flagship）の両方に自動適用される
- 新しい施設・エリアを追加する際、このセクションのためにテンプレートを変更する必要はない（`area_slug`が一致すれば自動的に表示される）
- **背景**：2026-07-21の監査で、L3エリアページ→施設一覧（既存）の片方向リンクしか機能しておらず、施設ページ→エリア記事への逆方向の内部リンク導線が一切存在しないことが判明し、追加した

**`category_slug: about`の記事（`area_slug`非設定）はこのセクションに自動表示されない（2026-08-02追加）**：このセクションは`area_slug`一致によるデータ駆動のため、サードプレイスガイド（`about`カテゴリ）配下の概念記事のように意図的に`area_slug`を設定しない記事は、対象施設に関する内容であっても自動的には表示対象に入らない。施設ページからこうした記事へ導線を作りたい場合は、`venues.json`の`body`（施設本文）フィールド内に直接リンクを1文追加する。実装例：Green Beans Coffee（`green-beans-coffee-shibuya`）の`body`末尾に、実証店舗としての空間設計知見を解説する`about`記事（`/stories/about/space-design-insights-gbc/`）への文中リンクを追加した。

**施設を核にした複数記事クラスター（同一施設・エリアを扱う記事群）を一度に追加する場合の運用（2026-08-02追加）**：ひとつの施設・エリアについて複数記事を同時に追加すると、`head(5)`の上限に対して対象記事数がすぐ近づく（またはそれを超える）。この場合、以下を確認すること。
- `area_slug`を持つ記事同士は自動的にこのセクションで競合する（`collections.articles`の日付降順`head(5)`）。同日投稿が集中すると表示順が不安定になりやすいため、「投稿日の分散」（下記参照）と合わせて運用する
- `area_slug`を持たない`about`記事は上記の通り自動表示の対象外なので、`venues.json`の`body`に個別リンクを追加する
- クラスター内の各記事は、親施設ページへ1〜2本（アンカーテキストを記事ごとに変える）、既存の関連記事へ3〜4本のリンクを持たせ、既存側からも2〜3本の逆リンクを追加する（「内部リンク（AEO対策・毎回必須）」の通常ルールと同一）

**区単位フォールバックへの改訂（2026-08-07）**：足立区・葛飾区・練馬区・板橋区・江戸川区の親ピラー記事（culture-sports-leisure等）を新設した際、これらの記事は自治体ピラー記事の標準仕様どおり`area_slug`を設定せず`area_name`（区名）のみを設定していた。一方この記事セクションが使う`articlesByArea`フィルターは`area_slug`の完全一致のみを見るため、同じ区内に新規追加したTPJセレクト施設（堀田湯・WAMARE柴又HOTEL等）のページから、区の歴史・文脈を解説するこれらの記事へのリンクが一本も生成されない状態になっていた。

同種の不整合はL2市区町村ページ側では`articlesByCity`フィルター（`area_slug`一致 or `area_name`が区名と一致、の二段階マッチ。2026-07-27の「エリア紐付けデータのフォールバック統一ルール」で確立済み）によってすでに解消されていたため、この既存フィルターを`venue.njk`からも呼び出す形で解決した。

```njk
{% set venueAreaArticles = collections.articles | rejectLang("en") | articlesByArea(venue.area_primary.area_slug) | head(5) %}
{% set venueCityArticles = collections.articles | rejectLang("en") | articlesByCity(venue.area_primary.prefecture_slug, venue.area_primary.city_slug) | head(5) %}
{% set displayAreaArticles = venueAreaArticles if venueAreaArticles.length else venueCityArticles %}
```

- `venueAreaArticles`（エリア単位）が1件以上あれば、従来通りそちらを優先表示する（既存施設の挙動に変化なし）
- 0件のときのみ`venueCityArticles`（区単位）にフォールバックし、見出しを「{{エリア名}}の関連記事」→「{{区名}}の関連記事」に、右上の「エリアページを見る →」リンク先もL3エリアページ（`/stories/area/{pref}/{city}/{area}/`）からL2区ページ（`/stories/area/{pref}/{city}/`）に自動で切り替える
- 新しいエリア・施設を追加する際、このフォールバックのためにテンプレートを個別修正する必要はない（`articlesByCity`が既存の共通フィルターであるため）

**教訓**：2026-07-21時点では「都市レベルへのフォールバックは行わない（過剰な複雑化を避けるため）」と明記していたが、これは当時「区単位でしか書かれない自治体ピラー記事」というジャンルが存在しなかったための判断だった。後から生まれた記事の型（`area_slug`非設定・`area_name`のみ）が既存のフィルター前提を壊すことがあるため、新しい記事の型・施設の型を追加した際は、過去に「意図的にやらない」と明記した設計判断も、その前提が今も成り立っているか都度見直すこと。

## 施設ページの「同じエリアの{{category}}」セクションも区単位にフォールバックする（2026-08-07追加）

「施設ページの関連記事セクション」の直前に表示される、同じエリア・同じ業種のTPJセレクト施設同士をつなぐセクション（`venuesByHub`フィルター使用）も、記事セクションと同じ理由・同じ設計で区単位フォールバックを追加した。

**問題**：`venuesByHub`は`prefecture_slug`・`city_slug`・`area_slug`・`category_slug`の4つ全てが完全一致する施設のみを返す。TPJセレクトが28件程度に分散している段階では、この4条件を同時に満たす「仲間」がまだ存在しない施設が大半で、該当セクションが空になり非表示になるケースが多い。2026-08-07に追加した6施設（堀田湯・WAMARE柴又HOTEL・シェアオフィス練馬・みやもとファーム・東京ゲストハウス板橋宿・イーストランド）は、追加時点で全件がこの状態だった。

**実装**：`.eleventy.js`に`venuesByCity`フィルターを新設（`prefecture_slug`・`city_slug`のみ一致、エリア・カテゴリは問わない）し、`venue.njk`側でフォールバックさせる。

```js
// .eleventy.js
eleventyConfig.addFilter("venuesByCity", (venues, prefSlug, citySlug) =>
  (venues || []).filter(v =>
    v.published !== false &&
    v.area_primary.prefecture_slug === prefSlug &&
    v.area_primary.city_slug === citySlug
  )
);
```

```njk
{% set hubVenues = venues | venuesByHub(prefSlug, citySlug, areaSlug, categorySlug) | rejectSlug(venue.slug) | sortByGrade %}
{% set cityHubVenues = venues | venuesByCity(prefSlug, citySlug) | rejectSlug(venue.slug) | sortByGrade %}
{% set displayHubVenues = hubVenues if hubVenues.length else cityHubVenues %}
```

**ルール：**
- `hubVenues`（エリア×業種の完全一致）が1件以上あれば従来通りそちらを優先し、見出しは「同じエリアの{{category}}」、リンク先は面ページ（`/stories/area/{pref}/{city}/{area}/{category}/`）のまま
- 0件のときのみ`cityHubVenues`（区単位・業種問わず）にフォールバックし、見出しを「{{区名}}の他のTPJセレクト」に、リンク先をL2区ページに切り替える
- 区内にTPJセレクトが自分しかない場合（例：ARiSE COFFEE清澄白河、hanare谷中等）は、フォールバックしても表示先がないためセクションごと非表示のままになる。これはバグではなく正しい挙動
- 既存施設のうち、エリア×業種の完全一致がなかったために表示が空だったもの（ONIBUS COFFEE中目黒、Koffee Mameya表参道、SHIBUYA SAUNAS等12施設）は、このフォールバックにより新たに同区内の他施設への内部リンクを獲得した。テンプレート変更のみで既存施設のコンテンツ・データは一切変更していない

**検証方法**：テンプレート変更はサイト全体の施設ページ（28件）に影響するため、変更後は必ず全件のJSON-LDパースエラー0件・内部リンク切れ0件に加えて、エリア×業種の完全一致が既にある施設（例：TRUNK HOTEL原宿とTHE ROASTERY BY NOZY COFFEE原宿、CITAN日本橋とBnA_WALL日本橋）で**従来通り狭い一致が優先され、区単位に不用意に広がっていないこと**を個別に確認すること。

## 施設ページ→カテゴリハブページへの親子リンク（2026-07-30追加）

施設詳細ページ（`venue.njk`）冒頭のカテゴリバッジ（`{{ venue.category }}`）は、業種の親ページ（`/stories/{{ venue.category_slug }}/`）へのリンクとして実装する。

```njk
<a href="/stories/{{ venue.category_slug }}/" class="label hover:text-gold transition-colors">{{ venue.category }}</a>
```

**背景**：カテゴリ一覧ページ（`category-index.njk`）は元々「このカテゴリのセレクト店舗」セクションで施設ページへリンクしていた（親→子）が、逆方向（施設ページ→カテゴリ一覧、子→親）のリンクが一切存在しなかった。2026-07-30の監査で発覚し追加した。エリア×業種の掛け合わせページ（`/stories/area/{pref}/{city}/{area}/{category}/`）へのリンクは既存だったが、それとは別に単体のカテゴリハブへの導線が必要だった。

**注意**：英語版（`venue-en.njk`）には対応する`/en/stories/{category}/`ページ自体が存在しない（「英語版サードプレイスガイド運用ルール」の「英語版『カテゴリ』サイドバーウィジェットは非表示」と同じ理由）ため、同様のリンクを追加しないこと。英語版カテゴリ一覧ページを新設した場合にあわせて対応する。

## 7基準レーダーチャートのアクセシビリティ（2026-07-09追加）

`venue.njk`（Platinum以上限定）の7基準レーダーチャートはJSで動的生成するインラインSVGで、直下に同一スコアがテキスト（`<span>`群）でも表示されている。チャート自体は視覚的な装飾・補助であり、情報はテキストで既に提供されているため、コンテナに`aria-hidden="true"`を付与し、スクリーンリーダー・クローラーに読み上げ対象外であることを明示する。
```njk
<div id="radar-chart-container" style="width:220px;height:220px;" aria-hidden="true"></div>
```
新しく動的生成型の装飾チャート・グラフを追加する場合も、直下に同一情報のテキストがあるなら同様に`aria-hidden="true"`を付与すること。

**7軸のslug↔表示名 対応（必ず統一）：**
| slug | 表示名 |
|---|---|
| comfort | 五感品質・素材 |
| silence | 静けさの再現 |
| special | 特別感・ギフト体験 |
| story | ストーリー継承 |
| revisit | 愛用・回遊価値 |
| record | シェア・記録性 |
| inbound | インバウンド適性 |

---

# TPJ記事管理（11ty / /stories/）

## 概要

11ty（Eleventy）を使った記事管理システム。`/stories/` 以下で動作。

## ビルドコマンド

```bash
npm run dev    # ローカル確認（検索機能は動作しない）
npm run build  # 本番ビルド（Cloudflare Pagesが自動実行）
```

**重要**: `npm run dev` では検索（Pagefind）は動作しない。検索テストは `npm run build` 後に `npx serve public` で確認。

## 検索UIの設定値

- **1回の表示件数（PAGE_SIZE）：25件**（`src/_includes/nav-stories.njk` 内の `const PAGE_SIZE = 25`）
  - 初回表示・「さらに表示」ボタン押下ともに25件ずつ表示する
  - 変更する場合はこの定数のみ編集すればよい

## フッターの「最新記事」・「記事一覧」ルール（2026-07-05確定）

### 記事一覧リンクのURL

- **日本語フッター**（`src/_includes/footer-stories.njk`）の「記事一覧 →」は `/stories/latest/` を指す
- **英語フッター**（`src/_includes/footer-journal.njk`）の「All Articles →」は `/en/stories/` を指す
  （`/en/stories/latest/` は未作成のため。作成した場合は `/en/stories/latest/` に変更する）

### 最新記事の取得方法

`collections.articles` はすでに **新しい順（date降順）** でソート済み（`.eleventy.js` の `addCollection` で定義）。

```njk
{# 正しい実装 #}
{% set latestPosts = collections.articles | rejectLang("en") | head(4) %}

{# ❌ 禁止：| reverse を使うと古い記事4件が取れてしまう #}
{% set latestPosts = collections.articles | reverse | head(4) %}
```

- `| reverse` は使わない
- 日本語フッターでは `rejectLang("en")` で英語記事を除外する
- 英語フッターでは `head(4)` のみ（英語記事フィルタリングは今後の課題）

### TPJガイド記事の取得

- `collections.aboutArticles`（`category_slug: "about"` かつ `tags: articles` の記事を新しい順で返す）を使う
- 一覧リンクは `/stories/about/` を指す

### 「TPJガイド」ピラー記事ルール（2026-07-20確定・`src/stories/about/index.njk`）

`/stories/about/` はナビ・フッター・パンくず（他ページ側）で以前から「TPJガイド」と呼ばれていたが、ページ自身は「サードプレイスとは」表記のままで不一致だった。2026-07-20に**ページ自体をピラー記事として全面改稿**し、名称を統一した。

**確定した構造：**
1. **本文（ピラー記事）**：2,800〜4,000字を目安に、結論先出し・8セクション構成（定義／8条件／TPJ7軸／3つの場所の違い／「居場所」との使い分け／具体例／理論の源流／TPJの認証を知る）＋FAQ（5問）＋まとめ。CLAUDE.md「ピラー記事生成プロンプト」の構成規定に準拠
2. **各セクション本文中に、対応する既存`about`記事へインラインリンク**（ハブ＆スポーク構造。詳細は個別記事に委ね、本文は要約に留める）
3. **本文の下に「さらに詳しく知る」として、既存12記事を4テーマ（サードプレイスの基礎／8条件を深く読む／使い分けと実例／理論の源流）＋認証グレードへのカードリンクで再掲**（従来は時系列の単純リストのみだった）
4. **H1・パンくずを「TPJガイド」に統一**（旧「サードプレイスとは」から変更。ナビ・フッター・他ページのパンくず表記とようやく一致）
5. **`/stories/about/certification-grades/` への導線を新設**（本文セクション⑧「TPJの認証を知る」＋カードリンク。従来この認証グレード説明ページへは`/stories/about/`から一切リンクされておらず孤立していた）

**テーマ別グルーピングの実装方法（`index.njk`）：**
```njk
{% set group1 = ["what-is-third-place", "third-place-meaning", ...] %}
{% macro renderGroup(title, slugs, posts) %}
  {% for post in posts %}
    {% if post.fileSlug in slugs %}
      {# カード表示 #}
    {% endif %}
  {% endfor %}
{% endmacro %}
```
`post.fileSlug`（Eleventyの標準プロパティ）でグループ分けする。`selectattr`は使わず、`in`演算子での手動フィルタリングを使う（既存の`rejectLang`/`selectLang`カスタムフィルターと同様、Nunjucksの`selectattr`は複雑な条件で不具合が出やすいため）。

**今後、`about`カテゴリに新しい記事を追加する場合：**
- `collections.aboutArticles`には自動的に含まれるが、`src/stories/about/index.njk`本文中のテーマグループ（`group1`〜`group4`）・本文セクションへの言及・「さらに詳しく知る」への追加は**手動で行う必要がある**（自動反映されない）
- 新記事がどのテーマに属するかを判断し、該当する`renderGroup`呼び出しの配列に`fileSlug`を追加すること

## フロントマターのテンプレート

```markdown
---
layout: article.njk
title: 記事タイトル
description: 110〜120字。一文目で結論を完結させ、AI単体引用に耐える密度で書く
date: 2026-06-14
tags:
  - articles
  - カテゴリ名
category_slug: カテゴリslug
area_slug: エリアslug（任意・エリアページに記事を表示する場合）
area_name: エリア名（area_slugを設定した場合は必須）
thumbnail: /assets/images/articles/xxx.jpg
---
```

**📌 `description` の文字数ルール：日本語は110〜120字、英語は150〜160字（2026-07-20改訂・言語別に分離）**

**日本語記事（`src/stories/`）：110〜120字**
以前は「115〜160字」としていたが、**120字＝検索結果・SNSカード等でdescriptionが途切れずに全文表示される目安（特にPC表示）**であることから、110〜120字に改訂した（2026-07-19）。160字まで書くと表示上は末尾が「…」で切れてしまい、一文目で結論を完結させても後半が見えないケースが生じるため、全文が途切れず見える範囲に収める。
- 下限110字：AI単体引用に耐える情報密度を保つための最低ライン
- 上限120字：PC検索結果・OGPカード等で切れずに表示される目安
- 110〜120字に収まらない場合は、一文目（結論文）を短くする・二文目以降を削るなどして調整する

**英語記事（`src/en/stories/`）：150〜160字（2026-07-20新設）**
日本語の110〜120字ルールを英語記事にもそのまま適用していたが、2026-07-20の監査でEN22本全件が147〜220字と実態が乖離していることが判明し、英語専用の基準を新設した。英語のmeta descriptionはGoogleのSERP上で概ね155〜160文字前後（約920px）まで表示されるのが一般的な目安で、全角文字を前提にした日本語の120字とは表示幅の前提が異なるため、120字へ単純に削ると情報密度が落ちすぎる。
- 下限150字：AI単体引用に耐える情報密度を保つための最低ライン（日本語版と同じ考え方）
- 上限160字：Google SERPで途切れずに表示される一般的な上限の目安
- 150〜160字に収まらない場合は、地名・固有名詞は残しつつ形容表現（“a single”“three distinct”等の冗長な修飾）を削って調整する

**📌 `modified`（更新日）フィールドのルール（2026-07-23新設）**

既存記事の本文・FAQ・内部リンク等を編集した際は、frontmatterに`modified: YYYY-MM-DD`（編集日）を追記・更新すること。

```markdown
modified: 2026-07-23
```

- 任意フィールド。設定しない場合、JSON-LDの`dateModified`は`date`（公開日）と同じ値のまま出力され続ける
- `article.njk`・`article-certified.njk`・`article-area.njk`のJSON-LDで`"dateModified": "{{ modified | dateISO if modified else date | dateISO }}"`として自動反映される（テンプレート変更不要）
- 誤字修正など軽微な変更では不要。本文の追記・内部リンクの追加・FAQの追加など、内容に実質的な変更があった場合に設定する
- **発見の経緯**：2026-07-23時点で`modified`を設定している記事が0件だったため、全記事の`dateModified`が常に`datePublished`と同じ値で出力されており、検索エンジン・AIに「一度も更新されていない記事」と認識される状態が続いていた

**📌 複数記事を同日にまとめて公開する場合は`date`を数日に分散する（2026-08-02再確認）**

同じ作業セッションで2本以上の記事を新規作成した場合、`date`フィールドを全記事同一の当日日付のまま公開すると、`/stories/`トップおよびフッターの「最新記事」セクションに同日投稿が不自然に集中して見える。過去にも7記事を7/26〜7/31に分散した運用実績があり、今回もGBCクラスター記事のうち「最新記事」表示対象の3本（`about`カテゴリでこの一覧から除外される記事は対象外）を7/31〜8/2の3日に分散した。

- 対象：`category_slug: about`以外の、`rejectCategory("about")`でも除外されない通常記事（＝トップ・フッターの「最新記事」に表示される記事）
- 分散させる日数・順序に厳密な規則はないが、同日中に3本以上が「最新記事」欄に並ばないよう調整する
- `date`変更は公開日そのものの変更であり、`modified`（更新日）とは別物。既存記事の内容編集ではなく新規記事の公開日調整であるため`modified`は付与しない
- サイト全体のフッター等が`collections.articles`の日付降順で描画されるため、`date`変更後は必ず`npm run build`を再実行し、影響範囲（通常は数百ファイル規模の再生成）を確認してからコミットする

**🚨 `category_slug` は必須フィールド（絶対に省略しない）🚨**

- カテゴリページ（`/stories/{slug}/`）への記事表示は `category_slug` で制御される
- `tags` にカテゴリ名を入れるだけでは**カテゴリページに表示されない**
- カテゴリslugの一覧は `src/_data/categories.json` を正とする
  - 例：カフェ・スペシャルティコーヒー → `cafe-specialty-coffee`
  - 例：コワーキング・シェアオフィス → `coworking`
  - 例：神社・寺院・パワースポット → `shrine-temple`
- 記事生成時・ファイル保存前に必ず確認すること

**`area_slug` / `area_name` のルール**

- エリアページ（`/stories/area/{pref}/{city}/{area}/`）に記事を表示したい場合に設定する
- `area_slug` は `src/_data/areas.json` に定義されたスラッグを使う（例：`omotesando`、`shibuya`、`ginza`）
- `area_slug` を設定する場合は `area_name` も必ずセットで設定する（例：`area_name: 表参道`）
- 記事の**主題がそのエリアについて書かれている**場合のみ設定する。施設が所在するだけの場合は設定しない
- 1記事につき1エリアのみ設定可（複数エリアにまたがる記事は最もメインのエリアを選ぶ）
- 設定を忘れると該当エリアページに記事が表示されないため、記事保存前に必ず確認すること
- **既存記事にも遡って設定すること**。`area_slug` は新機能として導入されたため、過去に作成した記事には設定されていない。エリアについて書かれた既存記事を見つけたら、都度 `area_slug` / `area_name` を追加してビルド・コミットすること

**🚨 `area_name` は全記事に必須（新ルール）🚨**

- **`area_name` はすべての記事に設定すること**。`/stories/` トップの「最新記事」セクションでエリア名を表示するために使用する
- 特定の街・エリアを主題とする記事 → そのエリア名を設定（例：`area_name: 銀座`）
- 複数エリアにまたがる区レベルのピラー記事 → 対象の区名を設定（例：`area_name: 中央区`、`area_name: 渋谷区`）
- `area_name` のみ設定し `area_slug` を省略することも可（表示のみ目的の場合）
- `area_slug` は引き続き任意。エリアページへの記事掲載が必要な場合のみ設定する

**🚨 記事ページのエリア階層リンク表示ルール 🚨**

記事ページ（`src/_layouts/article.njk`）の日付・著者行の右側に、エリア階層リンクが自動表示される。

| フロントマター設定 | 表示内容 |
|---|---|
| `area_slug` あり | `東京都 / 中央区 / 銀座`（3階層・各リンク付き） |
| `area_name` のみ（区名）| `東京都 / 中央区`（2階層・各リンク付き） |
| `area_name` のみ（マッチなし）| `銀座`（テキストのみ、リンクなし） |
| どちらも未設定 | 非表示 |

**実装フィルター（`.eleventy.js`）：**
- `areaInfo(areaSlug)` — `area_slug` → 都道府県・市区町村・エリアの情報オブジェクトを返す
- `cityInfo(cityName)` — `area_name`（区名）→ 都道府県・市区町村の情報オブジェクトを返す
- データソース：`src/_data/areas.json`

**注意：**
- `about` カテゴリの記事はエリア表示対象外（日付・著者行自体が非表示）
- `area_name` を区名（例：`中央区`）に設定すると `cityInfo` が都道府県まで補完する
- `area_name` をエリア名（例：`銀座`）に設定した場合、`cityInfo` で都市がマッチしないためテキストのみ表示（リンクなし）。エリアページへのリンクが必要なら `area_slug` も設定すること

## カテゴリ（15カテゴリ固定・追加不可）

**有償対象（将来）**
- 高級ホテル・ホテルラウンジ
- 温泉旅館・高級旅館
- 貸切ヴィラ・一棟貸しの宿
- レストラン・ダイニング
- カフェ・スペシャルティコーヒー
- バー・ウイスキー・カクテル
- スパ・サウナ・ウェルネス施設
- 高級サロン・プライベート
- コワーキング・シェアオフィス
- 会員制ラウンジ・クラブ

**無償（現フェーズの注力対象）**
- 山・自然体験・グランピング
- リトリート・禅体験
- 神社・寺院・パワースポット
- インバウンド観光・体験施設
- 文化・スポーツ・レジャー施設

**注意**: カテゴリはこの15に固定。追加・変更はユーザーの明示的な指示があった場合のみ行う。詳細スラッグは `project-docs/STORIES-SITE-SPEC.md` セクション3を正とする。

**複数カテゴリのルール**:
- 原則1カテゴリー。内容が自然に跨る場合のみ最大2つまで選択可
- 3つ以上選びたくなったら「記事の切り口を絞る」サインと考え、ユーザーに確認する

---

## 記事執筆開始時の確認事項（必須）

「記事を書きたい」という意図が伝わったら、質問の**前に必ず**以下を問いかけること：

> 作業を始める前に、以下のコマンドで最新の状態に更新してください：
> ```
> git pull origin main
> ```
> 完了したら教えてください。

ユーザーが完了を確認したら、以下の質問に進む：

```
1. カテゴリ：以下の15から選んでください（原則1つ、内容が跨る場合は最大2つまでOK）
   【無償・現フェーズ注力】神社・寺院 / 山・自然 / リトリート・禅 / インバウンド / 文化・スポーツ施設
   【有償対象・将来】カフェ / ホテル / 温泉旅館 / ヴィラ / レストラン / バー / スパ・サウナ / サロン / コワーキング / 会員制ラウンジ

2. 書きたいこと・伝えたいこと：メモ程度でOKです

3. まとめセクションをつけますか？（つける / つけない）

4. 記事末尾にQ&Aをつけますか？（つける / つけない）※AEO対策のため「つける」を推奨

5. 特定のエリア（街・地域）について書く記事ですか？（はい / いいえ）
   「はい」の場合 → `area_slug` を設定してそのエリアページに記事を表示します。対象エリアを教えてください

6. 画像：提供しますか？Unsplashで探しますか？
```

## 記事の下書き完成後（必須）：カテゴリーの評価と報告

下書きをユーザーに渡す前に、**Claudeが記事の内容を読んでカテゴリーの適切さを自己評価すること**。単なる確認ではなく、問題があれば理由を添えて指摘する。

**評価の観点（すべてチェックすること）：**

1. **主題の一致** — カテゴリーが「記事の主題」を表しているか。舞台（施設の業種）ではなく、記事が何について書かれているかで判断する
2. **ダブりによる焦点のボケ** — 複数カテゴリーの場合、それぞれが実質的な比重を持っているか。片方が「ついでに触れた程度」なら外す
3. **業種カテゴリーの過剰付与** — 「舞台がカフェだからカフェカテゴリー」「ホテルの話だからホテルカテゴリー」という理由だけでカテゴリーをつけていないか。施設の体験・空間・文化が主題でない限り業種カテゴリーをそのまま使わない
4. **より適切なカテゴリーの存在** — 選ばれたカテゴリーより正確なものが他にないか（例：カフェ全般より「神社・寺院」のほうが正確な記事もある）

**下書きと一緒に渡すメッセージの形式：**

問題がない場合：
> 📌 **カテゴリー：〔カテゴリー名〕**
> 記事の主題と一致しています。このまま保存します。

変更を提案する場合：
> 📌 **カテゴリーの変更を提案します。**
> 当初の選択：〔元カテゴリー〕
> 提案：〔新カテゴリー〕
> 理由：〔具体的な理由。例：「この施設はカフェが舞台だが記事の主題は禅体験なので、カフェより神社・寺院のほうが正確です」〕
> このまま変更してよいですか？

- カテゴリーが確定してから `frontmatter` に記入してファイルを保存する
- ユーザーが最初に選んだカテゴリーを尊重しつつ、完成した記事の内容と照合して判断する

---

## 自動生成されるファイル

**gitignore対象（コミット不要）：**
- `public/articles/` — ビルド時に生成
- `public/articles-sitemap.xml` — 記事サイトマップ（記事追加時に自動更新）

**コミット必須（検索機能のため）：**
- `public/pagefind/` — 検索インデックス。**`npm run build` 後は必ずコミットに含めること**
  - gitignore対象から外してあるため、`git add public/pagefind/` でステージングされる
  - コミットしないと Cloudflare Pages に検索インデックスが届かず、検索窓が表示されない

**`public/pagefind/pagefind-entry.json` の非決定的な差分について（2026-07-20確認）：**
- コミット・プッシュ後に確認のため`npm run build`を再実行すると、内容が同一でも`pagefind-entry.json`内の`languages`オブジェクトのキー順（`en`/`ja`の並び順）だけが入れ替わり、`git status`に差分として表示されることがある
- これはPagefindのビルド処理がキー順を保証しないために起きる**非本質的な差分**。`hash`値・`page_count`が変わっていなければ実害はない
- コミット後の検証ビルドでこの差分だけが出た場合は、`git checkout -- public/pagefind/pagefind-entry.json`で元に戻してよい（実質的な変更ではないため再コミット不要）

---

# ピラー記事生成プロンプト（AEO対応・定義ハブ型）

## 使い方

入力変数（分野・地域・地域の階層・想定読者）を差し替えて使う。
記事執筆開始時の確認事項（git pull・カテゴリ選択等）は通常フローに従うこと。

## プロンプト本文

```
# 役割
あなたは Third Place Japan（TPJ）の編集者です。TPJは家でも職場でもない
「第三の居場所＝サードプレイス」を、独自の7軸基準で評価・認証する日本初の
プラットフォームです。社会学者レイ・オルデンバーグのサードプレイス理論を
土台に、現代日本向けに再解釈した視点で記事を書きます。

# タスク
以下の【分野 × 地域】について、AEO（AI回答エンジン最適化）を前提とした
「定義ハブ ＋ 推薦」両立型のピラー記事を、本文として完成形で生成してください。

# 入力変数（ここだけ差し替える）
- 分野: 〔例：カフェ・スペシャルティコーヒー〕
- 地域: 〔例：表参道〕
- 地域の階層: 〔例：東京都 → 渋谷区/港区 → 表参道エリア〕
- 想定読者: 〔例：ひとり時間を求める20〜40代／出張者／インバウンド〕

# TPJ 7軸評価基準（記事中で必ずこの語彙を使う）
1. 居心地・空間品質（五感品質・素材）
2. 静寂性・プライバシー（静けさ）
3. 特別感・非日常性（ギフト体験）
4. ストーリー・背景への共感（物語の継承）
5. 再訪・継続価値（また来たくなるか）
6. 記録・シェア体験（記録性）
7. インバウンド・多言語対応（外国人適性）

# 絶対ルール
- 実在の店名・施設名は一切出さない。「〇〇通りの角にある築古ビルを改装した一軒」
  のように、立地・空間・素材・客層の「条件と特徴」で具体的に描写する。
- 事実として断定するのは、地域の文化・歴史・地理・街の性格など検証可能な背景のみ。
  個別店の営業情報・価格・評判は書かない。
- 誇大表現を避け、TPJの落ち着いた知的なトーンを保つ。
- AIが引用しやすいよう、各見出し直下の最初の1〜2文で結論を述べる
  （結論先出し）。

# 記事の構成（この見出し順で本文を書く）

## H1（タイトル）
「〔地域〕で〔分野〕をサードプレイスにする：〔地域〕の第三の居場所完全ガイド」
の型を基本に、自然な日本語で。

## リード文（120〜180字）
この地域でこの分野が、なぜサードプレイスとして機能するのかを一段落で要約。
AIがそのまま引用できる密度で書く。

## 1. 〔地域〕という街と「第三の居場所」
- 地域の文化的背景・歴史・街の性格（検証可能な事実のみ）
- なぜこの街が、家でも職場でもない居場所を必要とする／生み出すのか
- 地理的特徴（人の流れ、表通りと裏通り、再開発の有無 等）

## 2. 〔地域〕で〔分野〕が果たすサードプレイスの役割
- この分野がこの街でどんな居場所機能を担うか
- オルデンバーグの中立性・常連・アクセス性の観点で解説

## 3. 〔地域〕の〔分野〕をTPJ7軸で読む
- 7軸それぞれについて、この地域・分野なら何が問われるかを小見出しで解説
- 「こういう空間は居心地軸が高い」と条件ベースで語る（店名なし）

## 4. 〔地域〕で"いいサードプレイス"を見分ける条件
- 読者が自分で良い場所を選ぶためのチェックポイントを、特徴・立地・空間で
- 表通り型／裏通り型、滞在時間別、ひとり/同伴別など切り口を分ける

## 5. 〔地域〕のサードプレイス文化を支える街の文脈
- 関連する地域イベント・季節性・周辺施設の性格（事実ベース）
- この街でしか得られない非日常性・特別感（軸3・4と接続）

## 6. インバウンド視点：〔地域〕の〔分野〕
- 外国人来訪者にとっての分かりやすさ・体験価値（軸7）
- 言語・予約・文化的ハードルの観点

## よくある質問（FAQ・5問）
読者が実際にAIへ尋ねそうな質問を5つ。各回答は2〜3文で結論先出し。
例:「〔地域〕で静かに過ごせる〔分野〕は？」「〔地域〕の〔分野〕の特徴は？」

## まとめ
この街×この分野のサードプレイスの核心を3文で。TPJ認証への自然な言及で締める
（「TPJではこうした空間を7軸で評価・認証しています」程度、宣伝過多にしない）。

# 文字数
本文 2,800〜4,000字。FAQ含む。

# トーン
知的・落ち着いた・断定しすぎない。読者を急かさない。
オルデンバーグ理論への敬意を土台にしつつ、現代日本の生活実感に接続する。
```

## 使用上の注意

- **店名・施設名は一切出さない**のがこのプロンプトの根幹ルール。条件・空間描写で代替する
- 生成後は通常の記事フロー（カテゴリ確認・画像・内部リンク・FAQ書式）を適用する
- `〔地域〕 × 〔分野〕` の組み合わせ数だけピラー記事を量産できる設計
- 地域階層（都道府県→市区町村→エリア）を指定することで、SEOの地名キーワードが自然に埋め込まれる

---

# 記事品質チェックルール（制作憲法違反パターン）

記事生成・編集の完了前に、以下の違反パターンがないか必ず確認すること。
過去の実際の訂正事例から抽出したルールのため、再発防止のために厳守する。

## ❌ 価格の断定（最頻出違反）

**禁止**：入場料・利用料・コーヒー代など具体的な金額を固定値として記載する
- ❌ 「入場料500円」「¥500」「60分施術で8,000円〜」
- ❌ 「For ¥500, the inner garden offers...」

**許容表現**：
- ✅ 「有料・要事前確認」
- ✅ 「small entry fee — confirm current rates before visiting」
- ✅ 「有料入場（料金は公式サイトで確認）」
- ✅ 「施設によって価格帯が異なる」

**理由**：料金は変動する情報。誤情報になるリスクがある。

---

## ❌ 混雑・混み具合の断定

**禁止**：混雑状況を断定的・固定的に記述する
- ❌ 「混雑は激しく、静寂性は低い」
- ❌ 「週末は必ず混む」

**許容表現**：
- ✅ 「混雑しやすく、時間帯によっては静寂性が下がる傾向がある」
- ✅ 「ピーク時間帯は混雑が予想される」
- ✅ 「平日早朝が比較的落ち着いている」

**理由**：時間帯・季節・イベントによって変動する情報のため。

---

## ❌ オルデンバーグ「8条件」・TPJ「7軸」のどちらでもない第三の分類の混入（2026-08-02追加）

**禁止**：サードプレイスの定義・条件・基準に言及する際、`project-docs/DEFINITION.md`のオルデンバーグ8条件（中立の地・平等性・会話・利用しやすさ・常連の存在・控えめな佇まい・遊び心・もう一つの家）にも、TPJ7軸評価（居心地・空間品質、静寂性・プライバシー、特別感・非日常性、ストーリー・背景への共感、再訪・継続価値、記録・シェア体験、インバウンド・多言語対応）にも一致しない、独自の項目数・項目名を創作する
- ❌ 「Third Place Japanの定義では…中立性・常連性・アクセスの容易さ・居心地の良さ・会話や静寂が共存できること——の5条件を基盤としています」（オルデンバーグの用語を一部借用しつつ、TPJの7軸由来の要素を混ぜ、存在しない「TPJの定義」に誤って帰属させた実例）

**許容表現（帰属を正確にする）**：
- ✅ オルデンバーグの原典に言及する場合：「オルデンバーグが定義するサードプレイスの中立性・平等性・会話の場・気軽なアクセス性」のように、**必ず「オルデンバーグが」を主語にする**（8条件のうち一部だけを選んで言及してもよいが、その場合も出典はオルデンバーグのままにする）
- ✅ TPJ独自の基準に言及する場合：「居心地・静けさ・特別感・ストーリー・再訪価値・記録性・インバウンド適性の7軸」のように、`tpj-7-axis-evaluation-guide.md`で確立された短縮形の軸名をそのまま使う
- ✅ 1つの条件・軸を記事内でさらに分解して説明することは問題ない（例：`third-place-accessibility.md`が「利用しやすさ」を物理的・心理的・時間的の3要素に分解する等）。ただしこれは特定1条件の下位区分であり、8条件・7軸と並ぶ新しい上位分類として提示しないこと

**理由**：AGENT-INSTRUCTIONS.mdの品質基準「オルデンバーグの『8特徴』とTPJの『7軸』を必ず区別する」は主に両者の**取り違え**を防ぐルールだが、今回発見された問題はそれとは別に、**どちらでもない第三の分類を創作し、しかも誤った出典（TPJの定義）に帰属させる**という失敗パターンだった。エンティティ・用語の一貫性はAEO上重要で、AIが「TPJの定義」を学習・引用する際に不正確な情報源になるリスクがある。

**発見の経緯・調査手法（再利用可能な技法）**：GBC施設ページのFAQに「Third Place Japanの定義では…5条件」という記述が見つかった際、`git log --oneline -S "<該当フレーズ>" -- src/_data/venues.json`でこの記述が**いつ追加されたか**を特定し、`tpj-7-axis-evaluation-guide.md`・`third-place-8-conditions.md`（現行版）の確定日と時系列で比較した。結果、この記述は両記事が正本として確定する**前**（2026-07-06追加、7軸ガイドは2026-07-20新設、8条件記事の現行版は2026-07-28改稿）に書かれた叩き台の名残であり、意図的な独自分類ではなく更新漏れと判断できた。**用語の食い違いを発見した際は、まず該当箇所がいつ書かれたかを`git log -S`で調べ、正本となる定義記事の確定時期と比較する**と、意図的な設計か更新漏れかを切り分けやすい。

**チェック方法**：`grep -rnoE "[0-9１-９]+(つの)?(条件|要素|特徴|基準|要件)" src/_data/venues.json src/stories/`で数値＋分類語のパターンを機械的に洗い出し、各ヒットが①オルデンバーグ8条件の全部/一部（出典明記）②TPJ7軸の全部/一部（正式な軸名使用）③1条件・1軸の記事内下位分解、のいずれかに分類できるか確認する。いずれにも当てはまらない場合は本ルール違反の可能性が高い。

---

## ❌ 施設名の断定・推薦（description / FAQ / 見出しに特に注意）

**禁止**：特定施設名を「おすすめ」「〜がある」と断定する
- ❌ `description` に「東京都現代美術館・国立新美術館などは〜」
- ❌ FAQ回答に「円覚寺・東慶寺周辺がおすすめです」
- ❌ 本文に「高尾山薬王院は〜を提供する」（個別施設の断定）

**許容表現**：
- ✅ 「鎌倉の臨済宗の大寺院」「北鎌倉の大寺院・小規模な禅寺」
- ✅ 「高尾山の真言宗の山岳寺院」
- ✅ 「静寂性・特別感の高い美術館・文化施設」

**補足**：地名（鎌倉・高尾・浅草）の言及はOK。「〇〇寺」「〇〇美術館」の固有名が問題。
ピラー記事では施設名を出さないのが大原則。コンセプト記事でも FAQ・descriptionは同ルールを適用する。

**例外①：TPJが実際に認証・セレクトした施設名は名指ししてよい（2026-07-24追加）**
上記の禁止事項は「TPJが評価・検証していない外部施設を無根拠に推薦する」ことが問題なのであって、TPJ自身が認証・TPJセレクトした施設（`venues.json`に実在する施設）を名指しすることは禁止の対象外。むしろAI回答エンジンへの引用確度を上げるため、`/stories/`トップ等のハブページのFAQでは実在するTPJ認証・セレクト施設名を積極的に挙げる（例：「Green Beans Coffee（TPJ実証店舗）」「CITAN（ホテルラウンジ）」等）。ただし対象はピラー記事・コンセプト記事（店名を一切出さない原則が適用される記事群）を除く、サイト全体のハブ・トップレベルのFAQに限る。

**例外②：全国的に著名な公共ランドマークは地名相当として扱う（2026-07-24追加）**
明治神宮のような、商業店舗ではなく全国的に知名度の高い公共のランドマーク（神社仏閣・公園等）は、「〇〇寺」レベルの固有名詞であっても地名（鎌倉・高尾等）に準じる扱いとし、言及してよい。ただし「TPJが認証・評価した」という誤解を招く書き方はしない（TPJ認証施設の紹介文とは文を分け、あくまで一般的な第三の居場所の例として言及する）。

---

## ❌ 英語記事のog:locale設定

**禁止**：英語記事に `og:locale: ja_JP` を設定する（自動設定に任せていれば問題なし）

**ルール**：`base.njk` が `lang` フロントマターで自動切り替えする（2026-07-01実装済み）
- `lang: en` → `og:locale: en_US`
- `lang: ja`（または未設定）→ `og:locale: ja_JP`

**確認方法**：英語記事を追加したらビルド後に `grep "og:locale"` で確認する。

---

## ❌ 英語記事と日本語記事のサムネイル共有

**禁止**：英語記事と日本語記事で同じサムネイル画像を使い回す
- ❌ 日本語インバウンド記事と英語トラベラーガイドが同じ `tokyo-inbound-visitor-cafe-experience.webp` を使う

**ルール**：英語記事には英語版専用のサムネイルを用意する
- Unsplashで別キーワードで検索し、ファイル名に `-en` サフィックスをつけて区別する
- 例：`tokyo-third-place-guide-travelers-en.webp`

---

# JSON-LD実装ルール（Schema.org バリデーション対応・2026-07-02追加）

Schema.org バリデーターで検出された実際のエラー・警告から抽出したルール。
実装・テンプレート編集時に必ず確認すること。

## ✅ Nunjucksテンプレートでの動的テキスト出力（必須パターン）

**禁止**：`"name": "{{ post.data.title }}"` または `| replace('"', '\\"')` で直接出力
- NunjucksがHTML自動エスケープを行い `"` → `&quot;`、`'` → `&#39;` になる
- `<script type="application/ld+json">` 内でも同様に発生する

**正しい実装**：
```njk
"name": {{ post.data.title | dump | safe }}
```
- `dump` = JSON.stringify（適切なエスケープ）
- `safe` = Nunjucksによるさらなるエスケープを防ぐ
- ダブルクォート込みの文字列として出力されるため、`"name":` の後に引用符を書かない

**実際に発生したインシデント（2026-07-20）**：`l3-area.njk`の`hasPart`（関連記事・施設一覧）ブロックで`{{ p["@type"] | dump }}`のように`| safe`が抜けており、L3エリアページ27件のJSON-LDが`&quot;`エスケープで構文崩壊していた（`npm run build`ではエラーが出ないため長期間気づかれなかった）。**`| dump`を書いたら必ずその場で`| safe`が付いているか確認する。** 新しいJSON-LDブロック・一覧系ループを書く／レビューする際は、`grep -n "| dump }}" src/`で`| safe`が欠けた箇所がないか機械的に確認できる（`| dump | safe }}`にマッチしない行が対象）。

---

## ⚠️ eleventyComputed内の`{{ }}`補間による二重エスケープ（2026-07-22発見）

`select/venue.njk`・`select/venue-en.njk`のように、`eleventyComputed`のtitle/description等を`"{{ venue.citation }}"`という形でNunjucks文字列として組み立てているページは、値が**2段階のNunjucksレンダリングを経由する**ため、値に`'`や`"`が含まれると二重にエスケープされ壊れる。

**発生の仕組み**：
1. `eleventyComputed`側の`"{{ venue.citation_en }}"`が1段階目のレンダリングでHTML自動エスケープを行い、`'` → `&#39;`が実データとして`description`に格納される
2. `base.njk`の`<meta name="description" content="{{ description }}">`が2段階目のレンダリングでもう一度エスケープし、`&` → `&amp;`となる
3. 結果：`Tokyo's` が `Tokyo&amp;#39;s` という壊れた表示になる

**実際に発生したインシデント**：Green Beans Coffee英語版施設ページで、`citation_en`に含めた`Tokyo's`のアポストロフィが`Tokyo&amp;#39;s`と表示される形で発覚。日本語版は文中にアポストロフィ相当の文字（半角`'`）が使われることがほぼ無いため、同じ仕組みの`select/venue.njk`側では長期間顕在化していなかった（潜在バグ）。

**正しい実装**：`eleventyComputed`側の補間には`| safe`を付け、1段階目でのエスケープを防ぐ。最終的なエスケープは`base.njk`側の1回だけで行われる。
```njk
title: "{% if venue.title_override %}{{ venue.title_override | safe }}{% else %}...{% endif %}"
description: "{{ venue.citation | safe }}"
```
`select/venue.njk`・`select/venue-en.njk`の両方に適用済み。**`eleventyComputed`で文字列データ（title・description等）を`{{ }}`補間で組み立てる新しいテンプレートを書く場合は、必ず`| safe`を付けること。**`| safe`を付けても最終的なHTML出力時のエスケープは`base.njk`側で1回行われるため、XSSリスクは変わらず安全。

---

## ❌ Organization に使えないプロパティ（警告の原因）

以下のプロパティは `Organization` スキーマには不適切なため、使用しない。

| プロパティ | 問題 | 代替 |
|---|---|---|
| `sameAs: []` | 空配列は無意味・警告の原因 | URLが確定するまでプロパティ自体を省略 |
| `knowsAbout: [文字列配列]` | Schema.org は `Thing` オブジェクト型を期待する | 省略するか `Thing` 型で記述 |
| `inLanguage` | `CreativeWork` 用プロパティ。`Organization` には不適切 | 省略する |

**現在の `base.njk` Organization 実装（確定版）：**
```json
{
  "@type": "Organization",
  "name": "Third Place Japan",
  "alternateName": ["TPJ", "サードプレイスジャパン"],
  "url": "...",
  "logo": { "@type": "ImageObject", "url": ".../tpj-logo.png", "width": 900, "height": 275 },
  "description": "...",
  "foundingDate": "2025",
  "areaServed": { "@type": "Country", "name": "Japan" }
}
```
これ以上プロパティを追加する場合は、Schema.org の `Organization` 定義で有効かを事前確認すること。

---

## ❌ speakable を使えない型に設定する

**禁止**：`speakable` は `CreativeWork` 系（Article / BlogPosting / WebPage）**専用**プロパティ。以下には使用不可：

| 型 | speakable | 理由 |
|---|---|---|
| `WebSite` | ❌ 禁止 | 警告が出る（2026-07-02検出・修正済み） |
| `LocalBusiness` | ❌ 禁止 | 警告が出る（2026-07-06検出・修正済み） |
| `Organization` | ❌ 禁止 | 同上 |
| `Article` / `BlogPosting` | ✅ 使用可 | CreativeWork系のみ有効 |

**現状の実装（確定）**：
- `src/stories/index.njk`（WebSite）→ `speakable` なし ✅
- `src/_layouts/venue.njk`（LocalBusiness）→ `speakable` なし ✅
- `src/_layouts/article.njk`（BlogPosting）→ `speakable` あり ✅
- `src/_layouts/article-area.njk`（Article）→ `speakable` あり ✅

---

## ❌ ItemList.ListItem に url プロパティを直接使う / @type:Article を指定する

**禁止①**：`url` を `ListItem` の直接プロパティにする
```json
{ "@type": "ListItem", "position": 1, "url": "https://..." }
```

**禁止②**：`item` に `@type: Article` を指定する
```json
{
  "item": { "@type": "Article", "@id": "https://...", "name": "タイトル", "url": "https://..." }
}
```
→ Google が「Article なら image・author・headline も必要」と警告を出す（2026-07-06 検出）

**正しい実装**：`@type` を省略し `@id`・`name`・`url` のみ記述する
```json
{
  "@type": "ListItem",
  "position": 1,
  "item": {
    "@id": "https://...",
    "name": "記事タイトル",
    "url": "https://..."
  }
}
```
- `@type` を省略しても `ItemList` として有効。余計な型宣言が警告を生む
- 対象ファイル：`src/stories/index.njk`（ItemList ブロック）

---

## ❌ Organization ロゴにヒーロー画像を使う

**禁止**：Organization/Publisher の `logo.url` にサイトのキービジュアル・ヒーロー画像を使う
- ❌ `"url": "{{ site.url }}/assets/images/main/japan-cafe-interior-hero.webp"`

**ルール**：ロゴ専用画像を指定する
- ✅ `"url": "{{ site.url }}/assets/images/main/tpj-logo.png"`
- ロゴ画像の推奨サイズ：横幅600px以上、アスペクト比1:1〜4:1程度
- 現在の画像：`public/assets/images/main/tpj-logo.png`（900×275px、白背景）

**影響ファイル**（すべて `tpj-logo.png` に統一済み）：
- `src/_layouts/base.njk`
- `src/_layouts/article.njk`
- `src/_layouts/article-certified.njk`
- `src/_layouts/article-area.njk`

---

## ✅ Article系スキーマの必須フィールド（article.njk / article-certified.njk / article-area.njk）

Article/BlogPosting スキーマでは以下を必ず守ること（2026-07-06 修正・確定）。

**`image` は `ImageObject` で記述する（文字列URL禁止）**
```json
"image": {
  "@type": "ImageObject",
  "url": "https://thirdplacejapan.com/assets/images/articles/xxx.webp",
  "width": 800,
  "height": 450
}
```
- ❌ `"image": "https://..."` — 文字列URLは Rich Results Test で「重大ではない問題」として検出される

**`width`/`height` は実測値を使う。固定値`800/450`のハードコードは禁止（2026-07-20改訂）**
- Unsplashダウンロード時に`w=800`を指定してもアスペクト比は画像ごとに異なり、実際のサムネイルは16:9とは限らない（4:3・3:2等が混在）。`width: 800, height: 450`を全記事に固定していたところ、実測とズレた不正確な値をGoogleに申告し続けていたことが2026-07-20の監査で判明した
- 正しい実装：`.eleventy.js`の`imageDimensions`フィルター（WebPファイルのヘッダーを直接読んでwidth/heightを返す。VP8/VP8L/VP8Xの3形式に対応）を使う
  ```njk
  {% set imgDim = thumbnail | imageDimensions %}
  "image": {
    "@type": "ImageObject",
    "url": "...",
    "width": {{ imgDim.width }},
    "height": {{ imgDim.height }}
  }
  ```
- 適用済みテンプレート：`article.njk`・`article-certified.njk`（`thumbnail or v.image`の解決後の値を渡す）・`article-area.njk`・`about/index.njk`
- **新しく画像を伴うJSON-LD `ImageObject`をテンプレートに追加する場合は、必ず`imageDimensions`フィルター経由にする。`width: 800, height: 450`等の固定値を書かない**

**`publisher.logo` に `width`・`height` を付ける**
```json
"publisher": {
  "@type": "Organization",
  "name": "Third Place Japan",
  "logo": {
    "@type": "ImageObject",
    "url": "https://thirdplacejapan.com/assets/images/main/tpj-logo.png",
    "width": 900,
    "height": 275
  }
}
```

**`author.knowsAbout` は使わない**
- ❌ `"knowsAbout": ["サードプレイス", ...]` — Organization に不適切（文字列配列）
- `author` に含めてよいのは `@type`・`name`・`url`・`description` のみ

---

## ✅ ItemList の言語フィルタリング（日英混在環境）

**問題**：`collections.articles` には日本語記事・英語記事の両方が含まれる
- 日本語ページ（`/stories/`）の ItemList に英語記事URLが混入してしまう

**実装**：カスタムフィルター `rejectLang` を使用
```njk
{% set recentPosts = collections.articles | rejectLang("en") | head(8) %}
```

**フィルター定義**（`.eleventy.js`）：
```js
eleventyConfig.addFilter("rejectLang", (array, lang) =>
  (array || []).filter(item => item.data && item.data.lang !== lang)
);
```

**注意**：Nunjucks の `selectattr` フィルターはドット記法（`data.lang`）のパス解決に不具合があり使用不可。必ず `rejectLang` カスタムフィルターを使うこと。

### 英語ページ側は `selectLang` を使う（2026-07-13追加）

英語ページ（`/en/stories/` 配下）で「英語記事のみ」を取得したい場合は、`rejectLang("ja")` ではなく専用の `selectLang("en")` を使う。

```njk
{% set enArticles = collections.articles | selectLang("en") %}
```

**フィルター定義**（`.eleventy.js`）：
```js
eleventyConfig.addFilter("selectLang", (array, lang) =>
  (array || []).filter(item => item.data && item.data.lang === lang)
);
```

**背景（2026-07-13発見・修正済みの不具合）**：`src/en/stories/index.njk`（英語記事トップ）が3本の記事カードをハードコードしており、新規に英語記事を追加しても一覧に反映されない状態だった。また `src/_layouts/article.njk` のサイドバー「最新記事」ブロックが `collections.articles | head(5)` と言語フィルタなしで実装されており、英語記事ページなのに日本語記事タイトルが混在して表示されていた。

**恒久対応（今後この不具合を再発させないためのルール）**：
- `src/en/stories/index.njk` は `collections.articles | selectLang("en")` を全件ループで表示する方式に変更済み。**英語記事を追加する際、このページのテンプレートを個別に更新する必要はない**（`lang: en` を設定するだけで自動反映される）
- `src/_layouts/article.njk` のサイドバー「最新記事」は `collections.articles | selectLang(lang or "ja")` でページ自身の言語に絞り込み済み。ラベル・カテゴリ名も `lang == "en"` の場合は英語表記（`postCat.name_en`）に切り替わる
- **新しく「記事一覧」「最新記事」「関連記事」等の一覧系UIを追加する場合は、必ず対象ページの言語で `selectLang` / `rejectLang` によるフィルタリングを行うこと。** `collections.articles` を言語フィルタなしでそのまま使うと同じ不具合が再発する

**🚨 `relatedPostsByCat`・`prevPost`・`nextPost` フィルターも対象（2026-07-19追加・重要）🚨**

上記の「一覧系UI」ルールは `head()` を使う単純な一覧だけでなく、**`relatedPostsByCat`（関連記事）・`prevPost`／`nextPost`（前の記事・次の記事）フィルターにも同様に適用される。** これらは一見「一覧」に見えないため見落としやすく、実際に2026-07-19の監査で `src/_layouts/article.njk`・`article-certified.njk`・`article-area.njk` の計5箇所が未対応のまま放置されており、日本語記事の「関連記事」「前の記事・次の記事」に英語記事が混入する不具合が本番で発生していた（例：`/stories/culture-sports-leisure/koto/` の前後記事・関連記事が英語タイトルで表示される）。

**正しい実装パターン**：
```njk
{# 関連記事 #}
{% set relatedArticles = collections.articles | selectLang(lang or "ja") | relatedPostsByCat(page.url, category_slug, 3) %}

{# 前の記事・次の記事 #}
{% set langArticles = collections.articles | selectLang(lang or "ja") %}
{% set prevArticle = langArticles | prevPost(page.url) %}
{% set nextArticle = langArticles | nextPost(page.url) %}
```

**理由**：`collections.articles` は日英混在の全記事を日付降順でソートしたコレクション（`.eleventy.js` の `addCollection("articles")`）。`prevPost`/`nextPost` はこの配列上の位置（インデックス）で前後を決めるため、日本語記事の直前直後にたまたま英語記事が公開されていると、そのまま前後記事リンクとして表示されてしまう。`relatedPostsByCat` も `category_slug` 一致のみで絞り込み、言語は見ないため、同カテゴリに英語記事があれば関連記事に混入する。

**チェック方法**：新しいレイアウト・テンプレートを追加/編集する際は、`collections.articles` を直接（`selectLang`/`rejectLang` を挟まずに）参照している箇所がないか `grep -n "collections\.articles" src/_layouts/ src/stories/ src/en/` で確認する。`sitemap.njk`（全言語を含めるべきサイトマップ）のような例外を除き、原則すべて言語フィルタを挟むこと。

**「最新記事」系ウィジェットは `selectLang`/`rejectLang` だけでなく `rejectCategory("about")` も必ずペアで付けること（2026-08-01追加）**：`selectLang`/`rejectLang` は言語混入を防ぐが、`about`カテゴリ（サードプレイスガイド記事）の混入は別途`rejectCategory("about")`を明示的に挟まないと防げない。日本語版の同種ウィジェット（`footer-stories.njk`・`stories/index.njk`・`stories/latest.njk`・`article.njk`等）は2026-07-20に`rejectCategory("about")`を追加済みだったが、`footer-stories-en.njk`の「Latest Articles」列だけこの対応から漏れており、`about`記事4本が2026-07-28に一括公開された直後、英語版フッターの「Latest Articles」欄が通常記事ではなくガイド記事4本で埋まってしまう不具合が本番で発生していた（ユーザーからのスクリーンショット指摘で発覚）。「最新記事」「Latest Articles」を名乗るウィジェットを新規追加・複製する場合は、`selectLang`/`rejectLang`と`rejectCategory("about")`の両方が付いているか確認すること。

---

## ✅ LocalBusiness @type のカテゴリ別マッピング（venue.njk・2026-07-09追加）

`hasMenu`・`servesCuisine` プロパティは Schema.org 上 `FoodEstablishment` 系の型にしか存在しない。汎用の `@type: "LocalBusiness"` のままこれらを出力すると、Schema.orgバリデーターが「プロパティが認識されない」と警告する（GBCの`hasMenu`で実際に検出・修正済み）。

**実装（`venue.njk` 冒頭で定義）：**
```njk
{%- set foodEstablishmentTypes = {"cafe-specialty-coffee": "CafeOrCoffeeShop", "bar-whisky-cocktail": "BarOrPub"} -%}
{%- set businessType = foodEstablishmentTypes[venue.category_slug] or "LocalBusiness" -%}
```
`LocalBusiness` JSON-LD本体（`@type`）と、Review内の `itemReviewed.@type` の**両方**に `{{ businessType | dump | safe }}` を使う（2箇所とも揃える）。

**ルール：**
- `venue.category_slug` が食・飲料系（カフェ／バー等）の場合のみ、Schema.orgの `LocalBusiness > FoodEstablishment` のサブタイプに切り替える
- 該当しない業種（ホテル・スパ・文化施設等）は従来通り `LocalBusiness` のまま
- 新しい食・飲料系カテゴリを追加する場合（レストラン等）は `foodEstablishmentTypes` にマッピングを追加する（例：`"restaurant-dining": "Restaurant"`）。追加前に対象の型がSchema.orgで現行有効か（廃止されていないか）を確認すること
- `hasMenu`・`servesCuisine` 以外の食関連プロパティを追加する場合も、必ず対象の `@type` がそのプロパティをサポートするか事前に確認する

## ✅ author/editor（E-E-A-T）と取材根拠の表示ルール（2026-07-23追加）

記事・施設ページのJSON-LD `author` と、可視のbyline行（「サードプレイスジャパン編集部」表記）には、以下を必ずセットで実装する。

**JSON-LD（Article/BlogPosting/Review 共通）**
```json
"author": {
  "@type": "Organization",
  "name": "サードプレイスジャパン編集部",
  "url": "{{ site.url }}/about-company/#editorial-team",
  "description": "日本全国のサードプレイスを独自の7軸基準で評価・認証するThird Place Japan編集部。"
},
"editor": {
  "@type": "Person",
  "name": "加瀬俊幸",
  "jobTitle": "Third Place Japan編集長",
  "url": "{{ site.url }}/about-company/#editor"
}
```
- `author`は引き続き`Organization`（実態が編集チームによる制作のため）。個人が単独執筆したかのような`Person`型authorへの変更は行わない
- `editor`（Person）を新設し、実在する編集責任者（加瀬俊幸）をGoogleが認識できるようにする。プロフィール（実名・経歴・取材方針）は`/about-company/`に既存
- 対象テンプレート：`article.njk`・`article-certified.njk`・`article-area.njk`・`venue.njk`・`venue-en.njk`

**可視byline（記事・施設ページ本文）**
- 「サードプレイスジャパン編集部」の表記は必ず`/about-company/#editorial-team`へのリンクにする（プレーンテキストのまま放置しない）
- `category_slug: about`記事（サードプレイスガイド）は既存ルール通り**日付は表示しない**が、編集部リンクは表示する（evergreenな見せ方を維持しつつ著者帰属だけ明示する設計）

**取材根拠（sourcing basis）の grade_slug 別文言（venue.njk・venue-en.njk・article-certified.njk）**

Googleおよび読者に「何を根拠にこの評価を書いたか」を正確に伝えるため、byline行の末尾に`grade_slug`別の一文を追加する。**誇大表現禁止ルールと同様、現地訪問を伴わないグレードに「覆面調査員」等の訪問を示唆する文言を使わない。**

| grade_slug | 文言 |
|---|---|
| `select`（TPJセレクト） | 「編集部が発掘した、行ってみたいサードプレイス」 |
| `certified` | 「公開情報に基づく編集部の基礎確認」 |
| `silver` | 「公式情報・資料・ヒアリングに基づく評価」 |
| `gold` | 「覆面調査員による複数回の現地訪問に基づく評価」 |
| `platinum` | 「複数チームによる覆面調査・現地訪問に基づく評価」 |
| `flagship` | 「実証店舗として、開業時から店舗と伴走した継続的な検証に基づく評価」 |

**🚨 `flagship`の文言は「実証店舗」を前提にした特別な表現である点に注意（絶対厳守）🚨**

CLAUDE.mdの用語定義上、**実証店舗はFlagshipの部分集合であり、すべてのFlagshipが実証店舗とは限らない**（「『実証店舗』と『TPJ認証』の関係」セクション参照）。2026-07-23時点ではFlagship施設がGreen Beans Coffee（実証店舗）1件のみのため上記の文言で正確だが、**将来「実証店舗ではないFlagship施設」を追加する場合は、この文言をそのまま流用せず、Gold/Platinumと同様の「覆面調査員による複数回の現地訪問」系の文言に個別で差し替えること**。新しいFlagship施設を追加する際は、`venue.njk`・`venue-en.njk`・`article-certified.njk`の`sourcingBasisText`辞書（`flagship`キー）を必ず確認し、対象施設が実際に実証店舗としての伴走関係にあるかを判断してから文言を決定する。

**ピラー記事・エリア記事（`article.njk`・`article-area.njk`）**
- 店名を出さない条件描写型の記事（CLAUDE.md「TPJ ピラー記事 制作ルール」参照）のため、「編集部による地域情報の調査・編集に基づく記事」を固定文言として使う。個別施設への現地取材を主張する文言は使わない

**記事本文中の取材根拠表現も、上記grade_slug別文言と矛盾しないこと（2026-07-24追加）**
- 記事本文（`src/stories/`配下のMarkdown）中で取材・評価の根拠に触れる場合も、byline表示と同じgrade_slug別の実態に合わせる。特にFlagship／実証店舗の記事で「実地調査」「複数回の訪問調査」等、覆面調査（Gold/Platinum向けの文言）を示唆する語を使わない
- 発見経緯：Green Beans Coffeeのレビュー記事（`src/stories/concept/green-beans-coffee-review.md`）本文4箇所に「実地調査」「リピート調査」という表現が残っており、記事上部のbyline（「実証店舗として、開業時から店舗と伴走した継続的な検証に基づく評価」）と矛盾していた。「継続的な検証」「伴走」等の表現に統一して解消した
- **新規に作成する記事にも同様に適用する（2026-08-02確認）**：この原則はbylineの自動出力だけでなく、GBCのスコア・評価軸に触れる新しい記事を執筆する際にも本文中で明示的に踏襲する。2026-08-02に追加したGBCクラスター記事4本では、GBCのスコアを引用する箇所ごとに「TPJが開業時から店舗と伴走し、継続的な検証を重ねてきた実証店舗としての評価」という一文を添え、「覆面調査員が一度だけ訪れて評価する認証プロセスとは異なる」という対比も明示した（`src/stories/about/space-design-insights-gbc.md`が参照実装）。GBC以外の施設についても、スコア・評価軸を引用する記事を新規執筆する際は、対象施設の`grade_slug`に対応する取材根拠の実態と矛盾しない表現を都度確認すること

## ✅ 認証施設の Review + reviewRating（venue.njk 必須）

認証店舗詳細ページには `Review` JSON-LD が必要（Google リッチリザルト対象）。

**グレード → 数値マッピング**（`venue.njk` 内で定義）：
```njk
{%- set gradeNums = {"Certified": 1, "Silver": 2, "Gold": 3, "Platinum": 4, "Flagship": 5} -%}
```

**JSON-LD 出力形式**：
```json
{
  "@type": "Review",
  "itemReviewed": {
    "@type": "LocalBusiness",
    "name": "施設名",
    "address": { "@type": "PostalAddress", "addressLocality": "エリア", "addressCountry": "JP" }
  },
  "author": { "@type": "Organization", "name": "サードプレイスジャパン編集部" },
  "reviewRating": {
    "@type": "Rating",
    "ratingValue": 3,
    "bestRating": 5,
    "worstRating": 1
  },
  "reviewBody": "引用文",
  "additionalProperty": [
    { "@type": "PropertyValue", "name": "五感品質・素材", "value": 8 },
    { "@type": "PropertyValue", "name": "静けさの再現", "value": 7 },
    { "@type": "PropertyValue", "name": "特別感・ギフト体験", "value": 6 },
    { "@type": "PropertyValue", "name": "ストーリー継承", "value": 7 },
    { "@type": "PropertyValue", "name": "愛用・回遊価値", "value": 8 },
    { "@type": "PropertyValue", "name": "シェア・記録性", "value": 5 },
    { "@type": "PropertyValue", "name": "インバウンド適性", "value": 6 }
  ]
}
```

**`LocalBusiness.aggregateRating` の `ratingCount` ルール：**

```json
"aggregateRating": {
  "@type": "AggregateRating",
  "ratingValue": 6.7,
  "bestRating": 10,
  "worstRating": 0,
  "ratingCount": 1,
  "reviewCount": 1
}
```

- **`ratingCount` は必ず `1`**（TPJ編集部による1回の評価）
- ❌ `ratingCount: 7` は禁止。「7軸で評価した」からといって7を入れると、Googleは「7人が評価した」と解釈してしまう
- `ratingValue` は7軸スコアの平均値（0〜10スケール）
- 7軸の個別スコアは `LocalBusiness.additionalProperty` と `Review.additionalProperty` の両方に記載する

---

## ✅ LocalBusiness の検索最適化プロパティ（venue.njk グレード条件付き）

以下のプロパティはグレードに応じて条件出力する（`thisLevel` による制御）。

### openingHours（Silver以上・`thisLevel >= 2`）

```json
"openingHours": ["Mo-Su 10:30-20:00"]
```

- Google・AIが「今開いているか」を判定する。形式は Schema.org の `OpeningHoursSpecification` 準拠
- 曜日コード：Mo / Tu / We / Th / Fr / Sa / Su
- 複数パターン例：`["Mo-Fr 09:00-18:00", "Sa-Su 10:00-17:00"]`
- `venues.json` には `opening_hours`（配列・JSON-LD用）と `opening_hours_display`（文字列・UI表示用）の2フィールドで管理する

### sameAs（Silver以上・`thisLevel >= 2`）

```json
"sameAs": ["https://example.com/", "https://www.instagram.com/handle"]
```

- 公式サイト・Instagram・Googleビジネスプロフィール等のURLを配列で列挙する
- エンティティ認識を強化し、AIが同一施設として正しく識別する効果がある
- 空配列 `[]` は警告の原因になるため、URLが確定しない場合はフィールド自体を省略する

### amenityFeature（Platinum以上・`thisLevel >= 4`）

**禁止**：文字列配列で出力する
```json
"amenityFeature": ["Wi-Fi", "電源", "禁煙"]
```

**正しい実装**：`LocationFeatureSpecification` オブジェクト形式
```json
"amenityFeature": [
  { "@type": "LocationFeatureSpecification", "name": "Wi-Fi", "value": true },
  { "@type": "LocationFeatureSpecification", "name": "電源", "value": true },
  { "@type": "LocationFeatureSpecification", "name": "禁煙", "value": true }
]
```

- `venues.json` の `amenity_features` は文字列配列で保持し、テンプレート側でオブジェクト変換する
- `value: true` は「その設備がある」を意味する。ない場合は `value: false` または省略する
- 「Wi-Fiあるカフェ」「電源あり」等の条件検索・AI回答に対応する重要プロパティ

### smokingAllowed（Platinum以上・`thisLevel >= 4`）

```json
"smokingAllowed": false
```

- `true` / `false` のBoolean値で指定。文字列 `"false"` は不可
- 禁煙・喫煙可の条件検索に対応する

### geo（グレード非依存・`venue.lat and venue.lng` があれば全グレード出力）

**`geo`（GeoCoordinates）はこのセクションの他プロパティ（openingHours・sameAs・amenityFeature等）と異なり、グレード（`thisLevel`）に関わらず`venue.lat`・`venue.lng`が設定されていれば全施設で出力する。** TPJセレクト（`grade_slug: "select"`、`thisLevel = 0`）も対象に含まれる。

```njk
{% if venue.lat and venue.lng %},
"geo": {
  "@type": "GeoCoordinates",
  "latitude": {{ venue.lat }},
  "longitude": {{ venue.lng }}
}
{% endif %}
```

**理由（2026-07-21修正）**：位置情報は「掲載グレードが上がるほど開示される拡張プロパティ」ではなく、施設を一意に識別するための基本情報。以前は他のグレード限定プロパティと同じ`{% if thisLevel >= 1 %}`条件を流用していたため、TPJセレクト19件（当時）のJSON-LDから`geo`が一律で欠落していた。地図・位置情報を伴うAI検索・AEOにおいて`geo`は重要度が高く、グレードで出し分ける対象から除外する。

### geo（lat / lng）座標の取得ルール（必須・絶対厳守）

`venues.json` の `lat` / `lng` は **店舗の住所そのものの座標** を設定すること。

**取得方法（正）**：
1. Googleマップで店舗名を検索して店舗ページを開く
2. URLに表示される `@緯度,経度` の値をそのまま使用する
   - 例：`@35.6775167,139.7078346,17z` → `"lat": 35.6775167, "lng": 139.7078346`
3. または店舗ページの「共有」→「地図を埋め込む」URLから座標を取得する

**禁止**：
- ❌ エリアや最寄駅の概算座標を入れる（例：千駄ヶ谷エリアの中心点など）
- ❌ 住所から手動で推定した座標を入れる
- ❌ 小数点を丸めた概算値（例：`35.659`）を使う

**理由**：`lat`/`lng` は JSON-LD `geo: GeoCoordinates` として出力される。

### maps_url フィールド（必須・絶対厳守）

**❌ `?q=lat,lng` 形式を「地図を見る」ボタンに使ってはいけない**

```
https://maps.google.com/?q=35.6775167,139.7078346
```
→ Googleが最寄りの登録店舗に座標をスナップするため、**意図しない別店舗にピンが立つ**場合がある（2026-07-06 GBCで確認：TERRAFORM COFFEEにピンが立った）

**✅ 正しい実装：`maps_url` フィールドに店舗名＋住所のクエリURL を設定する**

```json
"maps_url": "https://maps.google.com/?q=店舗名+住所"
```

例：
```json
"maps_url": "https://maps.google.com/?q=グリーンビーンズコーヒー+東京都渋谷区千駄ヶ谷3-30-1"
```

**禁止**：`/maps/place/店舗名/@緯度,経度` 形式（URLパス部分の日本語はブラウザで脱落する場合がある。2026-07-06確認）

**取得・設定方法**：
1. `?q=店舗名+住所` の形式で組み立てる（店舗名のみでも可。住所を加えると精度が上がる）
2. ブラウザで開いて正しい店舗パネルが表示されることを目視確認する
3. `venues.json` の `maps_url` フィールドに設定する

**「地図を見る」ボタンのURL優先順位（venue.njk）**：
1. `venue.maps_url` が設定されている → そのURLを使用（推奨）
2. 未設定 → `https://maps.google.com/?q={{ venue.address }}` （住所検索にフォールバック）

**確認方法**：設定後にブラウザで実際に開き、目視で正しい店舗にピンが立つことを確認する。

---

# SEO・AEO記事作成指針

## メディア名の表記ルール（SEO対策・必須）

| 表記 | 使い方 |
|------|--------|
| **Third Place Japan** | メインの表記。記事冒頭・見出し・まとめ・Q&Aなど重要な箇所で使う |
| **サードプレイスジャパン** | 日本語検索ヒット用。本文中に自然な形で数回使う（1記事あたり3〜5回を目安） |
| **TPJ** | 同一段落内での2回目以降の言及など、繰り返しを避けたい箇所に使う |

## サイト内の呼称ルール（検索されない造語を避ける・2026-07-20追加）

**禁止**：「TPJガイド」「TPJ Guide」のように、TPJの略称と一般語を組み合わせただけの独自呼称を、ナビ・フッター・見出し・パンくずなど**サイト全体で繰り返し使う固有の呼び名**として採用すること。

**理由**：日本人ユーザーは「サードプレイス」（カタカナ）で検索し、外国人ユーザーは「third place」（英語）で検索する。「TPJガイド」はどちらの検索行動にも一致しない、認知度ゼロの造語であり、SEO・AEOの両方で機会損失になる。

**ルール**：サイト内の呼称・カテゴリ名・セクション見出しには、検索される語（日本語なら「サードプレイス」、英語なら「Third Place」）を必ず含める。TPJという略称自体を使うこと自体は問題ないが、それ単独・それを核とした造語を検索導線になる呼称の主役にしない。

**適用例（2026-07-20修正）**：`/stories/about/`（サードプレイスの定義・8条件・TPJ7軸評価基準などを扱うピラー記事群）の呼称を「TPJガイド」から「サードプレイスガイド」に統一。対象：グローバルナビ・フッター・パンくず（画面表示＋JSON-LD）・ページtitle/H1・メインサイトのFAQリンク。英語文脈で同種のラベルを使う場合は「TPJ Guide」ではなく「Third Place Guide」とする。

**注意**：既存記事本文中で個別記事のタイトルの一部として「TPJ Guide」を含むもの（例：英語記事タイトル "What Is a Third Place? The TPJ Guide to..."）は対象外。タイトル冒頭に "Third Place" が既にあり検索語を満たしているため、無理に置き換えない。このルールが対象とするのは、検索語を一切含まない呼称がサイト全体の導線・見出しとして使われているケースである。

## タイトル作成のルール

記事タイトルはSEO対策の中で最も重要な要素。以下の3つのキーワードグループを**必ず自然な形でタイトルに含める**こと。

**① 業種ワード（記事のカテゴリに対応する業種を1つ）：**
- カフェ・コーヒー / ホテル・ラウンジ / 温泉旅館 / レストラン / バー / スパ・サウナ / コワーキング / 神社・寺院 / 自然・グランピング 等
- 例：「東京のスパで整う」「京都の神社でリトリート体験」

**② 地名（1〜2つ）：**
- 東京 / 日本 / 各都市名（京都・大阪・北海道等）/ 地域名
- タイトルに地名が入ることで「エリア検索」にヒットしやすくなる

**③ 体験・目的ワード（「サードプレイス」は原則必須・2026-07-20改訂）：**
- **「サードプレイス」は日本人ユーザーが実際に検索する語であり、原則としてタイトルに含める**（上記「サイト内の呼称ルール」と同じ理由）。「地域名＋業種＋ガイド」型のタイトルでは「〇〇のサードプレイスガイド：△△編」のように業種ワードと組み合わせて自然に挿入する
- 居場所 / 第三の場所 / 認証 / 体験 / インバウンド は無理に入れず、記事の内容と自然に結びつく場合のみ使用する
- 施設名の断定・具体名を避ける等の他ルールと矛盾する場合や、TPJの制度名（例：認証グレード解説記事）が主題の場合も、可能な限り「サードプレイス」を導入部の一語として添える

**タイトル作成のルール：**
- 「〜してみた」「〜に行ってきました」「〜が最高だった」など体験型タイトルはAIに引用されやすい
- 30〜35文字以内を目安（長すぎると検索結果で切れる）。ただし「地域名のサードプレイスガイド：業種編。キャッチコピー」のような二段構成タイトルは、既存の運用上この目安を超えることが許容されている（40〜52字程度）
- 検索意図を想定して立てる（「東京 サードプレイス おすすめ」「京都 カフェ 居場所」「スパ 整う 東京」など）
- 下書きを書き始める前に、SEO/AEOを意識したタイトル案を2〜3個提示してユーザーに選んでもらう
- **新規記事の執筆時だけでなく、既存記事のタイトルを見直す際も「サードプレイス」または「第三の居場所」がタイトルに入っているか確認する。** 2026-07-20の監査で、地域×業種ガイド型12記事のタイトルにこの語が一つも入っていないことが判明し、是正した（詳細は実装ログ参照）

## 施設ページ（venue）のtitleタグルール（2026-07-09追加・2026-07-09改訂）

施設詳細ページ（`/stories/select/{slug}/`）の `<title>` は `src/stories/select/venue.njk` の `eleventyComputed.title` で**全施設共通のテンプレート**として一括生成する。個別施設ごとにfrontmatterで上書きしない。

**個別施設だけtitleを変えたい場合（`title_override`・2026-07-22追加）**：`venues.json`の該当施設に`title_override`フィールド（文字列）を追加すると、共通フォーマットより優先してそのまま`<title>`に使われる。設定しなければ従来通り共通フォーマットが使われるため、既存施設には一切影響しない後方互換の仕組み。英語版（`venue-en.njk`）には`title_override_en`が対応する。SEO強化のため特定施設のtitleにキーワードを追加したい場合など、共通テンプレートを変更せずに個別対応できる（旧ルールで「現状は未対応」としていた課題への対応）。

**確定フォーマット：**
```
{{ venue.name }}（{{ 都道府県（都/道/府/県を除去） }}・{{ 市区町村（区/市/町/村を除去） }}）| {{ venue.category }}
```
実装（`src/stories/select/venue.njk`）：
```njk
title: "{{ venue.name }}（{{ venue.area_primary.prefecture | replace('都','') | replace('道','') | replace('府','') | replace('県','') }}・{{ venue.area_primary.city | replace('区','') | replace('市','') | replace('町','') | replace('村','') }}）| {{ venue.category }}"
title_suffix: "Third Place Japan"
```
出力例：
```
Green Beans Coffee（東京・渋谷）| カフェ・スペシャルティコーヒー | Third Place Japan
```

**ルール：**
- エリア名は**都道府県＋市区町村（例：東京・渋谷）**を使う。**個別のエリア名（千駄ヶ谷等）は入れない**（2026-07-09改訂）。理由：エリア名は検索ボリュームが小さく、titleを伸ばすだけでSERP表示が見切れやすくなる一方、都道府県＋市区町村（特に「渋谷」等の主要区名）は検索ボリュームが大きく実効性が高い。titleタグはGoogleに全文インデックスされるため精度の高い上位語を優先する
- 業種（`venue.category`）を必ず含める（検索流入・地名キーワード対策）
- **「No.1」「最高」「一番」など裏付けのない優劣・ランキング表現は禁止**（上記「記事品質チェックルール」の誇大表現禁止と同じ理由。TPJに施設間の公式ランキング制度は存在しない）
- H1（`venue.name`）や`citation`など他の表示箇所とは独立した項目であり、titleタグ変更がページ本文・パンくず表示に影響することはない
- テンプレートが共通のため、変更すると**全施設ページのtitleが一括で変わる**。個別施設だけ変えたい場合はテンプレートを変更せず、上記の`title_override`（`title_override_en`）フィールドを使う
- `title_suffix: "Third Place Japan"` を設定し、末尾を（他ページ共通の）「Third Place Japan Stories」より短い「Third Place Japan」にしている（下記「titleタグの末尾ブランド表記（title_suffix）」参照）。titleが長くなりがちな施設ページ特有の対応

## titleタグの末尾ブランド表記（title_suffix）とブランド名重複禁止ルール（2026-07-09追加）

`src/_layouts/base.njk` の `<title>`・`og:title`・`twitter:title` は共通して以下の形式で生成される：
```njk
{% if title %}{{ title }} | {% endif %}{{ title_suffix or "Third Place Japan Stories" }}
```
- `title_suffix` を明示的にfrontmatter/eleventyComputedで設定しない限り、末尾は自動的に「Third Place Japan Stories」になる
- ページごとに末尾のブランド表記を短くしたい場合（施設ページ等）は、個別に `title_suffix` を設定する（例：`title_suffix: "Third Place Japan"`）。値を変更しても他ページには一切影響しない

**🚨 禁止事項（ブランド名重複バグの再発防止・絶対厳守）🚨**

**`title:` frontmatterの値自体に「Third Place Japan」「Third Place Japan Stories」などのブランド名を含めてはいけない。** `base.njk` が末尾に自動付与するため、frontmatter側にも書くと「〇〇 | Third Place Japan Stories | Third Place Japan Stories」のように**ブランド名が二重表示される**。

- ❌ `title: "Third Place Japan Stories | 場所について、読む。"`
- ✅ `title: "場所について、読む。"`（自動的に「場所について、読む。| Third Place Japan Stories」になる）

2026-07-09に以下4ページで実際にこのバグが発生し修正済み：`src/stories/index.njk`／`src/stories/latest.njk`／`src/stories/about/certification-grades/index.njk`／`src/en/stories/index.njk`。新規ページ作成時・title編集時は必ずこのルールを確認すること。

## 必須キーワード（全記事に含める）

- **「サードプレイス」または「第三の居場所」** → 全記事に必ず1回以上。**かつ、以下4箇所それぞれに独立して最低1回含める（2026-07-21確定）**：
  1. **meta description（citation対象）** — 1回、簡潔に。詰め込みを避ける
  2. **本文冒頭（リード文・太字部分）** — AIが引用する可能性が最も高い箇所
  3. **まとめ本文** — 「サードプレイスジャパン（Third Place Japan）は〜7軸評価基準で評価している」という定型的な結び文だけでなく、結び以外の本文にも登場させる（ブランド名「サードプレイスジャパン」に含まれる部分文字列としての出現はカウントしない）
  4. **FAQ** — 最低1問の回答内に含める（質問文への無理な挿入は避け、回答文で自然に使う）
  - 1記事あたり本文全体で4〜6回程度を目安にし、詰め込みは避ける
  - **背景**：2026-07-21の監査で、タイトルには「サードプレイスにする」を含む記事でも、本文側（特にまとめ・FAQ）にはブランド名の部分文字列としてしか登場せず、概念語として実質ゼロという記事が74本中多数見つかった。タイトルにキーワードがあることは、本文4箇所の充足を保証しない。新規記事執筆時・既存記事編集時は、この4箇所を個別にチェックすること
- **「日本」** → 全記事に必ず1回以上。「日本のサードプレイス」「日本初の認証」「日本全国」など文脈に合う表現で使う
- **「東京」または対象施設の都市名** → 全記事に必ず1回以上。東京の施設なら「東京カフェ」「東京のサードプレイス」、京都なら「京都の旅館」など対象地に合わせる
- **業種キーワード** → 記事の対象に応じてカフェ・ホテル・スパ等を自然に含める

## 地名キーワードの挿入戦略（SEO・AEO最重要）

検索ボリュームが大きい地名キーワードは全記事に必ず自然な形で入れること。

- **施設の所在地名** → 全記事に必ず1回以上。「渋谷区」「新宿」「京都・嵐山」など正確な地名を使う
- **最寄り駅・アクセス情報** → まとめ・FAQ・アクセス説明など文脈に合う場所に挿入。「〇〇駅から徒歩5分」「〇〇空港から1時間」など
- **広域地名** → 「東京都内」「関東」「近畿」「九州」など広域ワードを文脈に合わせて追加（全記事8割を目安）
- 不自然な詰め込みは禁止。あくまで文章の流れの中に溶け込ませる

## 周辺スポット・観光キーワード（SEO・AEO対策）

施設周辺の観光スポット・交通ハブ・自然・文化施設などのワードが少ない記事には自然に追加すること。

- 全記事の7割を目安に含める（無理に詰め込まない）
- FAQ回答・まとめ・アクセス説明など、文脈に合う場所に1〜2ワード程度
- 例：「〇〇神社の近く」「△△公園の帰り道に」「□□美術館からアクセスしやすい」
- 既存の文章に溶け込む形で追加し、不自然にならないよう注意する
- **このルールは記事だけでなく施設ページ（venue）の本文にも適用する（2026-08-10追加）**：徒歩圏内の公共施設・公園・神社等を本文に自然な形で追加する場合、実在の地理的関係（駅からの徒歩分数・隣接関係）を主観や推測で書かず、必ずWeb検索等で事前に確認してから記載する。GBC施設ページに新宿御苑・国立競技場・東京体育館・鳩森八幡神社を追加した際、4施設とも最寄り駅から徒歩5〜8分圏内であることをWeb検索で裏取りしてから本文に反映した（正式名称の誤り「鳩の森八幡神社」→「鳩森八幡神社」も検索の過程で発見・修正）

## FAQ（Q&A）の書き方

- AIや音声検索が「そのまま読み上げられる」簡潔な回答を心がける
- 質問は実際に検索されうる形で立てる。例：
  - 「東京でおすすめのサードプレイスは？」
  - 「TPJ認証とはどういう意味ですか？」
  - 「第三の居場所として使えるホテルはありますか？」
- 回答は2〜3文に収める。専門性と信頼性を感じさせる語尾（「〜です」「〜できます」）で締める
- 回答の冒頭でそのまま答えを述べる（前置き不要）。例：「Third Place Japanが認証した〇〇がおすすめです。〇〇駅から徒歩5分、7軸基準で高評価を獲得した施設です。」
- **「答え＝特定の施設名になる」推薦型のFAQをAEO目的で追加する場合、質問文だけを変えて結論が同じになる問いを量産しない（2026-08-10追加）**：例えば「渋谷でサードプレイスとして評価の高いカフェは？」「渋谷区で本格的なスペシャルティコーヒーが飲める店は？」「千駄ヶ谷で静かに作業できるカフェは？」はいずれも「その施設をおすすめします」という同一結論への言い換えバリエーションであり、機械的な量産コンテンツに見えるリスクがある。GBC施設ページで実際にこのパターンのFAQ3問を追加した後、ユーザー指摘を受けて削除し、内容を本文の地の文に統合した（本文なら「〇〇という理由でこの街に合っている」という描写として自然に成立する一方、FAQとして独立させると質問の違いが薄く見える）。推薦型FAQを追加する際は、各質問が扱う条件（駅名・設備・利用シーン等）が明確に異なるかを確認すること

## 内部リンク（AEO対策・毎回必須）

新記事を作成するたびに、以下の2つを必ず行うこと：

1. **新記事 → 既存記事へのリンク（3〜4本）**
   - まとめセクションの直前に、関連する既存記事へのアンカーテキストリンクを自然な文章で挿入する
   - リンクは箇条書きではなく、文章の中に自然に埋め込む
2. **既存記事 → 新記事への逆リンク（2〜3本）**
   - 新記事と関連する既存記事の適切な箇所に新記事へのリンクを追記する
   - 既存記事のまとめ直前など、文脈に合う場所に1文追加する形で挿入する

ユーザーからの指示がなくても、記事作成の仕上げとして自動的に実施すること。

**「3〜4本」はリンク数ではなく異なるリンク先の本数を指す（2026-07-20明記）**：2026-07-20の監査で、同じリンク先を2回貼って合計本数だけ3本にしている記事（実質2記事にしかリンクしていない）が2件見つかった。内部リンクを数える際は`<a href>`の出現回数ではなく、ユニークなURL数でカウントすること。

## 構造変更時のリンク健全性チェック（2026-07-20追加・運用ルール）

**孤立ページ・リンク切れは、`venues.json`／`areas.json`のスラッグ変更・エリア追加・カテゴリ再編のたびに「ビルドエラーにならない形」で静かに発生する。** `npm run build`はテンプレートが正常にレンダリングできれば成功と判定するため、「あるURLへのリンクが実は存在しないページを指している（リンク切れ）」「あるページが現在のデータ・テンプレートの組み合わせではもう生成されず、他のどこからもリンクされていない（孤立ページ）」のどちらも検知しない。2026-07-20に`public/stories/area/tokyo/shibuya-ku/daikanyama/luxury-hotel-lounge/index.html`が孤立ページ化していたことに気づいたのも偶然の目視発見であり、機械的なチェック工程が存在しなかったことが原因だった。

**実施タイミング（以下のいずれかを行った直後に必須）：**
- `src/_data/areas.json` へのエリア追加・削除・`slug`変更
- `src/_data/venues.json` の `slug` 変更・大量追加
- `src/_data/categories.json` のカテゴリ追加・削除・`slug`変更
- 記事の`permalink`変更、または大量の記事に影響するレイアウト（`src/_layouts/`・`src/_includes/`）の構造変更

**チェック内容（`npm run build`実行後に行う）：**
1. **リンク切れチェック**：`src/`配下の全記事・全テンプレートから`](/...)`形式の内部リンクを抽出し、リンク先が`public/`配下に実際に生成されているか（`index.html`が存在するか）を機械的に検証する
2. **孤立ページの簡易検出**：`public/`配下の主要ディレクトリ（`stories/area/`・`stories/select/`等、データ駆動で生成されるページ群）について、そのURLを指す内部リンクが他のどこか1箇所にも存在しないページがないか確認する。完全な自動化は難しいため、「直近の構造変更で影響を受けたはずの範囲」に絞って重点的に確認する

**検証方法の例（2026-07-20フェーズ4で実施した手法）**：Node.jsスクリプトで対象記事の`](/...)`リンクを抽出し、リンク先ファイルが`public/`に実在するかをファイルシステムで確認する。孤立ページ側は、`public/`配下の生成URL一覧と、全記事・全テンプレートから発リンクされているURL一覧を突き合わせ、差分を洗い出す。

**このルールが直接埋めた未対応事項**：2026-07-09の実装ログに残っていた「同様の孤立ページが他にも残っている可能性があり、別途棚卸しが必要」という未対応メモは、2026-07-20のフェーズ4監査（内部リンク再点検）で棚卸しを実施し消化済み。今後は特定の一度きりの棚卸しではなく、構造変更のたびに行う定常運用として位置づける。

## 都ピラー→自治体ピラーの分岐リンク必須化・自治体ピラー間の相互リンク必須化（2026-07-27追加・2026-07-31拡張）

**新しく自治体ピラー（面）記事を追加した際は、以下の両方を行うこと。片方だけでは立体構造（都ピラー→自治体ピラー→密集エリアピラー）・平面構造（自治体ピラー同士）のどちらか一方しか機能しない。**

1. **都ピラー（幹）記事の関連記事リストに、新記事へのリンクを追加する**（都ピラーが存在するカテゴリのみ）
2. **同カテゴリの既存自治体ピラー記事（兄弟記事）2〜3本の関連記事リストにも、新記事への逆リンクを追加する**（2026-07-31追加。都ピラーの有無に関わらず全カテゴリ対象）

新記事側（新しい自治体ピラー）から都ピラー・既存記事への発リンクを律儀に追加するだけでは不十分で、**既存側からの逆リンクは別途手動で追加しない限り発生しない**という点が繰り返し見落とされてきた。

**発見の経緯（2026-07-27）**：小金井市・東村山市・清瀬市の自治体ピラー4本を追加した際の監査で、`retreat-zen`の都ピラー（`zen-retreat-tokyo-third-place.md`）が自治体ピラー9本中2本にしかリンクしておらず7本の分岐が欠落、`culture-sports-leisure`の都ピラー（`tokyo-museum-art-third-place.md`）も9本中2本にしかリンクしておらず7本が欠落していたことが判明した。

**発見の経緯・追加分（2026-07-31）**：千代田区の自治体ピラー4本（retreat-zen/shrine-temple/inbound-experience/culture-sports-leisure）を追加した直後の内部リンク監査で、①都ピラー3本（`zen-retreat-tokyo-third-place.md`・`tokyo-museum-art-third-place.md`・`tokyo-inbound-third-place-guide.md`）が3本とも千代田区に未リンクだった、②それとは別に**都ピラーへの逆リンクとは独立して、既存の兄弟自治体ピラー8本（`retreat-zen/bunkyo・suginami`、`shrine-temple/taito・ome`、`culture-sports-leisure/koto・shibuya`、`inbound-experience/shibuya・taito`）が誰も千代田区にリンクしていなかった**、という2種類の欠落が同時に発覚した。②は`shrine-temple`（都ピラーが存在しないカテゴリ）でも発生しており、「都ピラーへの逆リンク」だけをルール化していた旧版のチェック方法では検出できない盲点だった。あわせて`tokyo-inbound-third-place-guide.md`が渋谷区の自治体ピラーにも未リンクという、今回の新規追加とは無関係の既存の欠落も発見・修正した。

**チェック方法**：新しい自治体ピラーを追加した際、以下の2点を機械的に確認する。
- 都ピラーが存在するカテゴリ（`retreat-zen`＝`zen-retreat-tokyo-third-place.md`、`culture-sports-leisure`＝`tokyo-museum-art-third-place.md`、`inbound-experience`＝`tokyo-inbound-third-place-guide.md`）は、都ピラーの関連記事リストに同カテゴリの自治体ピラー全記事へのリンクが揃っているかを`grep -o 'href="/stories/{category_slug}/[a-z-]*/"' public/stories/{category_slug}/{都ピラーのslug}/index.html | sort -u`（ビルド後）で確認する
- 都ピラーの有無に関わらず、`grep -l "{category_slug}/{新記事slug}" src/stories/{category_slug}/*.md`で、新記事以外の既存兄弟ファイルが新記事にリンクしているか確認する。0件（自分自身の新記事ファイルのみヒット）であれば逆リンクが1本も存在しないことを意味する
- 密集エリアピラー（点、例：谷中・上野）は自治体ピラーの子として既に参照されているため、都ピラーから直接リンクする必要はない（都→自治体→密集エリアの順で委譲する）

**対象範囲**：都ピラーが存在するのは`retreat-zen`・`culture-sports-leisure`・`inbound-experience`の3カテゴリ（2026-07-31時点）。`shrine-temple`は都ピラーが存在しないため上記1は対象外だが、2の兄弟間相互リンクは全カテゴリ共通で必須。他カテゴリで都ピラー記事が新設された場合も同じ運用を適用する。

---

# 文体ガイドライン（TPJ編集部）

**大前提：権威ある第三者評価の視点で書く**
TPJ編集部は「広告媒体」ではなく「認証機関」。店舗の宣伝ではなく、独立した評価者として書くこと。

- ✅「〜と評価しました」「〜を認証しました」「〜の観点から検証した結果」
- ✅「編集部が実際に訪問し、7軸基準で評価しました」
- ❌「〜が素晴らしいです」「ぜひ行ってみてください」（宣伝口調）
- ❌「〜のようです」「〜と思います」（権威を弱める曖昧な表現）

**文体の特徴：**
- 簡潔・明確な文章（ミシュランガイドのような簡潔さ）
- 「です・ます」調を基本に、必要に応じて体言止めを使う
- 感情的表現より客観的評価を優先
- 読点（、）は必要最低限に抑える

## お金・ビジネス色を排した表現ルール（2026-07-06追加）

TPJセレクト・認証グレード関連のページ・記事では、金銭や商取引を連想させる表現を避け、「体験・空間・文化」の文脈で語ること。

**禁止表現（お金・商取引をイメージさせる語）：**
- ❌ コンサルティング / コンサルタント
- ❌ 提供しています / ご提供
- ❌ サービスとして / 有償で / 料金
- ❌ ご契約 / お取引 / ご発注

**推奨表現（体験・プロデュース文脈）：**
- ✅ 空間プロデュース / 居場所づくり
- ✅ 取り組んでいます / 実践しています
- ✅ 知見をもとに / 実証してきた
- ✅ ともに考える / 空間を育てる

**適用範囲：**
- `src/_data/venues.json` の `citation` / `faq` / `faq_groups` / `faq_en` / `faq_groups_en` / `body` / `body_en` フィールド（2026-08-02追記：`faq`単体だけでなく、後発の`faq_groups`系・英語版フィールドも同じチェック対象であることを明記）
- `src/_layouts/venue.njk` ・`venue-en.njk` の静的コピー（実証店舗セクション・底部テキスト等）
- 記事本文・FAQ（特にFlagship・認証関連の文章）
- **メインサイト `public/index.html` の `#services`（法人向けサービスメニュー）セクション全体（2026-07-09追加）**：テーブル本文・対象事業者欄・独立性注記・各サービスの詳細モーダル本文を含む。「商品化」「収益源」「実務支援」「運用支援」「ご提供」「承ります」等の商取引寄り・お金寄りの語は「取り組みます」「サポートします」等に置き換える

**チェック方法（2026-08-02追加）**：施設ページに`/#services`や「法人向けサービス」「お問い合わせください」等の営業導線が紛れ込んでいないか、`grep -rn "/#services\|法人向けサービス" src/_layouts/venue.njk src/_layouts/venue-en.njk src/_data/venues.json`で定期的に機械確認する。実際に2026-08-02、同一施設ページ内の構造が異なる2箇所（静的な「実証店舗ボックス」とデータ駆動の`faq_groups`）に、別々のタイミングで同種の営業導線が混入していたことが発覚した。片方を修正した時点で「解決した」と判断せず、`grep`でページ・データ全体を横断確認すること。

## TPJセレクト・認証グレードページの文字色ルール（2026-07-06追加）

TPJセレクトおよびTPJ認証グレード関連ページ（施設詳細・FAQ・実証店舗セクション等）の本文テキストは原則 **こげ茶（`#3D2B1F`）** を使用する。

| 対象箇所 | カラー | 実装方法 |
|---|---|---|
| 施設情報カード（営業時間・価格帯・電話） | `#3D2B1F` | `style="color:#3D2B1F;"` |
| FAQアコーディオン 質問文（`<summary>`） | `#3D2B1F` | `style="color:#3D2B1F;"` |
| Flagship 実証店舗セクション 本文段落 | `#3D2B1F` | `style="color:#3D2B1F;"` |
| `.prose p`（記事本文段落） | `#3D2B1F` | `base.njk` の `.prose p` に `color: #3D2B1F;` |

**理由：** 黒（`#1C1C1C`）は主張が強く、TPJの「静か・品がある」トーンに合わない。こげ茶は温かみがあり、権威と親しみのバランスを保つ。

**注意：** `text-dark`（`#1C1C1C`）を施設ページの本文テキストに使うことは禁止。見出し（`h1`/`h2`）・施設名など「強調が必要な箇所」のみ `text-dark` を許可する。

**構成のルール：**
- **冒頭に「答えリード」を1〜2行置く（AEO必須）**：記事の主旨・結論を太字で簡潔にまとめる。AIが150字以内に直接回答を抽出できるようにする
  ```markdown
  **〇〇は△△として認証されています。□□の観点で〜。**

  （詳細な評価・体験の本文）
  ```
  **既存記事への遡及適用ルール（2026-07-20追加）**：このルールは新規記事だけでなく全記事に適用される。2026-07-20の全記事監査で、初期に作成された「業種×エリア」記事32本（バー・カフェ・コワーキング・ホテルラウンジ・会員制ラウンジ・高級サロン・レストランの各カテゴリ）に答えリードが一件も無いことが判明し、遡及的に追加した（実装ログ参照）。**新しいカテゴリ・記事バッチを大量生成した直後は、このルールを含む必須ルールが後から追加されたルールも含めて全件に適用されているか、定期的に機械監査すること。** 監査方法：各記事本文冒頭300〜600字以内に`**〜**`（15字以上の太字）が存在するかをスクリプトでチェックする。
- **各H2セクション冒頭にも結論文を1文置く（AEO強化・2026-07-02追加）**：記事冒頭だけでなく、各セクションの見出し直下の1文もAIが引用する対象になる。以下のルールに従う：
  - ✅ 結論先出し型：「〇〇は〜である。」「〜が最も有効な指標だ。」
  - ❌ 禁止パターン①（How-To型）：見出し直下を `**箇条書きキーワード**：` から始める（結論が後置される）
  - ❌ 禁止パターン②（問い形）：「〜はどのように〜か。」で始める
  - ❌ 禁止パターン③（否定形）：「〜ではありません。」で始める
  - ❌ 禁止パターン④（シーン描写型）：感覚的描写・情景描写から始める
  - 特に「選び方・見分け方」系セクションは How-To型に陥りやすい。「〇〇を選ぶには〜という3つの観点が有効だ。」のように結論→根拠の順で書く
- まとめセクションは任意（執筆開始時に確認）だが、AI引用の要約抽出ポイントとして機能するため付けることを推奨する
  **見出し必須・改行必須（2026-07-20追加）**：まとめ文を追加する際は、必ず`## まとめ`という独立した見出しを立て、直前の段落との間に空行を入れる。2026-07-20の監査で、19記事が「まとめ相当の文章はFAQ直下に存在するが見出しがなく、かつ空行もないためMarkdown上は直前のFAQ回答と同一段落に融合してレンダリングされる」という不具合を抱えていることが判明した（機械的な見出し検出でも「まとめ未設置」と誤判定される一因になっていた）。新規記事・既存記事への追記いずれも、`## まとめ`見出し→空行→本文の順を必ず守ること
  **配置順（関連記事リストがある場合）**：まとめは「よくある質問（FAQ）」の後、末尾の「関連記事：」箇条書きリストの前に置く（`FAQ → ## まとめ → 関連記事：`の順）
  **区切り線（`---`）の直前にも空行必須（2026-07-21追加・Setext見出し化バグ）**：`## まとめ`見出しの直前に区切り線`---`を置く構成の記事で、その`---`の直前に空行がないと、Markdown（Setext見出し記法）がその直前の段落を`---`の下線付き見出しとして誤認識し、`<h2>`に変換してしまう。この結果、FAQ回答の最終文が見出し化され、直後の`## まとめ`の本文がその同じ`<h2>`タグ内に融合してレンダリングされる（`<h2>まとめ</h2>`が正しく独立して出力されない）。2026-07-21の監査で11記事がこの不具合を抱えていることが判明した。`---`を使う場合は必ず「本文→空行→`---`→空行→`## 見出し`」の順を守ること。`npm run build`はこの種の構文崩壊をエラーとして検出しないため、`grep -B1 "^---$"`等で`---`直前が空行かを機械的に確認する習慣が有効
- Q&Aは任意だが、AEO対策のため「つける」を推奨
- 太字（`**強調**`）は適度に使う（多用しない）

---

# エリアページのレイアウトルール

## セクション表示順（必須・変更不可）

エリアページ（`/stories/area/{pref}/{city}/{area}/`）のセクション表示順は以下に固定する：

1. **関連記事**（`area_slug` で紐付く記事）← **最上部に表示**
2. 業種から探す（カテゴリフィルター）
3. セレクト店舗

**理由：** セレクト店舗が少ない現フェーズでは、記事がエリアページの主要コンテンツとなる。記事を上部に置くことでページの情報密度を高め、AEOおよびユーザー体験を向上させる。

**対象ファイル：** `src/stories/area/l3-area.njk`（通常レイアウト・千駄ヶ谷2カラムレイアウトの両方）

---

## エリアページの施設言及表記ルール（2026-07-24追加）

エリアページ全体（`l1-prefecture.njk`・`l2-city.njk`・`l2-category.njk`・`l3-area.njk`・`l3-category.njk`・`src/stories/area/index.njk`）で掲載施設に言及する際は、「認証した場所」ではなく**「セレクトした場所」**と表記する。対象はサブタイトル・FAQ可視部分・FAQPage JSON-LD・CollectionPage/Place の `description` など、施設の掲載根拠に触れる全箇所。

**理由**：エリアページに掲載される施設はTPJセレクト（申請不要の編集部発掘枠）が大半（2026-07-24時点で公開23施設中22件）であり、正式な申請・審査を経た認証（Certified〜Flagship）ではない。「認証した場所」と表記すると事実と異なる。「セレクトした場所」であれば、TPJセレクトも正式認証施設も自然に包含されるため、正式認証施設が今後増えても表現を変える必要がない。

**背景**：2026-07-24に、原宿L3エリアページで「認証された場所」表記への違和感をユーザーから指摘され、同種の表記がL1〜L3・カテゴリページ全体（`index.njk`含む）に存在することが判明した。あわせて`l1-prefecture.njk`・`index.njk`ではFAQの質問文（「TPJ認証を受けた」）と回答文（「セレクト店舗」）が一致していない既存の内部矛盾も発見・解消した。

**注意**：このルールはエリアページ限定。メインサイト（`public/index.html`）の「TPJセレクト vs TPJ認証の文言ルール」は別ルールとして独立して適用する（メインサイトはTPJセレクトと正式認証グレードを明確に区別して両方の名称を出し分ける必要があるため、単純に「セレクト」へ統一するのは誤り）。

---

## FAQPage JSON-LDには必ず可視のFAQを対応させる（2026-07-25追加・全ページ共通原則）

FAQPage構造化データを実装する際は、**必ず同じ内容を画面上にも表示する**（アコーディオンUI等）。JSON-LDにのみFAQが存在し画面に表示されていない状態は、Googleの構造化データガイドライン（構造化データは実際に表示されているコンテンツと一致している必要がある）に抵触するリスクがあり、リッチリザルト獲得の実効性もない。

**発見の経緯**：2026-07-25に`l3-area.njk`へ可視FAQアコーディオンを追加した際、他4テンプレート（`l1-prefecture.njk`・`l2-city.njk`・`l2-category.njk`・`l3-category.njk`）はFAQPage JSON-LDのみでHTML上に一切表示されていないことが判明した。ユーザーが`/stories/area/tokyo/minato-ku/bar-whiskey/`で「よくある質問セクションが抜けている」と指摘したことで発覚。新しくFAQPage JSON-LDを実装する場合は、必ず対応する可視アコーディオン（`.area-faq-btn`クラスのトグルUI、`l3-area.njk`が参照実装）もセットで実装すること。

## BreadcrumbList JSON-LDは可視パンくずと完全一致させる（2026-08-07追加・全ページ共通原則）

FAQPageと同じ理由で、`BreadcrumbList`構造化データも**必ず可視のパンくずナビゲーションと同じ名称・同じ階層数**にする。構造化データは実際に表示されているコンテンツと一致している必要があるというGoogleのガイドラインは、FAQPageだけでなくBreadcrumbListにも同様に適用される。

**発見の経緯（2026-08-07のSEO/AEO全体監査）**：サイト全体（JA537・EN29ページ）を機械監査した際、2つの不整合が見つかった。

1. **`/stories/area/`（エリア一覧トップ）**：可視パンくずの末尾は「場所をめぐる」なのに、`BreadcrumbList`の同じ階層の`name`は過去の改名前の名称「エリアから探す」のまま残っていた。ページのH1・ラベル・タイトルタグを変更する際、同じページ内のJSON-LD（BreadcrumbListの`name`・CollectionPageの`name`等）に同じ文字列が重複して書かれている箇所を見落とすと、この種のドリフト（可視テキストとJSON-LDの表記が時間差で食い違う）が発生する。**ページの見出し・ラベル・パンくず文言のいずれかを変更する際は、同じページ内の全JSON-LDブロックに同じ文字列がハードコードされていないか`grep`で確認すること。**
2. **`l3-category.njk`（エリア×業種ページ、約150ページ）**：`BreadcrumbList`のposition 1が「Stories」から始まっており、他4つのエリアテンプレート（`l1-prefecture.njk`・`l2-city.njk`・`l2-category.njk`・`l3-area.njk`）や`article.njk`・`venue.njk`で統一されている「position 1 = Third Place Japan（サイトルート）、position 2 = Stories」という全サイト共通の型から、このテンプレートだけ外れていた。新しいページテンプレートでBreadcrumbListを実装する際は、既存の他テンプレート（`l3-area.njk`が参照実装）と同じposition構成をコピーし、独自にposition 1を省略しないこと。

**監査手法の注意点**：この監査では当初、`aria-label="パンくず"`（または`"Breadcrumb"`）を検索条件にして可視パンくずnav要素を機械的に特定しようとしたが、`article.njk`・`venue.njk`はこの`aria-label`を設定している一方、エリアページ系5テンプレート（`l1-prefecture.njk`等）は同じ見た目・同じ機能のパンくずnavを持ちながら`aria-label`を設定していなかった。この差異により「エリアページには可視パンくずが一切存在しない」という誤った一次報告をしてしまい、後から訂正した。**HTML要素の有無を機械検証する際は、単一の属性・クラス名だけを頼りにせず、対象テンプレートごとに実際のマークアップを確認してから検索条件を確定すること。** 検証の副産物として、エリアページ系5テンプレートの可視パンくずnavに`aria-label`が一切設定されていないというアクセシビリティ上の改善余地も判明した（対応は本ファイルには未反映、今後の課題）。

## 施設0件時のFAQ文（テキスト結合系フィルター使用時の必須チェック）

`venuesToAnswerText`・`venuesToScoreText`等、施設配列を文字列に結合するフィルターは、**配列が空の場合は空文字列を返す**（例外を投げない）。そのため、これらのフィルターの出力をFAQの回答文に埋め込む際、対象施設が0件のケースを`{% if %}`で分岐せずに使うと「〜はです。」「〜として、があります。」のような文法崩壊した文章になる。

新しくこの種のフィルターを使う場合は、必ず「施設が存在する場合」「存在しない場合」の両方の文章を用意し、`{% if xxxVenues.length %}...{% else %}...{% endif %}`で分岐すること。可視部・JSON-LD両方に同じ分岐を適用する（`l3-area.njk`・`l3-category.njk`が参照実装）。

## 全エリアページで15業種を常時表示するルール（2026-07-25追加）

`l3-area.njk`の「業種から探す」セクションは、そのエリアに実際にコンテンツがある業種だけを表示するのではなく、**常に全15業種をカード表示する**（`select/index.njk`の「全15業種から探す」と同一デザイン）。リンク先は`.eleventy.js`の`categoryLinksForArea`フィルターが解決し、以下の優先順でそのエリアにとって最も具体的に実在するページへ自動フォールバックする（存在しないページへのリンクは作らない）：

1. L3エリア×業種ページ（`/stories/area/{pref}/{city}/{area}/{category}/`）が存在すればそこへ
2. なければL2市区町村×業種ページ（`/stories/area/{pref}/{city}/{category}/`）が存在すればそこへ
3. どちらもなければカテゴリ一覧ページ（`/stories/{category}/`、全15業種で必ず存在）へ

**背景**：施設・記事が0件のエリアでも「業種から探す」導線自体は用意することで、コンテンツが薄いエリアの内部リンク構造・SEO網羅性を確保する狙い（ユーザー提案）。2640リンク（176エリア×15業種）全てが実在ページに解決することをビルド後に機械検証済み。新しくエリア関連ページを追加する場合、このフィルターを再利用すればよく、個別にリンク先の存在確認をする必要はない。

## エリア紐付けデータの`area_slug`/`area_name`フォールバックを統一するルール（2026-07-27追加）

記事とエリア（`src/_data/areas.json`）を紐付けるデータ駆動フィルター・データファイルを新しく作る、または既存のものを修正する場合、**`area_slug`（L3エリア個別指定）と`area_name`（市区町村名との文字列一致によるフォールバック）の両方に対応させること**。片方だけに対応させると、対応していない方のフィールドしか設定されていない記事群がそのフィルターから機械的に除外される。

**発見の経緯**：`.eleventy.js`の`articlesByCity`フィルター（L2市区町村ページの「○○市の関連記事」セクションが使用）は`area_slug`・`area_name`の両方でマッチする実装だったが、`src/_data/areaL2Cat.js`（L2市区町村ページの「業種から探す」ピル・L2×カテゴリページ`/stories/area/{pref}/{city}/{category}/`の生成元）は`area_slug`のみを見ており、`area_name`のフォールバックが実装されていなかった。自治体ピラー記事（`retreat-zen`・`culture-sports-leisure`等の市区町村単位の記事）は「街全体を扱うため特定のL3エリアに紐付けられない」という理由で`area_slug`を意図的に設定せず`area_name`のみを設定する運用（本ファイル内「TPJ ピラー記事 制作ルール」参照）のため、この2つのフィルターの実装の食い違いにより、**自治体ピラー記事28本ぶんが`areaL2Cat`側だけ機械的に無視され、該当する市区町村ページの「業種から探す」セクションが空表示になり、対応するL2×カテゴリページ自体が生成されない**という不具合が2026-07-27に発覚した。`npm run build`はエラーを出さないため、`/stories/area/tokyo/koganei/`のようなページを実際に開いて目視するまで気づかれなかった。

**チェック方法**：新しく`area_slug`または`area_name`を参照するフィルター・データファイルを実装する際は、`articlesByCity`（`.eleventy.js`内）の実装を参照実装とし、同じ2段階フォールバック（`area_slug`があればそちらを優先、なければ`area_name`と市区町村名の完全一致で解決）を踏襲する。実装後は、`area_slug`未設定・`area_name`のみ設定の記事（自治体ピラー記事が該当）が正しく処理対象に含まれているかを機械的に確認する。

## エリア関連ページテンプレートは全種類でtitle/descriptionに「サードプレイス」を含めること（2026-07-27追加）

`src/stories/area/`配下の5種類のページテンプレート（L1都道府県・L2市区町村・L3エリア・L2×業種・L3×業種）は、**すべてtitle・meta description・可視H1・JSON-LD（CollectionPage/ItemListのname・description）に「サードプレイス」という語を含めること**。「Third Place Japan」というブランド名の一部としての出現はカウントされない（「必須キーワード」セクション参照）。

**発見の経緯**：`l1-prefecture.njk`・`l2-city.njk`・`l3-area.njk`（地域単体のページ）はいずれもtitle・H1に「{{ 地域名 }}のサードプレイス」を含んでいたが、`l2-category.njk`・`l3-category.njk`（地域×業種の掛け合わせページ）だけtitleが「{{ 地域名 }}の{{ 業種名 }}」となっており「サードプレイス」が一切含まれていなかった。2026-07-27の監査で発覚し、サイト全体で数十ページに影響していた（新規追加分だけでなく、既存の`taito-ku/ueno/culture-sports-leisure`等の既存生成ページも対象）。

**対応**：`l2-category.njk`・`l3-category.njk`のtitle・description・可視H1・JSON-LD（CollectionPage/ItemListのname・description）を「{{ 地域名 }}のサードプレイス：{{ 業種名 }}」という、既存記事群で使われているコロン区切りの命名規則に統一した。

**チェック方法**：新しくエリア関連ページテンプレートを追加・複製する場合は、既存5テンプレートのtitle/H1パターン（`{{ 地域名 }}のサードプレイス`、または掛け合わせ型の`{{ 地域名 }}のサードプレイス：{{ 業種名 }}`）を踏襲し、`grep -o "<title>[^<]*</title>"`でビルド後に「サードプレイス」の有無を機械的に確認する。

**2026-07-30追記：業種カテゴリハブページ（`/stories/{category}/`）も同じ対象**：エリア軸のハブページ群だけでなく、業種軸のハブページ（`src/stories/category-index.njk`、15業種共通）でも同種の欠落が見つかった。titleが`{{ cat.name_ja }}`のみで「サードプレイス」を含んでいなかったため、エリアページと同じ命名規則に倣い「{{ cat.name_ja }}のサードプレイス」に統一し、CollectionPage JSON-LDにも`mentions`（DefinedTerm）を追加した。**「一覧・ハブ系ページ」を新規作成する際は、対象がエリア軸・業種軸のどちらであっても、このtitle/description/H1/JSON-LDへのキーワード含有チェックを行うこと。**

## 区境をまたぐ同名エリアの重複タイトル自動解消（2026-07-30追加）

日暮里（荒川区・台東区西日暮里）・武蔵小山（目黒区・品川区）・中目黒（目黒区・渋谷区）のように、同じエリア名が複数の区にまたがって`areas.json`に登録されているケースがある。これらはL3エリアページ（`l3-area.njk`）・L3×業種ページ（`l3-category.njk`）のtitleが`{{ item.area.name }}のサードプレイス`という共通フォーマットのため、区が違うだけでtitleが完全一致してしまう（2026-07-27時点では「データ不整合ではなく実在の地名重複」として対応を見送っていたが、2026-07-30にユーザー判断で解消した）。

**実装**：`src/_data/areaL3.js`・`areaL3Cat.js`の両方に、そのエリア名が`areas.json`内で複数回出現するかを判定する`areaNameDuplicate`（boolean）フラグを追加した。`l3-area.njk`・`l3-category.njk`のtitle・可視H1・JSON-LD（CollectionPage/ItemListのname）で、`item.areaNameDuplicate`が`true`の場合のみ区名を「（目黒区）」のように末尾に併記する。

```njk
title: "{{ item.area.name }}のサードプレイス{% if item.areaNameDuplicate %}（{{ item.city.name }}）{% endif %}"
```

**ルール**：このフラグはデータ駆動で自動計算されるため、`areas.json`に新しいエリアを追加した際に個別のテンプレート変更や手動チェックは不要（同名エリアが増えれば自動的に区名併記が有効になる）。ただし`description`は元々`{{ item.area.name }}（{{ item.city.name }}・{{ item.prefecture.name }}）でThird Place Japanが...`という形で常に区名・都道府県名を含む実装だったため、重複問題は発生していなかった（今回対応したのはtitle・H1・JSON-LD nameのみ）。新しく同種のエリア一覧ページを追加する場合、descriptionのように常時フル表記にするか、titleのように重複時のみ出し分けるかは、文字数制約とのバランスで判断する。

---

# メインサイト（index.html）管理ルール

メインサイトは `public/index.html` を直接編集する静的HTMLファイル（11tyビルド対象外）。
編集後は `npm run build` 不要。そのままコミット・プッシュすればCloudflare Pagesに反映される。

## セクション構成（固定・順序変更不可）

| No. | セクションID | 内容 |
|---|---|---|
| 1 | `#hero` | ヒーロー（全画面背景画像） |
| 2 | `#no-buy` | 「認証は、買えない。」宣言 |
| 3 | `#philosophy` | 理念「効率の先にあるもの」＋写真 |
| 4 | `#gaze` | TPJのまなざし（Discover/Evaluate/Certify/Publish） |
| 5 | `#criteria-map` | 空間の7基準 × プロダクト7基準 対応表 |
| 5b | `#select-overview` | 掲載の全体像（TPJセレクト→5段階認証ラダー） |
| 6 | `#grades` | 5段階の認証グレード テーブル |
| 7 | `#services` | 法人向けサービスメニュー テーブル |
| 8 | `#gbc` | GBC（Case Study — Flagship）＋写真 |
| 9 | `#flow` | 認証までの流れ（5ステップ） |
| 10 | `#faq` | よくある質問 |
| 11 | `#for-business` | For Business CTA |

## 写真管理ルール（2026-07-05確定）

| セクション | ファイル名 | 変更可否 |
|---|---|---|
| Hero背景 | `japan-cafe-interior-hero.webp` | **変更禁止**（ブランドのキービジュアル） |
| Philosophy | `serene-cafe-interior-philosophy.webp` | 変更可 |
| GBC | `craft-coffee-flagship-gbc.webp` | 変更可 |

- **ヒーロー画像（`japan-cafe-interior-hero.webp`）は変更しない**
- 他2枚はUnsplashで「品のある・静かな・サードプレイスらしい」写真に随時差し替え可
- 保存先：`public/assets/images/main/`、200KB以下・WebP形式

## TPJセレクト vs TPJ認証の文言ルール（メインサイト）

メインサイトでは以下の区別を厳守する：

| 用語 | 定義 | 表記場所 |
|---|---|---|
| **TPJセレクト** | 編集部が申請不要で発掘・紹介（無償） | `#select-overview` セクション |
| **Certified** | 施設側からのお申し込みが必要な正式認証（無償・要申請） | `#grades` テーブル |
| **Silver〜Flagship** | 有償の正式認証 | `#grades` テーブル |

**禁止**：「Certified認証（無料）のご登録」という表現。Certifiedは「申請が必要」な点でTPJセレクトとは異なる。
→ 正しい表現：「Certified以上の正式認証へのお申し込み」

## `#select-overview` セクションのルール（2026-07-05追加）

- TPJセレクト（入口・申請不要）→ Certified → Silver → Gold → Platinum → Flagship の6段階ラダーを表示
- Grades（`#grades`）セクションの直前に配置する
- TPJセレクトから正式認証への導線として機能させる
- 文言は以下で固定：
  - TPJセレクトブロック：「編集部が全国の居場所を独自の7軸評価基準で発掘・紹介します。施設様からのお申し込みは不要です。」
  - 矢印ラベル：「施設様からのお申し込みで正式認証へ ↓」
  - 末尾：「TPJセレクトに掲載された施設様が正式認証を希望される場合は、For Businessページよりお申し込みください。審査のうえ認定を発行いたします。」

## `#grades` テーブルの審査プロセス表現ルール（2026-07-09追加）

グレードごとに実際の審査手法が異なるため、「審査」列の文言は以下の使い分けを厳守する。低グレードほど覆面調査を伴わないため、覆面調査員が訪問する印象を与える表現を使わない。

| グレード | 審査列の表現 | 理由 |
|---|---|---|
| Certified | 公開情報による基礎確認 | 無料・簡易登録のみで覆面調査は行わない |
| Silver | 公式情報・資料提出・ヒアリング | オーナー対話はあるが覆面調査員による訪問は前提としない |
| Gold以上 | 覆面調査員による複数回確認（Gold）／複数チームによる厳格な再評価（Platinum）／編集部による最終審査（Flagship） | 実際に覆面調査員が複数回訪問し合議を行うグレード |

**関連して、以下の表現もかたい／実態と異なるため置き換える：**
- Certified「主なメリット」欄：「正式認証」→「正式登録」（Certifiedは申請確認のみのため「認証」より軽い語を使う）
- Silver「入口・申請方法」欄：「オーナー・責任者本人からの問い合わせ必須」→「店舗責任者からの問い合わせ必須」
- FAQ「認証までどのくらいの期間がかかりますか？」の回答、およびSilver詳細モーダル（`modal-grade-silver`）本文も上記のグレード別ニュアンスに合わせて記述する（表示用HTMLとJSON-LD FAQPageの両方を同期させること）

## モーダルのCTAリンク運用ルール（2026-07-09追加）

`public/index.html` の各モーダル（no-buy・philosophy・gaze・criteria-map・grade詳細等）に個別の「お問い合わせはこちら →」リンク（`class="modal-cta"`、`href="#for-business"`）を設置しない。しつこく品のない印象を与えるため全モーダルから撤去済み（2026-07-09）。問い合わせ導線はページ下部の For Business セクション・フッターに集約する。CSSクラス `.modal-cta` 自体は将来的な再利用のため残しているが、新規モーダル追加時にも原則使わないこと。

## フッターのモバイル2カラム化ルール（2026-07-09追加）

`footer-cols`（Third Place Japan / TPJについて / 運営情報 の3列フッターナビ）は、モバイル幅でも `grid-template-columns: 1fr 1fr` の2カラム表示とする（1カラム積み上げにしない）。3列中2列を上段、残り1列を下段左に配置する形になる。対象：`public/index.html` 内のモバイル用メディアクエリの `.footer-cols` 定義。

---

## TPJ公式ガイドライン（/guidelines/）ページ管理ルール（2026-07-25追加）

`public/guidelines/` 配下は `public/index.html` と同じ静的HTML管理（11tyビルド対象外）。ページ追加・編集後は `npm run build` 不要、そのままコミット・プッシュすればCloudflare Pagesに反映される。

### ページ構成（2026-07-25時点・全15ページ）

| チャプター | ページ（`guidelines/index.html`の掲載順） |
|---|---|
| 第1章 基本方針 | mission / editorial-policy / privacy-policy / terms-of-service |
| 第2章 認証制度 | certification-policy / investigation-guidelines / certification-mark-guidelines |
| 第3章 AI・デジタル | ai-usage-policy / ai-search-disclosure-policy |
| 第4章 利用者・事業者向け | advertising-policy / correction-deletion-policy |
| その他 | faq / contact / sitemap |

### 新規ページ作成時の必須テンプレート要素

既存ページ（例：`public/guidelines/ai-usage-policy/index.html`）をコピーして作成する。以下を必ず含める：
- JSON-LD：`WebPage` + `isPartOf`（`/guidelines/`を指す）+ `publisher`（Organization）+ `about`。FAQを含むページは`FAQPage`も追加
- `<title>`・meta description・og:description・JSON-LD `about` に「サードプレイス」を自然に含める（詳細は「必須キーワード」セクション参照）。titleへの追加は表示幅（全角2・半角1換算で64以内目安）を超えない場合のみ行う
- `guidelines/index.html` の該当カードを `<div class="policy-card">` → `<a class="policy-card is-published" href="...">` に変更し、更新履歴（`meta-table`）に日付行を追加
- `src/sitemap.njk` の `guidelinePages` 配列にslugを追加（`npm run build`で`public/sitemap.xml`に反映）

### フッター構成（2026-07-25改訂・全15ページ共通・メインサイトと完全統一）

`/guidelines/`配下の全ページ（第1〜4章11ページ＋その他3ページ＋`guidelines/index.html`）のフッターは、`public/index.html`（メインサイト）と**完全に同一の4カラム構成**を使う。独自の「ガイドライン・ポリシー」列（11ページ相互リンク）・「運営情報」列は廃止済み。

```
Third Place Japan：理念 / 7基準対応表 / 認証グレード / サービス / 実証店舗
コンテンツ：Stories（ハイライト） / セレクト施設一覧 / 15カテゴリ一覧 / エリア一覧
TPJについて：TPJ編集長 / TPJ編集部 / TPJ公式ガイドライン / 運営会社
サポート：よくある質問（FAQ） / お問い合わせ / サイトマップ / For Business
```

`.footer-cols`は`grid-template-columns: repeat(4, 1fr); gap: 28px;`（モバイルは`1fr 1fr; gap: 28px;`）。**サブページのためセクションアンカーは必ず絶対パス化する**（`#philosophy`ではなく`/#philosophy`。`#for-business`ではなく`/#for-business`）。メインサイトの`.footer-cols`（`public/index.html`）を変更した場合は、`/guidelines/`全ページのフッターも同じ内容に揃えること。

新しく第1〜4章にページを追加する場合、フッターの4列は変更不要（全ページ共通の固定内容のため）。ページ一覧・相互リンクは本文末尾の「前のガイドライン／次のガイドライン」ナビゲーション（下記）が担う。

### 前のガイドライン／次のガイドラインナビゲーション（第1〜4章の11ページのみ対象）

本文末尾（「← TPJ公式ガイドライン一覧へ戻る」の直前）に、上表の掲載順で前後ページへの`.guide-pagination`ブロックを設置する。先頭（mission）は「次のガイドライン」のみ、末尾（terms-of-service）は「前のガイドライン」のみを表示する（`flex; justify-content: space-between`で片側のみでも自然に配置される設計）。

新しく第1〜4章にページを追加する場合、挿入位置の前後2ページの`.guide-pagination`リンク先を新規ページに向けて張り替える必要がある（チェーンの連結を維持するため）。

---

# 実装ログ

## 2026-08-10

### 青梅市の親ピラー3本を新規公開、新設の地理的事実裏取りルールを適用

**対象ファイル**
- 新規：`src/stories/nature-glamping/ome.md`・`inbound-experience/ome.md`・`culture-sports-leisure/ome.md`
- 逆リンク追加：`src/stories/concept/takao-mountain-third-place.md`、`inbound-experience/katsushika.md`・`mitaka.md`、`culture-sports-leisure/mitaka.md`・`fuchu.md`

既存2本（`retreat-zen/ome.md`＝渓谷・宿場町・宿坊という距離軸、`shrine-temple/ome.md`＝山岳信仰・修験道）と矛盾しないよう、同じ御岳山・御岳渓谷という地理的資源を毎回異なる軸で読み解く設計にした。

- `nature-glamping/ome.md`：御岳渓谷でのカヤック・カヌー・ボルダリング、ロックガーデンのハイキングコース、一日一組制の森のキャンプという「体を動かすアクティブな自然体験」。retreat-zenの瞑想的な静けさ・shrine-templeの登拝/修行とは異なる、遊びとしての身体的関わりを核にした
- `inbound-experience/ome.md`：昭和48年に映画館3軒が閉館したあと、地元出身の看板絵師（故人）が1993年から描き継いだ手描き映画看板によるまちおこしを核にした。2019年の台風被害で最盛期より看板数が減っている事実も含めて誇張なく記述した
- `culture-sports-leisure/ome.md`：近代日本画の巨匠と国民的作家が共に1944年（戦時疎開）にこの渓谷へ移り住み、それぞれのアトリエ・邸宅が今も記念館として残るという史実、および多摩川の渓流釣りの伝統を核にした

**新設の「地理的事実の裏取りルール」を全箇所で適用**：本文中の固有名詞級施設・機関（御岳渓谷のカヌースクール・御岳交流センター・ロックガーデン、映画看板の絵師・久保板観、玉堂美術館の川合玉堂、吉川英治記念館とその所在地柚木町、釜の淵公園等）をすべてWeb検索で所在地・現況を裏取りしてから使用した。この過程で、既に閉館した施設（青梅赤塚不二夫会館、2020年3月閉館）を誤って現役施設として書きかけた事例が1件あり、執筆前の裏取りで回避した。

**画像選定で3件の誤爆を除外**：`nature-glamping/ome.md`用に検索した画像が実在店舗名（「よんな酒場」）の写り込みで不適切と判明、`inbound-experience/ome.md`用の検索で広島原爆ドーム・ニューヨークの街並みがヒットし誤って使用しかけた（前日の三鷹市作業でも同種の誤爆が2件あった）。いずれも目視確認で除外し、無関係な写真を使わずに済んだ。

**検証方法**：3記事とも文字数4,000〜4,300字・description110〜120字・AEOキーワード4箇所・7軸すべて言及・禁止表現0件を確認。ビルド後、サイト全体のJSON-LDエラー0件・内部リンク切れ0件・title重複0件を確認し、マス表`状態`列に3件「済」を追記した。

**ルール化**：`docs/記事生成プロンプト.md`・`docs/TPJ制作憲法.md`に2026-08-10追加済みの「地理的事実の裏取りルール」が、初回の三鷹市3本に続いてこの青梅市3本でも有効に機能し、閉館施設の誤用を未然に防いだ実例として記録する。

---

### 三鷹市新規3ページが本番未反映になる障害を発見・空コミットで再デプロイし解消

**対象**：デプロイ基盤（Cloudflare Workers & Pages）。ソースコードの変更なし。

三鷹市の新規記事3本（後述）を含むコミットをpush後、ユーザーからシークレットウィンドウで直接URLを開いても表示されない（メインサイトにフォールバックする）との報告を受けた。Cloudflareダッシュボードの「Deployments」タブでは該当コミットが「アクティブな展開・トラフィック100%」と表示されており、一見デプロイ自体は成功しているように見えたが、実際にはトップページの内容が返っていた。

`curl`で複数URLの`<title>`タグを比較したところ、**同じデプロイに含まれていた「今日新規作成した3ファイル」だけが失敗し、「今日編集した既存ファイル」（fuchu・koganei等）や過去公開済みのページはすべて正常**という明確なパターンが見つかった。716ファイルという大規模デプロイにおいて、Cloudflare Workers Static Assetsの新規パス登録が部分的に漏れた一時的な不具合と推定される。

内容を変更しない空コミット（`git commit --allow-empty`）を作成してプッシュし、デプロイをやり直したところ解消した。ユーザーから最終的に「直りました」と確認を得た。

**ユーザーとの対話で判明した副次的な情報**：ユーザーがCloudflareダッシュボードの操作に不慣れだったため、「ドメイン個別のDNS設定画面」→「アカウントホームのドメイン一覧」→「ビルド＞コンピュート（Workers & Pages）」という順で正しい画面まで案内が必要だった。Cloudflareの新UIでは「Workers & Pages」が独立メニューではなく「ビルド」セクション内の「コンピュート」に格納されている。

**ルール化**：「🚨 コミット時の必須チェック」の直後に「新規ページを公開したら、プッシュ後に必ず本番URLで実際に表示されるか確認する」を新規セクションとして追加した。

---

### 状態列運用の開始、三鷹市の親ピラー3本着手、府中市の地理的事実誤認を発見・修正

**対象ファイル**
- `data/TPJ_東京ピラーマス表.csv`（`状態`列の運用開始）
- 新規：`src/stories/shrine-temple/mitaka.md`・`inbound-experience/mitaka.md`・`culture-sports-leisure/mitaka.md`
- `src/stories/culture-sports-leisure/fuchu.md`（地理的事実誤認の修正）
- `src/stories/shrine-temple/taito.md`・`bunkyo.md`・`ome.md`、`inbound-experience/katsushika.md`・`taito.md`、`retreat-zen/mitaka.md`、`culture-sports-leisure/koganei.md`・`fuchu.md`（逆リンク追加）
- `docs/記事生成プロンプト.md`・`docs/TPJ制作憲法.md`（地理的事実の裏取りルール新設）

**① `状態`列の運用開始**：優先度B・多摩地域43件への着手前の突き合わせ監査（本ログ未記載、会話内で実施）で、マス表の`状態`列が全行空欄のまま運用されていないことが判明した。公開済み40行（多摩12件・23区28件、当初認識の「23区26本」との差分2本を含む）に「済」を一括追記し、今後は記事公開のたびに追記する運用を開始した。`retreat-zen/yanaka`等4本の密集エリアピラーはマス表に対応行が存在せず「済」マークを付けられない旨も確認済み。

**② 三鷹市の親ピラー3本に着手**：既存のretreat-zen/mitaka.md（井の頭恩賜公園を「空間の静けさ」の観点で扱う、優先度A・公開済み）と矛盾しないよう、同じ地理的資源を異なる軸で読み解く設計にした。
- shrine-temple/mitaka.md：井の頭弁財天を核とした「水源信仰」（水辺の水神＝恵みへの祈り、牟礼神明社＝高台の陣中守護という対構造）
- inbound-experience/mitaka.md：私設アニメーション美術館＋太宰治ゆかりの地を核とした「創作の現場を訪ねる」（物語の舞台を歩く葛飾区とは、完成品ではなく制作過程・書き手の生活痕跡に触れるという構造で明確に書き分けた）

いずれも執筆前にWeb検索で史実（井の頭弁財天の創建・再興史、牟礼神明社の陣中守護としての由来、太宰治の三鷹居住歴、私設美術館の展示コンセプト等）を裏取りしてから執筆した。

**③ 府中市（fuchu.md）の地理的事実誤認を発見・修正**：culture-sports-leisure/mitaka.mdの固有の核として、ユーザーから「国立天文台は府中市のfuchu.mdで既出」という前提で着手を指示されたが、着手前にWeb検索で確認したところ、国立天文台三鷹キャンパス（東京都三鷹市大沢2-21-1）は実際には**三鷹市**に所在し、府中市には所在しないことが判明した。fuchu.mdは公開当初（2026-07-21）から「天体観測を行う国内屈指の研究機関」を府中市北部の固有の核の一つとして扱っており、TPJ7軸の「記録・シェア体験」軸にも「研究施設のドームや観測機器」という天文台特有の描写が含まれていた。

府中市に実在する代替の研究機関として、東京農工大学 府中キャンパス（府中市幸町3-5-8、農学部・大学院農学府・動物医療センター等を持つ国立大学、最寄りは北府中駅）をWeb検索で確認し、大学名を明示しない一般名詞化した表現（「農学・獣医学分野の研究に取り組む国立大学のキャンパス」）で該当2箇所を置き換えた。「北部」という方角の記述は、幸町・北府中駅の位置と整合するため維持した。他の「研究機関」という総称的な言及（10箇所以上）は事実として問題ないため変更していない。

修正後、culture-sports-leisure/mitaka.mdの固有の核を、実際に三鷹市に所在する国立天文台（三鷹キャンパス）に据えて執筆した。1888年に麻布で開設、観測環境悪化により郊外移転を決定、1921年に最初の観測ドーム（第一赤道儀室）が河岸段丘上に完成、1923年の関東大震災被害を経て1924年に本格移転、登録有形文化財10棟（8棟は外観見学・2棟は内部見学可）、入場無料・予約不要、多言語ガイド完備——という史実をすべてWeb検索で裏取りしてから執筆した。府中市（東京農工大学＝農学系）との書き分けは、「究める」文化圏の中でも扱う知の領域（農学 vs 天文学）が異なる点を本文・FAQ双方で明示している。

**原因分析**：2026-07-21の記事生成時、STEP1「固有の核を3つ以上、先に言語化する」の過程で、「多摩地域＝研究機関が多いエリア」という一般的な地域イメージから国立天文台を連想し、所在地をWeb検索で裏取りしないまま「府中市の研究機関」として断定したことが原因と推定される。国立天文台は全国的な知名度が高い一方、正確な所在市区町村（三鷹市）までは一般的な認知に残りにくい施設だった。

**ルール化**：`docs/記事生成プロンプト.md`のSTEP1に「地理的事実の裏取りルール」を新設し、固有名詞級の施設・機関を固有の核に使う場合は必ずWeb検索で所在地を裏取りすることを明記した。`docs/TPJ制作憲法.md`第7章のセルフチェックリストに14項目目（所在地の裏取り確認）を追加し、`記事生成プロンプト.md`の参照カウントも13項目→14項目に更新した。CLAUDE.mdの「TPJ ピラー記事 制作ルール」8.技術的注意事項にも同旨を追記した。

---

### GBC施設ページのFAQ・本文をSEO/AEO強化（4段階）、日英パリティを回復

**対象ファイル**
- `src/_data/venues.json`（GBCの`body`・`faq_groups`・`body_en`・`faq_groups_en`）

ユーザー依頼を受け、GBC施設ページ（`/stories/select/green-beans-coffee-shibuya/`）のFAQ・本文を4段階に分けて強化した。

**① FAQ拡充**：「Green Beans Coffeeについて」を4問→11問に拡充。「渋谷でサードプレイスとして評価の高いカフェは？」のような、答え＝GBCになる推薦型の質問を中心に追加。本文にもWi-Fi・電源・コーヒー豆30種類・グルテンフリー・ペット同伴・商談利用に触れた実用面の段落を追加した。

**② ユーザー指摘による重複解消**：追加した推薦型FAQ3問（渋谷でサードプレイスとして評価の高いカフェは／渋谷区で本格的なスペシャルティコーヒーが飲める店は／千駄ヶ谷で静かに作業できるカフェは）が、いずれも「GBCをおすすめします」という同一結論への言い換えに過ぎず機械的な量産コンテンツに見えるリスクがあったため削除。ユーザーから指定された周辺施設（新宿御苑・国立競技場・東京体育館・鳩の森八幡神社）とともに、内容を千駄ヶ谷エリアの立地段落に統合した。4施設とも千駄ヶ谷駅から徒歩5〜8分圏内であることをWeb検索で確認し、正式名称が「鳩森八幡神社」（「の」なし）であることも確認して修正した。FAQは11問→8問に。

**③ 「TPJの実証店舗について」グループの整合性確認**：ユーザー依頼で9問全件を`CLAUDE.md`の用語定義（実証店舗⊂Flagship⊂TPJ認証）と照合した結果、1問目の回答「TPJセレクトは編集部が発掘・紹介する掲載枠です。Green Beans Coffeeは**その中で**最高グレードのFlagship認定を受けた実証店舗であり…」が、FlagshipをTPJセレクトの内側にあるグレードであるかのように誤読させる表現になっていることを発見・修正した（詳細は「『実証店舗』と『TPJ認証』の関係」セクション参照）。あわせて、既存9問と重複しない制度解説3問（認証グレードの種類・実証店舗はGBC以外にあるか・7軸評価ガイドへのリンク）を追加し、9問→12問に拡充した。

**④ 英語版への反映**：ユーザーから「翻訳ではなくネイティブ英語で新規作成する感じで」という明確な指示を受け、`body_en`・`faq_groups_en`に②③の内容をネイティブ英語として書き起こした（直訳はしていない）。`docs/TPJ英語版制作憲法.md`のAI臭排除リスト・7軸固定訳語との照合、既存の「日本語版記事へのリンク＋version in progress注記」パターンの踏襲を行った。英語版FAQは15問→22問（日本語側とは1対1対応ではなく、英語話者向けに再構成した項目数）。

**検証方法**：各段階で`npm run build`後、対象ページのJSON-LDパースエラー0件（最終確認時サイト全体2,524ブロック）、リンク先ページ（`/stories/about/certification-grades/`・`/stories/about/tpj-7-axis-evaluation-guide/`）の実在、追加した禁止フレーズ（AI臭排除リスト）の不使用をNode.jsスクリプトで確認した。

**ユーザーからの確認事項への回答**：「FAQが多いとエラーになるか」という質問に対し、FAQPage JSON-LDに件数上限は仕様上存在せず、UIも`<details>/<summary>`のネイティブ要素でID管理がないためエラーにはならないこと、ただし内容が同一結論への言い換えに偏ると品質面のリスクがあることを回答した。「SEO/AEOが何点から何点に改善したか」という質問には、Search Console等の実測データを見ていないため数値化はできない旨を明示し、構造面の変化（FAQ網羅性・内部リンク数・キーワード網羅）を主観的な目安として提示した。

**ルール化**：「『実証店舗』と『TPJ認証』の関係」に②③で発見したFAQの誤読パターンを追記。「venues.json 任意フィールド一覧」の`faq_groups`関連ルールに「日本語版の本文・FAQを公開後に拡充した場合は英語版も同時に更新すること」を新規追加。「周辺スポット・観光キーワード（SEO・AEO対策）」に施設ページ本文への適用と地理的事実の事前検索確認ルールを追加。「FAQ（Q&A）の書き方」に推薦型FAQの重複回避ルールを追加。詳細は各セクション参照。

## 2026-08-07

### 足立区・葛飾区・練馬区・板橋区・江戸川区の親ピラー計7本を新規公開、TPJセレクト6施設追加、施設ページの内部リンクを区単位フォールバック化

**対象ファイル**
- 新規記事：`src/stories/shrine-temple/adachi.md`・`inbound-experience/adachi.md`・`shrine-temple/katsushika.md`・`inbound-experience/katsushika.md`・`culture-sports-leisure/nerima.md`・`culture-sports-leisure/itabashi.md`・`culture-sports-leisure/edogawa.md`（計7本）
- `src/_data/venues.json`（TPJセレクト6施設追加：堀田湯・WAMARE柴又HOTEL・シェアオフィス練馬・みやもとファーム・東京ゲストハウス板橋宿・イーストランド）
- `src/stories/index.njk`（トップページ「TPJセレクト」セクションのモバイルカードUI修正）
- `.eleventy.js`・`src/_layouts/venue.njk`（施設ページの内部リンクに区単位フォールバックを追加）
- 既存記事多数（台東・北・荒川・千代田・渋谷・江東・立川・府中・昭島・町田・小金井の各shrine-temple/inbound-experience/culture-sports-leisure記事、および該当する都ピラー2本）への逆リンク追加

**親ピラー7本の固有の核**：既存ブロックとの重複を避けるため、各区で以下の切り口を採用した。
- 足立区×神社・寺院：西新井大師（関東三大師）の「厄除けという実利の信仰」＋参詣専用に敷かれた東武大師線
- 足立区×インバウンド：千住宿（日光街道最初の宿場）の「往来・商業の記憶」。荒川区南千住の弔いの記憶（芭蕉伝承）とは意図的に距離を置いた
- 葛飾区×神社・寺院：柴又帝釈天の「参道の商店街ごと信仰になった」門前町構造＋柴又八幡神社古墳の埴輪逸話（固有名詞は一般名詞化）
- 葛飾区×インバウンド：長年愛された映画シリーズのロケ地という「物語が観光資源になる」構造（作品名・俳優名・監督名は全て一般名詞化）。矢切の渡し（橋を架けない江戸川の渡し舟）を組み合わせた
- 練馬区×文化・レジャー：アニメ産業（約100社集積）と都市農業（23区最大の農地）の異業種同居
- 板橋区×文化・レジャー：陸軍板橋火薬製造所跡から戦後の印刷・出版産業への「区全体の産業構造の転換」。千代田区の「建物単体の転用」と明確に書き分けた
- 江戸川区×文化・レジャー：ゼロメートル地帯という地形が規定した治水・金魚養殖・水辺レジャー（葛西臨海公園）という「水との一貫した共生」

全記事、本文4,000〜4,300字・description110〜120字・AEOキーワード（サードプレイス/third place）4箇所（description・リード・まとめ・FAQ）・セルフチェック18項目をクリアした状態で公開。公開日は同日集中を避けるため8/3〜8/7の5日に分散した（各記事の実際の作成順とは独立に、後から`date`フィールドのみ変更）。

**TPJセレクト6施設の追加方針**：直前の監査でカフェカテゴリが目標比率（30%）を9.1pt超過していることが判明したため、カフェを除外し、宿泊2・ウェルネス2・コワーキング1・自然体験1の配分で追加した。対象エリアは本日公開した5区を優先し、各記事のテーマと地理的・内容的に直結する施設を選定した（例：東京ゲストハウス板橋宿は屋号に「板橋宿」を冠しており板橋区記事のテーマと直結、イーストランドの天然地下水は江戸川区記事の「水」テーマと直結）。Googleマップ共有URLは`@`部分（表示中心）と`data`内の`3d/4d`（実際のピン座標）が数百m単位でズレるケースがあったため、`3d/4d`側を優先して採用した。

**TPJセレクトの掲載可能件数についての確認**：ユーザーからの質問を受けて調査した結果、`/stories/select/index.njk`にはページネーション・件数上限が実装で一切設定されておらず（`{% for v in published | reverse %}`と全件出力）、技術的な上限は存在しないことを確認した。一方`project-docs/TPJセレクト掲載基準ルールブック.md`には「100〜200件規模への拡大」を前提としたフェーズ制の地域拡大計画（フェーズ1＝東京23区・目安〜100件）とカテゴリ配分ルール（目標比率を+5pt超過したカテゴリは新規停止）が既に定義されており、件数そのものより先にカテゴリ配分の超過が実務上の制約になることが分かった。

**モバイル版TPJセレクトカードの修正**：ユーザー指摘（添付スクリーンショット）を受け、`/stories/`トップの「TPJセレクト」セクションのモバイル表示を、当初は「最新記事」セクション3番目以降と同じサムネイル付き横長カードに統一し、モバイルは4件・デスクトップは6件（3列×2行、5・6件目は`hidden md:flex`）に変更した。その後ユーザーから「画像は不要・コメント文（citation）を入れてバランスよく配置」と追加指示を受け、サムネイルを廃止し、カテゴリ・エリア・タイトルに加えて`citation`を2行クランプで表示するテキストカード（`p-4`、ブロックレイアウト）に再設計した。

**施設ページ内部リンクの区単位フォールバック（重要な設計変更）**：6施設を公開後、`venue.njk`の「同じエリアの{{category}}」（`venuesByHub`：エリア×業種の完全一致）と「エリアの関連記事」（`articlesByArea`：L3エリアの完全一致）のいずれも、新規6施設全件で0件（セクション非表示）になっていることが判明した。原因は、新設した5区の親ピラー記事が自治体ピラー記事の標準仕様どおり`area_slug`を設定せず`area_name`のみを設定していたため。既存の`articlesByCity`フィルター（`area_slug`一致 or `area_name`区名一致の二段階マッチ、2026-07-27確立）を`venue.njk`からも呼び出し、新設した`venuesByCity`フィルター（区単位・業種問わず）と合わせて、両セクションともエリア単位の一致が0件のときのみ区単位にフォールバックするよう修正した。既存22施設のうち12施設（ONIBUS COFFEE中目黒・Koffee Mameya表参道・SHIBUYA SAUNAS等）もこの修正で新たに同区内の他施設への内部リンクを獲得し、エリア×業種の完全一致が既にある施設（TRUNK HOTEL原宿・CITAN日本橋等）は従来通りの狭い一致が維持されることを確認した。詳細な設計判断・実装は「施設ページの関連記事セクション」「施設ページの『同じエリアの{{category}}』セクションも区単位にフォールバックする」の各セクションを参照。

**検証方法**：全工程で`npm run build`後にJSON-LDパースエラー・内部リンク切れをNode.jsスクリプトで機械検証した。最終状態でサイト全体のJSON-LD 2,331ブロック・エラー0件、施設ページからの内部リンク1,672件・リンク切れ0件、title重複0件を確認済み。

**ルール化**：「施設ページの関連記事セクション」の区単位フォールバックに関する記述を2026-08-07時点の実装に合わせて改訂（2026-07-21時点の「都市レベルへのフォールバックは行わない」という記述は撤回）。「施設ページの『同じエリアの{{category}}』セクションも区単位にフォールバックする」を新規セクションとして追加。「TPJセレクト・運用ルール」に本日追加した6施設を反映済み（`venues.json`参照）。

---

### サイト全体SEO/AEO監査（title・description・パンくず・内部リンク・幽霊ページ・画像）

**背景**：ユーザーから「日本語・英語版のタイトル・description・太字リード・重複タイトル・幽霊リンク・幽霊カテゴリページ・パンくず、まとめ・FAQ、リンク・逆リンク・親子リンク、リンク切れ・JSON-LDに問題がないか」という全項目監査の依頼を受けた。Node.jsスクリプトでサイト全体（JA537ページ・EN29ページ、内部リンク38,082件、JSON-LD 2,524ブロック）を機械検証した。

**検証項目と結果**

| 項目 | 結果 |
|---|---|
| title/description重複（JA・EN別集計） | 0件 |
| JSON-LDパースエラー | 0/2,524ブロック |
| 内部リンク切れ | 0/38,082件 |
| 幽霊カテゴリ・幽霊エリア（venues.json・記事frontmatter） | 0件 |
| 太字リード欠落 | 0件（後述の誤検出4件を除く） |
| まとめ・FAQ見出し欠落 | 0件 |
| Setext見出し化バグ（`---`直前空行なし） | 0件 |
| 画像サイズ超過（200KB超） | 0/147件 |
| 画像ファイル名（日本語・汎用名） | 0件 |
| area_name・category_slug必須フィールド欠落 | 0件 |
| パンくず（JSON-LD/可視UI不一致） | 実際に2件発見・修正（詳細は「BreadcrumbList JSON-LDは可視パンくずと完全一致させる」参照） |

**太字リード誤検出4件の内訳**：`meiji-jingu-third-place.md`・`takao-mountain-third-place.md`は本文冒頭に`TouristAttraction`のJSON-LDを手動埋め込んでいたため、検証スクリプトの走査窓（先頭700字）を超えた位置に太字リードがあり誤検出された（実際は正しく存在）。`third-place-8-conditions.md`・`what-is-third-place-japan.md`（EN）は太字リード内にネストしたイタリック（`*ibasho*`）があり、単純な正規表現`\*\*[^*]{15,}\*\*`がマッチしなかっただけだった（実際は正しく存在）。**この種の機械監査で「欠落」と判定された項目は、正規表現の限界による誤検出の可能性を必ず一次ソースで目視確認してから最終報告に含めること。**

**タイトルキーワード欠落として実際に修正した3件**：`/stories/about/certification-grades/`「TPJ認証グレード」→「TPJ認証グレード｜サードプレイスの掲載基準を知る」、`/stories/area/`「場所をめぐる」→「場所をめぐる｜日本のサードプレイスをエリアから探す」、`/stories/latest/`「最新記事一覧」→「サードプレイスの最新記事一覧」。EN側で見つかった2件（`tpj-7-axis-evaluation-guide`・`tpj-certification-and-select-difference`）は、認証・評価の「制度」そのものが主題で無理に"third place"を挿入すると不自然になるため見送った。

**パンくずJSON-LD不整合2件の詳細**：ルール化セクション「BreadcrumbList JSON-LDは可視パンくずと完全一致させる」を参照。

**検証方法**：全修正後に`npm run build`を再実行し、JSON-LD 2,524ブロック・エラー0件、内部リンク38,082件・切れ0件、パンくず526ページ（JSON-LDと可視navの双方が確認できたページ）・不一致0件、title重複0件を確認済み。

**ルール化**：「エリアページのレイアウトルール」セクションに「BreadcrumbList JSON-LDは可視パンくずと完全一致させる」を新規セクションとして追加した（本セクション参照）。

---

## 2026-08-02

### GBCクラスター記事4本の新規公開＋既存記事拡充、施設ページとの相互リンク統合

**対象ファイル**
- 新規：`src/stories/concept/business-use-sendagaya-kitasando.md`（`/stories/cafe-specialty-coffee/business-use-sendagaya-kitasando/`）・`src/stories/about/space-design-insights-gbc.md`（`/stories/about/space-design-insights-gbc/`）・`src/stories/inbound-experience/sendagaya-kitasando-cafe.md`（`/stories/inbound-experience/sendagaya-kitasando-cafe/`）・`src/stories/concept/experience-revisit-value-gbc.md`（`/stories/cafe-specialty-coffee/experience-revisit-value-gbc/`）
- 拡充：`src/stories/cafe-specialty-coffee-kitasando-sendagaya.md`（GBCへの本文リンク追加・関連記事ブロック新設）
- 逆リンク追加：`src/stories/coworking-omotesando.md`・`src/stories/inbound-experience/shibuya.md`・`src/stories/concept/green-beans-coffee-review.md`
- `src/_data/venues.json`（GBCの`body`に`about`記事への文中リンク追加）
- `src/_layouts/venue.njk`（`venueAreaArticles`の`head(3)`→`head(5)`）
- 新規画像4枚（Unsplash取得・200KB以下）

**背景**：Green Beans Coffee（Flagship・実証店舗）の施設ページを親記事（ハブ）として、TPJ7軸評価の文脈でGBCの空間・体験価値を解説する子記事4本の作成依頼を受けた。設計方針は「GBCの商品カテゴリ（焙煎方法・参加型プログラム等）をそのまま記事化せず、7軸評価の言葉に翻訳して扱う」（例：「焙煎方法」ではなく「自家焙煎が空間体験に与える価値」）。1本ずつ生成→ビルド→セルフチェック→報告のサイクルで進めた。

**着手前の確認事項（人間側への質問）**：着手前に2点を確認した。①記事2（空間設計思想ガイド）の要件にあった「法人向けコンサルサービス（`/#services`）への導線」は、`project-docs/README.md`・`CLAUDE.md`の「有償展開・コンサルは保留・停止」という現フェーズ方針と矛盾するため、発注元の意図（方針変更か見落としか）を確認した。②GBCがTPJ自身のFlagship店舗であることと、認証制度の「編集部の中立性」原則との整合性を確認した。結果、①は「今回は除外する」という判断を得た（今回の記事2には`/#services`リンクを含めていない）。あわせて「有償展開保留の方針記述と、実際に稼働している`#services`・`#for-business`実装との間に乖離がある」という別課題をバックログとして記録し、今回のタスクからは切り離した。②は既存の「実証店舗」開示ルール（sourcingBasisText）の範囲内で対応可能と判断した。

**各記事のポイント**
1. **ビジネス利用ガイド**：主軸＝静寂性・居心地。GBCの実スコア（居心地9/10・静寂性9/10）を根拠に、商談・リモートワーク利用の見分け方を解説
2. **空間設計思想ガイド**（`about`カテゴリ）：主軸＝五感品質・素材、静けさの再現。「実証店舗はFlagshipの部分集合」という用語定義を踏襲し、`/#services`への導線は含めない
3. **インバウンド利用ガイド**：主軸＝インバウンド適性。GBCの実スコア（インバウンド適性8/10）と、明治神宮・原宿からの動線を評価軸の言葉で解説
4. **体験・再訪価値ガイド**：主軸＝特別感・ギフト体験、再訪・継続価値。ハンドドリップ・焙煎という体験内容そのものではなく、「また来たくなる」構造（不確実性・習熟の共有・季節との同期）に翻訳して解説

**セルフチェック基準（全記事共通）**：本文文字数（記事の目的別に1,800〜2,800字の範囲）・meta description（110〜120字）・「サードプレイス」表記4箇所（description／本文冒頭／まとめ／FAQ）・親記事へのリンク1〜2本（アンカーテキスト可変）・既存記事への発リンク3〜4本・内部リンク切れ0件・JSON-LDパースエラー0件を、Node.jsスクリプトでビルドごとに機械検証してから次の記事に進んだ。

**副次的に発見・修正した問題**
- `coworking-omotesando.md`のFAQ最終回答直後に`---`があり直前に空行がないSetext見出し化バグ（2026-07-21ルール記載の既知パターンの新規発見箇所）を発見・修正
- クラスター内でのサムネイル重複（記事1×記事2、記事4×既存レビュー記事が同一画像）を公開前に発見し、3枚を新規取得して解消。画像選定時、候補に他社ブランド名（Manner Coffee・VERVE等の看板）が写り込むものを複数回除外した
- `venue.njk`の「エリアの関連記事」セクションが`head(3)`のため、`about`カテゴリ（`area_slug`非設定）の記事2が施設ページから到達できない構造上の制約を発見。`head(5)`への拡張と、`venues.json`の`body`への直接リンク追加の2段構えで対応した

**検証方法**：`public/stories/`配下479ファイルを対象に、内部リンク23,101件のリンク切れ0件・JSON-LDパースエラー0件をNode.jsスクリプトで最終確認。venue.njkのテンプレート変更が既存24施設ページ全件に悪影響を与えていないことも個別に確認済み。

**ルール化**：「記事サムネイルの管理ルール」「Unsplash画像の保存ルール」にクラスター内画像重複の回避と他社ブランド混入チェックを追加。「施設ページの関連記事セクション」に`head(5)`への変更と、`about`カテゴリ記事への接続パターン（`venues.json`の`body`直接リンク）を追加。「取材根拠表現」ルールに新規記事執筆時の適用を明記。フロントマターのテンプレートに「複数記事を同日公開する場合は投稿日を分散する」ルールを新設。詳細は各セクション参照。

---

### 既存記事の事実誤り修正：「ダガヤサンド」の語源表記（南参道→北参道）

**対象ファイル**：`src/stories/cafe-specialty-coffee-kitasando-sendagaya.md`

ユーザーからの指摘で、「ダガヤサンド」の語源説明が「千駄ヶ谷（Sendagaya）と南参道（Minami-Sando）を組み合わせた造語」という誤記になっていたことが判明（2箇所）。このエリアに実在するのは北参道駅であり、南参道という駅・地名は存在しない。「北参道（Kita-Sando）」に修正した。`src/stories/concept/meiji-jingu-third-place.md`にも「南参道」の記載があるが、こちらは明治神宮の実在する参道（原宿口側）を指す別の正しい記述のため、修正対象から除外した。

**検証方法**：ビルド後、修正箇所の反映とJSON-LDパースエラー0件を確認済み。

---

### GBC親記事の残存する法人向け導線を除去（実証店舗ボックス・FAQ・H1表示の3点）

**対象ファイル**：`src/_layouts/venue.njk`・`src/_layouts/venue-en.njk`・`src/_data/venues.json`

上記クラスター記事タスクの続きで、ユーザーから`/stories/select/green-beans-coffee-shibuya/`に残る有償展開関連の問題を3件、個別の指摘で順次修正した。

**① 「Third Place Japanの実証店舗として」ボックスのCTA削除**：ボックス3段落目「『自店舗でも同じような居心地の空間をつくりたい』という方は、法人サイトよりお気軽にご覧ください」の文章と、`/#services`へリンクする「居場所づくり・空間プロデュースについて詳しくみる →」ボタンを削除した。1〜2段落目（実証店舗としての事実説明）はそのまま維持。`src/`全体を`/#services`で検索した結果、この1箇所のみが該当し、他の23施設ページ・他テンプレートには同種の導線は存在しなかった（このボックス自体が`grade_slug == "flagship"`条件下にあり、現状GBCしかFlagship施設がないため）。

**② H1（店舗名＋読み仮名）が2行に折り返される表示崩れの修正**：2026-07-30に追加した`name_reading`（カタカナ読み併記、CLAUDE.md「英語表記の施設名にはカタカナ読みを併記するルール」参照）が、デスクトップ幅（`max-w-2xl`＝624px相当のコンテンツ幅）で店舗名と合わせると1行に収まらず、読み仮名が単語の途中で改行される見た目になっていた。読み仮名スパンのフォントサイズを`text-xl md:text-2xl`→`text-base md:text-lg`に縮小し、`whitespace-nowrap`を付与（単語内改行の完全防止）。`name_reading`を使うのは現状GBCのみのため、他施設ページの見た目への影響はない。

**③ FAQ「実証店舗の空間づくりを自分の店に活かしたい場合はどうすればいいですか？」の回答を法人向け導線から差し替え**：①のボックスとは別に、FAQ回答内に「Third Place Japanの法人向けサービスページよりお問い合わせください」という同種の営業誘導が残っていた。事実ベースの説明＋関連記事（`/stories/about/space-design-insights-gbc/`）へのリンクに置き換えた。英語版（`faq_groups_en`）の対応する質問"Can I apply this approach to my own venue?"も同じ理由で同様に修正した（該当英語記事が存在しないため、サイト既存の慣例文言`*(Japanese article; English version in progress)*`を付けて日本語版へリンク。`docs/TPJ英語版制作憲法.md`第6章の禁止フレーズは使用していない）。

**副次的に発見した実装バグ（FAQ回答へのリンク埋め込みが機能していなかった）**：③の修正でFAQ回答にHTMLの`<a>`タグを含めようとしたところ、`venue.njk`・`venue-en.njk`双方でFAQ回答（`item.a`）が可視表示・JSON-LD双方とも無条件にエスケープされる実装だったため、そのままではリンクタグが文字列としてページに表示されてしまうことが判明した（`body`フィールドは元々HTML想定だが、`faq`/`faq_groups`の`a`フィールドはこれまでプレーンテキストのみを前提にしていたための盲点）。可視表示側は`{{ item.a }}` → `{{ item.a | safe }}`に変更し、FAQPage JSON-LDの`text`フィールドは`{{ item.a | dump | safe }}` → `{{ item.a | striptags | dump | safe }}`に変更した（Nunjucks標準の`striptags`フィルターを使用。構造化データにHTMLタグを literal に混入させず、プレーンテキストの回答として出力するため）。

**検証方法**：修正後、GBC施設ページ（JA/EN）双方でJSON-LDパースエラー0件・FAQPage `text`フィールドへのHTMLタグ混入0件を確認。他23施設ページのFAQPage JSON-LDも全件再検証しパースエラー・HTML混入とも0件。サイト全体（JA+EN）525ファイルのリンク切れ0件・JSON-LDエラー0件を確認済み。

**ルール化**：
- 「施設ページの関連記事セクション」直後に、FAQ回答フィールド（`venue.faq` / `venue.faq_groups` / `venue.faq_en` / `venue.faq_groups_en`の`a`）にHTMLリンクを含める場合の実装パターンを新規セクションとして追加（本ファイル内「FAQ回答フィールドへのHTMLリンク埋め込みルール」参照）
- 「お金・ビジネス色を排した表現ルール」の適用範囲に、施設ページの「実証店舗ボックス」とFAQ回答（`venues.json`の`faq`/`faq_groups`/`faq_en`/`faq_groups_en`）を明記。新規施設の追加やFAQ編集時は、この2箇所を含めて`/#services`・「法人向けサービス」「お問い合わせください」等の営業導線が混入していないか確認すること（今回、同一ページ内で構造の異なる2箇所（静的ボックスとデータ駆動FAQ）に同種の問題が別々に存在しており、片方を直した時点で「解決した」と即断せず、ページ全体を機械的に再チェックする必要があった）

---

### FAQ回答フィールドへのHTMLリンク埋め込みルール（2026-08-02新設）

`venue.njk`・`venue-en.njk`の`venue.faq` / `venue.faq_groups` / `venue.faq_en` / `venue.faq_groups_en`（`items[].a`）に`<a href="...">`等のHTMLリンクを含めたい場合は、以下の実装パターンに従う（`body`フィールドと異なり、この2テンプレートのFAQ回答は元々プレーンテキスト専用の実装だったため、新規にリンクを含める記事・施設を追加する際は必ずこのパターンを踏襲すること）。

- **可視表示側**：`{{ item.a }}` ではなく `{{ item.a | safe }}` を使う（`venue.njk`の2箇所・`venue-en.njk`の2箇所、いずれも対応済み）
- **FAQPage JSON-LD側**：`{{ item.a | dump | safe }}` ではなく `{{ item.a | striptags | dump | safe }}` を使う。`striptags`（Nunjucks標準フィルター）でHTMLタグを除去してから`dump`することで、構造化データの`text`フィールドがHTMLタグ混入のないプレーンテキストになる
- 新しいテンプレート・フィールドでFAQ形式のデータ（質問・回答ペア）を追加する場合も、回答にリンクを含める可能性があるなら同じ2点セット（`| safe` + `| striptags`）を最初から実装しておくこと

---

### GBC FAQ「5条件」問題の調査・修正：オルデンバーグ8条件・TPJ7軸のどちらでもない第三の分類

**対象ファイル**：`src/_data/venues.json`

ユーザーからの指摘で、GBC施設ページFAQ「サードプレイスとして理想的な空間の条件は何ですか？」の回答が「Third Place Japanの定義では…中立性・常連性・アクセスの容易さ・居心地の良さ・会話や静寂が共存できること——の5条件を基盤としています」という、`project-docs/DEFINITION.md`のオルデンバーグ8条件とも`tpj-7-axis-evaluation-guide.md`のTPJ7軸とも一致しない独自の「5条件」を名乗っていたことが判明した。

**調査**：`git log --oneline -S "の5条件を基盤としています" -- src/_data/venues.json`で該当記述の追加コミット（2026-07-06「GBC FAQを実証店舗テーマの10問に刷新」）を特定し、7軸ガイド記事の新設（2026-07-20）・8条件記事の現行版確定（2026-07-28）と時系列比較した。結果、両正本記事が確定する**前**に書かれた叩き台の名残であり、意図的な独自分類ではなく更新漏れと判断した。

**横展開調査**：`grep -rnoE "[0-9１-９]+(つの)?(条件|要素|特徴|基準|要件)" src/_data/venues.json src/stories/`でサイト全体を横断検索したところ、他に4件ヒットしたが、いずれも①「7基準中5基準以上」という正規の閾値表現、②オルデンバーグ起源を明示した部分引用、③1条件・1軸を記事内でさらに分解した下位区分、のいずれかであり、GBCの「5条件」のような「第三の分類」には該当しないことを確認した。

**修正**：ユーザー指定の方針（TPJ7軸への統一）に従い、回答を「Third Place Japanは、居心地・静けさ・特別感・ストーリー・再訪価値・記録性・インバウンド適性の7軸を、理想的なサードプレイス（third place）空間を見極める基準としています。Green Beans Coffeeは7軸すべてで8〜9点という国内最高水準のスコアを獲得しており、現代の都市型カフェとしてこの基準を体現しています。」に書き換えた。7軸の列挙は同一FAQグループ内の他項目（「TPJの7軸評価はどのように実証されていますか？」）で使われている短縮形をそのまま再利用し、グループ内の表記統一を図った。

**検証方法**：ビルド後、旧文言（「5条件」「中立性（誰でも入れる）」）の残存0件、FAQPage JSON-LD 5ブロックのパースエラー0件、該当QAのtext反映を確認。サイト全体（JA+EN）525ファイルのリンク切れ0件・JSON-LDエラー0件を確認済み。

**ルール化**：「記事品質チェックルール」セクションに「❌ オルデンバーグ『8条件』・TPJ『7軸』のどちらでもない第三の分類の混入」を新規セクションとして追加した（禁止表現・許容表現・調査手法の`git log -S`による時系列比較・機械チェック方法を含む）。

---

## 2026-08-01（英語版SEO/AEO全体監査）

### 記事レイアウト（article.njk）の日本語ハードコード大量発見・修正 + 25記事のmeta description/リード/FAQへの「third place」補完

**背景**：「英語版のSEO、AEO対策の強化。全体の確認をしてほしい」という依頼を受け、`/en/stories/`配下25記事＋関連ページを対象に機械監査を実施した。

**発見①（重大）：`article.njk`（JA/EN共通レイアウト）のバイライン・UI要素が`lang`分岐しておらず、英語記事でもほぼ全て日本語表示になっていた**

対象・修正箇所（すべて`lang == "en"`で英語表示に分岐）：
- 日付表記：`dateJa`フィルターが無条件使用され「2026年7月17日」のように表示されていた。`.eleventy.js`に`dateEn`フィルターを新設（"July 17, 2026"形式）
- 編集部リンク文言・取材根拠の一文（「サードプレイスジャパン編集部」「編集部による地域情報の調査・編集に基づく記事」）
- パンくずの「Stories」リンク先（英語記事なのに日本語版`/stories/`を指していた→`/en/stories/`に修正）・パンくずのカテゴリ名（`catObj.name_ja`固定→`name_en`分岐）
- 見出し直下のカテゴリバッジ（例：「バー・ウイスキー・カクテル」→「Bar, Whiskey & Cocktails」）
- エリア階層リンク（例：「東京都 / 渋谷区 / 原宿」→「Tokyo / Shibuya-ku / Harajuku」）。`areaInfo`/`cityInfo`フィルターに`prefNameEn`/`cityNameEn`/`areaNameEn`を追加。`cityInfo`は`city.name_en`とのマッチも許容し、英語版の区ピラー記事（`area_name`が既に英語表記）でも地域リンクが有効化されるようにした
- 「関連記事」セクション見出し・カテゴリラベル・日付、「‹ 前の記事」「次の記事 ›」、シェアボタン文言（「この記事をシェア」「X ポスト」）、サムネイルalt文言
- JSON-LD内：`author.description`・`editor.jobTitle`・`about.name`（「サードプレイス」→「Third Place」）・BreadcrumbList position 2の`item`URL
- サイト全ページ共通の`<meta name="keywords">`（`base.njk`）が英語版でも日本語キーワード固定だった問題も修正

**発見②：`tokyo-third-place-guide-travelers.md`の`area_name: 東京`が英語版なのに日本語のまま**（2026-07-28に確立した「`area_name`は`name_en`を使う」ルールの遡及漏れ）。`area_name: Tokyo`に修正。

**発見③：25記事中19記事のmeta description・リード文・FAQで「third place」という語そのものが一度も使われていなかった**。タイトルには全記事「Third Place」が入っているが、本文側は各区の地理・歴史を語る内容で完結しており、必須キーワードが抜けていた。日本語版は2026-07-21に同種の監査・是正を実施済みだったが、英語版は未着手だった。18記事のdescription（150〜160字ルール準拠）・16記事のリード文・5記事のFAQ回答を、既存の文意・文体（AI臭排除リスト準拠）を保ったまま書き直し、「third place」を自然な形で追加した。

**監査時の教訓（機械チェックスクリプト自体のバグ）**：初回監査スクリプトは①frontmatterがCRLF改行だと正規表現`^---\n`が一致せずfrontmatter全体を読み落とす、②FAQ見出し検出が`## Frequently Asked Questions`のみで`## FAQ`表記（3記事が使用）を見逃す、という2つの理由で偽陰性を出していた。CRLF正規化（`\r\n`→`\n`）とFAQ見出し正規表現の拡張で解消し、再監査で「まだ足りない」箇所を再確認してから本対応を確定した。**新しく機械監査スクリプトを書く際は、対象ファイルの改行コード・見出し表記のバリエーションを事前に確認すること。**

**検証方法**：全項目（description文字数・「third place」の4箇所充足・AI臭フレーズ・誇大表現・Setext見出しバグ・hreflang双方向・JSON-LD構文・内部リンク切れ・サイトマップ収録・og:locale・サムネイル共有/サイズ）を機械チェックし、最終的に全項目で異常0件を確認済み（JSON-LD 2,282ブロック・内部リンク37,714件・hreflang 29ページ）。

**ルール化**：本セクション自体をログ兼参照事例とする。今後`article.njk`・`base.njk`等JA/EN共通レイアウトに新しいUI要素・JSON-LDプロパティを追加する場合は、`lang`分岐を必ずセットで実装すること。新規追加時は`grep -nP '[぀-ヿ一-龯]' src/_layouts/article.njk | grep -v '{#'`のような機械チェックで、`lang`分岐に包まれていない日本語文字列がないか確認する習慣を推奨する。

---

## 2026-08-01

### 英語版ナビ・フッターから未完成のTPJ Selectセクションを削除、フッター「Third Place Guide」列を日本語版とパリティ化

**対象ファイル**
- `src/_includes/nav-stories-en.njk`（デスクトップナビ・ハンバーガーメニューの両方から「TPJ Select」リンクを削除）
- `src/_includes/footer-stories-en.njk`（「TPJ Select」列を削除しグリッドを3列→2列に、「Latest Articles」に`rejectCategory("about")`を追加、「Third Place Guide」列を動的リスト化）
- `src/en/stories/concept/meiji-jingu-third-place-tokyo.md`・`tokyo-third-place-guide-travelers.md`・`src/en/stories/culture-sports-leisure/koto.md`（日本語版限定のTPJセレクト一覧へのリンクに注記追加）

**背景①（ナビ・フッターのTPJ Select削除）**：ユーザーからスクリーンショットで、英語版フッターの「TPJ SELECT」列がGreen Beans Coffee 1件のみのリンクになっており不自然だと指摘を受けた。原因は英語版施設ページ（`has_en: true`）がGBC 1件しか存在しないため。英語版の施設一覧ページ（`/en/stories/select/`）自体も未実装（個別施設ページのみ存在）。ユーザー判断で「一覧ページを新設し複数施設を英語化する」ではなく「不自然な単独リンクを削除する」を選択。デスクトップナビ・ハンバーガーメニュー・フッターの3箇所から「TPJ Select」リンクを撤去し、英語記事本文から日本語版TPJセレクト一覧（`/stories/select/`）にリンクしていた3箇所には、`tpj-certification-and-select-difference.md`で既に使われていた「*(Japanese-language listing; an English version is not yet available)*」という注記を統一して追加した。

**背景②（フッター「Third Place Guide」列のパリティ化）**：続けてユーザーから、英語版フッターの「Latest Articles」列に本来表示されるべき通常記事ではなく`about`カテゴリのガイド記事4本（2026-07-28公開）が表示されている問題を指摘された。調査の結果、`footer-stories-en.njk`の「Latest Articles」クエリ（`collections.articles | selectLang("en") | head(4)`）に`rejectCategory("about")`が付いておらず、直近公開の`about`記事4本がそのまま「最新記事」枠を占拠していたことが判明した。日本語版フッター（`footer-stories.njk`）は2026-07-20の対応で`rejectCategory("about")`が付いていたが、英語版フッターはその対応漏れだった。あわせて「Third Place Guide」列も日本語版「サードプレイスガイド」列（`collections.aboutArticles`から動的に最新4本＋一覧リンク）と異なり、固定リンク1件のみの実装になっていたため、日本語版と同じ動的パターンに揃えた。

**検証方法**：`npm run build`後、サイト全体のJSON-LD 2,282ブロックのパースエラー0件、内部リンク37,676件のリンク切れ0件をNode.jsスクリプトで確認済み。英語版フッターの「Latest Articles」に通常記事4本、「Third Place Guide」に`about`記事4本が正しく分離されて表示されることを実際のビルド済みHTMLで確認した。

**ルール化**：「✅ ItemList の言語フィルタリング（日英混在環境）」セクション内、`relatedPostsByCat`・`prevPost`・`nextPost`に関する既存ルールの直後に、「最新記事」系ウィジェットは`selectLang`/`rejectLang`だけでなく`rejectCategory("about")`も必ずペアで付ける旨を追記した。

---

## 2026-07-31

### 千代田区の親ピラー4本を新規公開＋都ピラー・兄弟ピラー間の逆リンク欠落を発見・修正

**対象ファイル**
- 新規：`src/stories/retreat-zen/chiyoda.md`・`shrine-temple/chiyoda.md`・`inbound-experience/chiyoda.md`・`culture-sports-leisure/chiyoda.md`
- 既存5本（`hotel-lounge-chiyoda.md`・`cafe-specialty-coffee-chiyoda.md`・`hotel-lounge-marunouchi.md`・`cafe-specialty-coffee-akihabara.md`・`cafe-specialty-coffee-jimbocho.md`）に新4本への逆リンク追加
- 都ピラー3本（`zen-retreat-tokyo-third-place.md`・`tokyo-museum-art-third-place.md`・`tokyo-inbound-third-place-guide.md`）・既存自治体ピラー8本（`retreat-zen/bunkyo・suginami`、`shrine-temple/taito・ome`、`culture-sports-leisure/koto・shibuya`、`inbound-experience/shibuya・taito`）に千代田区への逆リンク追加
- 画像4枚新規（皇居の堀・屋根ディテール・二重橋・千鳥ヶ淵、いずれもUnsplash取得・200KB以下）

**優先度Bの千代田区×4分野を追加**。既存5本（丸の内・秋葉原・神保町・hotel-lounge・cafe-specialty-coffee）が確立した「皇居・官庁街の権威」と「秋葉原・神保町の異端（サブカルチャー・古書街）」という二面性の文脈と矛盾しないよう、新4本も権威側の新しい切り口（皇居の不可侵性／国家的祭祀と町人の氏神の併存／国家機構の見学／国家的建造物の市民転用）で固有の核を設計した。

**画像選定時の注意点**：Unsplashの検索結果は地名キーワードを含めても関連性が低いことが多く、「shinto shrine torii gate tokyo」等の検索で浅草寺（台東区）・伏見稲荷（京都）・明治神宮（渋谷区）といった**別の場所の写真が上位に出る**ことが複数回あった。目視確認せずに機械的に1枚目を採用すると、記事の地域と無関係な画像を使ってしまうリスクがあるため、候補は必ずダウンロード後にRead toolで目視確認してから確定させること。「国立競技場（新宿区）」が「japanese arena dome roof tokyo」の検索でヒットしたケースもあり、建築物系のキーワードは特に誤爆が多い。

**公開後の内部リンク監査で発見した問題（重大）**：新4本は既存記事へ発リンクしていたが、**逆方向（既存記事→新4本）がほぼ全滅**していた。具体的には、①都ピラー3本が3本とも千代田区に一切言及していなかった、②それとは別に、都ピラーへの逆リンクとは独立して、既存の兄弟自治体ピラー8本も誰一人千代田区にリンクしていなかった。②は都ピラーが存在しない`shrine-temple`カテゴリでも同様に発生しており、「都ピラー→自治体ピラー」のみを対象にしていた2026-07-27のルールでは検出できない範囲だった。あわせて`tokyo-inbound-third-place-guide.md`が渋谷区の自治体ピラーにも未リンクという、今回の追加作業とは無関係の既存の欠落も発見し、あわせて修正した。自分自身が書いた`shrine-temple/chiyoda.md`が文京区（学業成就の信仰）にリンクし忘れていた漏れも1件あった。

**検証方法**：`npm run build`後、`public/stories/`配下468ファイルを対象にNode.jsスクリプトでJSON-LD（2,121ブロック）のパースエラー0件、内部リンク（20,830件）のリンク切れ0件、title/description重複0件を確認。H1がtitleと完全一致すること、パンくず（可視UI・JSON-LD BreadcrumbList）が3階層で一致すること、まとめ・FAQ（各5問・自動抽出）の設置、画像altテキストの自動生成、`千代田区`31〜39回・`東京`3〜5回・`サードプレイス/third place`計13〜17回という自然なキーワード密度も確認済み。

**ルール化**：「都ピラー→自治体ピラーの分岐リンク必須化」を「都ピラー→自治体ピラーの分岐リンク必須化・自治体ピラー間の相互リンク必須化」に改題・拡張し、新規自治体ピラー追加時は都ピラーへの逆リンクだけでなく**既存の兄弟自治体ピラー2〜3本への逆リンクも必須**である旨を明記した。対象カテゴリに`inbound-experience`（都ピラーあり）を追加し、`shrine-temple`（都ピラーなし）も兄弟間相互リンクの対象である旨を明記した。

---

### 英語版フッターの誤リンク修正＋sitemap.xmlの重大な欠落2件・hreflang片方向リンク3件を発見・修正

**対象ファイル**
- `src/_includes/footer-stories-en.njk`（「Third Place Guide」リンク修正）
- `src/sitemap.njk`（英語版施設ページの判定条件バグ修正、`/en/stories/about/`のURL追加）
- `src/en/stories/index.njk`（title強化）
- `src/stories/concept/meiji-jingu-third-place.md`・`tokyo-inbound-third-place-guide.md`・`what-is-third-place.md`（`hreflang_en`追加）

**背景**：ユーザーから「フッターの英語版セクションで『Third Place Guide』が個別記事にリンクしている」と画像付きで指摘を受け修正した後、続けて英語版サイト全体のSEO/AEO監査を行った。

**発見①：フッターの誤リンク**：`footer-stories-en.njk`の「Third Place Guide」列が、ハブページ`/en/stories/about/`ではなく個別記事`/en/stories/concept/what-is-third-place-japan/`に直接リンクしていた。ナビ（`nav-stories-en.njk`）・記事サイドバー（`article.njk`）は既に正しくハブページを指しており、フッターだけが「6箇所目のハードコード地点」として既存ルールの記載から漏れていた。

**発見②：sitemap.xmlの英語版施設ページが1件も登録されていなかった（重大）**：`sitemap.njk`が英語版URLの出力可否を`{% if venue.slug_en %}`という**実際のテンプレートでは一度も使われていないフィールド**で判定していた。実装（`venue-en.njk`）は`permalink: "/en/stories/select/{{ venue.slug }}/"`のように日本語版と同じ`venue.slug`をそのまま使っており、`slug_en`は存在しないため条件が常にfalseになり、`has_en: true`の施設（Green Beans Coffee）の英語版ページ・hreflang alternateタグの両方がsitemapから欠落し続けていた。`npm run build`はエラーを出さないため、実ファイルとsitemap記載URLを突き合わせるまで気づかれなかった。

**発見③：`/en/stories/about/`（英語版ハブページ）自体もsitemapに未登録**：日本語版`/stories/about/`は個別`<url>`ブロックがあったが、英語版に対応するブロックが存在しなかった。

**発見④：日本語記事3本が英語版と片方向リンクだった**：`meiji-jingu-third-place.md`・`tokyo-inbound-third-place-guide.md`・`what-is-third-place.md`は、対応する英語版が既に公開されているにもかかわらず`hreflang_en`フロントマターが未設定で、英語版→日本語版の`hreflang="ja"`は出力される一方、日本語版→英語版の`hreflang="en"`が一切出力されない片方向リンクになっていた。2026-07-23の「hreflangの自己参照バグ修正」とは別種の不具合（あちらは自己参照の欠落、今回はフィールド未設定による相手言語参照そのものの欠落）。

**発見⑤（軽微）**：`/en/stories/`トップのtitleが`"English"`のみで検索キーワードを含まず、日本語版の`"日本のサードプレイスを探す"`と比べて著しく弱かったため、`"Discover Japan's Third Places"`に変更した。

**検証方法**：`npm run build`後、`public/stories/`・`public/en/stories/`配下503ファイルを対象にJSON-LD 2,260ブロックのパースエラー0件、内部リンク24,664件のリンク切れ0件、title重複0件を確認。EN記事26本のdescription文字数（150〜160字ルール）・太字リード／まとめ／FAQ設置・画像サイズ（200KB以下）・category_slugの正規性を個別に再検証し全件クリアを確認。sitemap.xmlは英語版29ページ全件が登録されたことを、hreflangは日英3本の相互参照が揃ったことをそれぞれNode.jsスクリプトで確認済み。

**ルール化**：「『Third Place Guide』の遷移先は5箇所にハードコードされている」を6箇所に改訂し`footer-stories-en.njk`を追加。「venues.json 任意フィールド一覧」の直後（英語版施設ページのルール群）に「英語版施設ページを`sitemap.xml`に載せる際は`venue.has_en`で判定する」を新規セクションとして追加。同箇所に「日本語記事に英語版を追加したら、必ず`hreflang_en`を設定する」チェック方法を新規追加した。

---

## 2026-07-30

### Green Beans Coffee施設ページが「グリーンビーンズコーヒー」「Green Beans Coffee」検索で上位表示されない件の診断

**背景**：ユーザーから、施設名でのブランド名検索で`/stories/select/green-beans-coffee-shibuya/`が上位表示にならないとの相談を受け、原因診断とSEO/AEO強化を行った。

**診断結果（技術的な問題は「ほぼ無い」ことを確認）**：
- 実URLをcurlで取得（Googlebot UA・通常ブラウザUAの両方）し200を確認。`meta robots`は`index, follow`、canonicalは正しく自己参照、hreflang（ja/en/x-default）も正常。noindexやクロールブロックは存在しない
- JSON-LD 5ブロック（LocalBusiness/Review/BreadcrumbList等）は既存ルール通り実装済み。sameAs（公式サイト・Instagram）も出力済み
- titleタグは`title_override`により「Green Beans Coffee（グリーンビーンズコーヒー）｜東京・渋谷のサードプレイス Flagship認証店」と、英語表記・カタカナ読み・地名・業種すべてを含む良好な状態

**発見した実際のギャップ（2件・修正済み）**：
1. **H1にカタカナ読みが欠落**：`venue.njk`のH1は`{{ venue.name }}`のみを出力しており、「グリーンビーンズコーヒー」という完全一致の検索クエリに対応する最重要オンページ要素（H1）に該当語が一度も出現していなかった。2026-07-21の「英語表記の施設名にはカタカナ読みを併記するルール」はcitation・本文・まとめ・FAQの4箇所を対象にしていたが、title・H1・JSON-LD alternateNameは対象外だった
2. **レビュー記事のtitle/H1にもカタカナ読みが欠落**：`/stories/cafe-specialty-coffee/green-beans-coffee-review/`のtitleは「Green Beans Coffee 渋谷」のみで、本文の答えリードには「グリーンビーンズコーヒー」があるにもかかわらずtitle/H1（`article.njk`はtitleをそのままH1として出力）には反映されていなかった

**対応**：
- `venues.json`に`name_reading`（任意フィールド）を新設し、GBCに`"グリーンビーンズコーヒー"`を設定
- `venue.njk`のH1を`{{ venue.name }}{% if venue.name_reading %}（{{ venue.name_reading }}）{% endif %}`に変更（他施設は`name_reading`未設定のため表示に変化なし、後方互換）
- LocalBusiness・Review（`itemReviewed`）の両JSON-LDに`alternateName`（`name_reading`使用時のみ出力）を追加
- レビュー記事のtitleを「【TPJ Flagship認証】Green Beans Coffee（グリーンビーンズコーヒー）渋谷：東京のサードプレイスを評価する」に変更し、`modified: 2026-07-30`を追記

**根本原因（コードの問題ではなく构造的な要因）**：WebSearchで実際の検索結果を確認したところ、「グリーンビーンズコーヒー 渋谷」「Green Beans Coffee Shibuya」いずれも上位10件はGBC公式サイト（greenbeanscoffeeambassador.com）・Instagram・Facebook・Tabelog・Retty・HotPepper・Yahoo!マップ・Tripadvisor等、**施設の公式チャネルか国内最大級のグルメ/地図ディレクトリ**で占められており、thirdplacejapan.comは影も形もなかった。さらに`site:thirdplacejapan.com`（ドメイン全体を対象にした検索）ですら結果が0件で、サイト自体の検索エンジンでの可視性が極めて低いことを確認した。当リポジトリの初回コミットは2026-06-23で、診断時点（2026-07-30）でサイト開設から約5週間しか経っておらず、新規ドメインには外部被リンク・ドメインの実績（トラスト）がほぼ無い。Googleは特にブランド名検索において、公式チャネルおよび既存の高権威サイトを優先して表示する傾向が強く、オンページの最適化だけでは短期間で追い抜くことが構造的に難しい状態にある。

**ユーザーへの推奨事項（コード修正の範囲外）**：
- Google Search Consoleでのプロパティ確認・インデックス登録状況の確認（URL検査ツールでこのページが実際にインデックスされているか）
- 施設側（Green Beans Coffee公式サイト・Instagram等）からTPJ施設ページへのリンク・言及を依頼する（実証店舗という関係性がある施設のため、最も効果が見込める被リンク獲得手段）
- 新規ドメインは数ヶ月単位で信頼を積み上げる必要があるため、継続的なコンテンツ追加・内部リンク・（可能であれば）外部からの被リンク獲得を並行して行う

**検証方法**：`npm run build`後、JSON-LD 2,226ブロックのパースエラー0件、内部リンク切れ0件、title重複0件を確認済み。

**ルール化**：「英語表記の施設名にはカタカナ読みを併記するルール」にtitle・H1・JSON-LD alternateNameの対象箇所を追加。「venues.json 任意フィールド一覧」に`name_reading`を追加。

### `/stories/select/`とその周辺ページのSEO/AEO監査・幽霊リンク/幽霊カテゴリページの発見と修正

**背景**：`/stories/select/`（TPJセレクト一覧）のSEO/AEO強化依頼を受け、ハブページ単体だけでなく、そこから展開する23施設ページ・15業種カテゴリページ・エリアページとの間のtitle/description/太字リード/重複タイトル/幽霊リンク/パンくず/まとめ/FAQ/内部リンクを横断的に機械監査した。`npm run build`後の`public/`配下を対象に、Node.jsスクリプトでtitle重複・description重複・JSON-LD構文・内部リンク（ハードコードだけでなく動的生成されたリンクも含む）を全件チェックする方式を用いた。

**発見①：ゴーストリンク2件（`venue.area_slug`と`areas.json`の不一致）**

`onibus-coffee-nakameguro`は住所（`目黒区上目黒2-14-1`）が目黒区にもかかわらず、`area_slug`が渋谷区側のスラッグ`nakameguro`のままになっており、存在しない`/stories/area/tokyo/meguro-ku/nakameguro/`へのリンクが生成されていた（正しくは目黒区側の`naka-meguro`）。`arise-coffee-roasters-kiyosumishirakawa`も同様に`area_slug: "kiyosumishirakawa"`（ハイフンなし）が`areas.json`の正式スラッグ`kiyosumi-shirakawa`と不一致だった。両者とも`area_slug`を修正して解消した。

**発見②：ゴーストカテゴリページ8件（`venue.category_slug`の架空値）——2026-07-27に発見した`luxury-hotel-lounge`バグと同一パターン**

渋谷SAUNAS・saunalab神田・小杉湯（3件）が`category_slug: "spa-sauna"`（`categories.json`に存在しない架空のslug。正式には`spa-sauna-wellness`）、THE SG CLUBが`category_slug: "bar-whisky-cocktail"`（正式には`bar-whiskey`）のまま登録されていた。この結果、4施設が正規のカテゴリページ（`/stories/spa-sauna-wellness/`・`/stories/bar-whiskey/`）から漏れており、代わりに8つの幽霊ページ（`/stories/area/.../spa-sauna/`・`/stories/area/.../bar-whisky-cocktail/`）が並行生成されていた。2026-07-27のTRUNK/BnA_WALLの事例と全く同じ発生パターンであり、`category_slug`の入力ミスがビルドエラーにならず静かに幽霊ページを生む構造的リスクが再確認された。4施設の`category_slug`を修正し、`npm run build`後に不要になった8つの静的ファイルを手動削除した。

**発見③：TPJセレクト施設23件中20件のFAQに必須キーワードが皆無**

`citation`には既に「サードプレイス」「third place」併記ルール（2026-07-21）が適用済みで全件クリアだったが、`faq`（FAQPage JSON-LD兼可視アコーディオン）側は誰もチェックしておらず、20施設で一問も概念語を含んでいなかった。各施設のFAQ1問目（「どんな場所か」を問う質問）に、その施設固有の文脈（コーヒーの産地対話、まちやどの実践、古民家改装など）を踏まえた一文を個別に追記して解消した。

**発見④：カテゴリ一覧ページ（`/stories/{category}/`、15業種共通テンプレート）のtitle/H1/JSON-LDに「サードプレイス」が皆無**

`src/stories/category-index.njk`のtitleは`{{ cat.name_ja }}`のみで、2026-07-27に確立した「エリア関連ページテンプレートは全種類でtitle/descriptionに『サードプレイス』を含めること」と同種の欠落が、エリア軸ではなく業種軸のハブページに残っていた。既存のエリアページ命名規則（`{{地域名}}のサードプレイス`）に倣い「{{業種名}}のサードプレイス」に統一し、CollectionPage JSON-LDに`mentions`（DefinedTerm）も追加した。

**発見⑤：施設ページからカテゴリ一覧への親子リンクが存在しなかった**

`venue.njk`のカテゴリバッジ（`<span class="label">{{ venue.category }}</span>`）はただのテキストで、業種の親ページ（`/stories/{category_slug}/`）へのリンクが一切なかった（エリア×業種の掛け合わせページへのリンクは別途存在していたが、単体のカテゴリハブへは繋がっていなかった）。バッジをリンク化し、23施設ページ全件から親カテゴリページへの導線を確保した。英語版（`venue-en.njk`）は対応する`/en/stories/{category}/`ページ自体が存在しないため対象外とした。

**発見⑥：区境をまたぐ同名エリアによる重複タイトル3組の解消**

`/stories/area/`配下のL3エリアページ・エリア×業種ページで、日暮里（荒川区・台東区西日暮里）・武蔵小山（目黒区・品川区）・中目黒（目黒区・渋谷区）の3組がtitleタグ完全一致という状態が2026-07-27時点から継続していた（当時は「データ不整合ではなく実在の地名重複」として対応を見送っていた）。今回ユーザー判断で解消することになり、`src/_data/areaL3.js`・`areaL3Cat.js`に、エリア名が`areas.json`内で複数回出現するかどうかを判定する`areaNameDuplicate`フラグを追加。`l3-area.njk`・`l3-category.njk`のtitle・H1・JSON-LD（CollectionPage/ItemList）で、このフラグが立っている場合のみ区名を「（目黒区）」のように併記するようにした。データ駆動のため、今後同名エリアが新たに`areas.json`に追加された場合も自動的に区名併記が適用される。

**検証方法**：全修正を通じて`npm run build`後に、①サイト全体のtitle重複チェック（Node.jsで全`index.html`の`<title>`を集計）、②動的生成リンクを含むリンク切れチェック（`public/`配下の実レンダリング済みHTMLから`href`を全件抽出し実在ファイルと突き合わせ）、③JSON-LD構文チェック（全`<script type="application/ld+json">`を`JSON.parse`）を実施。最終状態でtitle重複0件・リンク切れ0件・JSON-LDパースエラー0件（2,226ブロック）を確認済み。

**ルール化**：「venues.json 施設データ管理ルール」の`category_slug`ルールに`area_slug`の同種チェックを追記。「TPJセレクト 掲載仕様」のFAQルールに概念語必須の注記を追記。「エリア関連ページテンプレートは全種類でtitle/descriptionに『サードプレイス』を含めること」の直後に、業種カテゴリハブページも対象である旨を追記。「エリアページのレイアウトルール」に「区境をまたぐ同名エリアの重複タイトル自動解消」を新規セクションとして追加。詳細は各セクション参照。

## 2026-07-27

### 小金井市・東村山市・清瀬市の親ピラー4本を新規公開＋都ピラーの分岐リンク欠落を修正

**対象ファイル**
- 新規：`src/stories/retreat-zen/koganei.md`・`culture-sports-leisure/koganei.md`・`retreat-zen/higashimurayama.md`・`retreat-zen/kiyose.md`
- `src/stories/concept/zen-retreat-tokyo-third-place.md`・`tokyo-museum-art-third-place.md`（都ピラーの分岐リンク追加）
- 既存記事への逆リンク追加：`retreat-zen/kokubunji.md`・`mitaka.md`・`ome.md`・`suginami.md`・`bunkyo.md`、`culture-sports-leisure/taito.md`・`fuchu.md`・`akishima.md`

**新規記事4本の固有の核**：
- 小金井市×リトリート：玉川上水という一本の水路が街を貫く「線的な静けさ」（三鷹＝面/器、国分寺＝崖の段差とは異なる原理）
- 小金井市×文化・レジャー：江戸東京たてもの園（移築30棟）が刻む「時間の集積地」。実際に建物の中を歩いて生活動線を追体験できる点で台東区の鑑賞型と対比
- 東村山市×リトリート：狭山丘陵という「複数自治体にまたがる里山」（人の手が入り続けることで保たれる二次的自然）
- 清瀬市×リトリート：「静養のために雑木林が選ばれた」という土地利用の出発点（1931年の結核療養所開設が起点。観光・生活の副産物として緑が残った他の郊外エリアとは成り立ちが異なる）

いずれも個人名・個別施設名（医療機関名等）は一般名詞化し、事実は全てWeb検索で裏取りした。本文文字数は初回生成時に4,567〜4,602字と目安（4,000〜4,300字）を超過したため、ユーザー指摘を受けて7軸のインバウンド軸と後続インバウンド視点セクションの重複、固有の核の定義文の複数回反復を削って4,230〜4,271字に調整した経緯がある。2本目以降（東村山市・清瀬市）はこの教訓を踏まえ生成段階から重複を避け、後工程の圧縮なしで4,000〜4,300字に収めた。

**都ピラーの分岐リンク欠落を発見・修正**：新規記事公開後のSEO/AEO監査で、`retreat-zen`・`culture-sports-leisure`それぞれの都ピラー記事が、自治体ピラー9本中2本にしかリンクしておらず7本ずつが欠落していることが判明した（自治体ピラー側から都ピラーへの逆リンクは全記事にあったが、都ピラー→自治体ピラーの順方向リンクが記事追加のたびに更新されていなかった）。両都ピラーの関連記事リストに欠落分を追加し、2記事とも自治体ピラー9本全件への分岐を回復した。

**検証方法**：`npm run build`後、新規4記事および都ピラー2記事のJSON-LD（各4ブロック）をNode.jsで`JSON.parse`しパースエラー0件を確認。全記事の内部リンク切れ0件を確認。`grep -o 'href="/stories/{category}/[a-z-]*/"'`で都ピラーの発リンク先が自治体ピラー全件と一致することを確認済み。

**ルール化**：「内部リンク（AEO対策・毎回必須）」の直下に「都ピラー→自治体ピラーの分岐リンク必須化」を新規セクションとして追加した。

---

### `areaL2Cat.js`の`area_name`フォールバック欠落を発見・修正

**対象ファイル**
- `src/_data/areaL2Cat.js`

**背景**：上記の都ピラー分岐リンク修正に続けて実施したSEO/AEO監査で、`retreat-zen`・`culture-sports-leisure`の自治体ピラー記事が属する市区町村ページ（L2、例：`/stories/area/tokyo/koganei/`）の「業種から探す」セクションが軒並み空表示になっていることが判明した。原因は`areaL2Cat.js`（L2市区町村×業種の組み合わせを生成し、「業種から探す」ピルとL2×カテゴリページ`/stories/area/{pref}/{city}/{category}/`の両方を駆動するデータファイル）が記事の`area_slug`のみを見ており、`area_name`（自治体ピラー記事が使うフィールド）を見ていなかったこと。同種のデータを扱う`articlesByCity`フィルター（`.eleventy.js`、L2ページの「○○市の関連記事」セクションが使用）は`area_slug`・`area_name`の両方に対応済みだったため、2つの仕組みの間に一貫性の欠如があった。

**影響範囲**：`area_slug`未設定・`area_name`のみ設定の記事28本（本日追加した4本を含む、`retreat-zen`・`culture-sports-leisure`・`shrine-temple`等の自治体ピラー記事群）。台東区・渋谷区だけは施設（`venues.json`）データ経由で偶然カバーされていたため、この不具合は長期間気づかれていなかった。

**対応**：`areaL2Cat.js`に、`articlesByCity`と同じ2段階フォールバック（`area_slug`を優先し、なければ`area_name`と市区町村名の完全一致で解決）を追加した。市区町村名の逆引きマップ（`cityNameMap`）を新設し、`area_slug`または`area_name`のどちらかが設定されていれば処理対象に含めるよう条件式を変更した。

**検証方法**：`npm run build`後、小金井市・東村山市・清瀬市の「業種から探す」ピルが正しく表示されることを確認。新規生成された17件のL2×カテゴリページを含め、サイト全体（`public/stories/`配下）のJSON-LD 2,089ブロックをNode.jsで`JSON.parse`しパースエラー0件を確認。全記事の内部リンク453本をチェックしリンク切れ0件を確認済み。

**ルール化**：「全エリアページで15業種を常時表示するルール」の直後に「エリア紐付けデータの`area_slug`/`area_name`フォールバックを統一するルール」を新規セクションとして追加した。

---

### L2×業種・L3×業種ページのtitle/descriptionに「サードプレイス」が欠落していた不具合を修正

**対象ファイル**
- `src/stories/area/l2-category.njk`・`l3-category.njk`

**背景**：`areaL2Cat.js`修正の直後に実施した追加監査で、エリア関連ページ5種類のうちL1都道府県・L2市区町村・L3エリアの3種類はtitle・H1に「{{ 地域名 }}のサードプレイス」を含んでいたが、L2×業種・L3×業種（地域と業種の掛け合わせページ）の2種類だけtitleが「{{ 地域名 }}の{{ 業種名 }}」となっており「サードプレイス」が一切含まれていないことが判明した。meta description・JSON-LD（CollectionPage/ItemListのname・description）も同様に欠落しており、「Third Place Japan」というブランド名の一部としての出現のみで、必須キーワードとしての「サードプレイス」は皆無だった。本日新規生成した17ページだけでなく、`taito-ku/ueno/culture-sports-leisure`等の既存ページも含めサイト全体で数十ページに影響していた。

**対応**：2テンプレートのtitle・description・可視H1・JSON-LD（CollectionPage/ItemListのname・description）を「{{ 地域名 }}のサードプレイス：{{ 業種名 }}」という、既存記事群のコロン区切り命名規則に統一した。

**検証方法**：`npm run build`後、修正対象ページのtitle/descriptionに「サードプレイス」が反映されていることを確認。サイト全体のJSON-LD 2,089ブロックのパースエラー0件、内部リンク453本のリンク切れ0件を確認済み。

**ルール化**：「エリア紐付けデータの`area_slug`/`area_name`フォールバックを統一するルール」の直後に「エリア関連ページテンプレートは全種類でtitle/descriptionに『サードプレイス』を含めること」を新規セクションとして追加した。

---

### venues.jsonの誤ったcategory_slugによる幽霊カテゴリページを発見・修正

**対象ファイル**
- `src/_data/venues.json`（TRUNK(HOTEL)・BnA_WALLの2件）
- 削除：`public/stories/area/tokyo/chuo-ku/luxury-hotel-lounge/`・`chuo-ku/nihonbashi/luxury-hotel-lounge/`・`shibuya-ku/harajuku/luxury-hotel-lounge/`・`shibuya-ku/luxury-hotel-lounge/`

**背景**：L2×業種・L3×業種ページのキーワード修正に続けて、サイト全体の`<title>`重複を機械的にチェックしたところ、5組の重複タイトルが見つかった。このうち2組（`中央区のサードプレイス：高級ホテル・ホテルラウンジ`・`日本橋のサードプレイス：高級ホテル・ホテルラウンジ`）を調査したところ、`categories.json`に存在しない`luxury-hotel-lounge`という架空の`category_slug`が原因と判明した。`venues.json`のTRUNK(HOTEL)・BnA_WALLの2施設がこの誤slugのまま登録されており、`areaL2Cat.js`が`venue.category_slug`を`categories.json`と照合せずそのままカテゴリ名として使い回す実装だったため、正規の`hotel-lounge`ページと並行して「幽霊カテゴリ」ページが4件生成されていた。この2施設は正式カテゴリページ（`/stories/hotel-lounge/`）からも欠落していた。

残る3組の重複タイトル（日暮里・武蔵小山・中目黒）は区境をまたいで実在する地名が`areas.json`に区ごとの別エントリとして正しく登録されているために生じたもので、データ不整合ではなくL3エリアページのtitle設計（区名を含めるかどうか）に関わる別種の論点のため、今回は対応せず報告に留めた。

**対応**：2施設の`category_slug`を`hotel-lounge`に修正し、`npm run build`後に不要になった4つの静的ファイルを手動削除した（11tyは生成対象外になった旧ファイルを自動削除しないため）。

**検証方法**：修正後、`/stories/hotel-lounge/`に2施設が反映されたことを確認。サイト全体の重複タイトルが5組→3組に減少したことを確認。JSON-LD（2,081ブロック）のパースエラー0件、内部リンク（453本）のリンク切れ0件を確認済み。

**ルール化**：「venues.json 施設データ管理ルール」に「`category_slug`は必ず`categories.json`の正式15スラッグから選ぶこと」を新規セクションとして追加した。

---

## 2026-07-25

### TPJ公式ガイドライン「その他」セクション新規公開（AI検索ポリシー・広告ポリシー・削除ポリシー・FAQ・お問い合わせ・サイトマップ）

**対象ファイル**
- 新規：`public/guidelines/ai-search-disclosure-policy/`・`advertising-policy/`・`correction-deletion-policy/`・`faq/`・`contact/`・`sitemap/`
- `public/guidelines/index.html`（該当カードのリンク化、更新履歴追記、「口コミ・レビュー掲載ポリシー」の未公開カード削除）

**背景**：ユーザーからテキストで提供された3ポリシー文書を`/guidelines/`に公開する依頼を受け、既存ページ（`ai-usage-policy`等）と同一テンプレートで新規作成。続けて「その他」チャプターのFAQ・お問い合わせ・サイトマップも同様に依頼された。

**サイトマップページ制作時の事実確認**：ユーザー提供のサイトマップ原文に「※認証施設一覧、15カテゴリ一覧、エリア一覧は順次公開予定です」という記述があったが、実際には`/stories/select/`・`/stories/area/`は公開済みと判明。ユーザーに確認のうえ、実ページへの直リンクに修正し「準備中」表記を削除した。「15カテゴリ一覧」は単独ページがないため、`src/stories/index.njk`のカテゴリセクションに`id="categories"`を新設し`/stories/#categories`にリンク（要`npm run build`）。「認証施設一覧」は後日ユーザー指示で「セレクト施設一覧」に訂正。

**FAQページ制作後の修正**：Q2「TPJ認証とは何ですか？」の内容が`/stories/about/certification-grades/`の説明と重複・粒度不一致だったため、「セレクトと認証の違いは何ですか？」に変更し同ページへのリンクを追加（可視部・FAQPage JSON-LD両方）。

**検証方法**：各ページ作成・修正のたびにJSON-LDをNode.jsで`JSON.parse`しパースエラー0件を確認。

**ルール化**：本セクション上部「TPJ公式ガイドライン（/guidelines/）ページ管理ルール」を新規追加した。

---

### ガイドライン全15ページのSEO/AEO監査・強化

**対象ファイル**：`public/guidelines/`配下 全15ページ、`src/sitemap.njk`・`public/sitemap.xml`

**背景**：「その他」3ページ（faq/contact/sitemap）の制作完了後、ユーザーから①CLAUDE.mdルールへの準拠監査、②「TPJ」だけでなく「サードプレイス」「Third Place」を自然に含めるSEO/AEO強化、の2段階で依頼を受けた。

**監査で判明した点**：
- JSON-LD構成（WebPage+isPartOf+publisher+about）は15ページ全て既存パターンと一致し違反なし
- FAQページの単一カテゴリ見出し「TPJについて」が冗長と判断し削除（未使用化したCSS `.chapter-title`も削除）
- `public/sitemap.xml`に`/guidelines/`配下の個別ページが1件も登録されていなかった（トップページのみ）ため、`src/sitemap.njk`に`guidelinePages`配列を追加し14ページ分を機械的に列挙する形で追加
- 新規6ページのmeta description等に「サードプレイス」が一切含まれていない、既存8ページのうち`privacy-policy`は0回、複数ページがog:description/JSON-LD `about`のみ未対応、という状態を発見。全15ページのmeta description・og:description・JSON-LD `about`・本文冒頭に「日本初のサードプレイス認証Third Place Japan」パターン（既存ページの確立済み表現）を追加し、全ページ3回以上に是正
- titleタグは表示幅（全角2・半角1換算）を計算し、64幅以内に収まる12ページのみ「サードプレイス認証」を追加。長すぎる3ページ（ai-search-disclosure-policy・certification-mark-guidelines・mission）はSERP見切れを避けるため現状維持
- ページ間の内部リンクが「その他」2ページ以外すべて0本だったため、内容的に関連する12ページ間（広告掲載⇄編集ポリシー、AI利用⇄AI検索ポリシー、認証・評価→調査→認証マークの三角関係、プライバシー⇄利用規約 等）に自然な文中リンクを追加し、全15ページが最低1本の相互リンクを持つ状態にした

**検証方法**：Node.jsスクリプトで全15ページのJSON-LDパースエラー0件、「サードプレイス」出現数（全ページ3回以上）、titleタグ表示幅（64幅超過なし）を機械的に確認。

**スコープ外として保留**：`data-pagefind-body`未付与によりサイト内検索の索引対象外である点（`/guidelines/`全体・メインサイト・about-companyに共通する既存設計）はユーザーに確認のうえ、今回は対応せず保留とした。

---

### ガイドライン・ポリシー11ページへのフッター相互リンク・前後ナビゲーション追加

**対象ファイル**：`public/guidelines/`配下 第1〜4章11ページ（mission・editorial-policy・certification-policy・investigation-guidelines・certification-mark-guidelines・ai-usage-policy・ai-search-disclosure-policy・advertising-policy・correction-deletion-policy・privacy-policy・terms-of-service）

**背景**：ユーザーから、記事ページの「前の記事／次の記事」カードUIを参考に、ガイドライン・ポリシー11ページにも同様の相互リンク導線を求められた。

**対応**：
1. フッターを3列→4列に拡張し「ガイドライン・ポリシー」列（11ページ全リンク）を新設。モバイルは`1fr 1fr`の2カラム
2. 本文末尾に`.guide-pagination`ブロックを新設。`guidelines/index.html`の掲載順で前後ページへリンクするチェーンを構成（先頭・末尾は片側のみ表示）

**実装上の注意点**：対象ファイルがCRLF改行だったため、Node.jsでの一括置換スクリプトが当初LF前提でマッチせず失敗した。読み込み時に`\r\n`→`\n`に正規化して置換処理を行い、書き込み時に`\n`→`\r\n`へ戻すことで解決した。今後同様の一括編集スクリプトをこのリポジトリの`public/`配下HTMLに対して書く場合は、CRLF正規化が必要になる可能性を考慮すること。

**検証方法**：11ページ全てでJSON-LDパースエラー0件、`<div>`/`</div>`タグ数の一致、前後リンクチェーンが順序どおり接続されていること（mission→editorial-policy→…→terms-of-service）をNode.js/grepで確認済み。

**ルール化**：本セクション上部「TPJ公式ガイドライン（/guidelines/）ページ管理ルール」のフッター・ページネーションに関する項目として反映済み。

---

### メインサイトフッターの4カラム化（コンテンツ・サポート列新設）＋ガイドラインページへの展開

**対象ファイル**：`public/index.html`、`public/guidelines/`配下 全15ページ

**背景**：メインサイトのフッターが3列（Third Place Japan／TPJについて／運営情報）のみで、Stories関連の周遊導線（セレクト施設一覧・15カテゴリ一覧・エリア一覧）とサポート導線（FAQ・サイトマップ）が欠けていた。ユーザーから、`/guidelines/sitemap/`の本文で既に使っている「コンテンツ」「サポート」という分類語彙を使ってフッターを拡張する依頼を受けた。

**対応**：実装前にArtifactでBefore/After比較モックアップを作成しユーザーに確認を取ってから実装（現状3列→提案4列を並べて表示し、リンク対応表を添付）。承認後、`public/index.html`のフッターを4カラム化：
- Third Place Japan列からStoriesピルを「コンテンツ」列（新設）へ移動
- 「運営情報」列を廃止し、運営会社は「TPJについて」へ、お問い合わせ・For Businessは「サポート」列（新設）へ再配置

**続けて2段階の追加対応**：
1. ユーザーから「フッターのカテゴリ見出しを金色にしてほしい」との指示を受け、`.footer-cols h3`の`color`を`rgba(242,238,232,0.22)`（暗いグレー）から`var(--c-accent)`（金色）に変更
2. その後ユーザーが`/guidelines/`配下ページのスクリーンショットを提示し「このままです（変わっていない）」と指摘。確認したところ、メインサイトの変更は`public/index.html`のみに閉じており、`/guidelines/`配下15ページ（独自の「ガイドライン・ポリシー」列・グレー見出しのまま）には及んでいなかったことが判明。ユーザーに確認のうえ、まず金色見出しを15ページへ展開し、続けて「TPJ公式ガイドラインに関するすべてのページのフッターをメインサイトと同様にする」という明確な指示を受け、15ページの独自フッター構成（ガイドライン・ポリシー列／運営情報列）を廃止し、メインサイトと完全に同一の4カラム内容に置き換えた（サブページのため`#philosophy`等のアンカーは`/#philosophy`のように絶対パス化）

**検証方法**：`public/index.html`・`/guidelines/`全15ページでJSON-LDパースエラー0件、`<div>`タグバランス正常、全ページのフッター見出しラベルが完全一致（Third Place Japan／コンテンツ／TPJについて／サポート）することをNode.js/grepで確認済み。本文末尾の「前のガイドライン／次のガイドライン」ナビゲーションはフッターと独立した機能のため今回変更していない。

**ルール化**：「TPJ公式ガイドライン（/guidelines/）ページ管理ルール」の「フッター構成」項目を全面改訂し、メインサイトとの統一を明記した。今後メインサイトのフッターを変更する場合は`/guidelines/`全ページにも同じ内容を反映する必要がある旨を追記済み。

---

### FAQ拡充（16問）・お問い合わせフォーム修正・フッター「サポート」列整理・TPJセレクト評価方式の誤記述修正

**対象ファイル**
- `public/guidelines/faq/index.html`（7問→16問に拡充、BreadcrumbList JSON-LD追加）
- `src/stories/about/certification-grades/index.njk`（FAQページへの逆リンク追加）
- `public/index.html`（お問い合わせフォームの返信目安修正・紹介コード欄追加）
- メインサイト・`/guidelines/`全15ページ・`/about-company/`（フッター「サポート」列の重複リンク整理）
- `src/stories/about/tpj-certification-and-select-difference.md`（TPJセレクト評価方式の誤記述修正）

**FAQ拡充**：「TPJについて」「認証・セレクトについて」の2グループ・7問だったFAQを、「TPJについて」「認証・セレクトについて」「利用者の方へ」「運営・情報管理について」の4グループ・16問に拡充。ポジティブな内容（TPJの独自性、7軸評価基準、施設側のメリット、無料利用、インバウンド対応）を中心に追加した。ユーザーから2回の訂正指示を受けた：①競合の固有名詞（食べログ・ミシュラン）は使わず「グルメサイト・口コミサイト」という一般的な表現に統一、②「日本全国が対象」という言い切りを「エリアを順次拡大中」という実態に即した表現に修正。BreadcrumbList JSON-LDも新設し、`/stories/about/certification-grades/`から逆リンクを追加して双方向リンクを構成した。

**お問い合わせフォーム修正**：メインサイトの`#modal-contact-form`で「2営業日（48時間）以内」という表記を「3営業日以内」に統一（`/guidelines/contact/`側の表記と揃えた）。「紹介コード」を任意項目として新設（電話番号の後）。フォーム送信はFormData経由で全項目を自動送信する実装のためJS変更は不要だった。

**フッター「サポート」列整理**：「お問い合わせ」と「For Business」がどちらも`#for-business`を指す重複リンクだったため、ユーザー指摘を受け「For Business」を削除。空いた枠に、従来フッターに一切リンクがなかった「プライバシーポリシー」「利用規約」を新設し、列を4項目→5項目に拡張（他列との項目数バランスを考慮）。フッターを共通化している17ファイル（メインサイト・/guidelines/全15ページ・/about-company/）すべてに反映した。

**TPJセレクト評価方式の誤記述修正**：ユーザーから「神楽坂 和らく」（`/stories/select/kagurazaka-waraku/`）の実際の施設ページでは数値化されていると指摘を受けた。`tpj-certification-and-select-difference.md`に「TPJセレクトは数値化・レーダーチャート化を行わず◎・○・×の3段階簡易評価にとどめる」という記述があったが、実装（`venue.njk`）を確認したところ、TPJセレクトを含む全グレードが7軸を0〜10点で公開しており、レーダーチャートのみPlatinum以上限定という仕様だった。ユーザーから「他のページも確認。整合性を合わせる。」との指示を受け、`src/`全体を横断検索した結果、`tpj-7-axis-evaluation-guide.md`は既に正しい記述になっていたため、その表現に合わせて統一した。掲載基準の閾値（7軸中5基準以上が「○」以上、特別感またはストーリーで「◎」）自体は`project-docs/TPJセレクト掲載基準ルールブック.md`の記載と一致していたため維持した。

**検証方法**：各修正後にJSON-LDをNode.jsで`JSON.parse`しパースエラー0件を確認。フッター修正は17ファイル横断でサポート列の内容が完全一致することを確認。`tpj-certification-and-select-difference`の修正は`npm run build`後、`dateModified`が`modified: 2026-07-25`を反映していること、FAQPage JSON-LD（markdownから`extractFAQ`フィルターで自動抽出）が新しい回答文と一致していることを確認済み。

**ルール化**：ドキュメント（記事）の記述と実装の食い違いを1箇所修正する際は、`grep -rl`で同じ主張パターン（今回は「数値化・レーダーチャート化」「3段階簡易評価」等）を`src/`全体から検索し、他の記事に同種の誤りが残っていないか確認してから修正を確定させること。2026-07-23の「7軸評価スコアのAEO確認」で確立した「実装を見てから対応する」原則に加え、「複数ページに同じ主張が独立して書かれている場合、1箇所の修正だけでは不十分で全箇所の整合性を横断確認する」という点を明文化した。

---

### 施設0件エリアのSEO/AEO施策（広尾）とFAQPage文法バグの発見・修正、全15業種常時表示の実装

**対象ファイル**
- `src/stories/area/l3-area.njk`（FAQPage文法バグ修正、概念コンテンツ・関連FAQ・上位階層リンク・DefinedTerm追加、全15業種常時表示への変更）
- `.eleventy.js`（`categoryLinksForArea`フィルター新設）
- `src/stories/area/l1-prefecture.njk`・`l2-city.njk`・`l2-category.njk`・`l3-category.njk`（可視FAQアコーディオン新設、`l3-category.njk`は文法バグも修正）

**背景**：ユーザーから「`/stories/area/tokyo/minato-ku/hiroo/`はまだ何も掲載していないが、SEO/AEOを強化できる施策は？」と問われ調査したところ、施設0件のエリアではFAQPage JSON-LDに「Third Place Japanが広尾でセレクトした場所として、があります。」という文法崩壊した文章が出力されるバグを発見した（`venuesByArea`で0件になった場合の分岐が欠落）。214エリア中160エリアに影響しており、まずこのバグ修正を最優先で実施した。

**施設0件ページ向け施策（4点）**：バグ修正後、ユーザーから「セレクト店舗の全ページ表記には時間がかかる。サードプレイスの意味クラスターなどを当初から入れることで検索表示のきっかけにできないか」と提案を受け、以下4点を低リスク施策として実装：①施設0件時の説明文にサードプレイス概念（7軸基準）を追加、②「サードプレイス（第三の居場所）とは何ですか？」FAQを全エリア共通で新設、③施設0件時に上位階層（市区町村ページ）への内部リンクを追加、④CollectionPage JSON-LDにサードプレイス概念のDefinedTerm参照（`mentions`）を追加。都道府県1・市区町村49・エリア176という現状規模を踏まえ、「176エリア全てに個別ライティングは非現実的だが、地名パラメータの差し込みに留めた軽量テンプレートなら安全に展開できる」という設計判断を採った。

**全15業種常時表示**：ユーザーから「広尾エリア×15カテゴリのように、全エリア×15カテゴリを表記してはどうか」と追加提案があり、`select/index.njk`の「全15業種から探す」カードデザインを流用して実装。存在しないページへの直リンクを避けるため、L3エリア×業種→L2市区町村×業種→カテゴリ一覧の順で実在するページへ自動フォールバックする`categoryLinksForArea`フィルターを新設した（詳細は「エリアページのレイアウトルール」セクション参照）。

**FAQ可視化の横展開**：施策実装後、ユーザーから`/stories/area/tokyo/minato-ku/bar-whiskey/`（L2×カテゴリページ）を示され「よくある質問セクションが抜けている。FAQは3つだが4つで合っているか」と指摘を受け調査したところ、`l3-area.njk`以外の4テンプレート（L1・L2市区町村・L2×カテゴリ・L3×カテゴリ）が**FAQPage JSON-LDのみでHTML上に一切表示されていない**という、l3-area.njk修正前と同じ構造的欠落を抱えていることが判明した。あわせてL3×カテゴリ（5問构成）にも、施設0件時に同種の文法崩壊バグが独立して存在すること（46ページに影響）を発見し、修正の過程で合わせて解消した。4テンプレートに`l3-area.njk`と同じ可視FAQアコーディオンを追加し、いずれも「サードプレイスとは」を新設して質問数を統一（L1・L2市区町村・L2×カテゴリは4問、L3×カテゴリは既存5問+1で6問）。

**検証方法**：全316エリアページでJSON-LDパースエラー0件（1,489ブロック）。カテゴリ常時表示のリンク先2,640件（176エリア×15業種）全てが実在ページに解決することをNode.jsスクリプトで機械検証。文法崩壊パターン（「はです。」「として、があります」）はサイト全体で0件になったことを確認。

**ルール化**：「エリアページのレイアウトルール」セクションに以下3点を新規追加した。①「FAQPage JSON-LDには必ず可視のFAQを対応させる」という全ページ共通原則、②施設配列を文字列結合するフィルター（`venuesToAnswerText`等）を使う際は0件時の分岐を必須にするというチェック項目、③新しいエリア関連ページで「全15業種を常時表示」する場合は`categoryLinksForArea`フィルターを再利用する旨。

---

## 2026-07-24

### `/stories/`トップFAQ「東京でおすすめのサードプレイスは？」の回答に固有名詞を追加

**対象ファイル**
- `src/stories/index.njk` — FAQPage JSON-LD・可視アコーディオンの両方（同一テキストの2箇所）

**背景**：外部レビューで、当該FAQの回答が「渋谷・表参道エリアのスペシャルティカフェ」等の一般的な業種・エリア描写のみで固有名詞（店名）を含まず、AI回答エンジンが具体的な店舗名で引用しにくいとの指摘を受けた。

**対応**：`venues.json`に実在する公開施設からGreen Beans Coffee（渋谷区千駄ヶ谷・Flagship実証店舗）・CITAN（日本橋・ホテルラウンジ）・DOTEMA（世田谷区代沢・コワーキング）の3件を選び、回答に実名で組み込んだ。旧回答にあった「明治神宮」はユーザー指示により削除せず、TPJ認証施設の紹介文とは別の一文として残した（TPJが認証したかのような誤解を避けるため文を分離）。

**ルール化**：「❌ 施設名の断定・推薦（description / FAQ / 見出しに特に注意）」に例外①（TPJが実際に認証・セレクトした施設は名指し可）・例外②（明治神宮等の著名な公共ランドマークは地名相当として扱う）を追記した。

---

### エリアページ全体の「認証した場所」表記を「セレクトした場所」に統一

**対象ファイル**
- `src/stories/area/index.njk`・`l1-prefecture.njk`・`l2-city.njk`・`l2-category.njk`・`l3-area.njk`・`l3-category.njk`

**背景**：原宿L3エリアページのスクリーンショットで、ユーザーから「Third Place Japanが認証した場所」という表記への違和感を指摘された。調査の結果、エリアページに掲載中の施設は公開23件中22件がTPJセレクト（申請不要の編集部発掘枠）であり、正式な申請・審査を経た認証（Certified〜Flagship）ではないことが判明。「認証した場所」は事実と異なる表現だった。

**対応**：L1（都道府県）〜L3（エリア）・カテゴリページ・エリアトップ（`index.njk`）まで、施設に言及する全箇所（サブタイトル・FAQ可視部分・FAQPage JSON-LD・CollectionPage/Place description）を「セレクトした場所」表記に統一した。あわせて`l1-prefecture.njk`・`index.njk`で、FAQの質問文（「TPJ認証を受けた」）と回答文（「セレクト店舗」）が一致していなかった既存の内部矛盾も解消した。

**検証方法**：`npm run build`後、対象6テンプレートから生成される`public/stories/area/`配下（約280ファイル）を対象に、JSON-LDのパースエラー0件を確認。

**ルール化**：「エリアページのレイアウトルール」に新規セクション「エリアページの施設言及表記ルール」を追加した。

---

### GBCレビュー記事：「実地調査」表現を実証店舗の実態に合わせて訂正

**対象ファイル**
- `src/stories/concept/green-beans-coffee-review.md`

**背景**：Green Beans Coffeeは覆面調査員が訪問する認証グレードではなく、TPJが開業時から店舗と伴走し継続的に検証する「実証店舗」（Flagshipの特別な位置づけ）。記事本文4箇所に「複数回の実地調査を経て」「実地調査で確認された」「リピート調査（3か月間・複数回訪問）」という、覆面調査を示唆する表現が残っており、記事上部のbyline取材根拠表示（「実証店舗として、開業時から店舗と伴走した継続的な検証に基づく評価」）と矛盾していた。

**対応**：4箇所を「継続的な検証」「伴走」を軸にした表現に統一。冒頭とまとめで「実証店舗」という語を用いて文章をブックエンドし、中間2箇所は同じ趣旨を別表現で言い換えることで単調な反復を避けた。あわせて`modified: 2026-07-24`をfrontmatterに追加。

**ルール化**：「author/editor（E-E-A-T）と取材根拠の表示ルール」に「記事本文中の取材根拠表現も、上記grade_slug別文言と矛盾しないこと」を追加した。

---

### GBC施設ページ：「渋谷店」表記の統一削除、FAQのグループ分割、施設情報カードへの金枠追加

**対象ファイル**
- `src/_data/venues.json`（`body`・`faq`→`faq_groups`・`faq_en`→`faq_groups_en`）
- `src/_layouts/venue.njk`・`venue-en.njk`（`faq_groups`／`faq_groups_en`対応・施設情報カードの外枠追加）
- `src/stories/cafe-specialty-coffee-kitasando-sendagaya.md`・`concept/shibuya-cafe-third-place.md`・`concept/specialty-coffee-shibuya-kitasando-sendagaya.md`・`concept/green-beans-coffee-review.md`（「渋谷店」削除）

**① 「Green Beans Coffee渋谷店の評価レポート」の「渋谷店」削除**：施設ページ本文・関連記事3本・レビュー記事descriptionの計5箇所で表記を「Green Beans Coffeeの評価レポート」に統一した。

**② FAQのグループ分割**：日本語版FAQ13問が「GBCの利用案内」と「実証店舗という制度の説明」の話題が交互に並んでおり読みづらかったため、`faq`（フラット配列）を廃止し`faq_groups`（見出し付きグループ配列）に再構成。「Green Beans Coffeeについて」（4問）「TPJの実証店舗について」（9問）の2グループに分け、各グループ内も自然な流れに並べ替えた。`venue.njk`に後方互換の`faq_groups`対応を追加（他施設は従来通り`faq`のフラット表示のまま）。

**③ 施設情報カードへの金枠追加**：営業時間・価格帯・電話＋設備バッジのカードに外枠がなく浮いて見えていたため、隣接する「実証店舗」セクションと同じ`border border-gold/30`を追加して統一した（全施設共通テンプレートのため、営業時間等を表示する全施設ページに反映）。

**検証方法**：`npm run build`後、対象ページのJSON-LD（FAQPage 13問を含む）のパースエラー0件、他施設ページ（ONIBUS COFFEE等）で`faq`フォールバック表示に影響がないことを確認済み。

**ルール化**：「venues.json 任意フィールド一覧」に`faq_groups`／`faq_groups_en`を追加し、グループ分割の運用ルールを追記。「施設情報カード 設備バッジルール」にカード外枠のルールを追記した。

---

### GBC英語版ページ：施設情報カードへの金枠追加、FAQ拡充（7問→15問）とグループ分割

**対象ファイル**
- `src/_data/venues.json`（`faq_en`→`faq_groups_en`）
- `src/_layouts/venue-en.njk`

**背景**：日本語版と同様の対応を英語版ページにも求められた。加えて、英語版FAQは7問のみで、日本語版が持つ「実証店舗（verification venue）」概念の解説がほぼ存在しなかったため、コンテンツ拡充も行った。

**対応**：施設情報カードに`border-gold/30`を追加（日本語版と同じ処理）。FAQを7問から15問に拡充し、「About Green Beans Coffee」（利用案内・5問）「About Third Place Japan's Certification」（制度解説・10問）の2グループに分割。日本語版の「実証店舗」概念を直訳ではなく英語ネイティブが読んで自然な文章として新規9問に反映した。`venue-en.njk`に日本語版と同様の`faq_groups_en`対応（JSON-LD自動フラット化・グループ見出し付きアコーディオン）を追加。

**検証方法**：`npm run build`後、対象ページのFAQPage JSON-LD（15問）のパースエラー0件、サイト全体2,099ブロックのJSON-LDパースエラー0件を確認済み。

**ルール化**：上記「GBC施設ページ」エントリのルール化（`faq_groups`／`faq_groups_en`・施設情報カード外枠）に統合済み。個別の新規ルール追加はなし。

---

## 2026-07-23

### TPJセレクト一覧FAQのリンク切れ修正・内部リンク/AEO強化、titleタグ・hreflang・E-E-A-Tの外部指摘対応

**背景**：外部レビューから4点の指摘（①構造化データの実装状況確認、②トップページtitleタグの検索意図との乖離、③hreflangの未確認、④著者・E-E-A-T情報の薄さ）を受け、順に検証・対応した。すべて「指摘の言葉を鵜呑みにせず、実際のソース・ビルド結果で裏取りしてから対応する」方針で進めた。

**① 構造化データの実装状況確認・強化**
- FAQPage・BreadcrumbList・Article・LocalBusiness/Review/AggregateRatingは全テンプレートで実装済みと確認
- ただし`article-certified.njk`の`itemReviewed.@type`が常に固定`"LocalBusiness"`のまま`servesCuisine`（FoodEstablishment系専用プロパティ）を出力しており、Schema.org警告の原因になっていた（`venue.njk`側は2026-07-09に同種の問題を修正済みだったが、記事側テンプレートには未反映だった）。`venue.njk`と同じ`foodEstablishmentTypes`／`businessType`マッピングを追加して解消
- `dateModified`が実質機能していない問題を発見：`modified`frontmatterフィールドが1記事も設定されておらず、全記事の`dateModified`が常に`datePublished`と同値になっていた。`modified`フィールドの運用ルールを新設し、`article-area.njk`にも同フィールドのフォールバックを追加（既存は`article.njk`・`article-certified.njk`のみ対応済みだった）

**② トップページ（`/stories/`）のtitleタグ修正**
- title「場所について、読む。」がH1「日本のサードプレイスを知る」・meta descriptionの「日本のサードプレイスを探す」と乖離していたため、「日本のサードプレイスを探す」に統一

**③ hreflangの自己参照バグ修正**
- JA/EN双方に存在する記事21ペア全件で、自己参照タグ（`hreflang="ja"`が自分自身を指すタグ等）が欠落し、相手言語への一方向ポインタのみになっていた（Googleのhreflang仕様違反）
- 加えて`article.njk`が同内容のタグを`<body>`内に重複出力しており、`<head>`外のため無効な記述だった
- `base.njk`側のロジックを自己参照込みに統一し、`article.njk`側の重複ブロックを削除

**④ 著者・E-E-A-T情報の強化**（複数ラウンドに分けて対応）
1. JSON-LD `author`（Organization）に加えて`editor`（Person: 加瀬俊幸／編集長）を新設。`author.url`もサイトトップから編集部プロフィール（`/about-company/#editorial-team`）に変更。対象：`article.njk`・`article-certified.njk`・`article-area.njk`・`venue.njk`・`venue-en.njk`
2. 記事本文・施設ページの可視byline「サードプレイスジャパン編集部」をプロフィールページへのリンクに変更。フッターへの同種リンク新設は一度実施したが、ユーザー判断で撤回・削除
3. `category_slug: about`記事14本（サードプレイスガイド）は日付・著者行自体が非表示という既存設計だったため著者情報が皆無だった。「evergreenな見せ方」を維持するため**日付は表示せず**、編集部リンクのみの行を新設
4. 施設ページ（`venue.njk`/`venue-en.njk`）は元々「編集部」の可視テキストが一切存在しなかった（JSON-LDのみ）。掲載日表示の行にbylineリンクを追加
5. **取材根拠（sourcing basis）をgrade_slug別に明示**：当初「TPJセレクト・認証施設は一律で覆面調査員による現地取材」という文言案を提示したが、ユーザーから「TPJセレクトは行ってみたい施設であり訪問実績ではない」と事実訂正を受け、根本的に設計し直した。CLAUDE.mdの既存ルール（`#grades`テーブルの審査プロセス表現ルール：Certified/Silverは覆面調査を行わない）とも整合させ、grade_slug別の6パターンに整理
6. **さらにユーザーから「GBCは実証店舗であり、Flagship向け文言『覆面調査員の現地訪問』は実態と異なるのでは」と再度の事実確認**を受け、Flagship（＝現状GBCのみ）向けの文言を「実証店舗として、開業時から店舗と伴走した継続的な検証に基づく評価」に修正。この経緯から、`flagship`の文言が「実証店舗」を前提にした特別なものであり、将来的に非・実証店舗のFlagship施設が追加された場合は再修正が必要という注意点が判明した
7. 「執筆者が実質1名体制であること」はGoogleのE-E-A-Tが問うのは人数ではなく実在の専門性・経験の有無であるため、実名プロフィール・経歴・取材根拠の明示により対応済みと判断しクローズ。「プロフィールに顔写真・外部実績（登壇・寄稿・資格等）がない」点は実データが必要なため未着手のまま残す

**検証方法**：全ラウンドで`npm run build`後、サイト全体（`public/stories/`・`public/en/stories/`配下）のJSON-LDを機械的に`JSON.parse`し、最終的に2,091ブロック・エラー0件を確認。hreflang修正後は21ペア全件で自己参照＋相互リンク＋x-defaultが`<head>`内に揃うことを確認。

**ルール化**：「JSON-LD実装ルール」セクションに「author/editor（E-E-A-T）と取材根拠の表示ルール」を新規追加し、grade_slug別文言表と、flagship文言が実証店舗を前提にしている旨の注意書きを明記した。「フロントマターのテンプレート」に`modified`フィールドのルールを追加済み（本ログの③より前の対応、詳細は該当セクション参照）。

---

### 7軸評価スコアのAEO確認：既に数値表示済みと判明、ドキュメント側の誤記を修正

**背景**：外部から「7軸評価のスコアを記事内に表や数値として明示すると、AIが直接引用しやすくなる。現状は文章内に埋め込まれている可能性が高い」との指摘を受け、実際のテンプレート・ビルド結果を確認した。

**確認結果**：指摘とは逆に、スコアは既に数値として明示されていた。
- `venue.njk`「空間の7基準」セクション：全グレード共通・無条件で7軸すべてを0〜10のバー＋数値表示（TPJセレクトも含む）
- `article-certified.njk`「TPJ 7軸評価スコア」セクション：認証施設レビュー記事本文にも同様の数値一覧が実装済み
- 両テンプレートともReview JSON-LDの`additionalProperty`（PropertyValue形式）にも同じスコアが構造化データとして出力済み

**発見した矛盾**：確認の過程で、`src/stories/about/tpj-7-axis-evaluation-guide.md`に「TPJセレクトでは◎・○・×の3段階簡易評価にとどめ、数値化は行っていません」という記述があり、実装（TPJセレクトも0〜10点表示）と矛盾していることが判明した。

**対応**：数値スコア表示はAEO上望ましい実装のため、ドキュメント側を実装に合わせて修正する方針をユーザーに確認の上で決定。該当記事の本文・FAQ回答2箇所を「施設ページでは全グレード共通で0〜10点のスコアを公開しており、Platinum以上ではさらにレーダーチャート表示まで踏み込む」という実態通りの記述に修正した。

**検証方法**：`npm run build`後、修正箇所が正しく反映されていること、対象記事のJSON-LD 4ブロックにパースエラーがないことを確認済み。

**ルール化**：この一件は「ドキュメントとコードの記述が食い違った場合、外部からの指摘であっても鵜呑みにせず実際のテンプレート・ビルド結果を必ず確認してから対応する」という、本CLAUDE.md冒頭からの一貫した進め方を裏付ける事例として記録する。新しいルール項目の追加は行わない（既存の「答えは必ず実装を見てから」という運用姿勢の実例）。

---

## 2026-07-22

### TPJセレクト一覧ページ（`/stories/select/`）のSEO/AEO強化

**対象ファイル**
- `src/stories/select/index.njk` — 全面改修

**背景**：一覧ページのtitle・H1に「サードプレイス」「third place」が一切含まれておらず、太字リード・まとめが存在せず、FAQもJSON-LDのみで画面には非表示（可視アコーディオンなし）だった。また全15業種を横断掲載しているにもかかわらず、業種名を列挙する箇所がページ内に一つもなかった。

**対応**：
1. title・H1・太字リード・まとめ・FAQの4箇所に「サードプレイス」「third place」を自然に配置（本ルールは記事だけでなく一覧・ハブ系ページにも適用されることを明確化）
2. FAQを3問→5問に拡充し、`src/_data/categories.json`と揃えた業種名を含む問いを新設。あわせてJSON-LDのみだった構成を改め、`selectFaq`という単一のNunjucks配列をJSON-LD／可視アコーディオンの両方が参照する構成にした（データの二重管理・表示とJSON-LDの内容不一致を防ぐ）
3. 全15業種（`categories`グローバルデータ）をカードグリッドで新規表示し、各業種ハブページへリンク。日本語名・英語名を併記
4. ページ内コメントを削減し、可読性を優先した構成に整理

**副次的に発見・修正した問題**：
- FAQPage JSON-LDに「Certified・**Standard**・Silver・Gold・Flagship」という誤記が残っていた（正しくはStandardではなくPlatinum。2026-07-20に`about/index.njk`側では同じ誤記を修正済みだったが、このページには反映されていなかった）。`src/`全体を再検索し、他に同じ誤記が残っていないことを確認済み
- グレード凡例バーの表示順が、ドキュメント化されたルール（高グレード→低グレード＝Flagship→Certified）と逆（Certified→Flagship）になっていた
- ページ全体に`data-pagefind-body`が設定されておらず、サイト内検索の対象外だった（Pagefindのインデックス対象ページが100→101に増加したことを確認）
- `ItemList`のJSON-LD `description`が`dump | safe`を通さず直接出力されていた（「Nunjucksテンプレートでの動的テキスト出力」ルール違反）

**検証方法**：`npm run build`後、JSON-LD計4ブロック（BreadcrumbList・ItemList 23件・FAQPage 5問）のパースエラー0件、内部リンク61本中リンク切れ0件を確認。

**ルール化**：本セクション自体がログ兼ルールとして機能する。今後、記事以外の一覧・ハブ系ページ（`{category}/index.njk`、`area/l2-city.njk`等）を新規作成・改修する際も、記事と同様にtitle・太字リード・まとめ・FAQ・`data-pagefind-body`の欠落がないか確認すること。

---

### Green Beans Coffee 英語版：写真・7基準レーダーチャート・タグの追加

**対象ファイル**
- `src/_layouts/venue-en.njk` — 施設写真・施設情報カード（営業時間/価格帯/電話+設備バッジ英訳）・7基準レーダーチャート・タグセクションを新設
- `src/_data/venues.json` — `tags_en`（25件、`tags`の英訳）を新設

**背景**：日本語版施設ページには存在する「写真」「7基準レーダーチャート」「サードプレイス関連タグ」「施設情報カード（営業時間・価格帯・電話+設備バッジ）」が英語版には一つも実装されていなかった。

**対応**：日本語版`venue.njk`の該当ロジック（`hasDedicatedImage`判定、`gradeLevels`/`thisLevel`によるレーダーチャート表示条件、グレード別タグ上限数）をそのまま英語版に移植した。写真は`venue.image`が言語共通フィールドのため新規データ追加は不要。設備バッジ（自家焙煎コーヒー専門店・Wi-Fi・電源完備等）は英語ラベルに翻訳した。

**検証方法**：`npm run build`後、施設写真・レーダーチャート・タグ25件・設備バッジ6種すべての描画をHTML上で確認。JSON-LD 5ブロックのパースエラー0件を確認。

**ルール化**：「venues.json 任意フィールド一覧」に`tags_en`と、写真・レーダーチャートが日本語版と共通ロジックで英語版にも表示できる旨を追記した。

---

### Green Beans Coffee 英語版：カテゴリ・エリア・最寄駅の日本語残存を修正

**対象ファイル**
- `src/_data/venues.json` — `category_en`・`area_primary.area_en`・`area_primary.prefecture_en`・`nearest_stations_en`を新設
- `src/_layouts/venue-en.njk` — カテゴリバッジ・サブタイトル・aside・LocalBusiness/Review JSON-LD住所を英語フィールド参照に変更（未設定時は日本語にフォールバック）

**背景**：ユーザーから提供されたスクリーンショットで、カテゴリバッジ（カフェ・スペシャルティコーヒー）・エリア表記（千駄ヶ谷、東京都）・最寄駅（北参道駅／千駄ヶ谷駅）が英語版ページに日本語のまま表示されている箇所を指摘された。原因は`venue-en.njk`が`venue.category`・`venue.area_primary.area`等の日本語専用フィールドを英語版ページでもそのまま参照していたため。

**対応**：`src/_data/categories.json`・`src/_data/areas.json`に既に存在する`name_en`と同じ値を、GBCのvenueオブジェクトに直接`category_en`等として追加し、テンプレート側は`{{ venue.category_en or venue.category }}`のように未設定時フォールバック付きで参照するよう変更した。

**検証方法**：`npm run build`後、カテゴリバッジ・サブタイトル・aside・最寄駅の表示が英語になっていること、LocalBusiness/Review JSON-LDの`addressLocality`/`addressRegion`が"Sendagaya"/"Tokyo"になっていることを確認。ページ内に残る唯一の日本語文字列は`maps_url`（Googleマップ検索クエリの一部）のみで、表示テキストではないため対応不要と判断。

**ルール化**：「venues.json 任意フィールド一覧」に`category_en`・`area_primary.area_en`/`prefecture_en`・`nearest_stations_en`と、日本語専用フィールドを英語版テンプレートで直接使い回さない旨のルールを追記した。

---

### Green Beans Coffee 日本語版：施設ページ・レビュー記事のSEO/AEO強化

**対象ファイル**
- `src/_data/venues.json` — GBC（`green-beans-coffee-shibuya`）に`title_override`新設、`body`冒頭を`<strong>`化、レビュー記事へのリンク追加、FAQ1問にキーワード追記
- `src/stories/select/venue.njk` — `title_override`対応（後方互換・未設定なら従来通り）
- `src/stories/concept/green-beans-coffee-review.md` — 太字リード・まとめに「third place」追加、FAQを3問→5問に拡充（「サードプレイスとして認証されていますか」「そもそもサードプレイスとは」を新設）、施設ページ・8条件記事へのリンク追加
- `src/stories/concept/shibuya-cafe-third-place.md`／`specialty-coffee-shibuya-kitasando-sendagaya.md`／`src/stories/cafe-specialty-coffee-kitasando-sendagaya.md` — 施設ページ・レビュー記事への逆リンク追加
- `src/_layouts/article-certified.njk` — 存在しない`/stories/about/third-place-7-axes/`へのリンク切れを`/stories/about/tpj-7-axis-evaluation-guide/`に修正（全認証記事に影響する共通バグ）

**背景**：ユーザーからGBCの施設ページ・レビュー記事について、「サードプレイス」「third place」をtitle・太字・まとめ・FAQの4箇所に自然に含めてSEO/AEOを強化する依頼を受けた。調査の結果、施設ページのtitleタグ（`venue.njk`の共通フォーマット由来）にはキーワードが一切含まれておらず、かつ施設ページとレビュー記事の間に相互リンクが一本も存在しないという2つの構造的な欠落が判明した。

**対応**：`title_override`フィールドを新設し個別施設のtitleを上書き可能にした（詳細は「venues.json 任意フィールド一覧」参照）。施設ページ・記事ページ双方のtitle／太字リード／まとめ／FAQの4箇所にキーワードを配置し、双方向リンクおよび周辺3記事からの逆リンクを追加した。検証中に`article-certified.njk`のリンク切れも発見・修正した。

**検証方法**：`npm run build`後、対象5ページのJSON-LD計パースエラー0件、全内部リンクのリンク切れ0件（修正前1件）を確認。AEOキーワード4箇所（title／太字リード／まとめ／FAQ）を両ページで確認済み。

**ルール化**：「施設ページ（venue）のtitleタグルール」に`title_override`の説明を追記し、旧「現状は未対応」の記述を更新した。「venues.json 任意フィールド一覧」を新規セクションとして追加した。

---

### Green Beans Coffee 英語版：施設ページのコンテンツ拡充・SEO/AEO強化

**対象ファイル**
- `src/_data/venues.json` — GBCに`title_override_en`・`body_en`（7段落）・`faq_en`（7問）を新設、`citation_en`の事実誤り（実際はFlagshipなのに「a TPJ Select pick」と誤記）を修正し「third place」を含む文言に書き直し
- `src/_layouts/venue-en.njk` — 本文を空の`content`ではなく`venue.body_en`を参照するよう修正、FAQセクション＋FAQPage JSON-LD新設、Review + reviewRating JSON-LD新設、太字化、営業時間・価格帯・電話・地図リンクをaside情報に追加、存在しない`/en/stories/select/`へのパンくずリンクを`/stories/select/`にフォールバック修正
- `src/stories/select/venue-en.njk` — `title_override_en`対応、`| safe`追加（二重エスケープ修正）
- `src/stories/select/venue.njk` — 同様に`| safe`追加（日本語版側の潜在バグ予防、実害なし）
- `src/en/stories/concept/what-is-third-place-japan.md`／`tokyo-third-place-guide-travelers.md` — 施設ページへの逆リンク追加

**背景**：日本語版に比べて英語版施設ページ（`/en/stories/select/green-beans-coffee-shibuya/`）のコンテンツが薄いとユーザーから指摘があった。調査の結果、`venue-en.njk`が本文表示に`{{ content | safe }}`（そのページファイル自身の、常に空のMarkdown本文）を参照しており、`venues.json`側に英語本文・FAQを格納するフィールド自体が存在しなかったことが根本原因と判明した。またFAQセクション・FAQPage JSON-LDが英語版には一切実装されていなかった。

**対応**：`body_en`・`faq_en`という新しい任意フィールドを設計し、日本語版と同等の深さ（7段落・7問）の英語コンテンツを作成した。`venue-en.njk`を全面的に拡張し、日本語版に存在する主要機能（FAQアコーディオン、Review構造化データ、実用情報の充実、太字リード）を英語版にも実装した。

**副次的に発見した問題**：
1. **二重エスケープバグ**：`citation_en`に含めた`Tokyo's`のアポストロフィが、`eleventyComputed`と`base.njk`の2段階レンダリングで`Tokyo&amp;#39;s`と壊れて表示される不具合を発見。原因と対処は「eleventyComputed内の`{{ }}`補間による二重エスケープ」セクション参照
2. **パンくずのリンク切れ**：英語版のパンくず（表示・JSON-LD BreadcrumbList両方）が、生成されていない`/en/stories/select/`（英語版TPJ Select一覧ページは現状1施設のみのため未作成）を指していた。日本語版一覧`/stories/select/`へのフォールバックに変更して解消（英語記事一覧が存在しない場合に日本語版へフォールバックする、サイト既存の慣例に合わせた）

**検証方法**：`npm run build`後、対象4ページのJSON-LD計パースエラー0件（FAQPage 7件正常抽出）、全内部リンクのリンク切れ0件を確認。AEOキーワード4箇所（title／太字リード／まとめ／FAQ）を確認済み。descriptionを英語ルール（150〜160字）に合わせて159字に調整した。

**ルール化**：「venues.json 任意フィールド一覧」に`body_en`・`faq_en`と、英語版施設ページを新規に有効化する際の運用ルールを追記した。「eleventyComputed内の`{{ }}`補間による二重エスケープ」を新規セクションとして追加した。

---

## 2026-07-21

### 全74本の日本語記事で「サードプレイス」概念語の4箇所チェックを実施・完全対応

**背景**：日本語検索・AI引用の両方で最重要語である「サードプレイス」「第三の居場所」が、タイトルには含まれていても本文側（meta description・本文冒頭・まとめ本文・FAQ）には登場していない記事が多数存在することが判明した。特にまとめ・FAQでは「サードプレイスジャパン（Third Place Japan）」というブランド名の部分文字列としてしか出現せず、概念語としての実質的な出現がゼロという記事が目立った。

**対応**：全74本（`about`配下2本を含む）を対象に、meta description・本文冒頭（太字リード文）・まとめ本文（結びのブランド名文を除く）・FAQ（最低1問）の4箇所を機械監査するNode.jsスクリプトを作成し、欠落記事を洗い出した上で、優先度順（4項目全欠落→3項目欠落→2項目欠落→1項目欠落）に段階的に修正した（杉並区・三鷹市・立川市の3本、優先度1・2の15本、優先度3・4の26本、計44本を編集）。修正は既存の語順・文意を保ったまま「居場所」→「サードプレイス」の置換、または文末への自然な1文追加で行い、1記事あたり本文全体で4〜6回程度・meta descriptionは1回という詰め込み防止の目安を設けた。

**副産物として発見した構造バグ**：`## まとめ`見出しの直前に区切り線`---`を置く構成の記事で、`---`の直前に空行がないと、Markdown（Setext見出し記法）が直前の段落をその`---`の下線付き見出しとして誤変換し、FAQ回答の最終文と`## まとめ`の本文が同一の`<h2>`タグに融合してレンダリングされる不具合を発見した（`coworking-omotesando.md`で最初に発覚）。全記事を機械スキャンし、同じパターンを持つ11記事（`bar-whiskey-minato.md`・`bar-whiskey-roppongi.md`・`cafe-specialty-coffee-shibuya.md`・`coworking-omotesando.md`・`coworking-shibuya.md`・`premium-salon-omotesando.md`・`premium-salon-shibuya.md`・`restaurant-dining-azabujuban.md`・`restaurant-dining-minato.md`・`restaurant-dining-omotesando.md`・`restaurant-dining-shibuya.md`）を修正した。`## `見出し自体は直前に空行がなくても正しくレンダリングされる（ATX見出しの仕様）ため、この対象は`---`区切り線に限られる。

**検証方法**：`npm run build`後、サイト全体433ページ・JSON-LDブロック1,981個をNode.jsで機械的に`JSON.parse`し、パースエラー0件を確認。Setextバグ修正11本すべてで`<h2>まとめ</h2>`が独立レンダリングされることを確認。編集した全description（44本）が110〜120字ルール内に収まることを確認。最終横断監査で全74本が4箇所すべてクリアであることを確認済み。

**ルール化**：「必須キーワード（全記事に含める）」に4箇所チェックの詳細を追記。「まとめセクションは任意」の項目に、区切り線`---`直前の空行必須ルール（Setext見出し化バグ）を追記した。

---

### TPJセレクト施設（19件）のJSON-LDに`geo`が出力されていない不具合を修正

**対象ファイル**
- `src/_layouts/venue.njk` — LocalBusiness JSON-LDの`geo`出力条件を変更

**背景**：`geo`（GeoCoordinates）の出力条件が、sameAs・openingHours等と同じ`{% if thisLevel >= 1 %}`（グレードレベル制御）を流用していた。TPJセレクト（`grade_slug: "select"`）は`gradeLevels`に該当キーがなく`thisLevel = 0`になるため、`lat`/`lng`を保持しているにもかかわらず全施設で`geo`がJSON-LDから欠落していた。

**対応**：出力条件を`{% if thisLevel >= 1 %}` → `{% if venue.lat and venue.lng %}`に変更。位置情報はグレードで段階的に開示する拡張プロパティではなく施設を一意に識別する基本情報のため、グレード条件から独立させた。

**検証方法**：`npm run build`後、公開中の全23施設ページ（TPJセレクト22件＋認証施設）・計115個のJSON-LDブロックをNode.jsで`JSON.parse`し、パースエラー0件かつTPJセレクト22件全件に`geo`ブロックが出力されていることを確認済み。

**ルール化**：「LocalBusiness の検索最適化プロパティ」セクションに「geo（グレード非依存）」を新規追加し、`geo`のみグレード条件の対象外であることを明記した。

---

### SEO/MEO/AEO追加監査：TPJセレクト施設ページのキーワード欠落・関連記事導線の欠如を修正

**対象ファイル**
- `src/_data/venues.json` — TPJセレクト施設19件のcitationを書き直し
- `src/_layouts/venue.njk` — 「エリアの関連記事」セクションを新規追加

**背景**：直近の全記事監査（2026-07-19〜20）に続き、施設個別ページ（TPJセレクト・認証施設）まわりを追加監査した。公開中23施設のうち19件（TPJセレクトのほぼ全て）で、citation・FAQ・title・meta descriptionのいずれにも「サードプレイス」「第三の居場所」が一語も含まれていないことが判明した（JSON-LD著者名「サードプレイスジャパン編集部」にのみ登場、画面には非表示）。あわせて`venue.njk`を確認したところ、施設ページからエリア関連記事への内部リンク導線が一切なく、エリアページ→施設一覧（既存）の片方向のみが機能していた。

**対応①：citation書き直し（19件）**
各施設固有の文脈・エピソードは維持したまま、「サードプレイス」（カタカナ）と英語表記「third place」の両方を自然な形で含めるよう書き直した。citationバリエーション化ルール（同一フレーズの使い回し禁止）を守るため、19件それぞれ個別に文章を作成（テンプレート的な定型文の一括挿入はしていない）。100〜150字ルールの範囲内に収め、150字を超過した3件（switch-coffee-tokyo-meguro等）はトリムして調整した。

**対応②：施設ページに「関連記事」セクション新規追加**
`venue.njk`の「同じエリアの{{category}}」セクション直後に、`articlesByArea`フィルターでエリア一致記事を最大3件表示する「{{エリア名}}の関連記事」セクションを追加。全グレード共通テンプレートのため23施設全件に自動適用される。該当記事がないエリア（23施設中12施設が該当）では自然に非表示。

**実装上の注意点（今後の同様作業への教訓）**：venues.jsonの一括更新は当初`JSON.parse`→`JSON.stringify(data, null, 2)`の全体再シリアライズで行ったが、既存ファイルは配列を1行にまとめて書いている箇所（`"tags": ["a", "b"]`等）が多数あり、再シリアライズすると整形ルールの違いで対象19件以外に772行もの意図しない差分が発生した。**venues.json等、改行・インライン配列表記が混在する既存JSONファイルの一部だけを書き換える場合は、JSON.parse/stringifyによる全体再シリアライズを避け、対象の文字列のみを生テキストの文字列置換（置換前に一致件数が1件であることを確認）で更新すること。** 今回はこの方式に切り替え、`git diff --stat`で19行の変更のみに収まることを確認した。

**検証方法**：`npm run build`後、対象19施設のビルド済みHTMLに新citationが反映されていること、関連記事セクションが記事のあるエリア（例：表参道）で正しくレンダリングされ記事のないエリアでは非表示になることを、Node.jsスクリプトとgrepで機械的に確認。23施設全件のJSON-LD（LocalBusiness/Review/BreadcrumbList等）をNode.jsで再パースしエラー0件を確認済み。

**ルール化**：「citation（紹介文）バリエーション化ルール」にサードプレイス/third place併記の必須化を追記。「施設ページの関連記事セクション」を新規セクションとして追加。

---

## 2026-07-20

### 英語記事descriptionの文字数ルールを新設（150〜160字）＋EN22本を書き直し

**対象ファイル**
- `CLAUDE.md`（「フロントマターのテンプレート」の`description`文字数ルール）・`docs/TPJ英語版制作憲法.md`（frontmatter規約）
- EN17本のdescription書き直し：`bar-whiskey/chuo.md`・`minato.md`・`shinjuku.md`、`cafe-specialty-coffee/chiyoda.md`、`concept/meiji-jingu-third-place-tokyo.md`・`tokyo-third-place-guide-travelers.md`・`what-is-third-place-japan.md`、`culture-sports-leisure/koto.md`・`shibuya.md`、`hotel-lounge/chiyoda.md`・`chuo.md`、`members-lounge/chuo.md`、`restaurant-dining/chuo.md`・`minato.md`・`shinjuku.md`、`retreat-zen/bunkyo.md`、`shrine-temple/bunkyo.md`（残り5本は元々150〜160字の範囲内で書き直し不要だった）

**背景**：EN22本のdescriptionを実測したところ全件が147〜220字で、110〜120字（日本語向け）のルールがそのまま適用され続けていた実態と乖離していた。日本語のdescription文字数ルールは全角文字のPC表示崩れを基準に決めたものであり、英語のmeta descriptionはGoogleのSERP上で概ね155〜160字（約920px）まで表示されるのが一般的な目安のため、そのまま120字に削ると情報密度が落ちすぎると判断し、英語専用の基準（150〜160字）を新設した。

**対応**：まず`CLAUDE.md`・`docs/TPJ英語版制作憲法.md`に英語専用ルール（150〜160字）を追記。次にEN22本を実測し、17本（174〜220字）を150〜160字に書き直した。5本（147〜160字）は既に範囲内・境界近辺だったため変更しなかった。

**検証方法**：Node.jsスクリプトでEN22本全件のdescription文字数を再計測し、全件150〜160字に収まることを確認。`npm run build`後、EN配下24ページのJSON-LD（計92ブロック）にパースエラーがないことを確認済み。

**ルール化**：「フロントマターのテンプレート」の`description`文字数ルールを日本語（110〜120字）・英語（150〜160字）に分離して明記した。

---

### 内部リンク健全性の定期監査を運用ルール化（実装は行わず、ルールのみ追記）

**対象ファイル**
- `CLAUDE.md` — 「内部リンク（AEO対策・毎回必須）」の直下に「構造変更時のリンク健全性チェック」を新規セクションとして追加

**背景**：フェーズ4（内部リンク再点検）・孤立ページ修正（`daikanyama/luxury-hotel-lounge`）を経て、「同様の孤立ページ・リンク切れが`venues.json`／`areas.json`のスラッグ変更時に静かに発生する」という再発リスクが指摘された。2026-07-09の実装ログに残っていた「同様の孤立ページが他にも残っている可能性があり、別途棚卸しが必要」という未対応メモは、フェーズ4での棚卸しで一度消化されたが、一度きりの対応では次の構造変更で同じ問題が再発する。

**対応**：チェックの自動化・スクリプト化は行わず、「エリア追加・スラッグ変更・カテゴリ再編などの構造変更を行った直後に、簡易リンクチェック（リンク切れ検証＋孤立ページの簡易検出）を実施する」という運用ルールをCLAUDE.mdに明文化するに留めた。ユーザーからは「ルール化のみ」の指示だったため、チェックスクリプトの常設・CI組み込み等は今回のスコープ外とした。

**ルール化**：「内部リンク（AEO対策・毎回必須）」の直下に「構造変更時のリンク健全性チェック」を新規追加し、実施タイミング（areas.json/venues.json/categories.jsonの変更、permalink変更、大規模レイアウト変更）とチェック内容（リンク切れチェック・孤立ページの簡易検出）を明記した。

---

### description境界超過6記事の是正＋「まとめ」未設置22記事への追加

**対象ファイル**
- description境界超過6記事：`restaurant-dining-azabujuban.md`・`bar-whiskey-kabukicho.md`・`restaurant-dining-kagurazaka.md`・`restaurant-dining-shin-okubo.md`・`retreat-zen/yanaka.md`・`culture-sports-leisure/ueno.md`
- 「まとめ」追加22記事：JA19本（`cafe-specialty-coffee-kitasando-sendagaya.md`・`-koenji.md`・`-omotesando.md`、`concept/`配下6本、`culture-sports-leisure/taito.md`・`ueno.md`、`inbound-experience/asakusa.md`・`taito.md`、`retreat-zen/bunkyo.md`・`taito.md`・`yanaka.md`、`shrine-temple/bunkyo.md`・`taito.md`）、EN3本（`en/stories/concept/meiji-jingu-third-place-tokyo.md`・`tokyo-third-place-guide-travelers.md`・`what-is-third-place-japan.md`）

**背景**：前セッションの監査レポート（本ログに未記載の会話ベースの引き継ぎ）で「description境界超過6記事」「まとめ未設置38記事（EN17本含む）」という指摘を受けたが、着手前にNode.jsスクリプトで現在のリポジトリ状態を再検証したところ、後者の実数は38本ではなく**22本（JA19本・EN3本）**だった。EN22本中19本は既に`## In Summary`見出しを設置済みで、未設置は3本のみ（報告された「EN17本未設置」とは一致しなかった）。引き継ぎメモの数値は鵜呑みにせず、実行前に必ず現状のファイルを機械的に再確認する方針で対応した。

**description境界超過6記事の対応**：110〜120字ルールに対して108〜125字だった6記事の本文を書き直し、全記事110〜120字に収めた。

**「まとめ」未設置22記事の対応**：22記事を精査した結果、実態は3パターンに分かれていた。
1. **見出し欠落＋段落融合バグ（4記事）**：まとめ相当の文章はFAQ直下に存在するが、見出しも空行もなく直前のFAQ回答と同一段落としてレンダリングされていた。`## まとめ`見出し＋空行を追加して分離した
2. **完全に未設置（9記事）**：FAQで終わっており、まとめの内容自体が存在しなかった。3文構成（核心の要約＋TPJ7軸評価への自然な言及）で新規執筆した
3. **関連記事リストのみで本文まとめがない地域ピラー記事（9記事）**：FAQの後に「関連記事：」箇条書きが続くのみだった。既存の同種記事（`culture-sports-leisure/koto.md`等）の配置慣例に合わせ、FAQと関連記事リストの間に`## まとめ`を新設した

EN3本は`## In Summary`見出しで対応し、既存の関連記事リンク段落はそのまま残して直前に要約文を追加した。

**検証方法**：Node.jsスクリプトで全93記事（JA71・EN22）の`## まとめ`／`## In Summary`見出しの有無を機械的に再チェックし、未設置0件を確認。`npm run build`後、対象22記事のビルド済みHTMLで見出しタグ（`<h2>まとめ</h2>`等）が正しく分離レンダリングされていること、JSON-LD（計88ブロック）にパースエラーがないことを確認済み。

**ルール化**：「まとめセクションは任意」の項目に、見出し・空行の必須化と配置順（FAQ→まとめ→関連記事）を明記した。

---

### 内部リンク再点検（フェーズ4）：業種×エリア記事32本のリンク健全性を検証

**対象ファイル**
- `src/stories/restaurant-dining-minato.md` — インバウンド節に[東京のインバウンド旅行者向けサードプレイスガイド](/stories/inbound-experience/tokyo-inbound-third-place-guide/)へのリンクを追加
- `src/stories/restaurant-dining-shibuya.md` — 冒頭の街紹介節に[渋谷区でカフェをサードプレイスにする](/stories/cafe-specialty-coffee/shibuya/)へのリンクを追加

**背景**：フェーズ1・2で本文を再構成した32記事について、内部リンクが不自然にならないか・リンク切れが発生していないかを機械的に再点検した（フェーズ1・2提案時に予告していたフェーズ4）。

**検証内容と結果**
1. **リンク切れチェック**：32記事の全内部リンクが実際に存在するページを指しているか検証 → 0件（リンク切れなし）
2. **発リンク数チェック**：32記事それぞれが3〜4本の異なる既存記事にリンクしているか検証 → `restaurant-dining-minato.md`・`restaurant-dining-shibuya.md`の2記事のみ、同じリンク先を2回貼ることで合計本数を水増ししており、実質2記事にしかリンクしていなかった。それぞれ1本ずつ新規リンクを追加し是正
3. **被リンク数チェック**：32記事それぞれが他記事から2本以上リンクされているか検証 → 全32記事が基準を満たしていることを確認（対応不要）

**検証方法**：Node.jsスクリプトで各記事の`](/...)`形式リンクを抽出し、リンク先ファイルが`public/`に実在するか、ユニークなリンク先数はいくつか、逆に他の全記事（JA・EN）からその記事へのリンクが何本あるかを機械的に集計。修正後、`npm run build`とJSON-LD再検証（対象2記事4ブロック）でエラーがないことを確認済み。

**ルール化**：「内部リンク（AEO対策・毎回必須）」に、リンク本数は`<a href>`の出現回数ではなくユニークなリンク先URL数でカウントする旨を明記した。

---

### 構造化データ監査（フェーズ3）：画像サイズ実測値化＋L3エリアページのJSON-LDエスケープ不備を修正

**対象ファイル**
- `.eleventy.js` — `imageDimensions`フィルター新規追加（WebPヘッダーから実際のwidth/heightを読み取る。VP8/VP8L/VP8Xの3形式に対応）
- `src/_layouts/article.njk`・`article-certified.njk`・`article-area.njk`・`src/stories/about/index.njk` — `image.width/height`の固定値`800/450`を`imageDimensions`フィルター経由の実測値に置き換え
- `src/stories/area/l3-area.njk` — `hasPart`ブロックの`| dump`に`| safe`を追加（4箇所）

**背景**：フェーズ1・2に続く「業種×エリア記事32本の構造化データ拡張余地の確認」の過程で、32本自体には問題がない一方、**サイト全体（91記事・93サムネイル）に影響する2件の実害バグ**を発見した。

**発見した問題①：画像サイズの虚偽申告**：Article/BlogPosting JSON-LDの`image.width/height`が、実際のサムネイル画像のサイズに関わらず全記事で`800×450`（16:9）に固定されていた。サンプル検証の結果、実際は800×533・800×600・800×625等アスペクト比が画像ごとに異なっており、宣言値と実寸が一致していたのは偶然16:9だった一部の画像のみだった。Unsplashダウンロード時の`w=800`パラメータは幅のみを固定し、高さ・アスペクト比は元画像のクロップに依存するため、この前提が誤りだった。

**発見した問題②：L3エリアページのJSON-LD構文破損**：`l3-area.njk`の`hasPart`（関連記事・施設一覧）ブロックで`{{ p["@type"] | dump }}`のように`| safe`が欠落しており、既存の「Nunjucksテンプレートでの動的テキスト出力（必須パターン）」ルール違反が発生していた。結果、L3エリアページ27件のJSON-LDが`&quot;`エスケープで構文崩壊し、Googleがこれらのページの構造化データを一切認識できない状態が続いていた。`npm run build`はJSON構文をチェックしないため長期間気づかれなかった。

**対応**：問題①は`imageDimensions`フィルターを新規実装し、画像を伴うJSON-LDを持つ4テンプレートすべてに適用。問題②は該当4箇所に`| safe`を追加。

**検証方法**：Node.jsスクリプトでサイト全体（`public/stories/`・`public/en/stories/`配下）のJSON-LDブロックを機械的に全件抽出しパースを試行。修正前は27件のパースエラーがあったが、修正後は**2,181ブロック中エラー0件**を確認。画像サイズについても72件の`image`ブロックを再検証し、実測値と一致することを確認済み。

**ルール化**：「✅ Article系スキーマの必須フィールド」の`image`項目に実測値化ルールを追記。「✅ Nunjucksテンプレートでの動的テキスト出力（必須パターン）」に今回のインシデント記録と、`| dump`書いたら`| safe`の付け忘れがないか確認する手順を追記。

---

### 全記事監査（AEO準拠度）＋業種×エリア記事32本への答えリード追加・description是正

**背景**：「全記事で日本語はサードプレイス、英語はThird Placeを自然に入れ、SEO/AEO対策・引用/回答獲得の施策を立てて」という依頼を受け、JA69本・EN22本の全記事を機械監査した。タイトルへのキーワード導入（本セクション上部の12記事修正）は既に100%達成済みだった一方、以下2点の構造的な欠落を発見した。

**発見した問題**

| 問題 | 対象 | 深刻度 |
|---|---|---|
| 答えリード（太字の結論文）が本文冒頭に一切ない | 32記事（下記対象ファイル） | 🔴重大：AI引用の起点となる一文が存在しない |
| descriptionが110〜120字ルールを超過（126〜150字） | 23記事（32本のうち大半と重複） | 🟠中：SERP/AI引用スニペットで文末が欠ける |

いずれも「バー・ウイスキー／カフェ・スペシャルティコーヒー／コワーキング／高級ホテル・ホテルラウンジ／会員制ラウンジ／高級サロン／レストラン・ダイニング」という、初期（2026-06-27〜2026-07-02）に作成された「業種×エリア」記事群に集中していた。答えリードルールが本文執筆ルールとして確立する前に書かれた記事群が、ルール制定後も遡及されず放置されていたことが原因。

**対応（フェーズ1）**：32記事すべてに太字の答えリード（1〜2文・冒頭段落の直前に新規追加、既存の書き出しは残す）を追加し、23記事のdescriptionを105〜125字（実質110〜120字目安）に短縮した。

**対象ファイル（32本）**
- バー・ウイスキー：`bar-whiskey-chuo/ginza/kabukicho/minato/roppongi/shinjuku.md`
- カフェ：`cafe-specialty-coffee-akihabara/chiyoda/jimbocho/kitasando-sendagaya/koenji/omotesando/shibuya.md`
- コワーキング：`coworking-omotesando/shibuya.md`
- ホテルラウンジ：`hotel-lounge-chiyoda/chuo/ginza/marunouchi.md`
- 会員制ラウンジ：`members-lounge-chuo/ginza.md`
- 高級サロン：`premium-salon-omotesando/shibuya.md`
- レストラン・ダイニング：`restaurant-dining-azabujuban/chuo/ginza/kagurazaka/minato/omotesando/shibuya/shin-okubo/shinjuku.md`

**検証方法**：Node.jsスクリプトで32記事全件の本文冒頭300〜600字以内における太字（15字以上）の有無、および32記事全件のdescription文字数を機械的に再チェックし、全件が基準内であることを確認。ビルド後、サンプル112ブロックのJSON-LD（Article/BreadcrumbList/FAQPage等）がすべてパース可能なことを確認済み。

**ルール化**：「答えリードを1〜2行置く（AEO必須）」の直下に「既存記事への遡及適用ルール」を追加し、新カテゴリ・記事バッチの大量生成後は必須ルールの遡及適用状況を定期監査するよう明文化した。

---

### 「最新記事」からaboutカテゴリ記事を除外／最新記事セクションのバッジ表示修正

**対象ファイル**
- `.eleventy.js` — `rejectCategory`フィルター新規追加
- `src/stories/index.njk`・`src/stories/latest.njk`・`src/_includes/footer-stories.njk`・`src/_layouts/article.njk`・`article-certified.njk`・`article-area.njk`

**背景**：`/stories/about/`配下の記事（TPJ7軸評価基準ガイド・TPJ認証とセレクトの違いなど）が、トップページの「最新記事」セクションに新着として表示され、通常のエリア/業種記事を押しのけて表示される状態になっていた。当初は各カードの「サードプレイスとは」という固定カテゴリバッジのみを非表示にしたが、ユーザーから「最新記事に入れない。最新記事は通常記事のみ」と修正指示があり、方針を「非表示」から「除外」に変更した。

**対応**：`rejectCategory`フィルターを追加し、「最新記事」を表示するあらゆる箇所（トップページ本体・ItemList JSON-LD・記事一覧`/stories/latest/`・フッターの最新記事枠・各記事ページのサイドバー「最新記事」ウィジェット3種）で`category_slug: "about"`の記事を除外するよう統一した。about記事の一覧・導線は`/stories/about/`（サードプレイスガイド）およびフッター②枠・トップページの「サードプレイスガイド」セクションに一本化されている。

**検証方法**：ビルド後、トップページ・記事一覧・ItemList JSON-LDのいずれにもabout記事のURLが含まれないことを確認。フッターの「サードプレイスガイド」枠には引き続きabout記事が表示されることも確認済み。

---

### タイトルへの「サードプレイス」自然挿入＋description文字数是正（12記事）

**対象ファイル**
- `src/stories/culture-sports-leisure/koto.md`・`shibuya.md`・`taito.md`
- `src/stories/inbound-experience/shibuya.md`・`taito.md`
- `src/stories/retreat-zen/bunkyo.md`・`taito.md`
- `src/stories/shrine-temple/bunkyo.md`・`taito.md`
- `src/stories/concept/specialty-coffee-shibuya-kitasando-sendagaya.md`・`takao-mountain-third-place.md`
- `src/stories/about/tpj-certification-and-select-difference.md`

**背景**：「サイト内の呼称ルール」追加を機に、全記事（JA69本・EN22本）のtitleを機械的に監査したところ、本文には「サードプレイス」または「第三の居場所」が全記事に含まれている一方、**titleに限ると12本（すべてJA）がこの語を一切含んでいない**ことが判明した。該当は「地域名＋業種＋ガイド」型の区ガイド8本（culture-sports-leisure・inbound-experience・retreat-zen・shrine-temple の各shibuya/taito/koto/bunkyo）と、concept配下2本、about配下1本。同時に、このうち9本は`description`が110〜120字ルール（2026-07-19改訂）を超過（117〜151字）していた。

**変更内容**：12記事のtitleに「〇〇のサードプレイスガイド：△△編」または「〜をサードプレイスとして」のように「サードプレイス」を自然に挿入。区ガイド8本は「地域名のサードプレイスガイド：業種編。キャッチコピー」という二段構成に統一した（結果的にtitleが44〜52字とやや長くなるが、この型は元々30〜35字ルールの例外運用だったため許容）。あわせて9記事のdescriptionを110〜120字（110〜115字）に短縮した。

**検証方法**：`npm run build`後、対象12記事の`<title>`・`<meta name="description">`をNode.jsで抽出し、サードプレイス表記の有無とdescription文字数を機械的に確認。JSON-LD（計49ブロック）もすべてパース可能なことを確認済み。

**ルール化**：「タイトル作成のルール」の③体験・目的ワードを改訂し、「サードプレイス」を原則必須の語に格上げした。

---

### 「TPJガイド」呼称を「サードプレイスガイド」に統一＋新記事2本の逆リンク整備

**対象ファイル**
- `src/_includes/nav-stories.njk`・`src/_includes/footer-stories.njk`・`src/sitemap.njk`（コメントのみ）・`src/stories/index.njk`・`src/stories/about/index.njk`・`src/stories/about/certification-grades/index.njk`・`src/stories/concept/ginza-third-place-guide.md`・`public/index.html`（メインサイト直接編集）
- `src/stories/about/tpj-7-axis-evaluation-guide.md`・`src/stories/about/tpj-certification-and-select-difference.md`（前セッションで作成済みの新記事2本）と、既存about記事4本（`third-place-8-conditions`・`what-is-third-place`・`third-place-examples`・`third-place-ibasho-difference`）・`certification-grades/index.njk`に逆リンクを追加

**背景**：`/stories/about/`のサイト内呼称「TPJガイド」（英語文脈では「TPJ Guide」）は認知度ゼロの造語で、日本人の検索語「サードプレイス」にも外国人の検索語「third place」にも一致しないとユーザーから指摘があった。

**変更内容**：ナビ・フッター・パンくず（画面表示＋JSON-LD）・ハブページのtitle/H1・メインサイトFAQリンクなど、サイト全体で繰り返し使われる呼称を「サードプレイスガイド」（英語ラベルは「Third Place Guide」）に統一。個別記事タイトルの一部として使われている「TPJ Guide」（例：英語記事 "What Is a Third Place? The TPJ Guide to..."）は対象外とした（タイトル冒頭に検索語 "Third Place" が既にあるため）。

あわせて、前セッションで作成済みだった新記事2本（TPJ7軸評価基準の完全ガイド／TPJ認証とTPJセレクトの違い）について、CLAUDE.mdの内部リンク必須ルール（新記事→既存3〜4本／既存→新記事2〜3本の逆リンク）のうち未着手だった逆リンク側を実施した。

**副産物として発見した問題**：`public/stories/area/tokyo/shibuya-ku/daikanyama/luxury-hotel-lounge/index.html`が現行の`npm run build`で再生成されない孤立ページと判明（ナビラベルが「TPJガイド」よりさらに古い「読み物」表記のまま残っていた）。該当箇所のテキストのみ修正したが、根本原因（現在のデータ・テンプレートの組み合わせがこのURLを生成しなくなった）は未調査。同様の孤立ページが他にも残っている可能性があり、別途棚卸しが必要。

**ルール化**：「メディア名の表記ルール（SEO対策・必須）」の直後に「サイト内の呼称ルール（検索されない造語を避ける）」を新規セクションとして追加済み。

---

### TPJセレクトのカテゴリ目標比率改訂（カフェ40%→30%）＋施設6件追加

**対象ファイル**
- `CLAUDE.md`・`project-docs/CLAUDE.md`・`project-docs/TPJセレクト掲載基準ルールブック.md` — カテゴリ配分目標を改訂
- `src/_data/venues.json` — 6施設追加（DOTEMA・TatamiWorks・CITAN・studio ai architects・hanare・神楽坂 和らく）
- `src/_data/areas.json` — 代沢・東駒形の2エリアを新規追加

**変更内容**：カフェの目標比率を40%→30%に引き下げ、余剰の10ポイントを宿泊（15%→20%）とコワーキング（10%→15%）に再配分（ウェルネス・その他は据え置き）。あわせてコワーキング0→3件・宿泊2→4件・レストラン0→1件を新規追加し、TPJセレクトを16件→22件に拡充。詳細は「TPJセレクト 掲載仕様」「カテゴリ配分ガイドライン」の各セクションを参照。

---

### `/stories/about/` を「TPJガイド」ピラー記事として再構築

**対象ファイル**
- `src/stories/about/index.njk` — 本文を2,923字のピラー記事に全面改稿

**背景**：ナビ・フッター・パンくず（他ページ側）は以前から`/stories/about/`を「TPJガイド」と呼んでいたが、ページ自身は「サードプレイスとは」表記のままで不一致だった。また`/stories/about/certification-grades/`への導線がこのハブページから存在せず孤立していた。

**変更内容**：H1・パンくずを「TPJガイド」に統一。本文を8セクション構成のピラー記事（結論先出し・FAQ5問・まとめ）に改稿し、既存12記事へのハブ＆スポーク型リンクと、certification-gradesページへの新規導線を設置。本文下に既存12記事を4テーマ別カード一覧として再掲。

**ルール化**：「TPJガイド記事の取得」の直後に「『TPJガイド』ピラー記事ルール」を新規セクションとして追加済み。

---

### TPJガイドページのSEO/AEO追加強化＋既存記事からの逆リンク

**対象ファイル**
- `src/stories/about/index.njk` — Article JSON-LD新規追加・答えリードボックス化・`data-pagefind-body`付与・DefinedTermSet誤字修正
- `src/stories/concept/ginza-third-place-guide.md`／`shibuya-cafe-third-place.md`／`tokyo-museum-art-third-place.md` — 本文から`/stories/about/`への逆リンクを追加

**発見した問題と対応**

| 問題 | 対応 |
|---|---|
| DefinedTermSet「TPJ認証グレード」の説明文が「Certified・**Standard**・Silver・Gold・Flagship」と誤記（正しくは`Standard`ではなく`Platinum`） | 修正済み |
| 冒頭リード文が太字の「答えリード」になっていない（CLAUDE.mdの既存ルール違反） | 既存記事と同じ`.answer-lead`クラス（金の左ボーダー・強調背景）で実装 |
| Article/WebPage系JSON-LDが皆無（datePublished・author・publisher・wordCountなし） | `Article`スキーマを新規追加。`content`変数が使えない`.njk`直書きページのため、本文を`{% set pillarBodyHtml %}...{% endset %}`でキャプチャし`wordCount`フィルタに渡す方式を採用 |
| `data-pagefind-body`がなく、サイト内検索に本文が一切インデックスされていない | 本文ラップdivに付与。ビルド後のインデックス対象が90→91ページに増加したことを確認 |
| 全91記事中、本文からこのハブページへの逆リンクが0本 | 銀座・渋谷カフェ・東京美術館の3記事本文に自然な文脈でリンクを追加 |

**`content`変数が使えないページでのwordCount計算パターン（今後の同様ページで再利用）：**
```njk
{% set pillarBodyHtml %}
  ...本文HTML...
{% endset %}
{# JSON-LD内 #}
"wordCount": {{ pillarBodyHtml | wordCount }},
{# 表示側 #}
{{ pillarBodyHtml | safe }}
```
`article.njk`のようにMarkdownファイルをレイアウトが受け取る構造とは異なり、`.njk`ページ自身に本文を直書きする場合は`content`変数が存在しないため、この capture パターンで代替する。

**検証方法**：`npm run build`後、JSON-LD 5ブロック（Organization/Article/BreadcrumbList/DefinedTermSet/FAQPage）すべてをNode.jsで`JSON.parse`し構文エラーがないことを確認。Pagefindのインデックスページ数が91に増えたことを確認。3記事の逆リンクがビルド後のHTMLに実際に出力されていることを確認。

---

### コミット後の検証ビルドで発見：`pagefind-entry.json`の非決定的差分

コミット・プッシュ後に確認のため`npm run build`を再実行したところ、内容は同一だが`public/pagefind/pagefind-entry.json`内の`languages`オブジェクトのキー順（`en`/`ja`）だけが入れ替わり、`git status`に差分として表示された。`hash`・`page_count`は変化していないため実害のない非決定的な差分と判断し、`git checkout --`で元に戻した。

**ルール化**：「自動生成されるファイル」セクションに注記を追加済み。今後同様の差分が出た場合は`hash`/`page_count`が同一か確認したうえで、実質的な変更でなければ`git checkout -- public/pagefind/pagefind-entry.json`で戻してよい。

---

## 2026-07-19

### /stories/ 全体のSEO・AEO監査、関連記事・前後記事の言語混入バグ修正

**対象ファイル**
- `src/_layouts/article.njk` — 関連記事・前の記事/次の記事に `selectLang(lang or "ja")` を追加
- `src/_layouts/article-certified.njk` — 同上（最新記事・関連記事・前の記事/次の記事の計3箇所）
- `src/_layouts/article-area.njk` — 最新記事に `selectLang(lang or "ja")` を追加
- ビルドにより `public/` 配下91ファイル・`public/pagefind/` を再生成

**監査で発見した問題と対応状況**

| 問題 | 深刻度 | 対応 |
|---|---|---|
| 関連記事・前後記事ナビが言語フィルタなしで`collections.articles`を参照 | 🔴重大・修正済み | 全5箇所に`selectLang`追加。詳細は本セクション上部の「`relatedPostsByCat`・`prevPost`・`nextPost` フィルターも対象」を参照 |
| `meiji-jingu-third-place`・`what-is-third-place`のJA/ENサムネイル共有 | ✅修正済み（2026-07-21確認） | 実装ログには2026-07-01に修正済みと記載があったが、当時は実際のソースが未修正のまま残っていた。2026-07-21にソースを再確認したところ、JA/EN双方とも別ファイルの専用サムネイルに差し替わっており解消済みと判明した |
| `retreat-zen/yanaka.md`の`area_slug: yanaka`が`src/_data/areas.json`に未登録 | ✅修正済み（2026-07-21確認） | 2026-07-21にソースを再確認したところ、`areas.json`（台東区配下）に`yanaka`が登録済みで、`public/stories/area/tokyo/taito-ku/yanaka/`のL3エリアページも生成されていることを確認した |
| `concept/`配下23記事の`description`が115字未満 | ✅修正済み（同日中） | ルールを110〜120字に改訂した上で24記事すべて書き直し済み。詳細は本セクション下部「`concept/`配下24記事のdescription一括改稿」を参照 |
| 内部リンク1本のみの記事17本・被リンクゼロの孤立記事9本 | ✅修正済み（同日中） | 再計測の結果、実数は孤立記事2本・被リンク1本の記事16本だった（一部は本監査以前に別作業で解消済み）。全18記事に内部リンクを追加済み。詳細は本セクション下部「記事間の内部リンク不足（被リンク1本・孤立記事）の解消」を参照 |
| `base.njk`と`article.njk`でhreflangタグが重複出力 | 🟢軽微 | 実害小。テンプレート整理は次回検討 |

**検証方法**：修正前後で`/stories/culture-sports-leisure/koto/`等の実際のビルド済みHTMLを確認。修正前は日本語記事の「前の記事」「次の記事」「関連記事」に英語タイトルが表示されていたが、修正後は同言語の記事のみに統一されたことを確認。全91記事に対応するJA155ページ・EN22ページを機械的に再チェックし、関連記事・前後記事ブロックでの他言語混入はゼロ件になったことを確認済み。

---

### description文字数ルールの改訂（115〜160字 → 110〜120字）

**対象ファイル**
- `CLAUDE.md`（フロントマターのテンプレート／ピラー記事生成プロンプトの2箇所）
- `docs/TPJ制作憲法.md`（5章の構成規定／7章セルフチェックリストの2箇所）
- `docs/TPJ英語版制作憲法.md`（frontmatter規約）

**変更内容**：上記監査で`concept/`配下23記事のdescriptionが115字未満と判明したことを受けて基準を再検討した結果、**120字＝検索結果・OGPカード等でdescriptionが途切れずに全文表示される目安（特にPC表示）**という表示上の制約を優先し、110〜160字だった許容幅を110〜120字に縮小した。160字まで許容すると表示時に末尾が「…」で切れ、一文目で結論を完結させても後半の補足が読者に見えないケースが生じるため。

**理由**：情報密度（AI単体引用に耐えるための下限110字）と表示の完全性（切れずに見える上限120字）の両方を満たす範囲に絞り込んだ。上限を厳しくした分、二文目以降を削るなど本文の圧縮が必要になる記事が増える見込み。

**注意**：この改訂は2026-07-19時点で新規作成・改稿する記事から適用する。既存記事（`concept/`配下23記事など）の一括改稿は、下記「`concept/`配下24記事のdescription一括改稿」で同日中に対応済み。

---

### `concept/`配下24記事のdescription一括改稿（110〜120字ルールへの対応）

**対象ファイル**：`src/stories/concept/`配下24記事のfrontmatter `description`（旧ルールで115字未満だった23記事＋新ルールの上限120字を超えていた1記事の計24記事）。ビルドにより`public/`配下の該当記事ページ・カテゴリ一覧ページ（`/stories/{category}/`）・`/stories/`トップ等34ファイルを再生成。

**対応内容**：全24記事のdescriptionを110〜120字に書き直した。結論先出し（一文目で要点を完結）・TPJのブランド表記ルール（Third Place Japan／サードプレイスジャパン／TPJの使い分け）・誇大表現禁止を維持しつつ、既存の文意はできる限り保持して文字数のみを調整した。

**検証方法**：Node.jsスクリプトで全24記事の文字数を機械的にカウントし、110〜120字の範囲に収まることを確認してからfrontmatterに反映（目視カウントは誤差が出やすいため使わない）。ビルド後、`public/`配下の`<meta name="description">`タグに反映されていることを実際のHTMLで確認済み。

---

### 記事間の内部リンク不足（被リンク1本・孤立記事）の解消

**背景**：本セクション上部の監査では「内部リンク1本のみの記事17本・被リンクゼロの孤立記事9本」と報告していたが、シェルスクリプトで全記事（JA69本・EN22本）の本文中リンクを機械的に再集計したところ、実数は孤立記事（被リンク0本）2本、被リンク1本のみの記事16本（JA12本・EN4本）だった。ユーザー指摘のbar-whiskey-chuo⇔ginzaペアも実際には既に3〜4本ずつ他カテゴリへ発リンクしており、閉じたペアではなくなっていた（別作業で既に解消済みだったと推定）。

**対象記事（計18本）**

孤立記事2本：`concept/shinjuku-coworking-third-place.md`（新宿コワーキング）、`concept/aoyama-spa-third-place.md`（青山スパ）

被リンク1本のみ・JA12本：`concept/ray-oldenburg-third-place.md`、`concept/third-place-accessibility.md`、`concept/third-place-ibasho-difference.md`、`bar-whiskey-roppongi.md`、`concept/green-beans-coffee-review.md`、`cafe-specialty-coffee-jimbocho.md`、`concept/shimokitazawa-cafe-third-place.md`、`members-lounge-chuo.md`、`members-lounge-ginza.md`、`restaurant-dining-azabujuban.md`、`retreat-zen/bunkyo.md`、`shrine-temple/bunkyo.md`

被リンク1本のみ・EN4本：`en/cafe-specialty-coffee/chiyoda.md`、`en/hotel-lounge/chiyoda.md`、`en/members-lounge/chuo.md`、`en/shrine-temple/bunkyo.md`

**対応内容**：上記18記事それぞれについて、テーマ・エリアが近い既存記事（JA14ファイル・EN6ファイル）本文中に自然な文脈でのリンクを1〜2本追加し、全対象記事を被リンク2本以上にした。店名を出さない・誇大表現をしないというピラー記事の原則は維持し、既存の文章に1文を追加する形で挿入（新規セクションの追加はしていない）。

**検証方法**：シェルスクリプトでJA69本・EN22本全記事の被リンク数を修正前後で再計測し、修正後は全記事が被リンク2本以上になったことを確認。あわせて`npm run build`を実行し、追加した18記事分のURLが実際に`public/`配下に生成されていること（リンク切れがないこと）を確認済み。

---

## 2026-07-13

### 文京区ピラー2本の英語化・英語記事一覧/サイドバーの言語フィルタリング不具合修正

**対象ファイル**
- `src/en/stories/retreat-zen/bunkyo.md` / `src/en/stories/shrine-temple/bunkyo.md` — 新規作成（英語版）
- `src/stories/retreat-zen/bunkyo.md` / `src/stories/shrine-temple/bunkyo.md` — `hreflang_en` 追記
- `public/assets/images/articles/hongo-hilly-stone-steps-bunkyo-en.webp` / `vermillion-shrine-hall-bunkyo-en.webp` — 英語版専用サムネイル追加
- `.eleventy.js` — `selectLang` フィルター新規追加
- `src/en/stories/index.njk` — 記事一覧をハードコードから動的取得に変更
- `src/_layouts/article.njk` — サイドバー「最新記事」の言語フィルタリング修正

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 文京区2本の英語化 | `docs/TPJ英語版制作憲法.md` に従い翻訳ではなく再構成として執筆。retreat-zenは本郷の坂・小石川後楽園と六義園・根津千駄木の文人街、shrine-templeは湯島天満宮・聖堂・根津神社・傳通院の「学業成就」信仰を核に据えた。taito版との対比軸はFAQで自然に言及し、文京区2本同士の相互リンクを優先 |
| `/en/stories/` 記事一覧 | 3本ハードコード → `collections.articles \| selectLang("en")` で全件動的表示に変更。"More English articles coming soon." を削除 |
| サイドバー「最新記事」 | `collections.articles \| head(5)`（言語フィルタなし）→ `collections.articles \| selectLang(lang or "ja")` に変更。英語ページでは "Latest Articles" ラベル・`name_en` カテゴリ表記に切り替え |

**発見された不具合の背景**：英語記事を新規追加しても `/en/stories/` トップと記事ページのサイドバーに反映されない状態が続いていた。原因はテンプレート側（ハードコード・言語フィルタ欠如）であり、記事側frontmatterの不備ではなかった。

**ルール化**：「ItemList の言語フィルタリング（日英混在環境）」セクションに「英語ページ側は `selectLang` を使う」を追記済み。今後、記事一覧・最新記事・関連記事等の一覧系UIを新規実装する際は、対象ページの言語で `selectLang` / `rejectLang` によるフィルタリングを必須とする。

---

### 外部実装ガイド（tpj_implementation_guide.md）の内容検証 — 対応不要と判断

**経緯**：外部から提供された実装ガイド（`grade`フィールド導入・バッジ出し分け・JSON-LDグレード別出し分け・件数統一・タグ表示の5項目）の内容を、実際のコードベースと照合した。

**検証結果**

| ガイドの項目 | 判定 | 詳細 |
|---|---|---|
| 0章：施設ごとのYAMLファイル + `gradeConfig.js` への移行 | ❌ 前提が異なる | 実際は `src/_data/venues.json` の単一ファイルで一元管理し、`grade`/`grade_slug`フィールドは既に存在。移行提案は不要（既存設計を壊すだけ） |
| 1章：グレード別バッジ・文言の出し分け | ✅ 実装済み | `src/_layouts/venue.njk` の `gradeLevels`/`thisLevel` 判定で対応済み |
| 2章：グレード別JSON-LD（LocalBusiness）出し分け | ✅ 実装済み | Silver以上=openingHours/sameAs、Platinum以上=amenityFeature/smokingAllowed 等、venue.njk内で条件出力済み |
| 3章：トップページ「18件」と一覧ページ「17件」の件数ズレ | ❌ 現状は問題なし | `src/stories/index.njk`・`src/stories/select/index.njk` とも `selectCount`（`published.length - flagshipVenues.length`）という同一の動的計算式・同一データソースを使用。ビルド済み`public/`・本番サイトともに両ページ「16件」で一致していることを確認済み（2026-07-13時点） |
| 4章：タグプール・グレード別上限数 | ✅ 実装済み | `maxTagsMap`（Certified:5〜Flagship:25）で対応済み。CLAUDE.md「タグマスターリスト」と一致 |

**判断**：ガイドの提案はいずれも不採用。1・2・4章は既存実装と同等かそれ以上の内容が既に本番に反映済み、0章は既存のvenues.json一元管理設計と矛盾するため不採用、3章は検証の結果ズレが実在しないことを確認した。コード変更は行っていない。

---

## 2026-07-09

### モーダルCTA削除・グレード表現の調整・モバイル表示修正

**対象ファイル**
- `public/index.html` — 全モーダルのCTA削除、認証グレード表・法人向けサービスメニューの文言調整、フッターのモバイル2カラム化
- `src/_layouts/venue.njk` — 底部リンク行の改行をモバイルでも有効化
- ビルドにより `public/stories/select/*/index.html` 等17件も再生成

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| モーダルCTA | 全25箇所の「お問い合わせはこちら →」リンクを削除（しつこい・品がない印象のため） |
| `#grades` Certified行 | 審査「覆面調査員による基礎確認」→「公開情報による基礎確認」、主なメリット「正式認証」→「正式登録」 |
| `#grades` Silver行 | 入口・申請方法「オーナー・責任者本人からの問い合わせ必須」→「店舗責任者からの問い合わせ必須」、審査「ヒアリング＋複数回訪問・合議」→「公式情報・資料提出・ヒアリング」。Silver詳細モーダルの本文も同様に調整 |
| `#grades` Gold行 | 審査「再訪調査＋継続評価」→「覆面調査員による複数回確認」 |
| FAQ「認証までの期間」 | Silver/Gold以上の審査プロセスの違いが伝わるよう回答文を修正（表示用HTML・JSON-LD FAQPage両方） |
| `#services`（法人向けサービスメニュー） | お金・コンサル色のある表現（「商品化」「収益源」「実務支援」「運用支援」「ご提供」「承ります」等）を「取り組みます」「サポートします」等に統一。テーブル本文・6つの詳細モーダルすべてに適用 |
| 施設ページ底部リンク | `<br class="hidden md:block">` → `<br>` に変更し、モバイルで「TPJについて知る」が語の途中で改行される不具合を修正 |
| フッター（モバイル） | `.footer-cols` を1カラムから2カラム（`1fr 1fr`）に変更 |

**確認事項**：TPJセレクト施設が「Gold」「Certified」等の実グレードバッジで誤表示される不具合を調査したが、`venues.json`・ビルド済み `public/` とも既に正しく「TPJセレクト」表示になっており、追加対応は不要と判断（ユーザー指摘のスクリーンショットはグレード機能実装当日＝2026-07-06撮影の、修正前の状態と判明）。

---

### 施設情報カードの設備バッジ機能追加（メニューリンク廃止）

**対象ファイル**
- `src/_layouts/venue.njk` — 施設情報カードから「メニューを見る」リンクを削除し、設備バッジ（3列グリッド）を追加
- `src/_data/venues.json` — GBCに `bean_varieties_count: 30` を追加、タグ入れ替え（上質/贅沢/スイーツ/ビジネスパーソン → 一人時間/一人カフェ/一人ランチ/女子会）
- `CLAUDE.md` — タグマスターリストに「一人カフェ」「一人ランチ」「女子会」を追加、設備バッジルールを新規セクション化

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 施設情報カード | 「メニューを見る」リンクを削除。営業時間・価格帯・電話の下に区切り線付きの設備バッジ行を追加 |
| 設備バッジ | `tags`/`amenity_features`/`smoking_allowed`/`bean_varieties_count` から自動判定するデータ駆動方式。特定施設名をテンプレートにハードコードしない |
| レイアウト | `grid grid-cols-3` の3列固定グリッド。バッジ数に応じて自動的に複数行になる（GBCは6個で2段×3列） |
| アイコン | 各バッジに `data-lucide` でLucideアイコンを付与（coffee / bean / wifi / wheat-off / paw-print / cigarette-off）。事前に `unpkg.com/lucide-static@0.536.0` で実在確認済み |
| bean_varieties_count | 新規任意フィールド。コーヒー豆の取り扱い品種数を表示したい施設にのみ設定（現在GBCのみ：30種類） |

**ルール化**：「施設情報カード 設備バッジルール」を新規セクションとしてCLAUDE.mdに追加済み（`venues.json必須フィールド一覧`の直後）。「タグマスターリスト」に3タグ追加済み。

**ルール化**：本セクション上部の「レスポンシブデザイン原則（例外）」「お金・ビジネス色を排した表現ルール（適用範囲追加）」「TPJセレクト 運用ルール（底部テキストのモバイル改行）」「メインサイト管理ルール（`#grades`審査プロセス表現・モーダルCTA運用・フッター2カラム化）」に反映済み。

---

### 設備バッジの枠線を金枠に変更

**対象ファイル**
- `src/_layouts/venue.njk` — 設備バッジ6個の `border border-stone-200` を `border border-gold/50` に変更

**変更内容**：ユーザー指摘（枠が視認しにくい）を受け、既存の「タグ」セクション（`border-gold/30`）と統一感のある金枠に変更。

---

### 施設ページ（venue）titleタグの改善（エリア名・業種を追加）

**対象ファイル**
- `src/stories/select/venue.njk` — `eleventyComputed.title` のフォーマット変更
- ビルドにより全17施設ページの `<title>` / `og:title` / `twitter:title` が再生成

**変更内容**

| 変更前 | 変更後 |
|---|---|
| `{{ venue.name }}` | `{{ venue.name }}（{{ venue.area_primary.area }}）\| {{ venue.category }}` |

出力例：`Green Beans Coffee（千駄ヶ谷）| カフェ・スペシャルティコーヒー | Third Place Japan Stories`

**経緯**：外部からの改善提案で「静寂性No.1」等の文言を含むtitle案が出たが、施設間の公式ランキングが存在しないため誇大表現ルール違反と判断し不採用。エリア名・業種の追加のみ採用した。同提案にあった構造化データ（FAQPage/LocalBusiness/Review/BreadcrumbList）は確認の結果すべて実装済みだったため対応不要と判断。Flagship施設のパンくずURL不整合（ラベル「Flagship」→リンク先`/stories/select/`）も指摘があったが、現状維持と決定（専用ページ新設は見送り）。

**ルール化**：「施設ページ（venue）のtitleタグルール」を新規セクションとしてCLAUDE.mdに追加済み（「タイトル作成のルール」の直後）。

---

### LocalBusiness @typeのカテゴリ別マッピング（Schema.orgバリデーター警告解消）

**対象ファイル**
- `src/_layouts/venue.njk` — `businessType`変数を追加し、LocalBusiness本体とReview内`itemReviewed`の`@type`をカテゴリ別に切り替え

**経緯**：Schema.orgバリデーターでGBCページに「hasMenuが認識されない」警告を検出。`hasMenu`はSchema.org上`FoodEstablishment`系のみのプロパティで、`@type: "LocalBusiness"`のままでは無効だった。

**変更内容**：カフェ（`cafe-specialty-coffee`）は`CafeOrCoffeeShop`、バー（`bar-whisky-cocktail`）は`BarOrPub`に変更。他業種（ホテル・スパ・文化施設等）は`LocalBusiness`のまま。本番環境で警告解消を確認済み。

**ルール化**：「LocalBusiness @type のカテゴリ別マッピング」を新規セクションとしてCLAUDE.mdに追加済み（「認証施設のReview + reviewRating」の直前）。

---

### Flagshipパンくず4階層化・レーダーチャートのアクセシビリティ改善

**対象ファイル**
- `src/_layouts/venue.njk` — パンくずUI・BreadcrumbList JSON-LDを4階層化、レーダーチャートコンテナに`aria-hidden`追加
- `src/stories/select/index.njk` — Flagshipセクションに`id="flagship"`アンカー追加

**経緯**：外部レビューで「パンくずのラベル『Flagship』とURL（TPJセレクト一覧）の不整合」を再指摘。前回は現状維持としたが、「TPJセレクト」ノードを追加する4階層案を採用することに変更。

**変更内容**：Flagship施設のパンくずを「Stories／TPJセレクト／Flagship／店舗名」の4階層に変更。「Flagship」ノードは`/stories/select/#flagship`（TPJセレクト一覧のFlagship枠）にリンクし、「TPJセレクト」ノードの`/stories/select/`と重複しないようにした。非Flagship施設は従来通り3階層のまま変更なし。また7基準レーダーチャート（JS生成のインラインSVG、直下に同一情報をテキスト表示済み）に`aria-hidden="true"`を追加し装飾要素として扱うよう修正。

**ルール化**：「Flagship施設の特別表示ルール」のパンくず表を更新、「7基準レーダーチャートのアクセシビリティ」を新規セクションとして追加済み。

---

### 施設本文hero画像の追加・GBC写真差し替え

**対象ファイル**
- `src/_layouts/venue.njk` — エリアサブテキスト直下に条件付きhero画像表示を追加
- `src/_data/venues.json` — GBCの`image`を店舗入口の実写真（`green-beans-coffee-shibuya-storefront-entrance.webp`）に差し替え
- `public/assets/images/venues/` — 新画像追加・旧画像削除

**経緯**：外部レビューで「本文中の画像alt属性を確認」との指摘があったが、調査の結果、施設詳細ページ本文には画像が一枚も表示されていないことが判明（alt云々以前の問題）。ユーザーから店舗入口の実写真の提供を受け実装。なお別途提供された宣伝グラフィック画像（非公式ファンサイト由来・誤字あり・広告的文言）は、TPJの「広告媒体ではなく認証機関」という方針に反するため不採用とした。

**変更内容**：`venue.image`が共通デフォルト画像と異なる施設のみ、本文に写真を表示する仕組みを追加（データ駆動・全施設共通テンプレート）。デフォルト画像使い回しの施設（16件）には表示されない。GBCのみ対象画像あり。

**ルール化**：「施設本文画像の表示ルール」を新規セクションとしてCLAUDE.mdに追加済み。

---

### titleタグのブランド名重複バグ修正・施設タイトルの表記短縮

**対象ファイル**
- `src/_layouts/base.njk` — `title_suffix`変数を追加（ページごとに末尾ブランド表記を上書き可能に）
- `src/stories/select/venue.njk` — エリア表記を「千駄ヶ谷」単体から「東京・渋谷」（都道府県＋市区町村、都/道/府/県・区/市/町/村を除去）に変更、`title_suffix: "Third Place Japan"`を設定
- `src/stories/index.njk` / `src/stories/latest.njk` / `src/stories/about/certification-grades/index.njk` / `src/en/stories/index.njk` — title frontmatterに含まれていたブランド名を削除（重複表示バグ修正）

**経緯**：ユーザーから「東京・渋谷・千駄ヶ谷」への拡張提案を受けたが、Google表示幅の観点から「東京・渋谷」のみ＋末尾「Stories」削除を提案・採用。実装確認中に`/stories/`トップ等4ページで無関係の既存バグ（title frontmatterにブランド名がベタ書きされ、base.njkの自動付与と二重表示になっていた）を発見し、あわせて修正。

**ルール化**：「施設ページ（venue）のtitleタグルール」を最新フォーマットに更新、「titleタグの末尾ブランド表記（title_suffix）とブランド名重複禁止ルール」を新規セクションとして追加済み。

---

## 2026-07-06

### TPJグレード機能実装（2026-07-06）

**対象ファイル**
- `src/_layouts/venue.njk` — 6グレード対応バッジ・条件付きLocalBusiness JSON-LD・タグセクション追加
- `src/stories/select/index.njk` — グレード凡例バー・カードバッジ更新
- `src/stories/about/certification-grades/index.njk` — 新規作成（FAQPage JSON-LD付き）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| グレードバッジ | 旧「グレード表記禁止」を廃止。6グレード（TPJセレクト〜Flagship）のカラーバッジを全施設ページ・一覧ページに実装 |
| LocalBusiness JSON-LD | `gradeLevels` 変数でグレードを数値化し、スキーマ項目をグレード別に条件出力（geo=Certified+、openingHours/priceRange/sameAs=Silver+、telephone/menu=Gold+、amenityFeature/smokingAllowed=Platinum+） |
| タグセクション | `venue.tags` フィールドが存在する場合のみ表示。上限数はグレード別（Certified:5、Silver:10、Gold:15、Platinum:18、Flagship:20） |
| 一覧ページ凡例 | グレード凡例バー（Certified〜Platinum）をTPJセレクトグリッド上部に追加 |
| 認証グレード説明ページ | `/stories/about/certification-grades/` を新規作成。6グレードの詳細・FAQアコーディオン・For Business CTAを含む |

**ルール化**：「グレードバッジ表示ルール（2026-07-06改訂）」セクションを更新済み。

---

### LocalBusiness.speakable 削除（2026-07-06）

**対象ファイル**
- `src/_layouts/venue.njk` — LocalBusiness JSON-LD から `speakable` を削除

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `LocalBusiness.speakable` | 削除（schema.org バリデーターが「LocalBusinessには認識されないプロパティ」と警告） |

**ルール化**：「❌ speakableを使えない型に設定する」セクションを更新し、LocalBusinessも禁止リストに追加済み。

---

### Article系スキーマ修正・ItemList警告ゼロ化（2026-07-06）

**対象ファイル**
- `src/_layouts/article.njk` / `article-certified.njk` / `article-area.njk` — image・author・publisher.logo 修正
- `src/stories/index.njk` — ItemList の item から `@type: Article` を削除

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `image` | 文字列URL → `ImageObject`（width:800, height:450）に変更 |
| `author.knowsAbout` | 文字列配列を削除（Organization 不適切プロパティ） |
| `publisher.logo` | `width:900, height:275` を追加 |
| `ItemList.item` | `@type: Article` を削除（image/author/headline 欠如警告の原因） |

**ルール化**：JSON-LD実装ルールセクションに追加済み。

---

### Review JSON-LDへの7軸スコア追加・AggregateRating修正

**対象ファイル**
- `src/_layouts/venue.njk` — Review に `additionalProperty`（7軸スコア）追加、`aggregateRating.ratingCount` を修正

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `aggregateRating.ratingCount` | `7` → `1` に修正（7軸評価をGoogleが「7人の評価」と誤解しないよう） |
| `Review.additionalProperty` | 7軸スコア（0〜10）を `PropertyValue` として追加。UIのスコアバー表示と構造化データが一致 |

**ルール化**：`JSON-LD実装ルール` セクションの「認証施設の Review + reviewRating」に追記済み。

---

## 2026-07-03

### エリアページ（L3）・市区町村ページ（L2）のマップ削除

**対象ファイル**
- `src/stories/area/l3-area.njk` — 千駄ヶ谷専用2カラム＋マップブロックを削除し、通常レイアウトに統一

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 千駄ヶ谷専用分岐 | `{% if item.area.slug == 'sendagaya' ... %}{% else %}{% endif %}` を撤廃し、通常レイアウトのみに統一 |

**マップの現状（2026-07-03時点）**

| ページ種別 | マップの有無 | ファイル |
|---|---|---|
| L3エリアページ | **なし**（千駄ヶ谷の試験実装を削除済み） | `l3-area.njk` |
| L2市区町村ページ（東京都） | **あり**（全東京都市区町村に2カラム＋iframeマップ） | `l2-city.njk` |
| L2市区町村ページ（東京都以外） | なし | `l2-city.njk` |

**ルール**
- L3エリアページにはマップを設置しない（試験結果：AEO効果が低く、コンテンツ充実を優先）
- L2市区町村（東京都）のマップは現状維持。拡張・削除はユーザーの明示的な指示があった場合のみ行う
- マップを試験実装する場合は `{% if item.xxx.slug == '○○' %}` 条件分岐を使い、撤収時は条件ブロックごと削除して通常レイアウトのみに戻す

---

### 市区町村ページ（L2）に「業種から探す」追加

**対象ファイル**
- `src/_data/areaL2Cat.js` — 新規作成：市区町村×カテゴリの組み合わせデータ生成
- `src/stories/area/l2-category.njk` — 新規作成：市区町村×カテゴリページ
- `src/stories/area/l2-city.njk` — 「業種から探す」セクション追加（東京都2カラム・通常レイアウト両方）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| `areaL2Cat.js` | 認証店舗ベース＋記事ベースで市区町村×カテゴリの組み合わせを生成（`areaL3Cat.js` の市区町村版） |
| `l2-category.njk` | `/stories/area/{pref}/{city}/{category}/` のカテゴリページを新規生成。関連記事・認証店舗・他業種へのリンクを表示 |
| `l2-city.njk` | 「エリアから探す」の直上に「業種から探す」セクションを追加。`areaL2Cat` でその市区町村のカテゴリを絞り込んで表示 |

**URL構造**
- L3エリア×カテゴリ：`/stories/area/{pref}/{city}/{area}/{category}/`（既存）
- L2市区町村×カテゴリ：`/stories/area/{pref}/{city}/{category}/`（今回追加）

**データ生成ルール（`areaL2Cat.js`）**
- 認証店舗（`venues.json`）から：`area_primary.prefecture_slug + city_slug + category_slug` でユニーク集計
- 記事（`src/stories/` 配下の `.md`）から：`area_slug` を逆引きして city_slug を取得し、同様にユニーク集計
- 店舗ベースのエントリが既にある場合は記事ベースのエントリを追加しない（重複排除）

**注意点**
- `l2-category.njk` の「他の業種」セクションは Nunjucks ループ（`for cat in areaL2Cat`）で同市区町村の他カテゴリを列挙。`selectCityItems` などのカスタムフィルターは不要
- `articlesByCity` フィルター（既存）で市区町村内の記事を取得し、`articlesByCategory` で絞り込む

---

## 2026-07-02

### Schema.org バリデーション修正 第2弾（Organization 警告ゼロ化）

**対象ファイル**
- `src/_layouts/base.njk` — Organization から `knowsAbout`・`inLanguage` 削除

**変更内容**

| 削除プロパティ | 理由 |
|---|---|
| `knowsAbout: [文字列配列]` | Schema.org が `Thing` 型を期待するが文字列を渡していた |
| `inLanguage: ["ja","en"]` | `Organization` には不適切（`CreativeWork` 用プロパティ） |

これにより Organization の警告が0件になり、エラーなし・警告なしの完全クリーン状態を達成。

**ルール化**：「Organization に使えないプロパティ」として JSON-LD実装ルールセクションに追加済み。

---

### Schema.org バリデーション修正 第1弾（JSON-LD品質向上）

**対象ファイル**
- `src/_layouts/base.njk` — Organization.sameAs 削除・ロゴ画像を専用ファイルに変更
- `src/_layouts/article.njk` — publisher ロゴを `tpj-logo.png` に統一
- `src/_layouts/article-certified.njk` — publisher ロゴを `tpj-logo.png` に統一・Review+reviewRating 追加
- `src/_layouts/article-area.njk` — publisher ロゴを `tpj-logo.png` に統一
- `src/_layouts/venue.njk` — Review + reviewRating JSON-LD ブロック追加
- `src/stories/index.njk` — WebSite.speakable 削除・ItemList を `item` オブジェクト形式に修正・`rejectLang("en")` で日本語記事のみに絞り込み
- `.eleventy.js` — `rejectLang` カスタムフィルター追加
- `public/assets/images/main/tpj-logo.png` — 専用ロゴ画像追加（900×275px、白背景、53KB）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| Organization.sameAs | 空配列 `[]` を削除（警告解消） |
| Organization logo | hero画像 → `tpj-logo.png`（900×275px専用ロゴ）に変更 |
| WebSite.speakable | 削除（WebSiteには非推奨プロパティ・警告解消） |
| ItemList.ListItem | `url` 直接プロパティ → `item: { @type: Article, @id, name, url }` 形式に修正 |
| JSON-LDの動的テキスト | `{{ title }}` → `{{ title \| dump \| safe }}` 形式に統一（`&quot;`エスケープ問題解消） |
| ItemList言語フィルタリング | `collections.articles \| rejectLang("en")` で英語記事を除外 |
| Review + reviewRating | `venue.njk` に追加（Certified=1〜Flagship=5の数値評価） |
| 各記事publisher ロゴ | 全レイアウトで `tpj-logo.png` に統一 |

**新規ルール化**
- `JSON-LD実装ルール（Schema.org バリデーション対応）` セクションを CLAUDE.md に追加
- `各H2セクション冒頭の結論先出しルール` を CLAUDE.md 文体ガイドラインに追加
- 認証施設の `Review + reviewRating` を必須スキーマとしてルール化

**H2セクション結論先出し適用記事**
- `ginza-third-place-guide.md` — 「見分ける条件」「業種別」各セクション冒頭に結論文追加
- `shimokitazawa-cafe-third-place.md` — 「果たす役割」（否定形→肯定形）「見分ける条件」修正
- `shinjuku-coworking-third-place.md` — 「見分ける条件」「周辺サードプレイス」各セクション冒頭に結論文追加

---

## 2026-07-01

### SEO/AEO強化 第1弾（技術施策）

**対象ファイル**
- `src/_layouts/base.njk` — canonical URL 自動設定・Organization sameAs 拡張
- `src/_layouts/article.njk` — isPartOf 追加・Author schema 強化（description/knowsAbout）・data-pagefind-body 追加
- `src/_layouts/article-certified.njk` — gradeNums + numeric reviewRating + about プロパティ・Author schema 強化・data-pagefind-body 追加
- `src/stories/area/l3-area.njk` — BreadcrumbList 5階層化・Place+CollectionPage JSON-LD を全L3エリアに適用
- `src/stories/area/l2-city.njk` — AdministrativeArea JSON-LD を全市区町村に適用
- `public/robots.txt` — AI クローラー 13種許可（anthropic-ai・cohere-ai・Diffbot・Applebot・Meta-ExternalAgent 追加）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| canonical URL | base.njk で自動生成（frontmatter で個別上書き可） |
| Organization | sameAs 配列でエンティティ認識を強化 |
| Article isPartOf | WebSite への明示的な紐付けで E-E-A-T 強化 |
| Author schema | description + knowsAbout でOrganization author として確立 |
| data-pagefind-body | .prose div に追加して検索インデックスの精度向上 |
| reviewRating | グレードを数値化（Certified=1〜Flagship=5） |
| BreadcrumbList | L3 を5階層（TPJ→Stories→都道府県→市区→エリア）に深化 |
| JSON-LD 適用範囲拡大 | Place/CollectionPage/AdministrativeArea を全エリア・全都市に適用 |
| robots.txt | AI クローラー計13種を明示許可 |

---

### SEO/AEO強化 第2弾（コンテンツ・スキーマ）

**対象ファイル**
- `src/stories/about/index.njk` — DefinedTermSet に7軸を個別 DefinedTerm として追加
- `src/stories/index.njk` — SearchAction を EntryPoint 形式に更新（SiteLinksSearchBox 正式対応）
- `src/stories/concept/meiji-jingu-third-place.md` — 収益業種への送客リンク追加
- `src/stories/concept/takao-mountain-third-place.md` — 収益業種への送客リンク追加
- 新記事3本：retreat-zen・culture-sports-leisure・inbound-experience 各カテゴリ

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| DefinedTermSet | 7軸を個別 DefinedTerm（名前・alternateName・description・url）として構造化 |
| SearchAction | EntryPoint 形式 + query-input: required name=search_term_string |
| 送客リンク | 集客業種記事（神社・山）→ 収益業種（カフェ・コワーキング・ホテル・スパ・認証）へのリンク追加 |
| 新記事 | zen-retreat-tokyo / tokyo-museum-art / tokyo-inbound 各1本 |
| サムネイル | 3枚追加（各200KB以下・WebP） |

---

### コンテンツ・データ拡充（2026-07-01）

**対象ファイル**
- `src/_data/venues.json` — 認証施設8件追加（銀座・六本木・新宿・代官山・青山・中目黒・清澄白河・下北沢）
- `src/stories/concept/` — ピラー記事4本追加（銀座・新宿・下北沢・青山）
- `src/en/stories/` — 英語記事インフラ整備＋英語記事3本公開
- 全記事の `area_name` フィールド統一設定（17記事）
- 既存概念記事に `area_slug` 追加（meiji-jingu→harajuku・green-beans→sendagaya 等）

**追加venue一覧**

| slug | 名称 | グレード | エリア |
|---|---|---|---|
| ginza-tea-house-chuo | 銀座茶廊 | Platinum | 銀座 |
| roppongi-art-lounge | 六本木アートラウンジ | Gold | 六本木 |
| shinjuku-workspace-calm | 新宿静作室 | Certified | 新宿 |
| daikanyama-book-cafe | 代官山ブックカフェ | Silver | 代官山 |
| aoyama-wellness-studio | 青山ウェルネス離 | Gold | 青山 |
| nakameguro-river-cafe | 中目黒川沿いカフェ | Silver | 中目黒 |
| kiyosumi-roastery-koto | 清澄白河焙煎所 | Gold | 清澄白河 |
| shimokitazawa-vintage-cafe | 下北沢古民家珈琲 | Certified | 下北沢 |

### 記事品質訂正（2026-07-01）

10記事の制作憲法違反を修正し、違反パターンをルール化。

**訂正記事一覧**

| 記事 | 違反種別 | 修正内容 |
|---|---|---|
| shinjuku-coworking-third-place | 価格断定 | 「新宿御苑（1時間500円）」→価格削除、後に「有料・要事前確認」 |
| tokyo-museum-art-third-place | 施設名断定 | description「東京都現代美術館・国立新美術館など」→条件描写 |
| zen-retreat-tokyo-third-place | 施設名断定 | description・本文・FAQ の「円覚寺・東慶寺・高尾山薬王院」→条件描写 |
| meiji-jingu-third-place-tokyo（英語版） | 価格断定 | `¥500` 3箇所→「small entry fee — confirm current rates」 |
| meiji-jingu-third-place-tokyo（英語版） | og:locale誤設定 | `ja_JP`→`en_US`（base.njkを修正し全英語記事に自動適用） |
| tokyo-third-place-guide-travelers（英語版） | サムネイル共有 | 日本語記事と同一画像→英語版専用画像（-enサフィックス） |
| tokyo-inbound-third-place-guide | 混雑断定 | 「混雑は激しく静寂性は低い」→「混雑しやすく傾向がある」 |

**ルール化**：上記違反パターンは「記事品質チェックルール」セクションに追加済み。

---

### サムネイル画像追加（2026-07-01）

**対象ファイル**
- `public/assets/images/articles/ginza-cafe-third-place-tokyo.webp` — 銀座ピラー記事用（121KB）
- `public/assets/images/articles/shinjuku-coworking-space-tokyo.webp` — 新宿ピラー記事用（95KB）
- `public/assets/images/articles/shimokitazawa-cafe-culture-tokyo.webp` — 下北沢ピラー記事用（136KB）
- `public/assets/images/articles/aoyama-spa-wellness-tokyo.webp` — 青山ピラー記事用（44KB）

**変更内容**

| ファイル名 | 用途 | サイズ |
|---|---|---|
| ginza-cafe-third-place-tokyo.webp | 銀座カフェ×サードプレイス記事サムネイル | 121KB |
| shinjuku-coworking-space-tokyo.webp | 新宿コワーキング記事サムネイル | 95KB |
| shimokitazawa-cafe-culture-tokyo.webp | 下北沢カフェ記事サムネイル | 136KB |
| aoyama-spa-wellness-tokyo.webp | 青山スパ記事サムネイル | 44KB |

---

## 2026-06-27

### エリアページ検索機能・駅名エイリアス・エリア追加

**対象ファイル**
- `src/stories/area/l1-prefecture.njk` — 都道府県インデックスページ
- `src/_data/areas.json` — エリアデータ

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| 検索ボックス | エリア名・駅名で市区町村カードを絞り込む検索UIを追加 |
| エリアタグ表示 | 各市区町村カード内にサブエリア名をピル形式で表示、L3ページにリンク |
| 駅名エイリアス | 約230駅分の「駅名→エリア」マッピングをJS内に実装 |
| 検索ヒント | マッチした駅名を「北参道 → 原宿（渋谷区）」形式でヒント表示 |
| 新エリア追加（14エリア） | 代々木・千駄ヶ谷・新橋・浜松町・虎ノ門・有楽町・霞ヶ関・新大久保・信濃町・大崎・鶯谷・駒込・田端・西日暮里 |
| 近隣バリエーション | 西小岩→小岩、東蒲田→蒲田など方角・近隣地名を最寄エリアに紐付け |

**駅名エイリアスの仕組み**
```js
// src/stories/area/l1-prefecture.njk 内のJavaScript
var STATION_ALIASES = {
  '北参道': { citySlug: 'shibuya-ku', cityName: '渋谷区', areaSlug: 'harajuku', areaName: '原宿' },
  // ... 約230駅
};
// 検索時に areas のテキストマッチ + STATION_ALIASES のマッチを合算して表示
```

---

### Googleマップサイドバー試験実装（渋谷区・千駄ヶ谷限定）

**対象ファイル**
- `src/stories/area/l2-city.njk` — 市区町村ページ
- `src/stories/area/l3-area.njk` — エリアページ

**試験対象ページ**
- 渋谷区: `/stories/area/tokyo/shibuya-ku/`
- 千駄ヶ谷: `/stories/area/tokyo/shibuya-ku/sendagaya/`

**実装内容**
- 2カラムレイアウト（メイン2/3 + サイドバー1/3）、モバイルは1カラム
- サイドバー①：市区町村/エリアのGoogleマップ（zoom13〜15、インタラクティブ）
- サイドバー②：上位エリア（都道府県/市区町村）のGoogleマップ（zoom10〜13）
- 「Googleマップで開く」外部リンク
- デスクトップでは `position: sticky` でスクロールに追従
- `AdministrativeArea` / `Place` JSON-LDに `hasMap` プロパティを追加

**条件分岐による限定適用**
```nunjucks
{% if item.city.slug == 'shibuya-ku' and item.prefecture.slug == 'tokyo' %}
  {# 2カラム+マップ #}
{% else %}
  {# 通常1カラム #}
{% endif %}
```
拡張する場合は上記条件に `or item.city.slug == '○○'` を追記するだけでよい。

**施策評価（2026-06-27時点）**

| 軸 | 日本人向け | インバウンド向け |
|---|---|---|
| AEO効果 | ★★☆☆☆ | ★★★☆☆ |
| ユーザーUX | ★★★☆☆ | ★★★★☆ |
| MEO効果 | ★☆☆☆☆ | ★★☆☆☆ |
| ページ品質感 | ★★★★☆ | ★★★★★ |

**今後の施策に向けた知見**

- 本来のMEO（Googleビジネスプロフィール最適化）とは別物。「ローカルSEOのコンテキスト強化」が正確な表現
- **インバウンドへの効果が最も高い**：地図は言語の壁を越え、Googleマップ一極集中の外国人観光客の導線になる
- AEO改善の優先度順：①エリアページのテキストコンテンツ充実 → ②認証店舗の増加 → ③マップ施策（補完）
- 拡張する際はページ読み込み速度（iframe×2枚）に注意。モバイルで計測推奨
- インバウンド強化の次の一手：`areas.json` の `name_en` を活用した英語マップリンク・英語テキストの追加

---

## 2026-06-23

### 記事サイトトップ：最新記事セクションのレイアウトと機能追加

**対象ファイル**
- `src/stories/index.njk` — ソーステンプレート（編集対象）
- `public/stories/index.html` — ビルド成果物（自動生成）

**変更内容**

| 変更箇所 | 内容 |
|---|---|
| セクション順序 | 「カテゴリ」→「最新記事」から「最新記事」→「カテゴリ」に入れ替え |
| 表示件数 | 6件 → 8件 |
| フィーチャーカード | 1件目を横並びサムネイル付き大カードで表示 |
| リスト記事 | 2件目以降をスクロール可能なコンパクトリスト＋小サムネイルで表示 |
| 記事一覧ボタン | 「記事一覧はこちら →」ボタンを追加 |

### NEWバッジ（動的表示）

**対象ファイル**：`src/stories/index.njk`（記事サイトトップページのみ）

**仕様**
- フィーチャーカード（1件目）とリスト記事（2件目以降）の両方に対応
- 公開日から**3日間**のみNEWバッジを表示、4日目以降は自動非表示
- クライアントサイドJSで判定（`data-publish` 属性に `dateISO` フィルターで日付を埋め込み）
- バッジ色：ウォームブラウン `#6B4F3A`

**実装方式**
```html
<span class="new-badge hidden ..." data-publish="{{ post.date | dateISO }}">NEW</span>
```
```js
const diff = (today - pub) / (1000 * 60 * 60 * 24);
if (diff <= 3) badge.classList.remove('hidden');
```

**注意**：カテゴリページ・記事一覧ページには未適用。拡張する場合は各テンプレートに同様の実装が必要。

---

## 2026-06-22

### デザイン：TPJ記事サイトをGBCスタイルに柔らかく変更

**対象ファイル（ソーステンプレート）**
- `src/_layouts/base.njk` — CSS共通スタイル
- `src/stories/index.njk` — 記事サイトトップページ
- `src/_includes/venue-card.njk` — 認証店舗カード
- `src/_layouts/article.njk` — 通常記事レイアウト
- `src/_layouts/article-certified.njk` — 認証記事レイアウト

**重要：`public/` の生成ファイルを直接編集しない**
`public/stories/index.html` などはEleventyが `npm run build` で再生成する。
デザイン変更は必ず `src/` 配下のソーステンプレートを編集すること。
直接 `public/` を編集しても、次のビルドで上書きされる。

**デザイン変更内容**

| 変更箇所 | 変更前 | 変更後 |
|---|---|---|
| カード全般 | `border border-border hover:border-gold` | `rounded-xl shadow-sm hover:shadow-md` |
| カテゴリカード画像 | `aspect-video`（16:9） | `h-28`固定高（コンパクト） |
| カテゴリカードテキスト | `text-xs` | `text-sm` |
| セクション見出し | `label`（0.65rem 金色） | `font-serif text-xl/2xl font-light text-dark` |
| エリアタグ・評価軸タグ | 四角形ボーダー | `rounded-full`（ピル形） |
| grade-badge | 四角形 | `rounded-full` |
| venue-card CSS | `border 1px solid` | `rounded-xl shadow-sm` |
| サムネイル | 金色内枠オーバーレイあり | 削除、`rounded-xl overflow-hidden` |
| 最新記事グリッド | `md:grid-cols-2` | `lg:grid-cols-3`（3カラム） |
| 関連記事カード | 裸のリンク | `rounded-xl overflow-hidden shadow-sm`＋`p-4`内テキスト |
| 前後記事ナビ | `border hover:border-gold` | `shadow-sm hover:shadow-md` |
| シェアボタン | フラット枠線 | `bg-white shadow-sm hover:shadow-md` |

---

## TPJ ピラー記事 制作ルール

TPJ（Third Place Japan）の「地域 × 15分野」ピラー記事を制作する際の要点。
**詳細な制作憲法は `docs/TPJ制作憲法.md` を必ず参照すること**（このセクションは要約）。

### 参照ファイル
- 制作憲法（不変ルール全文）：`docs/TPJ制作憲法.md` ← 記事制作時は必読
- 記事生成の実行手順：`docs/記事生成プロンプト.md`
- 入力データ（1行＝1記事）：`data/TPJ_東京ピラーマス表.xlsx`
- 記事出力先：`articles/{分野slug}-{地域slug}.md`

### 絶対に守る5原則（詳細は制作憲法）
1. **店名・施設名を一切出さない**。立地・空間・素材・客層の「条件」で描く。
2. **断定してよいのは検証可能な地域情報のみ**（地理・歴史・交通・統計）。個別店の営業・価格・評判は断定しない。
3. **AI臭を出さない**。「近年」「いかがでしたか」等の定型句禁止。現地を知る編集者の温度で書く。
4. **7軸を背骨にする**（居心地／静寂性／特別感／ストーリー／再訪／記録性／インバウンド）。
5. **重複させない立体構造**。都(幹)→自治体(面)→密集エリア(点)。子は親を繰り返さず内部リンクで委譲。各記事は「その地域×分野でしか書けない核」を3つ以上持つ。

### 制作フロー
1. 入力＝マス表の該当行（優先度A→B→Cの順に着手）。
2. `docs/記事生成プロンプト.md` の STEP1〜3 に従う（先に「固有の核」3点を言語化）。
3. 構成・文体・リード規定は制作憲法に従う（リードは最大160字・一文目で結論完結）。
   metadescriptionは110〜120字（120字＝PC表示等で途切れずに見える目安。上記「フロントマターのテンプレート」参照）。断定可能な地域情報は言い切り、「〜傾向があります」多用禁止。
4. 出力前に制作憲法 第7章のセルフチェック**13項目**を実行。**全項目クリアまで確定しない**。
   （追加3項目：Markdown構文・metadescription字数・JSON-LD改行なし確認）

### 地域ピラー記事の追加ルール（2026-07-28確定）

小金井市・東村山市・清瀬市等、自治体単位の地域ピラー記事の量産を通じて確定した追加ルール。既存ルールと重複する項目は再掲せず参照先のみ示す。

**1. AEOキーワード必須ルール**
「サードプレイス」または「第三の居場所」（英語版は"third place"）を、meta description・本文冒頭のリード文（太字部分）・まとめ本文・FAQ（最低1問の回答内）の4箇所に必ず自然な形で含める。出現頻度の目安は本文全体で4〜6回程度。これは新規ルールではなく、既存の「必須キーワード（全記事に含める）」セクションの4箇所チェックと同一内容である。**地域ピラー記事は生成段階からこの4箇所を組み込み、後工程での追加修正を発生させないこと**（過去は遡及対応が必要になるケースが多発した）。

**2. タイトルフォーマット（新規記事は必ずこの形式）**
- 日本語：「{地域名}のサードプレイスガイド：{カテゴリ名}編。{サブタイトル}」
- 英語　：「{Ward/City}-ku（-shi） as a Third Place: {Subtitle}」

日本語版は「タイトル作成のルール」で許容例外としていた二段構成タイトル（40〜52字程度）を、地域ピラー記事に限り標準形式として確定する。**既存の旧フォーマット記事32本（業種×エリア記事群）はこの統一の対象外とし、そのまま維持する**（2026-07-28判断）。

**3. 本文文字数の目安**
自治体単位の地域ピラー記事の本文は**4,000〜4,300字程度**（frontmatter・関連記事リンク文を除いた実測値）を目安とする。生成時点でこの範囲を意識すること。超過した場合は、7軸のインバウンド軸と後続のインバウンド視点セクションの重複、固有の核の定義文の複数回反復など、同一内容の言い回し違いの重複を優先的に削る（3ゾーン構成・固有の核自体は削らない）。
※この4,000〜4,300字は自治体単位の地域ピラー記事専用の基準。「地域×15分野」ピラー記事（上記「制作フロー」の2,800〜4,000字）とは対象が異なるため混同しないこと。

**4. 固有ブランド名・個人名の一般名詞化（原則1の強化）**
上記「絶対に守る5原則」の1（店名・施設名を一切出さない）を、以下の対象にも明示的に適用する。
- 商業施設・商業ブランド名（例：大型商業施設→「大型複合施設」）
- 個人名（著名人・芸能人等）
- 作品名・メディア名（アニメ・映画等の固有タイトル）
- 医療機関等の個別施設名

例外的に固有名を使用してよいもの（既存の慣例。「記事品質チェックルール」の例外①②も参照）：
- 公共施設・史跡・都立庭園・国宝指定物件等（例：正福寺地蔵堂、小金井公園、江戸東京たてもの園）
- 神社・寺院の正式名称
- 公共交通機関名

**5. サードプレイスガイド（概念解説シリーズ）との接続義務**
地域ピラー記事で扱う「固有の核」が、既存の概念解説シリーズ（`src/stories/about/` 配下、`category_slug: about`）でカバーされていない新しいタイプの居場所原理を示す場合（例：二次的自然、線的静けさ、土地利用の歴史的経緯等）、以下を確認する。
- `third-place-examples.md`（具体例10選）に該当タイプがあるか
- `third-place-8-conditions.md` の「8条件を満たす日本の場所の例」に該当タイプがあるか
- `what-is-third-place.md` の該当セクションに言及があるか

無ければ、既存項目のトーン・フォーマットを踏襲した上で追加し、該当する地域ピラー記事へ実リンクする。2026-07-28に小金井市（水路）・東村山市（里山）の追加時に実施した対応が実装例。

**6. 英語化の技術要件（要約）**
詳細は `docs/TPJ英語版制作憲法.md` を参照（このセクションは要約）。
- 7軸固定訳語表を使用（Comfort & Sensory Quality等、統一済み）
- hreflang双方向設定（日本語版に`hreflang_en`、英語版に`hreflang_ja`）
- `area_name` は `areas.json` の `name_en` を使用
- 本文ワード数1,500〜2,200語（区・市単位のピラー記事の場合）
- AI臭排除リストを適用（"delve into" "nestled in" 等。全リストは制作憲法参照）
- 日本語文字混入のUnicode範囲チェックを実施
- 英語ページのフッター・ナビは `lang: en` 分岐で英語表示にする（`footer-stories-en.njk`・`nav-stories-en.njk`使用）
- 新規エリアの英語版記事を作る際、該当エリアのarea詳細ページが英語版に存在しない場合、パンくずのエリア名リンクは出ない仕様（既知の制約・優先度低）

**7. 新規エリア追加時のチェックリスト**
1. `src/_data/areas.json` に該当エリアが登録されているか（未登録だとエリアパンくずが表示されない）
2. 既存の類似記事との重複回避を明示的に行う（STEP1で固有の核を言語化し、既存記事との対比を記事内に残す）
3. 相互リンクを双方向で設定する（新記事→既存記事、既存記事→新記事の逆リンク追加を忘れない）
4. **固有ブランド名混入チェック**（要注意）。過去に商業施設名・商業施設ブランド名・スポーツチーム名等の混入を複数回検出・修正した実績がある

**8. 技術的注意事項（バグ再発防止）**
- FAQPage JSON-LDは `article.njk` の自動生成（`extractFAQ`フィルター）のみで出力する。地域ピラー記事のMarkdown本文に手動で`<script type="application/ld+json">`のFAQPageを埋め込むと二重出力の原因になるため禁止
- 「## まとめ」見出し直前の空行必須ルールは既存の「文体ガイドライン（TPJ編集部）」内、区切り線`---`直前空行ルール（2026-07-21追加・Setext見出し化バグ、11本で実際に発生）を参照。地域ピラー記事でも同様に注意する
- 記事本文に「第三の場所」という誤字表記が混入しないよう注意する（正しくは「第三の居場所」）
- **固有名詞級の施設・機関（大学・研究機関・博物館・史跡等）は、STEP1で連想した時点でWeb検索により所在地を裏取りすること（2026-08-10追加）**。全国的に知名度が高く「多摩地域のどこか」「23区のどこか」という漠然としたイメージで記憶されがちな施設は特に誤用リスクが高い。三鷹市の記事に着手する過程で、`fuchu.md`（府中市）が「天体観測を行う国内屈指の研究機関」として描写していた施設が、実際には国立天文台（三鷹市大沢2-21-1）であり府中市には所在しないことが判明した。生成時にWeb検索による裏取りを行わず、「多摩地域＝研究機関が多い」という一般的な地域イメージから具体的施設を連想し、所在地まで確認しないまま断定したことが原因と推定される。詳細な裏取りルールと該当セルフチェック項目は`docs/記事生成プロンプト.md`のSTEP1・`docs/TPJ制作憲法.md`第7章（14項目目）に追加済み

### このセクションの優先順位
TPJ記事制作に関しては、このルールと `docs/TPJ制作憲法.md` を最優先で参照する。
矛盾を感じたら制作憲法（docs）の記述に従う。

---

## 英語版サードプレイスガイド（about系）運用ルール（2026-07-28追加）

`category_slug: about` の英語記事群（`src/en/stories/concept/` 配下）を追加・拡張する際の必須ルール。2026-07-28に`/en/stories/about/`ハブページを新設した際に確定した。

### ハブページと個別記事は別ファイルにする（日本語版と同じ構造）
日本語版は「独立したハブページ」（`/stories/about/`、`src/stories/about/index.njk`）と、`what-is-third-place.md`をはじめとする個別記事14本が別ファイルとして存在する構造になっている。英語版も同じ構造にすること。**「Third Place Guide」ナビ・パンくずのリンク先を、個別記事（例：`what-is-third-place-japan.md`）に直接向けてはいけない**。必ず専用のハブページ（`/en/stories/about/`、`src/en/stories/about/index.njk`）を経由させる。ハブページ自体には`category_slug`・`tags`を設定しない（`collections.aboutArticles`に自分自身が混入しないようにするため。日本語版`src/stories/about/index.njk`も同様に未設定）。

### `src/en/stories/` にはディレクトリ既定レイアウトが存在しない（重要な技術的注意）
日本語版は`src/stories/stories.json`（`{"layout": "stories.njk"}`）がディレクトリ全体に既定レイアウトを適用するため、個別ファイルで`layout:`を省略できる（実際`src/stories/about/index.njk`も省略している）。**`src/en/stories/`配下には対応する`.json`/`.11tydata.js`が存在しない**ため、英語版で新規に`.njk`ページ（特にハブページのような`index.njk`）を作る際は、**必ずfrontmatterに`layout: stories.njk`を明記すること**。省略するとヘッダー・ナビ・フッター・CSSを一切持たない生のページが出力される（2026-07-28の`/en/stories/about/`新設時に一度この書き忘れが発生し、ビルド後に気づいて修正した実績あり）。

### 「Third Place Guide」の遷移先は6箇所にハードコードされている（2026-07-31修正・1箇所追加）
ハブページのURLを変更する場合、以下6箇所を同時に更新する必要がある（自動導出されていないため、1箇所でも直し忘れると新旧URLが混在する）：
- `src/_layouts/article.njk`：JSON-LD BreadcrumbListの`item`（1箇所）、可視パンくずのリンク（1箇所）、サイドバー「Third Place Guide」ウィジェットの「See all →」リンク（1箇所）
- `src/_includes/nav-stories-en.njk`：デスクトップナビ・モバイルメニューの各1箇所（計2箇所）
- `src/_includes/footer-stories-en.njk`：フッター「Third Place Guide」列の記事リンク（1箇所）

**発見の経緯**：2026-07-31のSEO/AEO監査で、`footer-stories-en.njk`の「Third Place Guide」列が`/en/stories/about/`（ハブページ）ではなく`/en/stories/concept/what-is-third-place-japan/`（個別記事）に直接リンクしていたことが判明した。この6箇所目はこれまでのルール記載から漏れていた。新しくハブ系リンクをハードコードする箇所を追加する場合は、必ずこのリストに追記すること。

### 英語版「カテゴリ」サイドバーウィジェットは非表示（2026-07-28確定）
`article.njk`のサイドバー「カテゴリ」ウィジェット（15業種一覧）は`{% if lang != "en" %}`で英語ページでは非表示にしている。英語版にはカテゴリ一覧ページ（`/en/stories/{category}/`）自体がまだ存在せず、英語ラベル・英語カテゴリ名を用意しても遷移先が日本語版ページになってしまうため、ユーザー確認の上で非表示を選択した。**将来英語版カテゴリ一覧ページを作る場合は、この`{% if %}`分岐を解除し、`cat.name_en`とリンク先を英語化すること。**

### 新しいEN about記事を追加したら、ハブページの「Go Deeper」に自動反映される
`src/en/stories/about/index.njk`の記事一覧は`collections.aboutArticles | selectLang("en")`で動的に取得しているため、新しい英語about記事を追加しても本ファイルを手動編集する必要はない（日本語版`about/index.njk`のようにfileSlugをハードコードした配列で管理する方式は取っていない）。
