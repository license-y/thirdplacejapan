---
title: "読み物"
description: "サードプレイスの文化・思想・歴史を伝える特集記事。Third Place Japan 編集部の読み物。"
canonical: "/journal/feature/"
---
<div class="max-w-4xl mx-auto px-6 py-24">

  <div class="mb-16" data-aos="fade-up">
    <p class="label mb-4">Feature</p>
    <h1 class="font-serif text-4xl font-light text-dark mb-4">読み物</h1>
    <div class="gold-rule mb-6"></div>
    <p class="text-sm text-muted">サードプレイスの文化・思想・歴史を、編集部が静かに伝えます。</p>
  </div>

  {% if collections.features and collections.features.length %}
  <div class="space-y-10">
    {% for post in collections.features %}
    <a href="{{ post.url }}" class="group flex gap-6 border-b border-border pb-10" data-aos="fade-up">
      {% if post.data.thumbnail %}
      <div class="w-32 h-24 shrink-0 overflow-hidden">
        <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" loading="lazy" decoding="async">
      </div>
      {% endif %}
      <div>
        <p class="label mb-2">{% for tag in post.data.tags %}{% if tag != "articles" and tag != "feature" %}{{ tag }} {% endif %}{% endfor %}</p>
        <h2 class="font-serif text-xl font-light text-dark group-hover:text-gold transition-colors leading-snug mb-2">
          {{ post.data.title }}
        </h2>
        {% if post.data.description %}
        <p class="text-sm text-muted line-clamp-2">{{ post.data.description }}</p>
        {% endif %}
        <p class="text-xs text-muted mt-2">{{ post.date | dateJa }}</p>
      </div>
    </a>
    {% endfor %}
  </div>
  {% else %}
  <p class="text-sm text-muted">特集記事を準備中です。</p>
  {% endif %}

</div>
