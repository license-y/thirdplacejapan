# PHASE1-EFFECT-VERIFICATION-PLAN.md ─ Phase 1 優先度A施設 効果検証計画（2026-08-17策定）

> 本ファイルは `CLAUDE.md` セクション11「AEO・AI推薦基盤 開発ロードマップ」
> 11-11「効果検証」、および `AEO-PLAYBOOK.md`「6. 効果測定」で定める
> 効果検証プロセスを、Phase 1優先度A・8施設（`PHASE1-TARGET-LIST.md`参照）
> に対して具体的にいつ・何を・どう観測するかに落とし込んだ運用計画である。
> 新しい測定プロセスを新設するものではなく、既存プロセス（月次AI検索実査・
> Search Console確認）を、Phase 1という具体的な実装イベントに適用する。

---

## 0. 前提・制約（先に明記する）

- **GA4／Search Console等のアナリティクス連携は本リポジトリ内に存在せず、
  実数はClaude Codeのローカル作業からは取得できない**（`PHASE1-TARGET-LIST.md`
  「0. データ取得方法と制約」と同じ制約）。本計画に基づく実観測の実行には、
  該当ツールへのアクセス権を持つ運用者（人間）が定期的に数値を取得し、
  本ファイルの「4. 記録テンプレート」または別途の運用シートに記録する
  作業が必要になる。Claude Codeはこの計画の整備・記録の補助はできるが、
  実数の自動取得はできない
- **AI引用状況は自社観測であり、第三者検証済みの確定データではない**
  （`CLAUDE.md` 11-1・11-11、`AEO-PLAYBOOK.md`「6. 効果測定」の既存注記と
  同じ扱い）。本計画で記録するAI引用データも、この前提を維持する
- **コンバージョンについて**：TPJセレクトは「編集部が発掘・紹介する、
  施設側の申請不要な掲載枠」（`CLAUDE.md`「TPJセレクト 掲載仕様」）である
  ため、施設側からの申込・問い合わせを前提としたコンバージョン導線が
  そもそも存在しない。優先度A・8施設（GBC以外の7件はいずれもTPJセレクト）
  に直接紐づくコンバージョンは限定的である。観測するとすれば、これらの
  ページ経由で正式認証（Certified以上）への問い合わせ（`#for-business`）に
  至った件数のような間接指標に限られる点に注意する

---

## 1. 比較基準日

### 1-1. 実装の時系列（git履歴で確認した事実）

優先度A・8施設それぞれの構造化データは、単一の日に一括実装されたのではなく、
以下のように段階的に積み上がってきた。日付はいずれも`git log`で実際の
コミット履歴を確認した事実であり、推測ではない。

| 施設 | TPJセレクト初出日 | 中核スキーマ安定化日 | last_verified_date反映日（2026-08-17確認） |
|---|---|---|---|
| Green Beans Coffee | 2026-06-23 | 2026-07-06（グレード機能実装・Flagshipとして全項目出力開始） | 2026-08-14 |
| The SG Club | 2026-07-04 | 2026-07-21（geoのグレード非依存化により全項目出力が安定） | 2026-08-14 |
| KOFFEE MAMEYA | 2026-07-04 | 同上 | 2026-08-15 |
| 森岡書店 銀座 | 2026-07-04 | 同上 | 2026-08-15 |
| Glitch Coffee & Roasters | 2026-07-04 | 同上 | 2026-08-17 |
| BnA_WALL | 2026-07-04 | 同上 | 2026-08-17 |
| 文喫 六本木 | 2026-07-04 | 同上 | 2026-08-16 |
| TRUNK(HOTEL) | 2026-07-05 | 同上 | 2026-08-16 |

**「中核スキーマ安定化日」の内訳**：GBC（Flagship）は2026-07-06のグレード
機能実装時点で`geo`を含む全項目が出力される状態だった。残り7施設
（TPJセレクト＝`grade_slug: select`）は、2026-07-21に修正されるまで
`geo`がグレード制御の対象になっており出力されていなかった不具合があり
（詳細はルート`CLAUDE.md`「LocalBusiness の検索最適化プロパティ」
geo項目参照）、この修正日をもって現行の構造化データが揃った。

### 1-2. 「実装前」基準日をどう置くか（重要な注意）

上記の通り、優先度A・8施設のLocalBusiness／AggregateRating／
Review＋reviewRating／BreadcrumbList／7軸スコアという**中核の構造化データは、
今回のセッション（2026-08-17）より3〜6週間前から本番稼働していた**。
2026-08-17に新規実装・本番確認したのは`last_verified_date`フィールドのみ
である（`CLAUDE.md` 11-8参照）。

したがって「実装前 vs 実装後」を単純に2026-08-17で区切ると、以下の誤読
リスクがある。

- 中核スキーマは既に3〜6週間本番稼働しており、Googleのインデックス・
  AI引用に一定程度反映済みの可能性がある。2026-08-17を「構造化データが
  ゼロから実装された日」と誤認しないこと
- 2026-08-17を基準日にした観測は、実際には主に「`last_verified_date`という
  小規模な追加の限界効果」と「Phase 1優先度A・8件が揃ったことの確認」を
  測ることになる。構造化データ全体（中核スキーマ）の効果を測る観測には
  ならない点を認識した上で結果を解釈すること

### 1-3. 採用する基準日（確定）

上記を踏まえ、目的の異なる2つの基準日を使い分ける。

- **基準日A（中核スキーマ・参考値）＝2026-07-21**：7施設の中核スキーマが
  出揃った日。ただしこの日を境にした厳密なbefore/after測定は、当時この
  目的での観測を開始していなかったため、GSC等でデータが遡って取得できる
  場合の**事後的な参考点**に留める
- **基準日B（Phase 1効果検証の実務上の起点）＝2026-08-17（確定・採用）**：
  優先度A・8施設全件の構造化データ（`last_verified_date`含む）が出揃い、
  本番で確認できた日。本計画における定点観測は、この日を起点とする

### 1-4. 観測結果を報告する際の必須ルール（絶対厳守）

基準日Aとの比較、基準日Bとの比較は、**測っている対象がまったく異なる別の問い**
である。

- 基準日A（7/21）との比較 → 中核スキーマ（LocalBusiness・AggregateRating・
  Review・BreadcrumbList・7軸スコア等）全体が本番稼働してからの効果を測る
- 基準日B（8/17）との比較 → `last_verified_date`追加分＋Phase 1優先度A
  8件が揃った効果のみを測る（中核スキーマは基準日Bの時点で既に3〜6週間
  稼働済みのため、基準日Bとの比較では中核スキーマ自体の効果はほぼ
  含まれない想定）

**この2つを区別せずに「実装後に変化があった／なかった」とだけ報告すると、
どちらの効果を指しているのか読み手に伝わらず、誤った解釈（例：
`last_verified_date`という小さな追加だけで大きな検索表示の変化が
起きたかのような誤解）を招く。**

→ **観測結果を報告・記録する際は、必ず基準日A比較か基準日B比較かを明記する。**
「4. 記録テンプレート」にもこの明記欄を設ける。

---

## 2. 観測項目

| # | 観測項目 | 取得方法 | 前提・注意 |
|---|---|---|---|
| 1 | Google検索での表示状況（リッチリザルト等の変化） | Search Console「拡張」レポート（Review スニペット・パンくずリスト等の表示状況）／実際の検索結果の目視確認 | GSC連携が必要（0節参照）。目視確認はClaude Codeでも一部代替可能（WebSearch等で検索結果を確認） |
| 2 | 指名検索数 | Search Console「検索パフォーマンス」でクエリ別（施設名・「施設名 third place japan」等）のインプレッション数を確認 | GSC必須。`AEO-PLAYBOOK.md`「6. 効果測定」の既存項目と同一 |
| 3 | 検索流入数・CTR | Search Console「検索パフォーマンス」で対象8ページ（`/stories/select/{slug}/`）のクリック数・表示回数・CTRを確認 | GSC必須 |
| 4 | コンバージョン | `#for-business`問い合わせフォームの参照元ページ（Referrer）集計 | 0節の通り、優先度A・8施設に直接紐づくコンバージョンは構造上少ない前提で見る |
| 5 | AI引用状況（ChatGPT・Gemini・Google AI Overview等） | 対象8施設名・関連クエリで月次に実際に問い合わせ、引用・言及の有無と文脈を記録 | **自社観測であり第三者検証なし**（`CLAUDE.md` 11-1・11-11と同じ扱い。Phase1対象施設リスト作成時の扱いを踏襲） |

**注記**：項目1〜4は`AEO-PLAYBOOK.md`「6. 効果測定」が既に定める
「Search Consoleで表示回数・CTRの変化を追う」「指名検索の増加を追う」を
Phase 1・8施設に絞って適用したものであり、新しい測定手法ではない。
項目5も同ドキュメントの「月1回、ターゲットクエリで実際にAI検索し、
TPJが引用されているか・どう言及されているかを記録」をそのまま踏襲する。

---

## 3. 観測開始タイミング

- Googleが構造化データを認識し、リッチリザルト等に反映するまでは
  一般的に数日〜数週間を要する
- **初回定点観測：2026-08-31（基準日Bから2週間後）を目安とする**
- 以降は`AEO-PLAYBOOK.md`「6. 効果測定」が定める月次のAI検索実査・
  Search Console確認のサイクルに合流させ、Phase 1専用の特別観測を
  都度別立てにしない
- 参考：GSCのクエリ別データが遡って取得できる場合は、基準日A（7/21）
  以降の指名検索・表示回数の推移も確認し、「中核スキーマ安定化」と
  「`last_verified_date`追加」それぞれの寄与を可能な範囲で切り分ける

### 3-1. 優先度B・C群を観測対象に含めるかどうか（未定・2026-08-17追記）

`PHASE1-TARGET-LIST.md`に基づき、優先度B（17件）への`last_verified_date`
実装が進行中である（本ファイル策定時点で優先度B 17件中17件が完了見込み）。
これにより、優先度A・8件以外の施設にも本計画と同種の構造化データ・
`last_verified_date`が揃いつつある。

しかし、**本計画（比較基準日A/B・観測項目・観測開始タイミング）の対象は
現時点でも優先度A・8件のみであり、優先度B・C群を将来的に観測対象へ
含めるかどうかは未定**。含める場合、対象施設ごとに`last_verified_date`
反映日が異なるため、基準日の設計を個別に検討し直す必要がある
（優先度A・8件の基準日A/Bをそのまま流用できない可能性が高い）。
優先度B・C群への展開作業自体は、効果検証の対象拡大を意味しない
（前回確認済みの「別トラック」原則をここでも維持する）。

---

## 4. 記録テンプレート

実観測の結果は、以下の形式で本ファイル末尾に追記していく運用とする
（運用者がGSC/GA4等から値を取得し、日付とともに追記する）。

```markdown
### 観測記録：{{ 観測日 }}

**比較基準日：基準日A（2026-07-21・中核スキーマ）／基準日B（2026-08-17・
last_verified_date）のいずれか一方を明記すること（1-4節参照。必須）**

| 施設 | 指名検索インプレッション | クリック数 | CTR | リッチリザルト表示 | AI引用（自社観測） |
|---|---|---|---|---|---|
| Green Beans Coffee | | | | | |
| The SG Club | | | | | |
| 森岡書店 銀座 | | | | | |
| KOFFEE MAMEYA | | | | | |
| TRUNK(HOTEL) | | | | | |
| 文喫 六本木 | | | | | |
| Glitch Coffee & Roasters | | | | | |
| BnA_WALL | | | | | |

**所感・特記事項**：
```

---

## 5. 実施責任・参照

- 本計画に基づく実データの取得・記録は、GSC/GA4アクセス権を持つ運用者が行う
- `CLAUDE.md` 11-11「効果検証」・`AEO-PLAYBOOK.md`「6. 効果測定」から
  本ファイルを参照する
- 優先度B・C群（残り27施設）への構造化データ・`last_verified_date`展開が
  進んだ場合、本計画の対象施設・基準日を拡張して再利用する
